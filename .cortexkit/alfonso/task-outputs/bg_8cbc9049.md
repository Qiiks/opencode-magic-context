---
task_id: bg_8cbc9049
agent: council-member
session_id: ses_0c79ae103ffeZyhIck803zmxc9
parent_session_id: ses_0c79b87d0ffeSSVKxxJ07oUaHQ
status: completed
completed_at: 2026-07-06T17:11:45Z
---

[assistant] 2026-07-06T17:08:18Z

[assistant] 2026-07-06T17:08:19Z

[assistant] 2026-07-06T17:08:21Z

[assistant] 2026-07-06T17:08:23Z

[assistant] 2026-07-06T17:08:24Z

[assistant] 2026-07-06T17:08:26Z
**Analyzing Emergency Recovery**

I've been drilling down into the emergency recovery process, specifically focusing on the conditional statement on line 685. It seems we clear any in-progress recovery when a session isn't in a wrap-up phase, which makes sense. I'm now examining how `drainReservation` is handled in relation to this and how `deferMarkerApplication` plays into the logic after a recovery clearance.

[assistant] 2026-07-06T17:08:28Z

[assistant] 2026-07-06T17:08:30Z

[assistant] 2026-07-06T17:08:31Z

[assistant] 2026-07-06T17:08:32Z

[assistant] 2026-07-06T17:08:33Z

[assistant] 2026-07-06T17:08:35Z

[assistant] 2026-07-06T17:08:36Z

[assistant] 2026-07-06T17:08:37Z

[assistant] 2026-07-06T17:08:39Z

[assistant] 2026-07-06T17:08:40Z

[assistant] 2026-07-06T17:08:42Z

[assistant] 2026-07-06T17:08:43Z

[assistant] 2026-07-06T17:08:44Z

[assistant] 2026-07-06T17:08:45Z

[assistant] 2026-07-06T17:08:48Z

[assistant] 2026-07-06T17:08:49Z

[assistant] 2026-07-06T17:08:50Z

[assistant] 2026-07-06T17:08:51Z

[assistant] 2026-07-06T17:08:52Z

[assistant] 2026-07-06T17:08:54Z

[assistant] 2026-07-06T17:08:55Z

[assistant] 2026-07-06T17:08:56Z

[assistant] 2026-07-06T17:08:57Z

[assistant] 2026-07-06T17:08:59Z

[assistant] 2026-07-06T17:09:00Z

[assistant] 2026-07-06T17:09:01Z

[assistant] 2026-07-06T17:09:02Z

[assistant] 2026-07-06T17:09:05Z

[assistant] 2026-07-06T17:09:07Z
**Investigating a Drain Gate**

I'm currently focused on the `pendingMarkerCoveredByConsumedBoundary` drain gate within the OpenCode post-processing. I'm reviewing the `transform-postprocess-phase.ts` file in the plugin's hooks to understand its behavior and logic. I'm hoping to identify the cause of the gate.

[assistant] 2026-07-06T17:09:10Z

[assistant] 2026-07-06T17:09:12Z

[assistant] 2026-07-06T17:09:13Z

[assistant] 2026-07-06T17:09:15Z

[assistant] 2026-07-06T17:09:16Z
**Revisiting Code Detail**

I'm currently re-examining lines 4350-4400 of `context-handler.ts` within the `pi-plugin` package. The focus is specifically on the 'drain predicate' logic and its integration with the OpenCode system. There are a few Pi-specific terms in that area that are of interest. I want to be certain about the interaction here.

[assistant] 2026-07-06T17:09:19Z

[assistant] 2026-07-06T17:09:20Z

[assistant] 2026-07-06T17:09:21Z

[assistant] 2026-07-06T17:09:22Z

[assistant] 2026-07-06T17:09:23Z

[assistant] 2026-07-06T17:09:25Z

[assistant] 2026-07-06T17:09:26Z

[assistant] 2026-07-06T17:09:28Z

[assistant] 2026-07-06T17:09:29Z

[assistant] 2026-07-06T17:09:32Z

[assistant] 2026-07-06T17:09:34Z
**Revisiting Code Implementation**

I'm currently re-examining `applyDeferredPiCompactionMarker` within `compaction-marker-manager-pi.ts`. Specifically, I'm focusing on the condition where `matches.length` is zero, using `getCompartmentsByEndMessageId`. This seems to be a pivotal point; I want to ensure my understanding is accurate and identify potential edge cases. I'll need to explore the logic flow thoroughly.


**Analyzing Marker Persistence**

I've just realized a critical aspect of the compaction marker persistence. The `applyDeferredPiCompactionMarker` function will successfully apply even if a new compartment is published after `injectM0M1Pi` but before `runPipeline`'s end. The `getCompartmentsByEndMessageId` will find the new compartment. Then the pending marker is cleared. Thus, the pipeline will mistakenly consider the pending compaction marker resolved.

[assistant] 2026-07-06T17:09:37Z

[assistant] 2026-07-06T17:09:39Z

[assistant] 2026-07-06T17:09:41Z

[assistant] 2026-07-06T17:09:42Z

[assistant] 2026-07-06T17:09:44Z

[assistant] 2026-07-06T17:09:45Z

[assistant] 2026-07-06T17:09:47Z
**Reviewing Cache Busting Logic**

I'm revisiting the cache busting logic within `context-handler.ts`, specifically lines 4300-4350. The focus is on the peek-then-drain approach for `historyRefreshSessions`, ensuring it only drains on success and cache bust. The goal is to verify the integrity and efficiency of this process in managing cached data.


**Analyzing Error Handling**

I'm now digging into the error handling of the cache busting process. Specifically, I'm examining the scenario where `injectM0M1Pi` throws an error. The key question is whether the `historyWasConsumedThisPass` flag is correctly managed in this case. It seems the flag is set *before* the call, potentially leading to inconsistencies if the injection fails. This requires a closer look at the interaction between the `try...catch` block and the flag's update.


**Clarifying Execution Paths**

I'm now tracing the execution paths around `injectM0M1Pi` and how they affect the `historyWasConsumedThisPass` flag. I'm focusing on scenarios where compartment publishing happens after the injection. This requires a deeper understanding of the `deferredHistoryRefresh` flag and its relationship with `recomputeM1ThisPass`.

[assistant] 2026-07-06T17:09:50Z

[assistant] 2026-07-06T17:09:52Z

[assistant] 2026-07-06T17:09:53Z

[assistant] 2026-07-06T17:09:55Z

[assistant] 2026-07-06T17:09:56Z
**Analyzing `runPipeline` Function**

I'm currently focusing on the `runPipeline` function, specifically lines 3430-3440. I've confirmed that it's an asynchronous function, but now I'm checking for any `await` calls within its body. My goal is to understand its execution flow, and possible concurrency points.


**Confirming Synchronicity of `runPipeline`**

I've made a significant discovery: `runPipeline` is actually synchronous! Despite being declared `async`, I found no `await` calls within its function body. This means that, while it returns a Promise, it executes synchronously, blocking the event loop until completion. I'm now considering the implications of this. A background historian run cannot publish a compartment *during* `runPipeline` execution, but what if a new compartment was available *just before*? The `deferredHistoryWasPendingAtPassStart` flag seems to be the key variable in this case.


**Validating Compaction Marker Conditions**

I've just validated the compaction marker's behavior. I can confirm that when `args.injection` is undefined, `historyWasConsumedThisPass` remains false, thus ensuring that `deferredHistoryDrainEligible` also stays false. Consequently, the compaction marker isn't drained. I've also now confirmed this is the correct behavior. I'm now exploring scenarios where a background historian publishes a new compartment during the LLM call.


**Verifying History Drain Behavior**

I've confirmed the expected behavior regarding history drain and compartment injection across passes. I've tracked the flow from DB updates to `contextHandler` re-invocation, confirming the role of `deferredHistoryRefreshSessions`, `deferredHistoryWasPendingAtPassStart`, and the eventual draining of the compaction marker. Specifically, I've verified that the compaction marker is drained correctly in the subsequent pass after a new compartment is published by the background historian. I'm satisfied.


**Investigating Pending Marker Drain**

I'm now focused on the scenario where `injectM0M1Pi` reuses a cached m[0]/m[1] pair due to contention. I've confirmed that if this happens, the new compartment is *not* injected. My key question now is whether, in this situation, `runPipeline` still drains the pending compaction marker. I've traced the execution and found that `historyWasConsumedThisPass` will be `true`, because `deferredHistoryRefresh` is `true`.


**Confirming `materializationSatisfiedThisPass`**

I've been meticulously examining the `materializationSatisfiedThisPass` flag and its implications on marker draining. I've discovered a critical condition: if `shouldApplyPendingOps` is `false`, `materializationSatisfiedThisPass` remains `false`, preventing the compaction marker from draining. Conversely, if `shouldApplyPendingOps` is true, then if `deferredHistoryRefresh` is `true`, the marker *will* drain. I'm now certain there's a scenario where history is *not* injected, but the marker is still drained, violating a key invariant. This appears to be correct.

[assistant] 2026-07-06T17:10:03Z

[assistant] 2026-07-06T17:10:06Z

[assistant] 2026-07-06T17:10:07Z

[assistant] 2026-07-06T17:10:10Z

[assistant] 2026-07-06T17:10:11Z

[assistant] 2026-07-06T17:10:13Z

[assistant] 2026-07-06T17:10:14Z

[assistant] 2026-07-06T17:10:16Z

[assistant] 2026-07-06T17:10:18Z

[assistant] 2026-07-06T17:10:20Z

[assistant] 2026-07-06T17:10:21Z

[assistant] 2026-07-06T17:10:23Z

[assistant] 2026-07-06T17:10:26Z

[assistant] 2026-07-06T17:10:28Z

[assistant] 2026-07-06T17:10:29Z

[assistant] 2026-07-06T17:10:31Z

[assistant] 2026-07-06T17:10:33Z

[assistant] 2026-07-06T17:10:34Z

[assistant] 2026-07-06T17:10:36Z
**Refining Cache Logic**

I'm now focusing on cache-busting logic. I'm considering limiting the first application of a mutation to cache-busting passes only. I'm also double-checking `isCacheBustingForTodo` to ensure no issues exist. Finally, I'm verifying the `isCacheBustingPass` implementation within OpenCode.

[assistant] 2026-07-06T17:10:38Z

[assistant] 2026-07-06T17:10:40Z

[assistant] 2026-07-06T17:10:42Z
**Analyzing Cache Busting Logic**

I'm currently dissecting the distinctions in cache busting mechanisms between OpenCode and Pi. I've noted a key divergence: `isCacheBustingPass` in OpenCode hinges on `shouldApplyPendingOps` or `shouldRunHeuristics`, while `isCacheBustingForTodo` in Pi combines `isCacheBusting` and `result.executedWorkThisPass`. Now, I'm examining what constitutes `isCacheBusting` in Pi – it seems tied to history, which is a subtle, but potentially impactful difference.

[assistant] 2026-07-06T17:10:45Z
**Locating Relevant Files**

I'm currently focused on pinpointing where the `isCacheBusting` flag is handled differently between OpenCode and Pi, and I'm not finding any occurrences of `todowrite` in `PARITY.md`. Right now I am looking in the `packages/pi-plugin` directory.


**Analyzing CacheBusting Divergence**

I've found a key difference in how `isCacheBusting` is used between OpenCode and Pi, specifically within `src/context-handler.ts`. Discovery in Pi is strictly tied to history-refresh passes due to a deliberate design choice related to placeholder message handling, unlike OpenCode. However, the `todowrite` divergence I'm investigating remains undocumented here! I am now checking for additional issues, such as session TTL with crashed wrapups.

[assistant] 2026-07-06T17:10:48Z

[assistant] 2026-07-06T17:10:50Z

[assistant] 2026-07-06T17:10:51Z

[assistant] 2026-07-06T17:10:53Z

[assistant] 2026-07-06T17:10:55Z

[assistant] 2026-07-06T17:10:57Z
**Checking for Additional Issues**

Okay, so I've confirmed that `SessionMeta` isn't where this is handled, which is expected. The wrap-up state is indeed managed via `getWrapupInProgressState`, `acquireWrapupInProgress`, `updateWrapupInProgress`, and `releaseWrapupInProgress`. Now I'm shifting focus to verify there are no other potential complications.


**Analyzing Compaction Marker Advance**

I'm now examining the logic surrounding compaction marker advancement, particularly within the OpenCode postprocess drain gate. I've re-reviewed the code and discovered that `applyDeferredPiCompactionMarker` is called without checking if the pending marker is covered by the consumed boundary, which is a potential issue. I'm focusing on the consequences of advancing the marker past history not injected in the current prompt, to understand the implications for history management and prompt construction. My current hypothesis involves considering the impact on LLM prompts and future compaction.


**Verifying LLM Context**

I'm now zeroing in on a serious potential correctness issue: data/context loss. My key concern is whether a new compartment's contents could be missed by the LLM, despite being considered compacted. I have been re-evaluating the case of a background historian run publishing a new compartment combined with a user message. The current thought is related to replay of m[0]/m[1] pair and the fact that raw messages are still sent. However, I need to continue.


**Clarifying Cache Invalidation**

I've been working through a very specific edge case and think I've figured something out. It appears the LLM doesn't miss information when it's compacted. The prompt cache will bust because the prompt will change between passes to include a compartment instead of raw messages, but this is an expected outcome from injection. The core thought is focused on the edge case, which appears to be safe and expected.


**Addressing Correctness Invariant**

I'm now fully immersed in a crucial correctness invariant concerning compaction markers and history rebuilds. I've been wrestling with a specific scenario where the marker advances without a corresponding history rebuild, leading to a potential for multiple cache busts. My exploration has revealed a single cache bust even in this edge case, despite the initial concern. However, I've identified a potential correctness issue, specifically regarding agent replies based on outdated prompts when the marker is advanced. I'm carefully re-examining the critical comment in `transform-postprocess-phase.ts` to fully understand the implications.


**Investigating OpenCode Marker**

Okay, I'm now wrestling with the OpenCode scenario, specifically the potential for inconsistencies when the compaction marker is advanced in OpenCode, but the corresponding history isn't yet rendered. I have re-evaluated the implications if the agent's reply was generated *before* the sessionManager compacts the raw history, which is still safe. However, there is a serious issue if the sessionManager removes the raw history and the next pass runs. The raw messages are gone, the compartment is in the DB, and everything is consistent! There is a key question if the new compartment is NOT in the DB.


**Revisiting Cache Busting**

I'm now re-evaluating the cache busting behavior in OpenCode more carefully. The core question is whether advancing the compaction marker in a pass without rendering the new compartment causes an additional cache bust. The critical comment emphasizes the need for a single cache bust covering both history rebuild and marker advance. It's becoming clearer: even though the current pass doesn't render the new compartment, the marker advance might still necessitate a cache bust due to editor/harness interactions, but I think only in the next pass.

[assistant] 2026-07-06T17:11:05Z

[assistant] 2026-07-06T17:11:07Z

[assistant] 2026-07-06T17:11:10Z
**Analyzing Cache Busting**

I've verified the replay logic and found it's sound. However, I've observed a divergence related to history-refresh-only passes. Specifically, in OpenCode, the `isCacheBustingPass` flag is false during these passes, since `shouldApplyPendingOps` returns `false` in that scenario. This is something that needs further digging.


**Investigating Divergence Causes**

I've zeroed in on the root cause of the cross-harness divergence: history-refresh-only passes. OpenCode replays the old todo state, while Pi injects the fresh todo state during these passes. This is caused by the different states of the `isCacheBustingPass` and `isCacheBustingForTodo` flags on these passes, where `shouldApplyPendingOps` returning `false` results in a false flag in OpenCode. I'm now looking into fixing the Pi compaction-marker drain logic. I've found a clue to follow that may be relevant to the issue.


**Clarifying Materialization Logic**

I've realized a critical flaw: `materializationSatisfiedThisPass` is incorrectly set. It's set within `shouldApplyPendingOps`, which only checks if materialization *was attempted*, not *succeeded*. This means if `injectM0M1Pi` fails (e.g., due to contention), the drain logic incorrectly proceeds. I'm now focusing on how `materializationSatisfiedThisPass` should correctly reflect successful materialization.


**Refining Materialization Logic**

I've discovered a critical issue: `materializationSatisfiedThisPass` is incorrectly set. It needs to reflect if materialization *succeeded*, not just *was attempted*. If `injectM0M1Pi` fails (due to contention, e.g.), the drain logic incorrectly proceeds. It's clear `materializationSatisfiedThisPass` should be set to `injectionResult.m0Materialized === true`, when an injection is attempted. OpenCode's implementation correctly tracks `m0RematerializedThisPass` using the result of `injectM0M1`, but Pi's code has this significant flaw, violating the bust invariant. I'm now verifying the TTL release in a wrapup-crash scenario.

[assistant] 2026-07-06T17:11:16Z
**Analyzing Potential Issues**

I'm currently focused on identifying potential issues. I've noted a critical severity violation: Pi Compaction-Marker Drain Invariant Violation, a SHIP-BLOCKER issue in `packages/pi-plugin`. I'm going to review the findings so far, to determine the implications.


**Pinpointing Critical Invariants**

I've zeroed in on a critical invariant violation related to Pi compaction markers. My analysis of `src/context-handler.ts` (lines 4369-4419) reveals that Pi advances the compaction marker even when `injectM0M1Pi` fails to materialize a new compartment due to contention, violating the "one bust" rule. The issue stems from `materializationSatisfiedThisPass` being set without regard to `injectM0M1Pi`'s success. I need to solidify this.

[assistant] 2026-07-06T17:11:22Z
**Checking Diagnostics Again**

I've decided to revisit the project's diagnostics, just to be thorough. Thankfully, the earlier scan showed zero errors or warnings, so I'm moving on to a third finding to ensure I'm getting a full understanding of the situation at hand.


**Analyzing Identity Resolution**

I'm now diving into the `resolveProjectIdentity` function and its interactions. I'm focusing on the logic around error handling, specifically within the `ProjectIdentityError` context. I see how `transientFailureCooldown` and `reuseLastKnownGitIdentity` are used when a strict resolution fails but git metadata is present. The interplay of cached identities and fallback mechanisms is becoming clearer.


**Investigating Ownership Handling**

I've hit a potential issue. When `resolveProjectIdentityStrict` throws a `dubious_ownership` error, a 5-minute cooldown is set, as expected. However, if the user immediately fixes the `safe.directory` configuration, Magic Context doesn't instantly recognize the Git repo. The cooldown prevents an immediate re-probe. This can lead to a 5-minute delay before the correct identity is detected, impacting usability. I need to determine if there's a workaround or if the delay is acceptable.

[assistant] 2026-07-06T17:11:28Z

[assistant] 2026-07-06T17:11:29Z

[assistant] 2026-07-06T17:11:32Z
**Investigating Discrepancies**

I've determined the initial concern is documented in `PARITY.md` §6a, confirming it's not a bug. Now, I'm examining other potential problems, focusing on the fourth finding: "Sidebar Facts row removal; CLI doctor onnxruntime-node load probe in a subprocess." Currently, my attention is on the CLI, hoping to find any related issues there.


**Analyzing Onnxruntime Probe**

I'm now deep-diving into the `onnxruntime-node` load probe script within `packages/cli/src/lib/embedding-runtime.ts`. I was initially focused on how the `require` call in the subprocess works. I verified the script correctly attempts to require `onnxruntime-node` from its package directory. It confirms that the package is CommonJS and that we should not expect issues with the required call.


**Verifying Require Calls**

I'm now certain `require` will work in the `onnxruntime-node` load probe script. I've confirmed `-e` runs as CommonJS and that backslashes in the `packageDir` environment variable are correctly handled. Further, `require("node:path").join` on Windows seems fine. Now, I'm verifying the parsing function of the probe output.


**Assessing Verdict Parsing**

I'm now focused on the `parseOnnxProbeVerdict` function within `embedding-runtime.ts`. I was reviewing how the probe output is parsed and I realized that the current logic is robust because the JSON verdict is printed last, ensuring it's the first JSON parsed. However, it looks like there's a problem if `onnxruntime-node` prints a message without a newline. The split, filter, and reverse logic depends on newlines, making an un-newline-terminated message break JSON parsing. I need to investigate if this can actually happen.


**Verifying Edge Case**

I've determined that `process.stdout.write` lacks a newline, potentially causing JSON parsing errors in the probe if a preceding native loader message isn't newline-terminated. However, this is unlikely in practice. Now, I'm shifting focus to `/ctx-wrapup` and its potential issues.

\n\n

**Checking Wrapup Logic**

I've confirmed mutual exclusion regarding recompilation, upgrade, and historian triggers for wrapup across multiple functions: `runPiWrapup`, `runManagedWrapup`, `executeContextRecompWithResult`, `startCompartmentAgent`, and `runPiHistorian`, and that both `ctx-recomp` and `ctx-session-upgrade` in Pi check `isWrapupInProgress`. Now, I'm examining the protected tail boundary logic.

\n\n

**Analyzing Boundary Logic**

I'm now investigating `resolveWrapupProtectedTailBoundary`. It appears correct: initial boundary, tool arc fencing, user snapping, then *re-fencing* to account for user snaps potentially *inside* a tool arc. The second fence ensures no tool invocation/result pair is split. The `fenceWrapupBoundaryForToolArcs` always moves the boundary *backward* to the tool arc start.


**Verifying Boundary Safety**

I'm now satisfied with the boundary-moving logic, confirming it's always backward towards the offset, which is safe. My concern was the potential for the boundary to reach offset, halting wrapup, which proves to be an expected outcome when eligible messages are exhausted. I'm now checking the behavior of `anchorRawMessageCount` in `runPiWrapup`, verifying the logic as messages are consumed by compartments.


**Confirming Count Anchoring**

I've verified the `anchorRawMessageCount` logic in `runPiWrapup`, ensuring the keep watermark doesn't drift. It's correctly anchored to the initial count, preventing issues with message ordinal. I also confirmed the `clearEmergencyRecovery` calls are implemented correctly in both `runPiWrapup` and `runManagedWrapup`, so no issues there. Finally, I've checked for any references to the removed `ctx_reduce_enabled` config flag, and there are none outside a test file.


**Assessing Cooldown Logic**

I'm now focused on the fallback cooldown logic in `project-identity.ts`, specifically around `transientFailureCooldown`. I've confirmed that if `getActiveCooldown` returns a value, then `hasGitDir` (which checks for the presence of `.git`) will return true, which means there is no re-probing until the 5-minute cooldown. The `reuseLastKnownGitIdentity` cached git identity is `undefined` on a cold start, so it correctly falls back to returning "dir:<hash>". This is a documented, acceptable tradeoff for performance, so I'll label it as a low-severity finding.
<COUNCIL_MEMBER_RESPONSE>
## Finding 1: Pi Compaction-Marker Drain Invariant Violation
- **Severity**: critical
- **Location**: `packages/pi-plugin/src/context-handler.ts` (lines 4369-4419)
- **Confidence**: high
- **Issue**: Pi drains the deferred compaction marker and advances it in the sessionManager (via `appendCompaction`) even when `injectM0M1Pi` failed to materialize the new compartment (e.g. due to database contention, where it falls back to replaying the cached m[0]/m[1] pair). This violates the ship-blocker invariant: *"One bust must cover BOTH a history rebuild AND its compaction-marker advance."*
- **Evidence**:
  In `packages/pi-plugin/src/context-handler.ts`, `deferredHistoryDrainEligible` is defined as:
  ```typescript
  const deferredHistoryDrainEligible =
      historyWasConsumedThisPass &&
      materializationSatisfiedThisPass &&
      (deferredHistoryWasPendingAtPassStart || hasPendingMaterializeSignal) &&
      !suppressDeferredHistoryDrain &&
      !casLost;
  ```
  `materializationSatisfiedThisPass` is set to `true` inside the `shouldApplyPendingOps` block (line 3775) when pending operations are applied, completely ignoring whether `injectM0M1Pi` actually materialized the new compartment or hit contention.
  If `injectM0M1Pi` hits contention, it catches `PiMaterializeContentionError` and falls back to replaying the cached m[0]/m[1] pair (which does NOT contain the new compartment).
  Yet, `deferredHistoryDrainEligible` remains `true`, so `applyDeferredPiCompactionMarker` is called, which calls `appendCompaction` and advances the compaction marker in the sessionManager past history that was NOT actually injected in the current prompt.
  In contrast, OpenCode's `transform-postprocess-phase.ts` correctly gates the drain on `m0RematerializedThisPass` (which is set to `result.m0Materialized` returned by `injectM0M1`).
- **Suggested Fix**:
  Modify `packages/pi-plugin/src/context-handler.ts` to set `materializationSatisfiedThisPass` based on the actual materialization outcome returned by `injectM0M1Pi`. Specifically, update `materializationSatisfiedThisPass` to `injectionResult?.m0Materialized === true` after `injectM0M1Pi` runs, matching OpenCode's `m0RematerializedThisPass` logic.

## Finding 2: Cross-Harness Prompt Divergence on History-Refresh-Only Passes
- **Severity**: high
- **Location**: `packages/pi-plugin/src/context-handler.ts` (line 2380) vs `packages/plugin/src/hooks/magic-context/transform-postprocess-phase.ts` (line 1056)
- **Confidence**: high
- **Issue**: On a history-refresh-only pass (where a new compartment was published, but no pending ops or heuristics ran), OpenCode and Pi diverge on whether they inject the fresh todo state or replay the persisted anchor. This causes prompt cache misses and potential prompt divergence when switching between harnesses.
- **Evidence**:
  In OpenCode's `transform-postprocess-phase.ts`, the fresh todo state is injected only when `isCacheBustingPass` is `true`:
  ```typescript
  const isCacheBustingPass = shouldApplyPendingOps || shouldRunHeuristics;
  ```
  On a history-refresh-only pass, `shouldApplyPendingOps` and `shouldRunHeuristics` are both `false`, so `isCacheBustingPass` is `false`. Thus, OpenCode replays the persisted anchor (using the old todo state).
  In Pi's `context-handler.ts`, the fresh todo state is injected when `isCacheBustingForTodo` is `true`:
  ```typescript
  const isCacheBustingForTodo = isCacheBusting || result.executedWorkThisPass;
  ```
  Since `isCacheBusting` is `true` on a history-refresh-only pass, `isCacheBustingForTodo` is `true`. Thus, Pi injects the fresh todo state and updates the anchor.
  This causes the prompts to diverge on history-refresh-only passes.
- **Suggested Fix**:
  Align the cache-busting gate for todo injection in Pi to match OpenCode. Specifically, in `packages/pi-plugin/src/context-handler.ts`, `isCacheBustingForTodo` should be set to `result.executedWorkThisPass` (which is equivalent to OpenCode's `isCacheBustingPass`), rather than including `isCacheBusting`.

## Finding 3: Git Cooldown Usability Delay on Manual Fixes
- **Severity**: low
- **Location**: `packages/plugin/src/features/magic-context/memory/project-identity.ts` (lines 400-415)
- **Confidence**: high
- **Issue**: When git identity resolution fails (e.g. due to `dubious_ownership`), the directory is placed on a 5-minute cooldown. If the user immediately runs the suggested fix (`git config --global --add safe.directory <dir>`), Magic Context will still ignore the repository and return the `dir:` fallback for the remainder of the 5-minute window.
- **Evidence**:
  ```typescript
  if (getActiveCooldown(canonical) !== undefined) {
      if (hasGitDir(canonical)) {
          const cachedGitIdentity = reuseLastKnownGitIdentity(canonical);
          if (cachedGitIdentity !== undefined) {
              return cachedGitIdentity;
          }
      }
      return directoryFallback(canonical);
  }
  ```
  On cold start, `reuseLastKnownGitIdentity` returns `undefined`, so it immediately returns `directoryFallback(canonical)` without re-probing git until the cooldown expires.
- **Suggested Fix**:
  This is a minor usability tradeoff to prevent slow git probes from stalling the transform path. However, we could clear the cooldown for a directory if a command like `/ctx-status` or `/ctx-recomp` is manually run, or simply document that a restart/wait is required.

## Summary
- **Total findings**: 3 (1 critical, 1 high, 1 low)
- **Overall Verdict**: **HOLD**
- **Rationale**: The release must be held because **Finding 1** violates a ship-blocker invariant ("One bust must cover BOTH a history rebuild AND its compaction-marker advance") on the Pi harness. Under database contention, Pi will advance the compaction marker in the sessionManager past history that was not actually injected, leading to potential prompt cache issues or incorrect state. Additionally, **Finding 2** introduces an undocumented prompt divergence between OpenCode and Pi on history-refresh-only passes.
</COUNCIL_MEMBER_RESPONSE>

