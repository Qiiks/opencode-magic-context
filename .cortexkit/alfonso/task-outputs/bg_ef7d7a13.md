---
task_id: bg_ef7d7a13
agent: council-member
session_id: ses_0b4657e0bffeL91a17FMZUnXyM
parent_session_id: ses_0b4664e56ffeKx8uRSceXLl3v9
status: completed
completed_at: 2026-07-10T11:14:57Z
---

[user] 2026-07-10T10:37:25Z
## Solo Analysis Mode
You MUST do ALL exploration yourself using your available read/search tools.
- Do NOT use task or any delegation tool under any circumstances
- Do NOT delegate to explore, librarian, or any other subagent
- Do NOT spawn background tasks
- Search the codebase directly — you have full read-only access to every file
- This mode produces the most thorough analysis because you see every result firsthand


## Analysis Intent: AUDIT

You are conducting an **audit** — your goal is to find discrete issues, risks, or violations.

**Focus:**
- Search for problems, anti-patterns, security risks, correctness issues, or violations of stated requirements
- Each finding must be a distinct, actionable item with concrete evidence
- Severity determines priority: critical (blocks/breaks), high (significant risk), medium (should fix), low (nice to fix)
- For each finding, provide the specific location (reference, section, or component where it occurs)
- State your confidence: high (clear evidence), medium (likely but needs verification), low (suspicion, investigate further)
- **This is a broad sweep, not a targeted trace.**

**Analytical standards:** Support claims with concrete evidence. State confidence (high/medium/low) for key assertions. Note caveats and limitations.

**Structure your response as:**
```
<COUNCIL_MEMBER_RESPONSE>
## Finding 1: [Title]
- **Severity**: critical/high/medium/low
- **Location**: [specific reference — e.g. component, section, endpoint, rule]
- **Confidence**: high/medium/low
- **Issue**: [what is wrong and why it matters]
- **Evidence**: [concrete reference, snippet, or observation that proves the issue]
- **Suggested Fix**: [actionable recommendation]

## Finding 2: [Title]
...

## Summary
[Total findings by severity. Overall risk assessment with confidence levels.]
</COUNCIL_MEMBER_RESPONSE>
```

## Analysis Question

BLIND ADVERSARIAL COUNCIL AUDIT — Magic Context SHADOW-MODE lane, pre-production soak gate.

Repo: /Users/ufukaltinok/Work/Projects/CortexKit/magic-context
Branch: subc-migration, HEAD 6ea3179f

You are one of several independent auditors. Work ALONE, hunt for the NEXT bug class. Do NOT assume the code is correct because it has tests — the first armed run already exposed two integration bugs the mocked tests missed (wire-shape mismatch, ordinal starvation), both already fixed at HEAD. Your job is to find what the mocks still miss. Read the ACTUAL code with file:line evidence; do not speculate abstractly.

=== WHAT SHADOW MODE IS ===
A dev-flag lane (config shadow_transform.enabled, user-tier only) where the OpenCode TS plugin mirrors every finalized transform pass to the Rust mc-module over the subc daemon socket for byte-comparison — WITHOUT affecting the live session. The TS side fire-and-forgets: per-session FIFO queue, state_sync (compartments/memories/mutations sync), shadow_transform (input + TS output + decision), shadow_reset (generation bump + wipe). The Rust side runs its own transform on the mirrored input against a shadow:<sid> store lineage, byte-compares against the TS output, and records divergences in shadow_divergences. The soak is OBSERVE-ONLY by design.

=== AUDIT SCOPE (areas that changed or are first-run-live today) ===

1. packages/plugin/src/plugin/hooks/create-session-hooks.ts — the config mapper was changed from a hand-maintained field list to a full spread (`...pluginConfig`) with two defaulted overrides (commit e932804c). HUNT: does spreading the ENTIRE plugin config into the hook config leak anything that shouldn't reach per-session hook code, shadow anything the hook type declares differently, or change behavior for any existing consumer that relied on a field being absent (e.g. `undefined` vs present-with-value)?

2. packages/plugin/src/hooks/magic-context/shadow-sender.ts — the full sender. HUNT ESPECIALLY:
   (a) HOT-PATH SAFETY: enqueue() runs inside the transform hot path. Verify EVERY code path in enqueue (including resolveOrdinalsForShadow with its new by-id DB fallback, denormalizeShadowOutput, cloneJson of potentially huge message arrays) is exception-safe and bounded — a throw or a multi-second stall here delays the user's real prompt. Is cloneJson(messages) on a 400-message array acceptable per-pass cost? Is the by-id DB read (readRawSessionMessageById) opening a DB handle per call, and what happens under SQLITE_BUSY?
   (b) WIRE SHAPE (commit 6ea3179f): toFlatWireBody flattens {method, params} to flat. Cross-check EVERY field the TS builders emit against the Rust serde parsers in crates/mc-module/src/lib.rs (ShadowStateSyncWire, ShadowTransformWire, ShadowResetWire, ShadowPassInputs, ShadowUsageWire, ShadowCompartmentWire, ShadowMemoryWire, ShadowMemoryMutationWire): field names, types, required-vs-optional, snake_case exactness. Name any field that will reject or silently default.
   (c) ORDINAL FALLBACK: the new below-floor by-id fallback in resolveOrdinalsForShadow. Can the fallback return an ordinal computed on a DIFFERENT basis than the cache (e.g. cache primed tail-only carries absolute ordinals from baseOrdinal while the by-id COUNT includes/excludes summary rows differently)? A silent basis mismatch would poison the shadow store with wrong ordinals rather than failing loud.
   (d) SubcShadowTransport: hand-rolled socket protocol (auth handshake, frame header, backoff). Route kind now tool_provider (commit 1555b231). HUNT: connection lifecycle bugs (socket close mid-request, reader waiters, backoff doubling), auth proof correctness, frame length handling, the 5s request timeout leaving the FIFO wedged.
   (e) QUEUE SEMANTICS: MAX_QUEUE_PER_SESSION=4 drop-oldest, blockedUntilReset, requireResetReason transitions. Any path where a session gets permanently wedged (blocked but no reset queued) or where a reset races a queued pass?

3. crates/mc-module/src/lib.rs shadow handlers (handle_shadow_state_sync_value, handle_shadow_transform_value, handle_shadow_reset_value, shadow_binding) + crates/mc-store shadow methods (apply_shadow_state_sync, reset_shadow_session). HUNT:
   (a) ISOLATION: can ANY shadow-lane write touch a non-shadow session row? The shadow session id is derived from binding (shadow: prefix). Verify shadow_binding rejects non-shadow bindings on shadow ops AND that plain transform ops reject shadow bindings (codes non_shadow_op_on_shadow_binding, plain_transform_on_shadow_binding — verify they're actually enforced on EVERY arm).
   (b) The shadow_transform handler runs the REAL transform + a byte-compare. Does a shadow divergence or panic in the compare path affect the real lane in any way (shared store handle, lease contention, pass-trace pollution)?
   (c) apply_shadow_state_sync CAS semantics: generation/seq mismatch arms, and what happens when state_sync carries compartments whose sequences overlap already-synced ones (idempotent or duplicating?).

4. CACHE SAFETY OF THE LIVE LANE: the sender captures cloneForShadow(messages) BEFORE the transform mutates and reads declared-trim state before/after. Verify the shadow capture path cannot mutate the live messages array (shared references through cloneJson boundaries — is the clone deep and taken at the right point?), and that shadow-sender DB reads (getCompartments, getMemoriesByProject, etc.) hold no transactions that could contend with the transform's own writes.

=== DELIVERABLE (per member) ===
- Independent findings with file:line evidence. Read the real code.
- Rank each finding Critical / High / Medium / Low.
- Weight by blast radius: (1) can affect the LIVE user session = WORST class; (2) corrupts shadow state silently = defeats the soak's purpose; (3) merely loses shadow coverage but logged = ACCEPTABLE.
- Explicit false-positive filtering: check PARITY.md files and existing test coverage before claiming a bug. State what you checked.
- Your own verdict: SHIP (arm the soak on production sessions) or HOLD, with the single most important reason.

Relevant reference files exist at packages/pi-plugin/PARITY.md and packages/plugin/src/features/magic-context/smart-notes/PARITY.md. Test files live alongside sources (*.test.ts) and in crates/*/tests or #[cfg(test)] modules.

[assistant] 2026-07-10T10:37:25Z
I'm going to start by examining the repository structure and working through the key files mentioned in the audit scope, looking at the actual code with specific line references.
I'll conduct a thorough independent audit. Let me start by reading the key files mentioned in the scope.

[assistant] 2026-07-10T10:37:41Z
I need to continue reading the rest of the shadow-sender.ts file to see what comes after line 1191.
The shadow-sender.ts was truncated. Let me read the rest of it and the Rust handlers in parallel.

[assistant] 2026-07-10T10:37:47Z
I'm spotting a critical issue with the ordinal fallback mechanism in audit point 2c. The problem is in how `resolveOrdinalsForShadow` handles the session message cache — it's calling `readRawSessionMessages` inside `withRawSessionMessageCache`, but this creates a new cache scope outside the transform's own context, which could lead to cache misalignment when called from `enqueue()`. So when `enqueue()` is called from inside the transform's hot path, it's already within a `withRawSessionMessageCache` scope that's been primed with only tail messages. When `resolveOrdinalsForShadow` creates its own cache scope, it reuses that outer tail-only cache instead of creating a fresh one, meaning `ordinalById` only contains the tail messages. Messages below the floor threshold fall back to the by-id lookup, which raises the critical question: does that fallback compute ordinals on the same absolute basis as the tail-primed cache?

The tail cache uses absolute ordinals (baseOrdinal+1, etc.) as documented in the code, so I need to verify that `readRawSessionMessageById` computes ordinals the same way. I'm checking the database read functions to see how each one calculates ordinals and whether there's a silent mismatch that could corrupt the shadow store. The by-id fallback also opens its own read-only session database connection per call rather than using the cache.
Now let me read the Rust handlers and the raw session reader (critical for ordinal-basis check).

[assistant] 2026-07-10T10:38:10Z
I'm spotting a critical mismatch in how ordinals are calculated between the two database read functions. The full read operation computes ordinal as a 1-based position among filtered non-summary messages ordered by creation time and ID, but the by-ID fallback uses a different SQL approach to calculate ordinal.

Wait, actually looking closer at the SQL query — it's counting non-summary messages with timestamps and IDs less than or equal to the target message, which should give the same 1-based position. So the basis is consistent there. But there's a subtlety I'm catching: when the full reader processes rows through flatMap, malformed entries return an empty array while still advancing the index counter, which could create gaps in the ordinal sequence even though the index itself continues incrementing. The filter chain removes summary rows, then uses flatMap with index to assign ordinals—malformed rows that fail JSON parsing return an empty array, which skips their ordinal slot but still consumes the index position. This matches the by-id COUNT behavior, so the ordinal basis is consistent and the comment is accurate. Now I'm noticing another layer: the tail reader includes the anchor row, and when the cache is primed with tail-only data, the ordinals need to account for that.

The baseOrdinal should equal the anchor's absolute position as computed by COUNT, since the compaction marker's boundaryOrdinal comes from an ordinal walk over the full reader. If the marker was computed when messages were added or removed before it, there could be drift, but the memo mismatch check would catch changes within a generation. I think part (c) is solid.

Now I'm moving on to examine the Rust handlers in the lib.rs shadow section.
Now let me read the Rust shadow wire structs and handlers.

[assistant] 2026-07-10T10:38:32Z
I'm comparing the field structures between the TypeScript and Rust implementations of the state sync payload to identify any mismatches or missing fields in the wire format. So the Rust struct expects flat fields like `start_message` and `end_message` as required i64 values, but TypeScript is emitting nested objects with `start: {flat_id, bare_message_id, absolute_ordinal}` instead — this would cause deserialization to fail on the required field `start_message` since it's never sent. This looks like a wire-shape mismatch, though the audit mentioned this was supposedly already fixed at HEAD. I'm noticing a potential mismatch in how the TS serialization produces compartment objects versus what the Rust deserializer expects—the TS output has nested `start`/`end` objects with specific fields, but Rust's `ShadowCompartmentWire` requires flat `start_message` and `end_message` fields without an `id`. I need to trace through the actual deserialization path to confirm whether this is a real incompatibility or if there's a conversion step I'm missing.
Critical wire-shape signal detected. Let me read the compartment/memory wire structs fully and the handlers.

[assistant] 2026-07-10T10:38:55Z
I'm spotting a structural mismatch in the wire format. The Rust `ShadowCompartmentWire` struct defines required fields like `sequence`, `start_message`, and `end_message` as i64 values, plus optional string fields for message IDs and metadata, but the definition appears to cut off mid-field list.

The TypeScript serializer is emitting a completely different shape—it wraps the start and end data in nested objects with `absolute_ordinal` values, while the Rust side expects flat fields. This is a fundamental incompatibility: the TS code sends `start: { absolute_ordinal, ... }` and `end: { absolute_ordinal, ... }`, but Rust is looking for `start_message: i64` and `end_message: i64` as direct properties. The mismatch is critical and will cause deserialization to fail. The Rust wire struct expects `start_message`, `end_message`, and related fields, but the TypeScript serialization is sending nested `start` and `end` objects instead — so the deserialization would fail when trying to parse the compartments. Let me verify whether there's a custom deserializer or conversion happening that I'm missing.
Wire-shape mismatch signal on compartments. Let me read the handlers and the compartment conversion carefully.

[assistant] 2026-07-10T10:39:16Z
I've found a critical signal in the wire shape. The `serializeCompartment` function in shadow-sender.ts is emitting an object that captures both start and end positions with their flat IDs, message IDs, and ordinals, along with metadata like title, content, importance, episode type, and timestamps.

But the Rust `ShadowCompartmentWire` struct expects a completely different shape—it's looking for flat `start_message` and `end_message` fields as required i64 values, plus separate `start_message_id` and `end_message_id` string fields. The TypeScript side is sending nested objects under `start` and `end` keys with `absolute_ordinal`, `bare_message_id`, and `flat_id` properties, which don't map to what Rust is expecting. This is a fundamental wire-shape mismatch that would cause deserialization to fail since those required fields are missing entirely from the TypeScript output.

The issue is that this bug might not surface immediately in tests because on a fresh session with no compartments, the compartments array stays empty and never triggers the deserialization failure. The error would only occur once a compartment actually forms and gets serialized, which mocked tests might not exercise. Let me trace through the exact shape the TypeScript `serializeCompartment` function produces to confirm the mismatch. The comment on `toFlatWireBody` describes a fix for flattening the top-level `{method, params}` structure, but that's separate from the nested compartment shape issue. The mismatch between TypeScript's nested `start`/`end` objects and Rust's flat `start_message`/`end_message` fields could be a new bug that's still present. If serde tries to deserialize without those required fields, it would error out, and I need to trace how the TypeScript side handles that error response.

If it's not a peer-reject code, it becomes a generic `invalid_params` error that gets logged and ignored, which means state_sync would silently fail whenever a compartment exists. That's a serious problem — it would break shadow coverage for any real long-running session with compartments, defeating the soak's purpose. The whole pass would fail before reaching shadow_transform, so I need to verify whether this is actually a connection failure or just a parameter validation error. Examining the struct definition...

The `start_message` and `end_message` fields are indeed required i64 values with no defaults, but the TypeScript is emitting nested objects instead. I'm checking if there's a custom serializer or transformation happening in `buildStateSyncPayload` — looking at lines 646-657, the serialized compartment output goes directly into the payload with no intermediate transformation, so this mismatch would definitely cause the deserialization to fail.

Let me verify this isn't a false positive by checking if there's a custom Deserialize implementation or field aliases on the Rust side, or if the test suite actually exercises this path and passes.
Wire-shape mismatch signal on compartments is strong. TS `serializeCompartment` emits nested `start:{...}`/`end:{...}`, but Rust `ShadowCompartmentWire` requires flat `start_message`/`end_message` (i64, no default). Let me verify the handlers, isolation guards, and store methods.

[assistant] 2026-07-10T10:39:59Z
I'm spotting a critical mismatch between how the TypeScript serialization shapes compartment data versus what the Rust wire format expects. The TS function outputs a flat structure with nested start and end objects containing position metadata, but the Rust struct definition appears to require a different field arrangement. are required fields without defaults, so deserialization fails when compartments are present. The TypeScript side emits a completely different structure — nested `start` and `end` objects with `bare_message_id` inside them, while Rust expects flat `start_message` and `end_message` i64 fields plus separate `start_message_id` and `end_message_id` strings. This structural mismatch means any state_sync with compartments will reject with invalid_params, especially on cold starts when all compartments get included.

Let me verify this against the actual serialization code to confirm whether this is the unfixed bug separate from the wire-shape flattening that was already corrected. Finishing the Rust struct defaults, then realizing the core issue: TypeScript is sending nested `start` and `end` objects instead of the flat `start_message` and `end_message` integers that Rust requires. Since those fields have no defaults, deserialization fails with invalid_params, which means every state_sync carrying compartments gets rejected. This breaks the entire shadow store synchronization—it never receives the compartment state it needs, causing guaranteed divergence or treating the rejection as a peer failure.

Now I'm tracing what happens when state_sync fails: the transport layer throws on the error frame, and the catch block checks if it's a peer rejection or connection failure. But invalid_params with a "missing field" error message doesn't match the peer-reject patterns, so it just gets logged as a send failure and the pass is dropped, losing shadow coverage entirely.

This only matters when compartments are present, which means the soak would either show constant divergence or constant state_sync failures — either way, no useful signal for sessions with compartments. But I need to verify this is actually happening. Let me check if there's some alternate serialization path that flattens the nested compartment structure into the wire format, or if the compartments array always goes through as-is.

Actually, wait — I should double-check the Rust struct definition to see if `start_message` and `end_message` have default values that would make deserialization work anyway. Looking at the struct definition around lines 268-270, I can see the field declarations...

No `#[serde(default)]` attribute on `sequence`, `start_message`, or `end_message`, which means all three are required fields. The TypeScript side provides `sequence` but omits `start_message` and `end_message` entirely. This is definitely a mismatch.

Let me verify this is actually a live bug by checking if the compartment serialization might have been updated to flatten the structure. Looking at the current `serializeCompartment` code, it's still emitting the nested `start` and `end` objects with `absolute_ordinal` inside them. The Rust struct expects those fields at the top level, so there's no way for the serde deserialization to map them correctly — no flatten directive, no aliases. This is a confirmed mismatch at HEAD.

This is a High severity finding because it silently defeats shadow coverage for any session with compartments. The soak would appear to run normally but produce no valid comparison data for compartmentalized sessions. The failures are logged in the counter, but the impact is that the coverage purpose is defeated without obvious visibility.

Let me double-check there's no other code path that could be building these payloads differently. The state sync payload goes through `serializeCompartment`, which is the only place compartments are serialized. Confirmed.

Now I need to verify the memory wire format matches expectations.

Comparing the TypeScript memory emission against what Rust's ShadowMemoryWire expects: TypeScript includes an extra `project_path` field that Rust ignores (serde doesn't have deny_unknown_fields configured), and TypeScript is missing `classified_at` which Rust has a default for. The required `id` field is present in TypeScript's output, and `verification_status` is also being emitted correctly. So the memories should parse without issues.

For memory mutations, TypeScript provides all the required fields that Rust expects: `id`, `mutation_type`, `target_memory_id`, plus the optional ones. The extra `project_path` gets ignored again. But I'm noticing that TypeScript is emitting an `m0_mutations` array that doesn't exist in Rust's `ShadowStateSyncWire` struct at all — checking the struct definition, there's no `m0_mutations` field, and no `watermarks` field either.

Since serde is configured to ignore unknown fields, both `watermarks` and `m0_mutations` are silently dropped when the message arrives. That's not causing a rejection, but it does raise a question about whether this silent dropping of `m0_mutations` is intentional, especially since the Rust shadow store doesn't seem to consume them. The Rust side expects `acked_watermarks` as optional, but TypeScript sends `watermarks` instead, so it defaults to None—that's fine. The real issue is compartments: when they're nested instead of flat, the whole state_sync gets rejected, and I need to check if any tests actually catch this or if it's a gap in coverage. Let me verify the required fields match up—`expected_shadow_seq` and `shadow_generation` are both being sent correctly from TypeScript, so the only blocking problem is the compartments structure mismatch. I need to check whether there's an integration test that would catch this compartment serialization mismatch, and examine the isolation guards and store methods that handle shadow state syncing to understand the full scope of the issue.
Strong wire-shape mismatch signal on compartments. TS `serializeCompartment` emits nested `start:{...}`/`end:{...}`, but Rust `ShadowCompartmentWire` requires flat `start_message`/`end_message` (required i64, no default). Let me verify isolation guards, the store CAS, and check test/PARITY coverage before claiming.

[assistant] 2026-07-10T10:41:05Z
I need to dig into the real_daemon test to see how it constructs the state_sync payload and whether it's using the flat Rust shape or matching the nested TS structure — this is likely where the wire-shape mismatch is happening. Let me check how compartments are built in that test and whether there's an integration test that goes through the full serialization path.

The core issue is clear: TS `serializeCompartment` outputs nested objects like `start: {absolute_ordinal, bare_message_id}` and `end: {...}`, but Rust's `ShadowCompartmentWire` expects flat required fields `start_message` and `end_message` at the top level. Since those fields have no `#[serde(default)]`, deserialization will fail when it can't find them — the entire payload will be rejected. The `start_message` and `end_message` fields in the Rust struct lack `#[serde(default)]` annotations, making them required during deserialization — so when the TypeScript serialization omits these fields, serde_json throws a "missing field" error that rejects the entire state_sync. This is a critical wire-shape mismatch in the compartments structure, distinct from the earlier flattening bug but equally problematic for the sync protocol.

The real_daemon.rs test constructs StoredCompartment directly in Rust rather than going through the TypeScript serializer, so it never actually exercises the TS-to-wire shape transformation. Similarly, the TypeScript tests likely mock the transport layer and only assert on the top-level `{method, params}` structure without cross-validating against what Rust's serde expects — exactly the kind of gap where mocks miss integration issues.

Now I need to verify whether `serializeCompartment` is actually the function being used in the wire path, or if there's some other builder or flattening step I'm missing. Looking at `buildStateSyncPayload`, it pushes serialized compartments into an array, then assigns that array to `params.compartments`, and `toFlatWireBody` spreads params to the top level with `{method, ...params}` — so the compartments array stays nested. Let me check if Rust has a custom deserializer or serde flatten attribute that might handle this differently.

The struct definition shows plain fields like `start_message: i64` with no flatten directive, which means a nested `start` object would be treated as an unknown field and ignored, while `start_message` itself would be missing — causing a hard error since these fields have no default. This is a critical issue: every state_sync message carrying a compartment gets rejected with invalid_params, so the shadow store never receives compartments, leading to guaranteed divergence. When the TS side gets this invalid_params response in `processPass`, it propagates to `runQueue`'s error handler, but invalid_params doesn't match the generation-related patterns that `isPeerReject` checks for, so it's not treated as a peer rejection. sessions, making the soak produce zero comparison data while appearing to run). Actually, it's worse than just losing coverage — the failure likely loops: state_sync fails, send_failures increments, but requireResetReason never gets set (not a peer reject, not a connection issue), so the next pass tries again with force=true since it never acked. This creates a permanent loss of shadow coverage for compartmentalized sessions, bounded to one attempt per pass and logged. By their weighting that's "class 3 acceptable," but it means the soak gathers no data on the exact sessions that matter most.

I'm ranking this HIGH because while it doesn't corrupt live data or silently corrupt shadow data, it effectively neuters the soak's ability to test compartmentalized sessions. Let me double-check whether I'm missing something — maybe there's a flat serialization step I overlooked, or the Rust side is intended to receive nested structures. Looking at the struct definition again: start_message is i64, and the wire-to-storage mapping preserves that directly, so the Rust side definitely expects flat fields.

This means either TypeScript should be emitting flat start_message and end_message fields but isn't, or there's a translation layer I haven't found. Let me search the TypeScript codebase for start_message to see if anything else is emitting that flat field, and I should also check what the real_daemon test actually sends in its wire JSON — that'll show me the ground truth of what shape Rust is expecting.
Compartment wire-shape mismatch is a strong signal. Let me verify against the integration test's actual wire JSON and search TS for any flat `start_message` emission.

[assistant] 2026-07-10T10:41:53Z
The grep search didn't turn up `start_message` or `serializeCompartment` in the shadow-sender.ts compartment serialization, but I can see from the code that `serializeCompartment` does emit `start` and `end` objects with the right fields like `flat_id`, `bare_message_id`, and `absolute_ordinal`. The Rust `ShadowCompartmentWire` struct expects `start_message` and `end_message` as required i64 fields without any `#[serde(default)]` annotation, so there's a mismatch in what's being serialized versus what's required.

Looking at the actual TypeScript output, it's emitting `sequence` correctly but the `start` and `end` are nested objects with `absolute_ordinal` instead of flat `start_message` and `end_message` fields, which means the deserialization would fail because those required fields are missing.

The issue only surfaces once compartments actually exist — on a fresh session with an empty array, the state_sync succeeds, but as soon as the first compartment is created and included in the payload, the entire request fails to deserialize with an invalid_params error. The mocks never catch this because they bypass actual serde parsing. Let me check if there's a field alias or flattening I'm missing. Tracing through what happens when state_sync fails with an invalid_params error — the transport call throws, and in the catch block it checks if this is a peer rejection, but invalid_params isn't in that list and the error message won't match the generation/seq/CAS patterns, so it just logs the send failure and increments the counter rather than treating it as a critical issue.

The real problem emerges: once any compartment exists in the session, the shadow lane's state_sync permanently fails, so the shadow store never receives those compartments. Every shadow_transform then runs against an empty or stale store, guaranteeing divergence on each pass. This defeats the whole purpose of the soak test — it's technically logged via send_failures, so it's not silent corruption, but it's still a class 2 or 3 issue that renders the shadow coverage useless.

But wait, let me reconsider whether I'm actually reading this correctly. Maybe `start_message` and `end_message` are supposed to live at the compartment top level and the TypeScript definition is wrong, or maybe the Rust side added nested parsing that I'm missing. This feels like the exact kind of wire-shape mismatch that mocked tests would miss — the same bug class that was already found and fixed for the transform body. The compartment nesting might be the next instance of that same problem.

The prompt mentions two integration bugs were already fixed: wire-shape mismatch and ordinal starvation. The wire-shape fix was in `toFlatWireBody` for flattening `{method,params}`. The compartment nested-versus-flat issue could be a separate unfixed instance of the same bug class. Looking at the real daemon test, it constructs `StoredCompartment` directly with `start_message` and `end_message` at lines 302-303, completely bypassing wire deserialization — which means the test never actually exercises the JSON shape that `serializeCompartment` produces. This strongly suggests the finding is real.

Now I'm checking whether `serializeCompartment` actually outputs `start_message`, and looking for any separate flattening paths or custom deserializers in `ShadowCompartmentWire` or its `From` implementation. I'm also comparing the TypeScript memory emission against the Rust `ShadowMemoryWire` structure to see if there's a field mismatch there too.

The TypeScript side emits a full set of memory fields including `project_path`, while Rust's version has `#[serde(default)]` for `classified_at` and ignores unknown fields like `project_path` by default. The `id` field is required on both sides, so memories should deserialize correctly. Now I'm looking at the memory mutations schema to see if those fields align as well. `0_mutations` field in the Rust struct, so TypeScript's m0_mutations get silently dropped by serde — that's a potential divergence if those mutations affect the shadow transform. I'm also noticing a mismatch with watermarks: TypeScript sends `watermarks` but Rust expects `acked_watermarks`, which could be another silent field drop depending on how serde is configured. The compartment structure mismatch is the real issue here — on a cold start when `acked.compartment_sequence` is -1, every compartment gets sent in the first state_sync, which triggers invalid_params and causes a permanent failure. I need to trace through whether this state_sync error actually blocks the transform or just gets logged in the catch handler.

Looking at the flow: if state_sync throws, it propagates up and gets caught, incrementing send_failures but the transform doesn't get sent that pass. The problem is that since state_sync never succeeds, `lastAckedWatermarks` stays null, forcing a retry every single time with the same compartments, creating an infinite failure loop. This is worse than just losing coverage — the soak gate would show near-100% send_failures for any real session with compartments, and if someone only checks divergence counts they'd see zero divergences and think everything's working when actually no transforms are running at all. teria is "divergences == 0", they'd wrongly pass. This is a critical gap that needs flagging.

Let me verify serializeCompartment one more time to confirm it doesn't include start_message or end_message fields. Looking at the return object, it only has id, sequence, start/end with flat_id and ordinals, plus metadata like title and content—but no start_message or end_message at the top level. Rust deserialization will fail without those required fields, so this is definitely a hard failure point.

Now I'm wondering whether `#[serde(default)]` on the compartments field would mask this error. If the compartments key is missing entirely, it uses an empty vec, but if the key exists with an array of objects, each object must deserialize successfully—a missing required field in any element causes the whole request to fail. There's no `deny_unknown_fields` override, so serde's default behavior applies: missing required non-Option fields error out. This is a very solid finding, so let me check the transform body wire structure next.

I've identified a few other candidates: `m0_mutations` sent by TypeScript but absent from `ShadowStateSyncWire` gets silently dropped, which is a medium-severity gap since it's unclear whether m0 mutations affect the transform output. There's also a name mismatch between `watermarks` on the TypeScript side and `acked_watermarks` in Rust, which is low to medium severity. Now I need to look at the hot-path safety and ordinal fallback basis mismatch, plus the isolation implications.

Looking at the `ShadowPassInputs` wire definition, TypeScript sends `now_ms`, `model_key`, `usage` with `input_tokens` and `limit`, `effective_execute_threshold`, `cache_ttl`, and `provider_error`. Rust's version expects the same fields with appropriate defaults and aliases — the usage fields map correctly through the aliases. That part checks out. The real concern is the ordinal fallback basis mismatch in `resolveOrdinalsForShadow`, where it first builds ordinals from tail-primed raw session messages using absolute ordinals via `baseOrdinal`, then falls back to reading individual messages by ID when needed.

The fallback uses a COUNT query that counts all non-summary messages up to and including the target message, ordered by creation time and ID, which gives the absolute 1-based ordinal in the same ordering as the full message list. The filter logic between the full reader and COUNT query should match semantically — both exclude summary messages with finish="stop" — but there's a potential issue with how ordinal assignment works. The full reader assigns ordinal based on the filtered row's position in the sorted order, while COUNT tallies all filtered rows up to and including the target, so I need to verify these produce the same ordinal value for the target row itself.

The real concern is whether the tail-primed cache's ordinals, which derive from the marker's stored boundaryOrdinal plus an offset, use the same ordinal basis as the COUNT-based fallback. If the marker was created when the filter logic differed or counted rows differently, the two ordinal systems could diverge, causing inconsistency between cached and computed ordinals. 500, but a below-floor message's COUNT-based ordinal says 501 even though it comes earlier — creating a discontinuity within the same pass that poisons the shadow store without triggering the memo check, which only validates consistency per message ID across passes, not monotonicity within one. The real question is whether the tail prime's baseOrdinal (derived from the marker's boundaryOrdinal) matches what COUNT would produce over the same filtered ordering, which hinges on whether the marker's ordinal was computed identically.

The risk surfaces because readRawSessionMessageById bypasses the tail-prime and does a fresh COUNT, while readRawSessionMessages might use the baseOrdinal — so if a single pass mixes tail-primed and below-floor messages, they need to agree at the boundary, and I can't fully verify that without tracing the marker-set logic. The real transform also uses that same tail-prime base ordinal, but it doesn't mix ordinal bases the way the shadow lane does—the shadow lane is where the divergence becomes problematic. If the base ordinal has drifted due to message deletions below the compaction boundary, the shadow store could silently diverge from the real lane without detection. This is a real correctness risk I should flag as Medium severity.

Now I'm looking at the hot-path safety implications. The enqueue function runs synchronously in the transform's critical path and calls resolveOrdinalsForShadow, which does a deep clone of all input messages—potentially hundreds of them—using JSON serialization. That alone could take tens of milliseconds. Then it either does a tail-primed read or falls back to by-id database lookups. All of this happens synchronously before the transform returns to the user. I need to find where shadowSender.enqueue is actually called to understand if this blocking work happens before or after the user gets their result.

But there's another layer: denormalizeShadowOutput performs a second deep clone on the output messages, which could be even larger than the input. So per pass, we're doing two full JSON deep-clones synchronously on the hot path. For a 400-message session with substantial tool outputs, that serialization and parsing could easily add 50-200ms of latency to every single user prompt in shadow mode. The real question is whether this is on the critical path that blocks the user's response or if it happens after they've already received their answer.

Now I need to check the call site and verify exception safety. If resolveOrdinalsForShadow or denormalizeShadowOutput throws—whether from cloneJson hitting a BigInt or circular reference, or from a database read failing with SQLITE_BUSY—that exception propagates directly out of enqueue with no try/catch wrapping it. If the caller doesn't guard the enqueue call, the user's transform could crash. That's a class 1 issue affecting the live session. Let me find where enqueue is actually called and whether it's protected.
Compartment wire-shape mismatch confirmed at the struct level. Now let me find the enqueue call site (hot-path guard) and check store CAS + isolation. Let me also verify the exact TS-vs-Rust field for `m0_mutations` and `watermarks`.

[assistant] 2026-07-10T10:43:54Z
I'm checking the transform.ts enqueue logic and the cloneForShadow implementation, then verifying how the state_sync wire is constructed—the TypeScript side builds it with nested compartment serialization, but the Rust ShadowCompartmentWire expects flat start_message and end_message fields that are required with no defaults, which creates a fundamental mismatch.

The state_sync deserializes directly from the flattened wire body, and each compartment gets serialized with id, sequence, and start/end objects containing flat_id and bare_message_id, but the Rust side is looking for start_message and end_message as i64 fields that are completely missing—so any state_sync carrying compartments will fail with an invalid_params error. The only exception is when compartments is empty, meaning sessions with actual compartments will reject state_sync as soon as a new compartment appears.

When transport.call throws on state_sync, it's caught generically as a send failure, logged, and the pass is dropped without advancing lastAckedWatermarks. This means every subsequent pass re-attempts and re-fails, completely losing shadow coverage for any mature session with compartments—which defeats the whole purpose of the soak. I'm checking whether serde actually requires these top-level fields or if there's some nested parsing I'm missing.

Looking at the serialization code, serializeCompartment definitely doesn't emit `start_message` or `end_message` as flat fields—it only outputs nested `start` and `end` objects. Since serde requires the missing `start_message` field and doesn't have `deny_unknown_fields`, this is a hard rejection. Now I need to verify whether there's test coverage for the state_sync wire format with compartments included. ify whether there's an actual round-trip test that deserializes TS-built state_sync JSON through the Rust parser. I need to check shadow-sender.test.ts and real_daemon.rs to see if they're sending real JSON, and also re-examine the ShadowCompartmentWire struct definition to confirm whether `start_message` and `end_message` are truly required fields — they appear to be (no `#[serde(default)]` annotation), which means TS must be emitting them, but I should verify that's actually happening.

This looks like a genuine, high-severity mismatch: the Rust schema expects `start_message` and `end_message` as required fields, but TypeScript is emitting nested `start` and `end` instead. I need to trace through the store's apply_shadow_state_sync function, the transform.ts enqueue path, and the cloneForShadow logic to confirm where this discrepancy originates and whether there's an undocumented alias or transformation I'm missing.
Compartment wire mismatch confirmed at struct level. Now the enqueue hot-path site, cloneForShadow, and the store CAS methods.

[assistant] 2026-07-10T10:44:32Z
Looking at the enqueue operation now. It's wrapped in a try/catch block for safety on the hot path, which is good. The enqueue function itself runs synchronously—it resolves ordinals for the shadow state through database reads, denormalizes the output with cloning, then queues work and schedules execution.

The synchronous cost is significant: resolveOrdinalsForShadow does a deep clone of the entire message array (expensive on large arrays) and reads raw session messages via a cache. The cache scope is created fresh inside resolveOrdinalsForShadow itself, so if there's no outer cache scope active, it triggers a full session read from the database—that's a potentially expensive O(session) operation right on the hot path. I need to verify whether enqueue is still running inside the transform's own cache scope or if it's outside of it.

Actually, looking at the full picture, there are three separate deep JSON clones happening per pass: one capturing the input before the transform, another inside resolveOrdinalsForShadow cloning the input again, and a third cloning the output array. With ~400-message arrays, that's three synchronous deep clones blocking the user's real prompt—this is definitely a Medium-level performance concern. Now I need to dig into the compartment wire mismatch issue. The TS serialization sends nested `start` and `end` objects with message details, but Rust's `ShadowCompartmentWire` expects flat `start_message` and `end_message` fields as required i64 values with no defaults—so deserialization would fail on every state_sync containing a compartment with a "missing field" error.

This only happens when compartments are actually present; empty compartment vectors skip the per-element parsing and succeed. So the shadow lane works fine until a compartment gets created, then state_sync starts rejecting with `invalid_params`. The error gets wrapped as `invalid_params_error`, and on the TypeScript side the peer rejection check looks for generation/seq/CAS codes to determine the failure type. So if state_sync throws an invalid_params error, that exception bubbles up and stops processPass entirely — the transform never gets sent, and the shadow comparison never runs. The session gets stuck in a loop where buildStateSyncPayload keeps generating the same compartment payload (since the watermarks haven't changed), hits the same failure, and never makes progress. Once any compartment exists, the watermarks shift to include compartment_sequence, which keeps triggering rebuilds of the same failing payload.

This is a critical bug that defeats the entire purpose of the soak for compartment scenarios — the session loses shadow coverage from the first compartment onward. Let me double-check the serialization output shape to confirm this is really what's happening.

Looking at the serialized compartment structure, the fields `start_message` and `end_message` are nested under `start` and `end` objects, not at the top level where Rust expects them. This will cause deserialization to fail since Rust requires those as top-level required i64 fields. But wait — the integration test constructs `StoredCompartment` directly with `start_message` and `end_message`, so there's a mismatch between what the test assumes and what's actually being serialized.

The real issue is that `toFlatWireBody` just spreads the params without flattening the compartment internals, so the nested structure stays intact. And checking the Rust side, `ShadowCompartmentWire` uses standard derived deserialization with no aliases or custom logic to handle the `start`/`end` naming — it's looking for `start_message` and `end_message` directly.

But wait, maybe this was already addressed in that earlier commit about wire-shape mismatches, or there's a custom deserializer I'm missing. Let me verify whether there's a `#[serde(flatten)]` or a `From` implementation, and check if the test actually sends compartments and validates the shape.

Looking at the test file to see what buildStateSyncPayload actually asserts when compartments are involved — that'll show me if the mock is expecting the wrong structure. Also, I need to verify the memory field names more carefully: the TypeScript side emits `project_path` but ShadowMemoryWire doesn't have that field, though serde ignores unknown fields by default so that's probably fine. But there's a bigger issue — TypeScript sends `m0_mutations` in the array, but ShadowStateSyncWire has no `m0_mutations` field at all. There's also a mismatch with `watermarks` — TypeScript sends a `watermarks` object but Rust expects `acked_watermarks` (optional), so that field gets silently dropped too. Rust computes its own acked_watermarks as a fallback, so it's probably fine for correctness but represents dead payload from TypeScript. The real issue is the compartment nested shape — I need to verify test coverage of the wire format and check the store CAS for isolation guarantees, plus confirm how `expected_shadow_seq` flows through the system.

Now checking the ShadowTransformWire required fields against what TypeScript actually sends. The `pass_inputs` field is required in Rust but I need to verify TypeScript is populating `now_ms` and `effective_execute_threshold` correctly.

The real issue surfaces when looking at state_sync compartments — they're breaking the flow. Since state_sync throws on invalid parameters before the transform even gets called, I need to trace through whether state_sync is only constructed when watermarks differ, and on a fresh session with compartments, what happens on that first pass.

With lastAckedWatermarks null, force becomes true, the payload builds with compartments, state_sync throws invalid_params, and that gets logged as a send failure without triggering a peer reject or connection failure. The watermarks stay null, so the next pass repeats the same cycle — permanently stuck with zero transforms compared for any session containing compartments. This is exactly what the soak test should catch, and it's a high-severity issue.

Now I need to check the test file to confirm the mock transport doesn't mask this and whether buildStateSyncPayload validates the nested structure properly.
Compartment wire mismatch is now confirmed at the struct level. Let me verify test coverage (false-positive filter) and read the store CAS/isolation methods.

[assistant] 2026-07-10T10:45:48Z
I need to check those specific test cases to see if they validate the compartment structure against what the Rust parser expects—that's the key to filtering out false positives.
Let me read the specific state_sync/compartment test to check whether it validates the wire against the actual Rust struct.

[assistant] 2026-07-10T10:45:54Z
I'm seeing the real issue now — the test at line 383 only checks that `compartments` is an array, but never validates what's actually inside each compartment. Since the test data has no compartments at all, this gap in coverage means the mismatch between the TypeScript type definition and the actual serialized shape would slip right through. Let me look at what the TypeScript serialization is actually producing.

The TypeScript `serializeCompartment` function outputs a structure with nested objects like `start` and `end` containing `flat_id`, `bare_message_id`, and `absolute_ordinal`, but the Rust side expects flat fields like `start_message` and `end_message` as required integers. When the TypeScript data gets sent to Rust, those required fields will be missing, causing a deserialization failure with a "missing field" error.

This breaks the entire `state_sync` operation whenever a session has compartments (which is nearly every non-trivial session), and it's the same class of wire-shape mismatch bug as the two already-fixed issues. The extra fields like `id`, `start`, and `end` from TypeScript get silently ignored by serde, but the missing required fields without defaults cause the whole operation to fail. The error gets caught in `runQueue`'s exception handler, but since `invalid_params` doesn't match any of the peer rejection codes (stale_generation, shadow_generation_mismatch, etc.) and the error message doesn't contain connection-related keywords, it's treated as neither a peer rejection nor a connection failure—just incremented as a send failure and logged.

This means the pass gets dropped and retried, but fails identically every time. For any session with compartments, `state_sync` permanently fails before `lastAckedWatermarks` can be updated, so `force` remains true and keeps re-sending the full compartment list on every pass. Since the throw happens before the shadow transform runs, the shadow store never receives compartments and shadow coverage stays at zero for those sessions.

The failure is logged (`shadow: send failed (ignored)`) and increments the send_failures counter, which technically classifies it as acceptable under the audit's weighting — but it completely defeats the soak's purpose since nearly every real production session has compartments. You'd end up with near-zero transform comparisons and a flood of send failures, making this a high-severity finding that undermines the entire soak.

Looking more carefully at the logic: state_sync only sends compartments where `sequence > acked.compartment_sequence`, and on cold start acked is -1, so all compartments get sent. Since any session with compartments fails this check, and production sessions inherently have compartments after the first boundary, the soak becomes essentially blind to meaningful data. This is a serious issue.

Let me verify that TypeScript never emits `start_message`/`end_message` directly — checking the grep results, the only TS emissions of those are in test files and SQL tests. The actual `serializeCompartment` in shadow-sender.ts emits nested `start`/`end` instead, which confirms the mismatch.

The real_daemon.rs test constructs `StoredCompartment` directly in Rust without going through the TypeScript serializer → wire → serde path, so the integration test completely bypasses the actual serialization flow that would catch this discrepancy. That's why the mock never caught it. Let me verify `serializeCompartment` is the only path and there's no alternate flat emission — re-reading the code confirms it's the sole serializer, returning an object with the nested structure.

So this is a confirmed wire-shape mismatch: Rust expects `start_message` and `end_message` as required fields at the top level, but TypeScript is emitting them nested under `start` and `end` instead. This is a high-severity bug that defeats soak coverage for any session with compartments — same class as the two already-fixed issues. Serde will reject this during deserialization since the required fields won't be found.

Even if `start_message` were somehow defaulted, the compartment boundary ordinals would end up as 0/0, poisoning the shadow store. But the hard rejection happens first.

This is my top finding. Now I need to quickly check the other areas: the store CAS operation, the SQLITE_BUSY issue on by-id reads, the cloneForShadow timing, and the reset race condition.

Actually, I'm noticing something else — the TypeScript side sends `m0_mutations` in the state sync payload, but the Rust struct doesn't have that field at all. Let me verify the Rust definition. Looking at lines 185-200, I see session_id, shadow_generation, expected_shadow_seq, compartments, memories, memory_mutations, last_todo_state, acked_watermarks — but no `m0_mutations`. Since there's no `deny_unknown_fields` set, the field is silently dropped when deserialized.

This is a real issue: if m0 mutations aren't reaching the shadow store, the m[0] materialization could diverge from what TypeScript has. The watermark tracks m0_mutation_id and last_todo_state_hash, so the infrastructure is aware of m0 mutations, but they're being silently discarded. That's a silent shadow desync risk — the soak tests might catch divergence, but only if they're actually comparing the right state.

Then there's the watermarks field mismatch: TypeScript sends a `watermarks` object in the state sync, but Rust's wire format expects `acked_watermarks` instead. Since TypeScript doesn't send `acked_watermarks`, Rust computes it from the compartments and memories max values as a fallback. I need to figure out whether that computed default actually matches what TypeScript intended. `watermarks` and `m0_mutations` are silently dropped during the conversion, but the real blocker is the compartments field mismatch from Finding 1, which causes the entire state_sync operation to fail. For fresh sessions with no compartments, these drops would matter, but the hard rejection on compartments dominates the impact.

Now I'm checking the memory wire field mappings — most fields align between TypeScript and Rust, but `project_path` is notably absent from the Rust `ShadowMemoryWire` structure. However, this is intentional since the Rust side derives it from the session's `shadow_project_path`, so the drop is expected.

Continuing through the field verification, all the timestamp and status fields match up correctly, and the Rust schema has the required fields populated. For `memory_mutations`, I'm comparing the TypeScript structure against the Rust `ShadowMemoryMutationWire` — the core fields like `id`, `mutation_type`, and `target_memory_id` are present and required as expected.

I notice that `m0_mutations` exists in TypeScript but is completely absent from the Rust schema, so that entire array gets dropped. Moving on to `pass_inputs`, I'm checking the TypeScript definition against the Rust `ShadowPassInputs` structure — the required fields like `now_ms` and `effective_execute_threshold` are there, while optional fields like `model_key` and `usage` are properly marked as optional in Rust.

There's also a `provider_error` field in the TypeScript interface that doesn't get set during the transform, and Rust has it as optional too, so that's fine. The `shadow_transform` function parameters in TypeScript include `shadow_generation`, `input`, `ts_output`, `normalizations`, `pass_inputs`, `ts_decision`, and `declared_trim`, and I'm verifying these map correctly to the Rust `ShadowTransformWire` structure.

Now I need to check the `DeclaredTrim` struct definition since it's sent with every transform and any shape mismatch would cause the transform to reject.

Looking at how ordinals are resolved in `resolveOrdinalsForShadow` — it first tries a full read of raw session messages, then falls back to reading by ID for messages below the floor. The by-ID path computes ordinals using a COUNT query with time-based ordering, while the full read assigns ordinals by index after filtering and sorting. Both paths use the same summary row filtering and time-based ordering logic, so the ordinal computation should be consistent.

Now I'm checking whether the cache gets primed correctly — `resolveOrdinalsForShadow` runs inside `enqueue`, which might be outside the transform's cache scope, so I need to verify the cache state at that point. So the COUNT-based ordinal computation is consistent, and the nested `withRawSessionMessageCache` call reuses the outer cache from the transform rather than creating a fresh one, which means it still gets the tail-only slice as expected. Now I need to double-check whether `enqueue` is actually executing within that transform's cache scope.

Shifting to the SQLITE_BUSY concern: if the OpenCode database gets locked during a write and `readRawSessionMessageById` tries to open a read-only handle through `withReadOnlySessionDb`, whether it throws depends on whether the database uses WAL mode and what the busy_timeout is set to. Under WAL, readers shouldn't block on writers, but if busy_timeout is zero and a write is in progress, a read could still throw SQLITE_BUSY.

That exception would bubble up through `resolveOrdinalsForShadow` inside `enqueue`, which is wrapped in a try/catch at transform.ts 2186-2228, so the error gets caught and logged with the shadow pass skipped — not a fatal path issue. But the timing matters: `enqueue` is called after the transform completes at line 2180, yet it's still inside the transform function body before the return, so any stall there would delay the entire response back to the user.

Now I'm looking at what makes `enqueue` expensive — the deep clone of all input messages via `cloneJson` could be tens or even over 100ms for a large context window, plus additional per-message processing.

Then `denormalizeShadowOutput` does another full deep clone of the output array, so we're looking at two synchronous JSON clones of potentially massive message arrays on every transform pass when shadow is enabled. For a 400-message context spanning 200KB to 2MB, that's a real latency hit that the audit flagged as a medium-high severity concern, especially once this ships to production. So there are actually three separate deep clones happening per shadow pass: one at the start before transform, then two more during the enqueue phase after transform. All of them are synchronous and sit directly in the user's request path, which means this contradicts the design goal of not affecting the live session. This is a legitimate latency concern—High severity because it measurably impacts the user's prompt latency, though the actual impact depends on message array size. JSON.parse(JSON.stringify) strips non-enumerable properties and functions, but since cloneForShadow operates on MessageLike (the transformed messages) rather than RawMessage, and the absolute_ordinal annotation gets added to the clone, this shouldn't be an issue — the message data is JSON-origin anyway, so the serialization behavior is acceptable.

Now I'm tracing through the reset-race and blockedUntilReset logic: resetSession queues a reset and sets blockedUntilReset to true, performReset clears it; the enqueue mismatch path does the same; and the runQueue catch handler for peer-reject also sets blockedUntilReset and queues a reset.

But there's a gap in the peer-reject path — it sets the flags without immediately pushing a reset work item. Instead, the reset happens on the next processPass call when it sees requireResetReason set. If no more passes are queued, the session stays blocked until a new pass arrives, which then triggers the reset through enqueue → resolveOrdinals → pushWork(pass) → processPass → performReset. So it's not permanently wedged as long as new passes keep coming.

There's a potential edge case though: if the queue empties right after a peer reject and the session goes idle with no new passes arriving, blockedUntilReset remains set with no reset queued. This just means shadow coverage is lost for an idle session, which is acceptable.

Now looking at the reset race condition where a reset races against a queued pass — the FIFO promise chain in SubcShadowTransport serializes...

Within a session, the running flag ensures only one runQueue executes at a time, so there's no concurrency there. But across operations, enqueue's resolveOrdinals mismatch path clears the queue synchronously while runQueue might be mid-iteration. Since JavaScript is single-threaded, when enqueue mutates the queue array, that change is visible at the next loop check in runQueue, so the already-shifted item is already being...

Now I need to check the store's CAS operation for idempotency and isolation, plus examine the DeclaredTrim struct and shadow methods. I'm also wondering whether a panic in the shadow_transform handler during a compare operation could affect the real lane — since both use the same Arc<McStore> handle but operate on different session IDs and project paths, a panic in an async handler might unwind or abort the whole process.

The key concern is whether McStore serializes all writes through a single mutex or connection, since that could create contention between shadow and real writes. Let me search for DeclaredTrim and read through the store's shadow section to understand the locking behavior.
Compartment wire mismatch confirmed. Now let me verify the store CAS/isolation, the `DeclaredTrim` Rust struct (sent every transform), and whether the store shares a lock between lanes.

[assistant] 2026-07-10T10:48:37Z
I'm comparing the DeclaredTrim struct between the TypeScript and Rust implementations, checking how the store handles shadow state synchronization and session resets. I'm also verifying the field mappings—particularly how `effective_execute_threshold` is aliased, and how the `usage` field differs between the two versions, with Rust using an optional type with a default serde attribute while TypeScript requires it as an object.
Let me read the Rust `DeclaredTrim` struct (sent on every transform) and the store CAS methods.

[assistant] 2026-07-10T10:49:18Z
Let me try again.

[assistant] 2026-07-10T10:49:36Z
I'm checking that the Rust `DeclaredTrim` struct has all the required fields matching what the TypeScript `ShadowDeclaredTrim` emits—`flat_boundary_id`, `boundary_bare_message_id`, `boundary_absolute_ordinal`, and `next_absolute_ordinal`—and they line up correctly.

Now I need to look at the store CAS methods to understand how they work.
DeclaredTrim matches TS exactly. Now the store CAS/isolation methods.

[assistant] 2026-07-10T10:50:08Z
I'm noticing something important about the isolation mechanism: when shadow compartments are written to the `mc_compartments` table, they're keyed by a session_id that's prefixed with "shadow:" to keep them separate from the real session. This ensures compartment isolation works correctly. But I need to check how memories are handled in the shadow state replacement logic to confirm the isolation holds there too.

Now I'm zeroing in on the actual problem: both the real lane and shadow lane read from the same `mc_compartments` table, but they use different session_id keys — the real lane uses the actual session_id while the shadow uses the prefixed version. So they shouldn't collide. The real issue appears to be a shape mismatch in how compartments are serialized on the TypeScript side versus what the Rust backend expects.

The TypeScript serializer outputs a nested structure with `start` and `end` objects containing flat_id, bare_message_id, and absolute_ordinal, plus an `id` field at the top level. But the Rust wire struct expects flat fields like `start_message` and `end_message` as required i64 values with no defaults, along with optional `start_message_id` and `end_message_id` strings. When the TS sends its nested object format, the Rust deserializer can't find those required top-level fields and fails.

The failure cascades through `handle_shadow_state_sync_value` at the `serde_json::from_value` call — even though `compartments` itself has a default, each compartment object in the array must deserialize successfully, and a single missing `start_message` field causes the entire `ShadowStateSyncWire` deserialization to fail and return an invalid_params_error.

This means every state_sync carrying at least one compartment (which is the typical case for the soak test) will be rejected. Let me verify whether the TypeScript side actually emits `start_message`/`end_message` — looking at the `serializeCompartment` function, it builds nested `start` and `end` objects with fields like `flat_id`, `bare_message_id`, and `absolute_ordinal`.

So there's a clear wire-shape mismatch: TypeScript sends `start`/`end` as nested objects containing `absolute_ordinal`, but Rust expects flat `start_message`/`end_message` fields (i64) plus `start_message_id`/`end_message_id` strings. When state_sync includes a compartment, the deserialization fails with `invalid_params_error`. Now I need to check what happens when the sender gets that invalid_params response.

The transport layer throws the parsed error, and in `runQueue`'s catch block, it checks `isPeerReject` — but invalid_params isn't in the recognized peer rejection codes (stale_generation, shadow_generation_mismatch, etc.) and the error message won't contain the expected keywords. So it returns false. Same with `isConnectionFailure` — the error code and message don't match connection-related patterns. This means the error just gets logged as a shadow error without triggering any special recovery logic.

The real problem: since the throw happens before `lastAckedWatermarks` gets updated, every pass rebuilds the sync payload with the compartment (because watermarks changed), sends it, gets rejected with invalid_params, logs the failure, increments the counter, and never reaches the shadow transform. So the shadow never actually syncs — it's stuck in a loop of failed attempts.

This completely defeats the soak for compartment-bearing sessions, which are exactly the ones that matter since they have history and boundaries. Sessions without compartments sail through fine since their empty compartments array deserializes without issue. So the soak would only cover the sessions that don't need the coverage.

But wait—let me double-check if this mismatch is actually real. I need to trace whether there's any transformation happening between `serializeCompartment`'s nested output and the wire format. Looking at the flow: `buildStateSyncPayload` puts compartments directly into `params.compartments`, then `toFlatWireBody` just spreads params, so the nested `start`/`end` shape hits the wire unchanged.

On the Rust side, `ShadowCompartmentWire` derives `Deserialize` normally with flat `start_message` and `end_message` fields—no custom deserialization, no aliases. So serde expects those as top-level i64 values. The nested objects would be treated as unknown fields and ignored, leaving `start_message` and `end_message` missing, which triggers a deserialization error. This is a real, critical wire-shape bug that mirrors the flat-vs-params issue already fixed.

The test at line 350 checks `compartments: expect.any(Array)` but doesn't validate the actual shape of compartment elements. Since the test session has no compartments inserted (the mock database only creates message and part tables, not compartments), the array is empty—so the mismatch stays completely hidden. This is exactly what the mocks are missing.

I need to verify the field-level mismatches more carefully. TypeScript emits an `id` field that Rust's `ShadowCompartmentWire` doesn't have, and the ordinal structure is completely different—TypeScript nests `absolute_ordinal` inside `start` and `end`, while Rust expects `start_message` and `end_message` as the actual ordinals. The ordinal values themselves are getting lost in the translation.

I've confirmed there's no other TypeScript builder emitting `start_message`/`end_message` for the wire format, so this structural mismatch is the core issue. Let me quickly check a few other areas like the `watermarks` field to see if there are additional problems.

The Rust side expects `acked_watermarks` but TypeScript sends `watermarks` instead, so that field gets silently ignored. Rust then synthesizes its own `acked_watermarks` value from a fallback since the parsed one is None, meaning the carefully-computed watermarks from TypeScript are discarded and replaced with a synthesized value on the Rust side.

There's also a naming mismatch in the keys themselves — TypeScript's watermarks use `compartment_sequence`, `memory_id`, `m0_mutation_id`, `memory_mutation_id`, and `last_todo_state_hash`, while Rust builds its own with `compartment_seq`, `memory_id`, `memory_mutation_id`, and `last_todo_state`. Since Rust synthesizes this server-side anyway, it's mostly benign, but worth documenting.

Now I'm noticing that TypeScript sends an `m0_mutations` array in the state_sync parameters, but Rust's `ShadowStateSyncWire` doesn't have an `m0_mutations` field at all. ations`) that Rust drops silently. So the wire contract has a mismatch — TS is sending fields that the Rust struct doesn't define, which means they're ignored by deserialization. This could cause the shadow state to diverge if those fields are actually needed to reconstruct M0 correctly, though it's unclear whether the Rust transform recomputes M0 from compartments alone or relies on the mutation log.

The compartment rejection is the real blocker though — it'll fail validation before any of this matters for sessions with compartments. For sessions without compartments, the dropped fields apply. But here's the critical issue: the audit classifies this as acceptable if it "merely loses shadow coverage," but this isn't a partial loss — it's a complete loss for compartment-bearing sessions, which are exactly what the soak is designed to validate. And the soak dashboard would show zero divergences because the sessions never make it past the compartment check, so the failure is invisible. Since the rejection happens on invalid_params, the state never resets between passes — it stays initialized with lastAckedWatermarks permanently null, so every subsequent pass force-syncs the compartments and rejects again. This creates a permanent rejection loop with no escape path.

For the ordinal-fallback concern, the issue is that resolveOrdinalsForShadow builds its ordinal map from either the full message set or a primed tail, but when it falls back to individual lookups for below-floor IDs, those queries compute ordinals differently — using a COUNT-based approach with the summary filter — which can diverge from how the full reader assigns ordinals sequentially after filtering. This mismatch could cause ordinal inconsistencies in the shadow state. The tail-primed cache assigns ordinals based on the compaction marker's boundaryOrdinal, but I need to verify whether that value actually matches what a fresh count-based ordinal would compute at read time. If messages were deleted between compaction and the read, the marker's cached ordinal could diverge from the current count.

This creates a potential inconsistency: below-floor messages resolved through the by-id fallback would use a fresh count-based ordinal, while above-floor messages from the tail cache would use the stale marker-anchored ordinal. The memo drift check would catch if the same ID resolves differently within a single generation, but it wouldn't catch this cross-basis discrepancy where different resolution paths produce different ordinals for the same message. The enqueue call happens outside the transform's cache scope, but resolveOrdinalsForShadow wraps its own withRawSessionMessageCache call, so it gets a fresh cache and performs a full read instead of using any cached state. So the by-id fallback gets triggered when enqueue runs inside the transform's cache scope, where the tail-only prime is active—any message below the prime floor won't be in the cache, forcing a lookup by id. The comment makes sense now because it's describing exactly this scenario where the cache reuse happens.

The real risk here is that the marker's boundaryOrdinal might not match what a fresh count would produce for the anchor message, which could lead to wrong ordinals being assigned to the tail messages. This is a narrow but real cross-basis concern—silent shadow-store poisoning if deletion happens below the boundary. I'd mark this as medium severity with medium confidence.

Let me wrap up the analysis and verify the findings.
Compartment wire mismatch is confirmed. Let me verify the enqueue cache-scope context (for the ordinal-basis concern) and check `shadow_project_path` isolation.

[assistant] 2026-07-10T10:52:34Z

[assistant] 2026-07-10T10:52:53Z
I'm trying to track down where `withRawSessionMessageCache` wraps the transform and how that affects the cache scope. The issue is that the cache might be primed with tail-only absolute ordinals from the base, while the by-id count handles summary rows differently. I need to see where `resolveOrdinalsForShadow` calls the cache function and whether the shadow enqueue happens within or outside that scope. So the key question is whether enqueue runs inside an outer withRawSessionMessageCache scope that was primed tail-only. If the transform's boundary resolution primes the tail cache and the whole transform executes within that cache scope, then when enqueue calls resolveOrdinalsForShadow, it would be working with a tail-only cached view rather than a fresh full read — which could create the ordinal basis mismatch I'm concerned about.

The tail cache builds ordinals as baseOrdinal plus index, where baseOrdinal is the last compartment end, and it filters summary rows before assigning ordinals. The by-id fallback then needs to compute ordinals differently, which is where the discrepancy might arise.

Actually, looking at the by-id fallback more carefully: it counts all non-summary messages up to the target using a time-based and id-based comparison. That COUNT(*) approach should produce the same ordinal as the full reader's index+1 method, since both are ordering by time_created and id in ascending order and filtering out the same summary rows. The fallback uses the identical summary filter and ordering, so the basis does match — the comment's claim holds up. This isn't a bug; the fallback is consistent with the full reader's logic.

But now I'm realizing there's a potential drift issue: the tail cache's baseOrdinal comes from lastCompartmentEnd, which is the compaction marker's stored ordinal value. The tail reader assumes this baseOrdinal equals the anchor message's true absolute ordinal based on the COUNT method. If the compaction marker's boundaryOrdinal has drifted from the actual COUNT-based ordinal — say, because a pre-boundary message was deleted, shifting all absolute ordinals down — then the tail cache would assign a stale ordinal to the anchor, while the by-id fallback would compute a different (correct) ordinal for messages below the floor.

This creates a subtle inconsistency: within a single pass, some ordinals come from the tail cache's stale basis while others come from by-id's fresh basis. The memo mismatch detection works per-message-id across passes, so a consistently wrong basis wouldn't be caught. That said, this is really a pre-existing marker-drift concern, and deletions in the compartmentalized region are rare, so the full reader's ordinals would all shift uniformly anyway.

The core question is whether the by-id fallback can return an ordinal on a different basis than the tail cache. The tail cache uses `lastCompartmentEnd + positional index within tail`, while by-id uses the `COUNT of non-summary rows with (tc,id) <= target`. For messages at or above the floor, these should be equal only if `lastCompartmentEnd` matches the anchor's true count.

The by-id fallback only applies to below-floor messages, where it gives the true count-based ordinal. If the marker is accurate, both bases agree. But if the marker has drifted, tail-cache ordinals become systematically wrong while by-id ordinals remain correct, creating an inconsistency within a single pass where below-floor messages get accurate ordinals but above-floor ones don't.

Now I'm realizing the input messages passed to the shadow resolution are the full tail from the post-compaction marker, not the entire session history. Most of these are resolvable from the tail cache, and the by-id fallback only handles the handful of messages just below the boundary where the marker has lagged.

The divergence scenario is pretty narrow, so I'll treat this as a lower-confidence concern for now. What I really need to verify is whether the compartment wire mismatch is actually material. I should check if the `m0_mutations` field that TypeScript sends is being parsed by Rust, and whether the `watermarks` object in the state sync is being read correctly. Looking at the Rust wire definition, the fields are session_id, shadow_generation, expected_shadow_seq, compartments, memories, memory_mutations, last_todo_state, and acked_watermarks, but the TypeScript side is sending shadow_generation, expected_shadow_seq, watermarks, compartments, memories, and m0—there's a mismatch in what's being sent versus what's expected.

TypeScript sends `watermarks` but Rust only has `acked_watermarks` with a default value, so the watermarks get silently ignored by Rust's serde (which ignores unknown fields by default). Rust computes acked_watermarks itself instead. That's not a hard failure, but it means the TypeScript watermarks are being dropped. More critically, TypeScript sends `m0_mutations` but Rust has no corresponding field for it, so those mutations are silently ignored too—which means m0 mutations never actually reach the shadow store, creating a coverage gap.

Now looking at the compartment serialization: TypeScript's `serializeCompartment` outputs an object with id, sequence, start/end coordinates, title, content, and other fields, but Rust's `ShadowCompartmentWire` struct requires `sequence` as an i64, `start_message` and `end_message` as required i64 fields with no defaults, while TypeScript is sending different field names and structures.

When the deserialization tries to parse a compartment from TypeScript, it fails immediately because those required fields are missing — the entire state_sync request errors out with invalid_params. This only manifests once a compartment exists; empty compartment arrays pass through fine. But once created, every subsequent state_sync with that compartment gets rejected, and since invalid_params isn't in the peer reject list, the session doesn't treat it as a peer error. then the loop moves to the next iteration. The real problem emerges: once a session gets any compartment, every state_sync operation fails because the shadow store never gets updated with compartments, memories, or mutations. This means the shadow transform runs against stale or empty data, causing massive byte-compare divergence and breaking the soak's ability to validate sessions with compartments—which is essentially all real sessions being tested. When state_sync throws an invalid_params error, it propagates up and gets logged as a send failure, but the exception doesn't block the pass from continuing, so the transform still executes against corrupted state.

The real issue is that lastAckedWatermarks never gets updated since state_sync never succeeds, so the next pass forces a rebuild of the same failing compartments, creating a permanent loop where shadow coverage is lost for any compartmentalized session. This is the same class of wire-shape mismatch as the earlier nested-params bug, but now it's happening at the compartment sub-object level—the flat-wire fix only flattened the top level, not the nested structures inside.

I'm checking the Rust side to confirm the mismatch: StoredCompartment expects flat fields like start_message, end_message, start_message_id, end_message_id with no defaults, but the TypeScript side is still sending nested start/end objects. This is a hard rejection that explains the permanent failure. —this is definitely a reachable bug on cold start when compartments exist and state_sync gets built. The integration test in real_daemon.rs doesn't actually catch it because it constructs StoredCompartment directly in Rust rather than going through the TS wire serialization path, so there's a gap between what the test exercises and what happens in production. I need to check if TS has a different compartment serialization function for the wire that I might've overlooked.

serializeCompartment is the only function used in buildStateSyncPayload, and the output flows through toFlatWireBody which only flattens the top-level method/params structure—the compartments array elements pass through untouched. So the nested compartment shape hits the wire exactly as-is, confirming the bug.

This is a high-severity finding with the same blast radius as the two bugs already fixed: silent corruption that defeats the soak's purpose. I'm reconsidering whether to adjust the severity classification.

The issue doesn't affect the live lane since isolation holds—the throw gets caught in shadow-sender's runQueue and the enqueue is wrapped in try/catch. But it's worse than just losing coverage because it permanently starves every compartmentalized session and produces garbage divergences. The shadow store has no compartments while the TS output does, making the soak results meaningless noise.

This defeats the soak entirely—high severity. The m0_mutations omission is also worth noting since TS builds and sends them while Rust ignores them, silently losing m0 mutation coverage, though that's probably medium/low priority. I should also check the remaining concerns: cloneJson cost across 400 messages, the resetSession versus queued pass race condition, and whether enqueue's resolveOrdinalsForShadow throw is properly caught.

The enqueue call is wrapped in try-catch at transform.ts:2226-2228, so exceptions are caught and logged without affecting the live lane—good for hot-path safety. But there's a real cost issue: enqueue runs synchronously without awaiting, and it performs two deep JSON clones of the entire message array before returning to the user. With 400 messages and large tool outputs, JSON.parse(JSON.stringify(...)) could easily add tens of milliseconds or more, directly delaying the user's actual prompt response. resolveOrdinalsForShadow runs, it might trigger a full database read of all session messages if there's no cache scope wrapping the enqueue call — that's a potentially expensive O(session) operation happening on the hot path.

I should verify whether the transform gets wrapped in a cache scope upstream in the hook layer, but given what I've found so far, this looks like a real latency concern for long sessions.

The audit prompt is asking exactly about this: whether cloning a 400-message array per pass is acceptable, and whether opening a DB handle per by-id read is problematic. The readRawSessionMessageById function opens a read-only handle each time, which means for messages below the floor marker, I'm spinning up one handle per message. If SQLITE_BUSY occurs, the try-catch in enqueue will catch it and drop the pass, so the live lane stays stable, but the handle churn is still bounded by how far behind the marker lag gets.

Now checking the queue-wedge scenario: resetSession sets blockedUntilReset to true and pushes a reset. If performReset throws, the catch block sets initialized to false and requireResetReason to "route_reopen", but blockedUntilReset stays true. On the next pass, processPass sees the uninitialized state and requireResetReason, so it retries performReset. If it keeps failing, blockedUntilReset remains true and passes return early, but the retry loop continues each pass. Once the connection recovers, the reset succeeds and unblocks. So it's not permanently wedged—it keeps retrying until recovery.

Also verifying: drop-oldest only removes "pass" items, never reset items, so a reset work item can't be dropped. And if a pass is currently running when resetSession clears the queue and pushes a reset, the running pass uses the old generation. After it finishes, the reset bumps the generation. Minor ordering issue but no corruption.

One more edge case: in the enqueue mismatch path, the queue is cleared but a pass might be mid-flight—

same situation as above, acceptable.

There's a potential for redundant resets: if blockedUntilReset is set and more passes get enqueued before the reset processes, they might resolveOrdinals again, mismatch again, and push another reset. So multiple reset items can queue up, but that's just redundancy, not a wedge.

For the compartment reject with invalid_params: it's not a peer reject, so it becomes send_failures and the session keeps retrying state_sync every pass with force=true since lastAckedWatermarks stays null.

This creates a permanent retry loop that never progresses—the force flag stays true because lastAckedWatermarks only gets set on successful state_sync, so it always rebuilds the full compartment list and always gets rejected. That confirms permanent shadow starvation for compartmentalized sessions. I should also verify there are no serde aliases on start_message that would accept nested structures, and check that ShadowCompartmentWire has no flatten directives that could bypass the validation.

Looking at the serialization path: buildStateSyncPayload is the only state_sync builder and it calls serializeCompartment, so that's the only compartment serializer for the wire. The Rust side doesn't have deny_unknown_fields, so extra top-level fields like watermarks and m0_mutations get silently ignored—only the compartments cause the hard reject. But wait, start_message on ShadowCompartmentWire has no #[serde(default)], meaning it's required, so that's where the actual rejection happens.

The TypeScript side sends `start: {...}` instead of `start_message`, which causes serde to error on the missing field, failing the entire Vec<ShadowCompartmentWire> deserialization and triggering invalid_params. That's confirmed. Now checking the memory wire fields—ShadowMemoryWire requires `id: i64` and TypeScript sends memory.id, so that matches up.

TypeScript includes `project_path` but Rust doesn't have that field (it uses `shadow_project_path` from the binding), so it gets ignored harmlessly. For `classified_at`, TypeScript doesn't send it but Rust has a default, which is fine. Both sides have `verification_status` and `verified_at`. So memories are actually fine—the only hard blocker is the compartments' nested start/end mismatch.

Now I need to verify the ShadowUsageWire fields match what TypeScript sends. Now I'm verifying the isolation guarantees across the write path. Shadow operations are properly gated through shadow_binding which requires is_shadow_session, while plain transform and ctx_reduce explicitly reject shadow sessions. For state_sync and reset operations, they both route through shadow_binding and scope their database writes to the shadow session ID and shadow project path, ensuring writes stay isolated to the shadow context.

The key insight is that upsert_compartment_tx and reset both operate on mc_compartments using the shadow session ID as a filter — since shadow session IDs are prefixed with "shadow:" they can never collide with real session IDs, maintaining isolation even though both shadow and real sessions write to the same table. But I need to check whether the primary key structure (likely session_id and sequence or id) could create any constraint violations.

Now I'm looking at the concurrency implications: both the shadow transform and the real lane share the same Arc<McStore> and use with_conn_fenced to serialize access through a single connection mutex. If the shadow transform holds that mutex for the entire duration of its transform plus byte comparison on large inputs, it could block the real lane from executing its own transform. I need to trace whether transform_with_projection releases the lock between individual store operations or holds it throughout.

The shadow lane effectively doubles the write load on the shared store — every real transform triggers a shadow transform that also loads and saves data. Under a single connection mutex, this adds contention that could increase lock-wait latency for the live lane. This is a medium-level concern since both the real and shadow transforms are separate async requests on different channels, so they'll serialize on the mutex if they collide. The shadow lane introduces measurable store contention to the live path, which is worth noting.

I should also double-check the ordinal basis concern more carefully — it seems consistent but I'll flag it as low-priority and informational, with a note about potential marker drift. Now I need to verify the compartment wire format more thoroughly to ensure the TypeScript side isn't also sending flat fields alongside the compartment structure. I've got the exact line references pinned down for all three findings, so I'm ready to write this up. Let me do one final check on the blast radius for the compartment bug—it doesn't affect the live session since the error gets caught in the shadow-sender's runQueue and in the transform layer, and it's logged via the send_failures counter and sessionLog, which puts it in the class 3 range by the rubric.

But wait—the real issue is that it doesn't just lose coverage, it permanently starves every compartmentalized session and generates massive false divergences. The shadow store ends up with no compartments, so the shadow transform output lacks the injected compartments, and the byte-compare reports hard divergence on essentially every pass. That's actually class 2 territory—it silently corrupts the shadow state and defeats the soak's purpose. The soak would appear to run fine, but every result would be a false divergence, making the divergence signal completely unreliable. That's high severity.

And here's the critical part: if state_sync fails, the transform doesn't send at all for compartmentalized sessions—zero coverage, not even false divergences. So the soak collects nothing on the sessions it most needs to validate, while cold and young sessions (the trivial ones without compartments) work fine. The soak would report everything looks good based only on those easy cases while silently collecting nothing on the hard ones. That's the dangerous part—a false green signal. HIGH, HOLD.
I have gathered strong evidence. Let me do two final confirmations: the exact TS memory/m0 field set vs Rust, and confirm no serde alias rescues the compartment shape.

[assistant] 2026-07-10T10:56:05Z

[assistant] 2026-07-10T10:56:44Z
The tool's timing out, so I'll try a more targeted grep approach on a smaller subset of the data.

[assistant] 2026-07-10T10:57:19Z
The grep approach is timing out too, so I need to try a different strategy.

[assistant] 2026-07-10T10:57:44Z
I'm noticing a mismatch between what the TypeScript code is emitting and what the Rust struct actually defines. The TS `buildStateSyncPayload` is sending `m0_mutations` as a parameter, but when I look at the `ShadowStateSyncWire` struct in the Rust code, there's no corresponding field for it—the struct has session_id, shadow_generation, expected_shadow_seq, compartments, memories, memory_mutations, last_todo_state, and acked_watermarks, but `m0_mutations` isn't there.

Since Rust's serde doesn't have `deny_unknown_fields` enabled, these extra fields from TypeScript (`watermarks` and `m0_mutations`) are silently dropped rather than causing a rejection. The `acked_watermarks` gets computed as a fallback instead of using the TypeScript watermarks directly, and the `m0_mutations` are completely discarded—meaning the m0 mutation log never actually syncs to the shadow. This is a potential silent coverage loss, though whether it matters depends on whether the shadow transform actually needs those mutations.

Now I'm checking the compartments shape to see what the TypeScript serialization is actually emitting versus what Rust expects. `invalid_params` doesn't match any of the peer rejection codes (stale_generation, shadow_generation_mismatch, etc.) or connection failure patterns, so when state_sync throws this error, it won't trigger the blockedUntilReset logic and the client will keep retrying indefinitely.

The real issue is that `state.lastAckedWatermarks` never gets updated when the throw happens before that assignment, so the next pass will build the same payload with the same compartments and fail again in a loop. So it's logged, which technically makes it class 3 per the rubric, but the real issue is that this causes *total* coverage loss for the exact sessions that matter most — anything with compartments — which defeats the entire purpose of the soak. Even though it's not corrupting the live lane, it's a critical severity problem for the soak itself. Let me trace through processPass again to confirm whether shadow_transform actually gets skipped or if I'm missing something. Checking the TypeScript serialization output against the Rust wire format, I'm seeing the field mismatches now. The TS sends a nested `start` object with flat_id, bare_message_id, and absolute_ordinal, but Rust expects a flat `start_message` field as a required i64 — so serde rejects it as a missing required field. The same flattening fix was applied to the top-level state_sync but never to the nested compartment objects, so the shape mismatch persists there. The test at line 383 only checks that the compartments array exists—it doesn't validate the structure of individual compartment elements, and since the test uses an empty array, it never actually exercises the deserialization mismatch. Let me check what the test actually validates.

The test database only creates messages, no compartments, so `getCompartments` returns an empty array, meaning the wire test never sees a real populated compartment. Meanwhile, the Rust integration test constructs `StoredCompartment` directly without going through the TypeScript-to-JSON-to-wire path, so it also bypasses the shape validation. Both test layers are missing coverage for the actual TS compartment JSON serialization.

Looking at the state sync payload fields, `watermarks` and `m0_mutations` are silently dropped during serialization — the Rust side has `acked_watermarks` instead and no `m0_mutations` field at all, so serde just ignores them. The compartments field is broken as noted, and `acked_watermarks` falls back to a computed default when missing.

The real issue is that `m0_mutations` never reaches the Rust shadow store, so if the shadow transform's historian or rendering depends on those mutations, the output will diverge from TypeScript. But since compartments already breaks the entire state sync, fixing that is the priority before worrying about m0. ybe the ordinal fallback basis-mismatch concern is worth digging into more carefully. The resolveOrdinalsForShadow function builds its ordinal map from the cached session messages, but if that cache is only primed with the tail slice, it's working with absolute ordinals rather than the full context — that's the subtle mismatch the prompt flagged. I should trace through that logic more concretely, though my context is getting tight and some tool calls are timing out, so I need to be strategic about what to focus on next.

For messages outside the tail, the code falls back to a database query that counts all non-summary messages up to the target message's timestamp and ID to compute its absolute ordinal in the full ordering.

The tail reader assigns absolute ordinals starting from a base value (the last compartment end), and this base should align with what the full reader would produce at that same anchor message position.

All three readers—the by-id count, the full reader, and the tail reader—filter out summary rows consistently using the same criteria, so they're counting the same set of messages. But there's a potential issue with `time_created` collisions: in the test database, `time_created` values are unique (matching the index), but in production, multiple messages could share the same timestamp, which might affect ordering.

The tie-breaker is `id ASC` in both the full/tail reader and the by-id count, so that's consistent. But I'm noticing a real discrepancy in the memo drift check—when resolving ordinals for a shadow message, the memo stores the messageId-to-ordinal mapping and flags a mismatch if the ordinal changes between passes. If a below-floor message gets resolved via by-id count in pass 1 but the tail cache isn't primed in pass 2 due to a different scope, the ordinal could shift, triggering a false mismatch.

Actually, wait—the real issue is whether the tail-prime happens during the shadow enqueue's withRawSessionMessageCache scope. In resolveOrdinalsForShadow, it opens its own cache scope to read raw session messages, and this gets called from enqueue synchronously in the transform hot path.

The question is whether there's an outer cache scope active at that point. I can't find withRawSessionMessageCache in transform.ts itself, so the prime/cache must be set up elsewhere—probably in trigger evaluation rather than the transform. If there's no active outer cache when enqueue runs, then resolveOrdinalsForShadow's own scope creates a fresh cache and does a full read with no prime, meaning all ordinals are populated and the by-id fallback never gets used in the common OpenCode path.

But there's a real performance concern here: resolveOrdinalsForShadow triggers a full database read of all messages and parts for the session when no prime is active—exactly the O(session) cost that primeTail was meant to avoid. And this happens synchronously during the transform.

ts enqueue call, which blocks the hot path. On top of that, there are two deep clones of potentially massive message arrays happening in the same synchronous flow, plus another database read in denormalizeShadowOutput. The user's original questions are whether cloning a 400-message array is acceptable per-pass and whether the DB read opens a handle each time. The readRawSessionMessages call opens one DB handle for the full session read on the hot path, and this whole operation is wrapped in a try/catch block in the transform code.

But the real issue isn't exception safety — it's that this synchronous full read plus clone directly stalls the user's prompt on large sessions, adding multi-second latency to every request. That's the actual High-severity finding here: it impacts live session latency even though correctness isn't affected. I should verify whether there's any caching mechanism in place that could reuse a primed in-memory tail instead of doing a fresh read each time. The real bottleneck is the unconditional cloneJson of the full input and output message arrays—for a 400-message array with large tool outputs, that JSON stringify-and-parse cycle runs twice per pass and can take tens to hundreds of milliseconds, making it a hot-path cost worth optimizing. Looking at the cache safety concern, the clone at line 459 happens before the transform mutates anything, so the live messages array is protected on the input side, and then at enqueue the shadowInputMessages (already cloned) gets passed through resolveOrdinalsForShadow.

On the output side, the live post-transform messages array is passed directly to enqueue without cloning, but denormalizeShadowOutput immediately clones it and only mutates the clone while reading from the originals, so the live array stays untouched.

I'm also checking whether denormalizeShadowOutput gets called twice — once in enqueue and potentially again in buildShadowTransformBody — but there's a guard that reuses the cached results if they're already defined, so it's not redundantly executed.

Looking at the cloning overhead: there's one deep clone of the output per pass in enqueue, one for the input in resolveOrdinals, and another pre-transform clone, totaling three deep clones per pass on the hot path. Now I need to examine the SubcShadowTransport connection lifecycle and the queue wedge behavior — specifically how resetSession blocks the queue and how performReset unblocks it.

The mismatch path in enqueue handles this cleanly: it zeros the queue, sets the block flag, and queues a reset that will unblock. But there's a potential issue in the peer reject catch handler — it sets the block flag and reset reason without actually queuing a reset, instead relying on the next enqueued pass to trigger performReset through the initialization check. If the session goes idle after a peer rejection with no further passes enqueued, the queue stays wedged.

Actually, looking closer at the processPass flow: the reset reason check at line 952 happens before the blockedUntilReset check at line 960, so performReset clears both flags before we even reach that guard. The reset happens immediately on the next pass, so there's no actual wedge — just a brief stall until activity resumes.

If performReset throws (like a connection failure), the exception bubbles up and the catch handler sets initialized=false with a reset reason, leaving blockedUntilReset still true. But that's fine because the next pass will attempt the reset again, and it only stays stuck while disconnected, which is acceptable.

Now looking at the connection lifecycle and how the pending promise chain serializes all calls — if a call hangs waiting for a response (with a 5-second timeout), that blocks the entire pending chain.  However, since runQueue is fire-and-forget, the stall only delays shadow processing, not the main lane, so it's acceptable.

There's a potential issue in SocketReader.readExact where closing mid-read throws "connection closed", but the pending chain catches that and maps it to undefined, then the socket close handler nulls out the reader and routes, and the next call reconnects properly. For readTerminalFor, it loops reading frames until finding one that matches the channel and correlation ID, but if a frame arrives for a different correlation (like a stale response from a timed-out request), it gets discarded.

Actually, the correlation matching handles stale frames correctly — they're skipped until the right one arrives. But there's a potential frame-header desync if a request times out mid-frame, after the header is read but before the full body is consumed...

except readExact doesn't partially consume bytes on timeout. It accumulates the buffer and only splices once it has enough data, so a timeout leaves the buffer intact at the header boundary. The next readFrame starts from the same position, keeping everything consistent.

Now I need to verify whether PARITY.md already documents this compartment shape issue, and dig into the config spread leak concern. The buildMagicContextHookConfig spreads pluginConfig into the hook config, but the hook type only declares specific fields. Extra keys should be inert unless downstream code iterates Object.keys or spreads the config further to something that validates strictly. I should trace what actually consumes the hook config to see if there's a real exposure here. The shadow_transform.enabled field flows through as intended since the hook reads it. Now I need to verify whether buildStateSyncPayload actually gets called with compartments in production — it would only happen if getCompartments returns rows, which occurs after the first compartment boundary. That's confirmed as high-impact. Let me check the PARITY documentation for any mention of the compartment wire shape to finalize my findings.
Key evidence is in hand. Let me do false-positive filtering against the PARITY docs and confirm the `m0_mutations`/`watermarks` silent-drop.

[assistant] 2026-07-10T11:00:14Z
I'm examining the shape of the serialized compartment output from TypeScript and comparing it against the Rust wire format to identify the structural differences.

The TypeScript serializer sends nested objects for `start` and `end` with fields like `flat_id`, `bare_message_id`, and `absolute_ordinal`, but the Rust struct expects flat top-level fields `start_message` and `end_message` as required integers with no defaults. This mismatch means any state sync containing compartments will fail deserialization with a "missing field" error, triggering a peer reject.

However, `invalid_params` isn't recognized as a peer reject or connection failure in the error classification logic—it just increments `send_failures` and gets logged without blocking or requiring a reset, so the transform for that pass gets skipped but the system continues running.

This creates a critical issue: every pass with compartments to sync fails silently with `invalid_params`, the shadow lane never progresses, and `lastAckedWatermarks` stays null since it's only set after a successful sync. The shadow soak ends up collecting nothing for exactly the long sessions with compartments that it's designed to validate, while short sessions without compartments would sync fine. The real issue is that operators monitoring `transforms_sent` would see it stuck at zero for compartment sessions, even though the system logs failures at a low level—the divergence between thinking we're collecting data and actually collecting nothing is the core problem. I need to verify whether there's any code path that flattens the nested compartment structure into the wire format, or if the compartments stay nested and get rejected upstream.

Looking at `toFlatWireBody`, it only spreads top-level params and doesn't recurse into compartments, so the nested shape persists. This means there's a second-layer wire mismatch separate from the already-fixed top-level one: TypeScript emits nested `start.absolute_ordinal`/`end.absolute_ordinal` while Rust's `ShadowCompartmentWire` struct expects flat `start_message`/`end_message` fields with no defaults. The existing test at line 383 only checks that compartments exist as an array, never validating the element shape against what Rust actually needs. ions are unambiguous; no alias/flatten; test doesn't validate element shape). This is a class 2/3 issue—it won't break the live session since state_sync failures are caught and ignored, but it silently disables shadow coverage for all compartment-bearing sessions. The soak is observe-only, so this defeats its entire purpose and produces a falsely-clean result because it never actually compares any compartment-bearing session. I'm ranking it HIGH—it's the single most important reason to hold. Now I need to verify whether the Rust `acked_watermarks` field maps to the TS `watermarks` field.

The TS state_sync params include `watermarks`, but the Rust `ShadowStateSyncWire` struct has `acked_watermarks: Option<Value>` with `#[serde(default)]` and no field named `watermarks`. This means the TS `watermarks` field is silently dropped because serde ignores unknown fields by default, and `acked_watermarks` defaults to None when absent, letting Rust synthesize it from max sequences instead. I'm checking whether this is actually a bug—the `watermarks` sent by TS carry critical metadata like `compartment_sequence`, `memory_id`, and mutation IDs.

The Rust side ignores those entirely and computes `acked_watermarks` from the compartments and memories in the current sync payload. This is a semantic divergence: TS tracks its own `lastAckedWatermarks` while Rust synthesizes its own, but since they don't need to match byte-for-byte and Rust's `acked_watermarks` is only used internally, it's probably benign. The real concern is whether TS's `watermarks` was meant to be preserved by Rust—if so, that information is lost. But given the field is named `acked_watermarks` and defaults to being synthesized, this looks intentional.

Then there's `m0_mutations`: TS sends it every sync, but the Rust `ShadowStateSyncWire` struct has no field for it at all, so it gets silently dropped. That means M0 mutations never reach the shadow store, which is a real coverage gap—any transform behavior depending on the M0 mutation log will diverge between the two sides. This is a medium-severity issue because it silently loses an entire data class without corrupting anything else.

I'm also double-checking the `id` field in compartments: TS emits it, but Rust ignores unknown fields, so that's not a problem. And reconsidering the ordinal fallback concern—the by-id fallback computes ordinal using a count query with the summary-row filter, while the full read path does something different. not produce an output element. So malformed rows still increment the index counter in the filtered array, meaning a message following a malformed row gets assigned an ordinal based on its position in the filtered list, not based on how many valid messages came before it.

The COUNT query for non-summary rows uses `json_extract(data, '$.summary')` which returns NULL on malformed JSON, and since `COALESCE(NULL, 0) = 0`, the NOT condition evaluates to true, so malformed rows get counted. Both approaches handle malformed data consistently.

The code comment at lines 386-396 confirms the by-id fallback basis matches the full-read basis, so that's not a bug. But there's a critical assumption: the marker's stored `boundaryOrdinal` is supposed to equal the COUNT-based absolute ordinal of the anchor message. The tail-only cache assigns ordinals as `baseOrdinal + i` where baseOrdinal comes from the marker's `boundaryOrdinal`, trusting that this value is correct. I need to verify whether the by-id approach actually guarantees this alignment.

If the marker's `boundaryOrdinal` is stale or was computed on a different basis than the current live COUNT—say, if messages were deleted below the boundary and shifted all absolute ordinals—then the cache and by-id fallback could return different ordinals for the same session in the same pass. A message in the tail would resolve via cache to `boundaryOrdinal + offset`, while a below-floor message would resolve via by-id to a fresh COUNT, and if a message was deleted below the boundary, the fresh COUNT could diverge from the stored marker value.

The memo mismatch check is per-message-id, so a given message-id resolves consistently through either cache or by-id depending on whether it's in the tail or below the floor—but when the marker advances, a message that was cache-resolved in one pass could fall below the floor in the next pass and switch to by-id resolution, creating a potential disagreement if the two bases diverge due to deletions below the boundary.

The real danger is within a single pass: if the annotated input mixes cache-resolved and by-id-resolved ordinals and the two bases disagree, the absolute ordinals in the same input array become internally inconsistent—messages could end up out of order or overlapping—which silently poisons the shadow store's view of the input ordinals and corrupts the compartment boundary matching.

This is the exact "silent basis mismatch" the prompt warned about, though it requires below-boundary row deletion, which rarely happens in OpenCode sessions, so it's a latent risk with medium-low confidence of triggering. I should flag it as medium severity with the caveat that it only manifests if the marker's boundaryOrdinal drifts from the live COUNT basis.

Let me trace through `resolveOrdinalsForShadow` more carefully, particularly how it's called in `enqueue`... So the transform is wrapped in `withRawSessionMessageCache` at a higher level with tail-only priming, and when `enqueue` calls `resolveOrdinalsForShadow`, it re-enters that same cache scope—which is a no-op since the cache already exists—allowing it to reuse the tail-primed cache. When `readRawSessionMessages` returns only the tail slice, messages below the floor miss the cache and fall back to the by-id lookup.

Within a single enqueue call, tail messages resolve from the primed cache using marker-based ordinals, while below-floor messages resolve fresh via by-id count. For consistency, these two resolution paths need to agree on ordinals, which only happens if the marker's boundary ordinal matches the live count ordinal of the anchor. If a summary row was added or removed, or a message was deleted below the boundary since the marker was created, the two bases diverge silently, poisoning the shadow with internally-inconsistent ordinals—a genuine class-2 risk that's hard to detect.

There's a more direct concern too: the tail cache assigns the anchor message a baseOrdinal from the marker's lastCompartmentEnd, but the by-id count computes the true current ordinal independently. Even a single off-by-one difference between marker and live state shifts every tail message by a constant offset relative to below-floor messages. Since the marker comes from compaction logic and below-boundary messages are normally stable, this divergence is subtle but real.

Now looking at the hot-path cost of cloneJson on large message arrays — both `resolveOrdinalsForShadow` and `denormalizeShadowOutput` do full JSON round-trips on potentially hundreds of messages with large tool outputs, and both run synchronously inside the transform hot path before returning to the user, so every pass pays double the serialization cost.  Adding `cloneForShadow` as a third full clone of input messages makes this a MEDIUM-HIGH severity issue — it's a live-session latency tax hitting the user's perceived responsiveness, though the try/catch around enqueue makes it exception-safe.

Actually, looking closer at what's synchronous in enqueue: `resolveOrdinals` and `denormalize` are both sync clones with DB reads, while the actual transport I/O is async and fire-and-forget. So the hot-path sync cost is really just those two full clones plus database lookups — still significant on large sessions but not the entire enqueue operation.

Now I'm digging into whether the by-id database reads are opening a fresh handle per call. Each `readRawSessionMessageById` invocation uses `withReadOnlySessionDb`, which means if the below-floor fallback triggers for multiple messages, that's multiple separate read-only handle operations per pass. Under SQLITE_BUSY conditions (which is exactly when the live transform is running), this could become a bottleneck.

The key distinction: opencode.db is read-only from the transform's perspective, while the magic-context storage uses a separate database file, so write contention isn't the issue. But if `withReadOnlySessionDb` opens a fresh connection each time rather than caching, then K below-floor messages means K connection opens per pass, which compounds the synchronous cost I already flagged.

Now checking isolation enforcement: shadow_binding validates that operations are actually shadow sessions (line 807), rejecting non-shadow ops. Plain transform and ctx_reduce both reject shadow bindings outright. But I need to verify facade and guidance operations—they might bypass these checks and inadvertently touch real session state when operating on shadow bindings.

The key insight is that shadow operations write to shadow-prefixed session IDs through apply_shadow_state_sync and reset, so shadow compartments are isolated to shadow-prefixed rows. The real concern is whether non-shadow operations could somehow write to real rows through a shadow binding, but the session ID prefixing should prevent that. The shadow_project_path function derivation is hard to verify due to timeout, but the physical separation of tables (shadow_memories vs memories) means isolation holds even if paths collided. Since the shadow transform operates on the same store handle but writes only to shadow-prefixed session cache rows, a divergence or panic in the shadow lane shouldn't affect the real lane.

However, there's a potential cross-lane risk: if the shadow transform panics and isn't caught, it could poison the shared store's internal Mutex via with_conn_fenced, which would then break the real lane on subsequent lock attempts. The question is whether the compare path actually panics—specifically if there's an unwrap that could fail. The serializer_profile is validated upstream at line 2324, so the `.expect()` at line 855 in evaluate_shadow_historian is actually safe — the TransformRequest already has a validated profile string by that point. The real concern is Mutex poisoning if a panic occurs in the shadow path while holding the connection lock, though that requires an actual panic to happen.

For the queue-wedge issue, when `resetSession` sets `blockedUntilReset=true` and the reset keeps failing due to transport issues, `performReset` throws an exception that gets caught and sets `initialized=false` with a new reset reason, but `blockedUntilReset` remains true from the original call — so the queue stays blocked even though the reset reason has changed.

However, the session isn't permanently stuck: when the next work item is enqueued, `pushWork` schedules a pass, and `processPass` sees the `blockedUntilReset` flag and triggers another reset attempt. So it self-heals on the next enqueue.

But there's a subtlety with peer-reject handling: after a peer-reject in `runQueue`, the code sets `blockedUntilReset=true` and clears `lastAckedWatermarks`, but doesn't queue a reset work item. Then `processPass` early-returns when it sees `blockedUntilReset` is set.

Actually, let me retrace this: when a peer-reject happens, `blockedUntilReset` is set to true and `requireResetReason` is populated. The next time `processPass` runs, it checks `if (!initialized || requireResetReason)` — since `requireResetReason` is set, it calls `performReset`. If that succeeds, it clears both flags and recovers. So the recovery happens on the next pass, not immediately. If the session goes idle right after a peer-reject and no new work arrives, the reset is never sent, but that's acceptable since there's nothing to soak up anyway.

The enqueue mismatch path also sets `queue.length=0` and `requireResetReason`, triggering a similar recovery flow.

Now I'm checking a subtle race condition where `resetSession` clears the queue and pushes a reset, but if `runQueue` is already mid-execution on an in-flight pass, that pass continues to completion. Once it finishes, `runQueue` loops back, finds the reset, and executes it. The tricky part is that the in-flight pass might have updated `lastAckedSeq` before the reset runs.

The in-flight pass's transform uses the old generation's watermarks, which the peer accepts since it's still on that generation. Once reset bumps the generation, subsequent passes use the new one, so there's no corruption. And since `SubcShadowTransport` is a single shared instance across all sessions with a `pending` chain that serializes all calls globally, if one session's call stalls (up to 5s timeout), other sessions' calls queue behind it.

This serialization happens in the detached runQueue rather than the hot path, so it delays shadow coverage but not the live session—acceptable but worth noting. A 5s timeout multiplied across many queued items could back up significantly, and since all sessions funnel through that single `transport.pending`, one wedged request blocks everyone's shadow calls for up to 5s.

When `readTerminalFor` times out, it throws "subc request timeout," which doesn't match the patterns for peer rejection or connection failure, so it just increments `send_failures` and gets logged. The socket stays open on timeout, so the pending frame's response is still waiting.

The tricky part: the request frame was already written to the socket before the timeout, so the server processes it and sends a response back. That response sits unread in the buffer until the next call comes in and reads frames, discarding non-matching ones by channel and correlation ID. The correlation numbers keep incrementing, so there's no aliasing. But if a stale large frame ends up in the socket buffer, the next `readFrame` reads its length-prefixed header and body correctly and discards it before reading the next one.

There's a subtle issue though: on timeout, the request was already sent, the server responds, and that response stays unread until the next request reads it. Auth proof construction uses HMAC with domain, client nonce, server nonce, and daemon ID, verified with timing-safe comparison after checking the daemon ID and proof lengths to avoid throwing on mismatches. I'm also reconsidering the silent drops in m0_mutations and watermarks, plus a mismatch where the TypeScript memory emits a `project_path` field that the Rust ShadowMemoryWire doesn't have, so it gets dropped.

The TypeScript memory object doesn't include `classified_at`, which Rust defaults to None, and it does emit `verification_status`, `verified_at`, `merged_from`, and `metadata_json` that Rust expects. Now checking the ShadowMemoryMutationWire fields to see if the TypeScript mutation data maps correctly.

The real blocker is the compartments structure — TypeScript sends nested objects with `start` and `end` properties, but Rust expects a flat structure. Let me verify this isn't being transformed somewhere upstream before the wire sends it out. Neither the Rust nor TypeScript tests actually verify the compartment wire format end-to-end — the integration test constructs compartments directly in Rust, bypassing deserialization, while the TypeScript test only checks that compartments is an array. This is a classic gap where mocks hide the real issue. I've confirmed there's no reshaping of compartments before the Rust parse, so the wire mismatch goes straight to `serde_json::from_value`, and I'm verifying that the other required fields like `expected_shadow_seq` and `shadow_generation` are properly sent and handled.

The real danger here is that sessions without compartments would parse fine and show up as successful in the soak dashboard, masking the fact that compartment-bearing sessions — the actual test target — silently fail. The metrics would appear green while the hard cases are completely broken, making this a strong argument for holding the change.

Let me verify a couple more details before finalizing my assessment. I'm checking whether `enqueue`'s cloneJson could throw on circular references, but since messages come from OpenCode and are JSON-serializable, that's not a concern. The non-enumerable `__magicContextPartUpdatedAt` property gets skipped by JSON.stringify anyway, so no issues there.

Now I'm ready to write up my findings and rank them by severity.

The wire-shape mismatch between compartment formats is the critical blocker—nested start/end versus flat start_message/end_message silently breaks the soak for primary population. The triple deep-cloning on the hot path is a close second, creating real latency issues that scale with session size. Then there's the silent dropping of m0_mutations by the Rust wire, the watermarks field mismatch, and the ordinal-basis inconsistency that could poison data if boundaries drift.

Let me trace through the exact clone count: cloneForShadow hits the messages array once at the entry point, then resolveOrdinalsForShadow clones again inside enqueue when processing the args.

messages that were already cloned, and denormalizeShadowOutput clones the output messages separately. That's three full deep clones per pass on the hot path—all synchronous and blocking before the transform hook completes. The first clone at 459 happens before any mutations, which is critical for capturing the original state.

Now I'm weighing which issue is the strongest reason to hold on arming this in production. The hot-path cloning is a class-1 problem that directly impacts live session latency—three full array clones on every transform at high context is a measurable user-facing regression. But the shadow output issue is a correctness problem that defeats the soak's entire purpose. Both are strong HOLD reasons, though the prompt treats class-1 (live session impact) as the worst category.

Let me verify the actual scope of what's being cloned—it's capturing the full messages array, which at context limits means the entire conversation history.
I have gathered sufficient evidence across the TS sender, the Rust wire structs/handlers, the store CAS methods, the ordinal readers, and the test coverage. Let me do one final confirmation of the compartment serialization shape and the hot-path clone sites.

[assistant] 2026-07-10T11:03:47Z
I've found another wire-shape mismatch between the TypeScript and Rust implementations—the third bug class the mocks aren't catching. The TypeScript serialization is missing the `start_message`, `end_message`, and `start_message_id`/`end_message_id` fields at the top level, only including `flat_id`, `bare_message_id`, and `absolute_ordinal` in the start and end objects.

The Rust `ShadowCompartmentWire` struct requires `start_message` and `end_message` as mandatory fields with no defaults, so when the state_sync payload arrives with compartments, deserialization will fail with an invalid_params error because those required fields are missing. This means any state_sync carrying compartments will be rejected outright, even though the `compartments` field itself has a serde default that would normally allow an empty vec.

Now I need to figure out whether invalid_params is treated as a peer rejection or a connection failure, since that determines how the sender responds to the error. -sends the same failing state_sync repeatedly. The shadow transform never executes for any session with compartments, so the soak produces zero meaningful comparisons for exactly the sessions that need testing most. This defeats the entire purpose of the soak for compartmentalized sessions. Let me trace through processPass again — the state_sync call happens before the transform attempt, so if it fails, the transform never runs at all.

This is the third wire-shape mismatch bug, same class as the previous two. It doesn't touch the live lane, but it creates a false-confidence problem: simple sessions without compartments would pass the soak and appear green, while every real compartmentalized session silently produces zero comparisons. That's a high-severity finding because it completely undermines the soak's purpose.

Let me verify this is actually reachable — on the first state_sync call, acked defaults to -1, so all compartments get included. That confirms it's a real issue. Now I'm examining the ShadowCompartmentWire struct to see if there's an alias or flatten directive I'm missing that would explain the divergence.

So the server sends nested `start: {absolute_ordinal}` but the Rust struct expects `start_message` as a flat i64 field — serde will fail to deserialize. The test at line 350 checks compartments but only with an empty array since the basePass DB has no compartments table populated, so it never actually exercises the wire format with real data. The Rust daemon test constructs StoredCompartment directly instead of deserializing it.

Neither side tests the full TS→wire→Rust compartment deserialization path, which means this gap silently defeats the soak for exactly the production scenario being targeted — compartmentalized sessions. The send_failures get logged but the divergence dashboard shows green instead of red, making it a critical finding that corrupts the soak silently.

Now I'm wondering whether state_sync only triggers on watermark changes, and whether there's even a compartment present on the first shadow pass.  If the session already has compartments, the initial state_sync includes them and gets rejected immediately. For fresh sessions, compartments build up over time, and the first one created causes a watermark shift that sends a state_sync with that compartment, leading to permanent rejection. So either way, once compartments exist, state_sync gets rejected from that point forward.

For the ordinal-basis issue, I need to check how the below-floor fallback computes ordinals. It's using a COUNT query that filters out summary rows and counts non-summary entries up to the target row...

The full reader assigns ordinals as 1-based positions over the same filtered set (non-summary rows ordered by time_created, then id). The COUNT query gives the same result — the number of non-summary rows with (time_created, id) <= target, which is the 1-based position. So the basis is consistent between the two approaches.

Now I'm checking whether the cache in the shadow enqueue path is primed correctly. When enqueue calls `resolveOrdinalsForShadow`, it uses `withRawSessionMessageCache` to call `readRawSessionMessages`. I need to verify if the cache is tail-only at that point... ache scope, then `withRawSessionMessageCache` creates a fresh empty cache, and `readRawSessionMessages` does a FULL read (all messages, absolute ordinals from 1). In that case the by-id fallback is never needed (everything is in the full read). The tail-only priming concern only bites if enqueue runs INSIDE an already-active tail-primed scope.

Let me reconsider: the comment in resolveOrdinalsForShadow explicitly says the active cache "can be primed TAIL-ONLY (post-boundary) by the transform on large sessions, so wire messages below the prime floor never appear in it". This implies enqueue DOES sometimes run inside the transform's tail-primed scope. But I found no withRawSessionMessageCache wrapping in transform.ts. The basis analysis shows the fallback is consistent, so even if it triggers, the ordinals are correct. So 2c is NOT a bug — the fallback basis matches. I verified this; it's a false positive I can rule out.

Now there's a subtle basis question about the tail-primed cache...

the absolute ordinals it carries (baseOrdinal, baseOrdinal+1, etc.) come from the compaction marker's end_message ordinal, while the by-id COUNT derives ordinals purely from counting non-summary rows. For these to align, the marker's boundaryOrdinal must match the COUNT-based position of the anchor message. Since the marker was presumably computed from a prior full read, the question is whether it could become stale if messages were deleted below the boundary after the marker was written.

The system already carries this risk inherently, and the memo mismatch check would catch per-session drift within a generation. For cross-basis drift (where the cache and by-id COUNT assign different ordinals to different messages), the memo stores per-messageId and each message resolves from either the cache or the fallback in a single pass, so a given messageId's ordinal comes from one source consistently. The real concern is if the marker's baseOrdinal is stale by some deletion count, causing messages in the tail to get cache-based ordinals while messages below the floor get by-id COUNT ordinals, creating a divergence.

This would poison the shadow store with inconsistent ordinals across sources within one pass, but it requires a stale marker—which is rare and would also affect the transform itself. I'm flagging this as a low-to-medium theoretical concern with the caveat that it depends on marker staleness. The audit explicitly calls out issue 2c, so I'll mention it but rank it low given that the COUNT filter matches the full-reader filter within a consistent marker, and the residual risk is pre-existing marker staleness.

Now I'm looking at another issue: the cloneJson cost on the hot path. The enqueue function calls resolveOrdinalsForShadow and denormalizeShadowOutput, each doing a full deep clone of the entire message array—up to 400 messages—using JSON.parse(JSON.stringify(...)) synchronously inside the transform hot path. I need to figure out whether enqueue is fire-and-forget or if the caller awaits it, since this could be blocking the result return to the user.

Actually, looking at this more carefully: cloneForShadow already ran at the start of transform (a third deep clone), so we're doing three full deep clones per pass on large sessions. With 400 messages and big tool outputs, each stringify+parse could take tens of milliseconds or more—this is a real latency concern for the live prompt. The severity is medium since it adds latency but doesn't break functionality.

The clone safety itself is correct though—the JSON deep clone fully severs references before mutation happens, so that part checks out. Now I'm looking at another issue: readRawSessionMessageById opens a database handle per call under SQLITE_BUSY conditions, which could be another bottleneck. The try/catch at transform.ts:2226 contains the throw, so a shadow loss is safe but a busy_timeout block could stall the live prompt. I need to check what withReadOnlySessionDb actually does — opening a fresh connection per call on the hot path seems inefficient and risky under concurrent writes. There's also a state consistency issue: if state_sync throws, lastAckedWatermarks stays unset, but if the transform call after state_sync succeeds and then throws, the watermarks get set but the ack doesn't propagate.

For the queue wedge scenario, the mismatch path in enqueue sets blockedUntilReset and pushes a reset work item, which should clear the block when performReset runs. But if the transport.call(shadow_reset) inside performReset throws due to a connection failure, runQueue might not properly handle the exception and the queue could remain wedged. empts reset. OK, self-heals. Unless connection stays down, in which case shadow is just off (acceptable). Not a wedge bug.

But there's a subtler race condition: in the enqueue mismatch scenario, the queue gets cleared and reset is pushed, but if a new pass is enqueued before reset runs while blockedUntilReset is true, the queue becomes [reset, pass]. The reset clears the block, then pass runs fine. Now considering the resetSession() race where it clears the queue and pushes reset—

if a pass is currently running (mid-await in processPass), the running pass might complete a state_sync against the old generation before reset bumps it. The completed pass already advanced the ack fields for the old generation, then reset nulls them and bumps generation, so no corruption occurs since reset supersedes. The in-flight pass's transform used the old generation which Rust validates, so it's not a bug. Now I'm looking at the m0_mutations field that TypeScript sends in state_sync params.

Rust's ShadowStateSyncWire doesn't have an m0_mutations field at all — it has compartments, memories, memory_mutations, last_todo_state, and acked_watermarks instead. Since serde ignores unknown fields by default, the m0_mutations sent by TypeScript are silently dropped. This means M0 mutations never actually sync to the shadow store, even though the watermark tracks m0_mutation_id and TypeScript computes and sends them. The shadow store's M0 state could diverge from live if M0 mutations matter to the transform, which could cause false divergences or miss real ones.

There's also a similar issue with the watermarks field — TypeScript sends `watermarks` but Rust's struct has `acked_watermarks` instead, so the carefully computed watermark inventory gets dropped and Rust recomputes its own from the synced rows. Both look like silent field mismatches that could be bugs.

Let me check against the audit requirements — I need to flag fields that reject or silently default. The `start_message` and `end_message` mismatch is a hard reject since TypeScript sends nested objects but Rust expects flat fields. The `m0_mutations` field is genuinely lost data. And `watermarks` silently defaults because of the name mismatch.

For the nested IDs, TypeScript sends `bare_message_id` inside the start/end objects, not top-level `start_message_id`, so those would default to empty strings anyway — but that's moot since the whole structure rejects. Looking at memory fields, TypeScript includes `project_path` in each memory object but Rust's ShadowMemoryWire doesn't have that field, so it gets ignored (which is fine since the project is derived from the path). The `classified_at` field isn't in TypeScript's memory object, so Rust just uses its default. For memory mutations, TypeScript sends several fields that Rust's ShadowMemoryMutationWire either matches or defaults — `project_path` gets dropped, but the rest align or have sensible defaults.

Pass inputs look good too — TypeScript sends the fields Rust expects, with proper defaults where needed. The real blocker is still that compartment rejection, so let me think through how severe that actually is relative to the overall impact.

It doesn't touch the live lane since the state sync failure gets caught in runQueue and the live transform already returned. But it does break the soak for compartmentalized sessions, which matters for categories 2 and 3. The failures are logged in the counter, yet the divergence dashboard stays silent. What's concerning is this is the third wire-shape mismatch of the same type — the first two were fixed, but the mocks aren't catching this one.

The real issue is that it silently zeros out the soak's coverage for exactly the production population we care about. The divergence dashboard would show green while actually comparing zero compartmentalized passes, which is why I'm ranking this HIGH and recommending we hold. I should verify that state_sync compartments actually get populated by checking if getCompartments returns objects with the right fields like startMessageId — and yes, serializeCompartment reads from args.compartment.startMessageId, so compacted sessions do have compartments.

Writing up the findings now...

The compartment wire-shape mismatch is critical: nested start/end in TypeScript versus flat start_message/end_message in Rust means every state_sync carrying a compartment gets rejected with invalid_params, zeroing shadow coverage for all production sessions and showing false-green on the soak. The m0_mutations field silently gets dropped by Rust since it doesn't exist there, causing shadow M0 state to diverge and miss real divergences. There's also a watermarks field-name mismatch between TypeScript and Rust that gets silently swallowed, though Rust recomputes it so the impact is lower. On the hot path, there's a performance cost from cloning the full array three times per transform pass when shadow is enabled. The isolation guards and deep cloning are holding up, but the real issue is that the compartment wire-shape rejection silently zeros out coverage on the exact production scenario, creating a false-green verdict. I'm tracing through the state_sync timeout concern: if it rejects with invalid_params, it fails fast and moves to the next item, but since the transform never executes before that rejection, lastAckedWatermarks stays null and force remains true, causing every subsequent subscription to retry.

This creates a repeated-work amplification where each pass rebuilds the entire state_sync (getCompartments, serialize, loadWatermarks) only to fail again. I need to verify whether invalid_params gets caught as a peer reject that would trigger blockedUntilReset—if it does, that queues a reset, bumps the generation, and then the next pass hits the same rejection in a loop.

Since invalid_params isn't treated as a peer reject, blockedUntilReset never gets set, so there's no reset loop—just repeated state_sync failures with zero transform coverage. And for the transform body itself, it only gets sent after state_sync succeeds, but state_sync is null only when watermarks are unchanged and force is false. Since force depends on lastAckedWatermarks being set (which only happens after a successful state_sync), and state_sync keeps failing due to the compartment rejection, lastAckedWatermarks stays null forever, force stays true, and state_sync keeps getting attempted and failing—meaning the transform never gets sent in a compartmentalized session.

But here's the insidious part: early sessions with no compartments yet will have an empty compartments array, so the deserialization succeeds, state_sync works, and transforms get sent. The soak test shows green for these early sessions, creating false confidence that everything's working, until the first compartment forms and that session goes silent.
I have gathered sufficient evidence across all four audit areas. Let me do a final false-positive check on the isolation guards and confirm the compartment rejection isn't rescued by a serde alias.

[assistant] 2026-07-10T11:06:14Z
I'm seeing a mismatch between what TypeScript's serialization function outputs and what the Rust implementation expects—the TS version includes fields like `id`, `sequence`, `start` and `end` objects with nested properties, plus metadata like `title`, `content`, `importance`, and timestamps, but the Rust side appears to be structured differently.

The critical issue is that Rust's `ShadowCompartmentWire` requires top-level fields like `start_message` and `end_message` as required i64 values with no defaults, while TypeScript is emitting these as nested objects (`start.bare_message_id`, `end.bare_message_id`). This means any state_sync containing compartments—which happens in every long session that hits a compaction boundary—will fail deserialization with a "missing field" error. Even though the compartments field itself has a default, each compartment element in the array still needs to deserialize successfully, and without defaults on those required fields, the deserialization will fail.

When this invalid_params error propagates up through `processPass` to `runQueue`'s error handler, it doesn't match any of the recognized peer rejection codes (stale_generation, shadow_generation_mismatch, etc.) or connection failure patterns, so it just gets treated as a generic send failure.

This creates a permanent loop: on cold start with `force=true`, the first pass builds a state_sync including all compartments, which immediately rejects with invalid_params. Since the error isn't recognized as a peer rejection or connection issue, `lastAckedWatermarks` never gets updated, so the next pass still has `force=true` and rebuilds the exact same failing state_sync, creating an infinite retry cycle.

The real danger is that this silently breaks the soak's entire purpose — no transforms ever get compared for any compacted session, so the byte-compare mission reports false confidence (all green) when nothing was actually validated. It's the same class of bug as the wire-shape mismatches already found. On cold start, `processPass` calls `performReset` first (which sets `state.lastAckedWatermarks = null`), then builds the state sync payload with `force=true`. This loads all compartments where the sequence exceeds the acked value, serializing them into the nested shape and sending them via `state_sync`. The Rust parser rejects this with `invalid_params` because the required `start_message` is missing — confirmed. The soak targets long-running production sessions that will have passed compaction boundaries, so essentially every relevant session will have compartments, making this a widespread issue.

Now checking whether extra fields like `m0_mutations` and `watermarks` that TypeScript sends but the Rust struct doesn't define could be problematic. The Rust struct has fields for `session_id`, `shadow_generation`, `expected_shadow_seq`, `compartments`, `memories`, `memory_mutations`, `last_todo_state`, and `acked_watermarks`, but no `m0_mutations` or `watermarks` fields. Since serde isn't configured with `deny_unknown_fields`, these extra fields from TypeScript are silently ignored rather than causing an error.

The `watermarks` field is particularly interesting — TypeScript sends the current watermark object, but Rust expects `acked_watermarks` (an optional field that defaults to None). This field name mismatch means Rust never actually receives the TypeScript watermarks and instead synthesizes its own `acked_watermarks` by computing the maximum across compartments, memories, and mutations. It's a divergence with a fallback, so not a hard failure, but it does mean the stored `shadow_acked_watermarks` could differ from what TypeScript thinks it should be. This matters only if `acked_watermarks` is used for correctness checks downstream.

Then there's `m0_mutations` — TypeScript sends this array, but Rust's `ShadowStateSyncWire` struct has no corresponding field, so it gets silently dropped. This means M0 mutations (pending drops) never sync to the shadow store. If the real transform relies on M0 mutations and the shadow transform never receives them, the shadow replay could diverge or skip those mutations entirely. It's unclear whether this is intentional (M0 handled differently) or a coverage gap, but the fact that TypeScript bothers computing and sending it while Rust discards it suggests at minimum wasted work and potentially a correctness issue.

Now I'm reconsidering the compartment finding's severity in context of the already-fixed bugs. The audit mentions two integration bugs were fixed: wire-shape mismatch and ordinal starvation. The wire-shape fix flattened the `{method, params}` nesting, but the compartment objects inside `params.compartments[]` still retain their nested `start`/`end` shape — the flattening didn't address that second-order mismatch. So this looks like a new bug of the same class the audit already caught.

Let me verify: buildStateSyncPayload pushes serialized compartments into the array, and toFlatWireBody spreads params but leaves compartments as a nested array of nested objects. On the wire that means `compartments: [{id, sequence, start:{...}, end:{...}, ...}]`, but Rust expects `[{sequence, start_message, end_message, start_message_id, ...}]` — the missing `start_message`/`end_message` fields would cause rejection. This is confirmed as real.

Now checking whether existing test coverage would catch this, particularly the wire test. The COUNT query filters out summary rows and uses time_created and id ordering to compute ordinal position, which matches how the full reader assigns ordinals by index after filtering — so the by-id fallback is consistent. But there's a potential issue: when the cache is primed with tail-only data, it holds absolute ordinals starting from baseOrdinal, and I need to verify that baseOrdinal aligns correctly with what the COUNT query would return. The risk is that the tail cache uses a frozen baseOrdinal from when the marker was created, while a fresh by-id count would compute the current live ordinal—and if rows were inserted or deleted below the boundary in the meantime, these two bases diverge, leaving the cache carrying stale ordinals while the fallback sees the updated state. This creates a silent poisoning of the shadow store—exactly what the audit flagged. Below-boundary deletions are rare since compacted history is stable, and both paths apply the same summary-row filtering, so the risk is low-medium in likelihood but the failure mode is severe. Let me reconsider whether the tail cache and by-id bases are actually equivalent.

The tail cache sets baseOrdinal to lastCompartmentEnd, with the anchor row included at that ordinal. A full read would assign the anchor an index one past the count of non-summary rows up to that point. For consistency, baseOrdinal must match that count. The question is whether lastCompartmentEnd—set when the boundary was declared—still equals the current full-read index of the anchor if no rows below have changed.

Now looking at the hot-path safety concern with cloning. The messages array gets cloned multiple times per pass: first in transform.ts as a deep copy before the transform, then again in resolveOrdinalsForShadow and denormalizeShadowOutput. This is a synchronous operation on the hot path that could impact performance. So the synchronous cost on the hot path is actually two full JSON deep-clones of the input and output messages plus a database read of all session messages, and I'm realizing that readRawSessionMessages might be doing a complete DB fetch every time enqueue runs because it creates a fresh cache scope that doesn't inherit the transform's cache context. I need to verify whether the cache is still active when enqueue executes or if it's already been torn down. I searched for withRawSessionMessageCache in the transform file and found nothing, which means the cache scope isn't established at the transform level itself — it's set up deeper in the call chain. This means when enqueue runs, there's no active cache, so resolveOrdinalsForShadow triggers a full session read every time instead of reusing a primed tail cache. On a large session, that's a synchronous O(session) operation with DB reads and JSON parsing happening on the hot path, plus two full-array deep clones — a real performance concern. The comment claims these extra reads stay off the hot path, but that only applies to the fallback reads; the primary ordinalById lookup at line 375-378 does a full readRawSessionMessages call on every enqueue pass regardless, wrapped in its own isolated cache scope with no reuse between the two clones, making it a genuine hot-path stall for large production sessions. The enqueue operation compounds this by adding a full database read plus two additional deep clones on top of the one already accepted in cloneForShadow — on a large session with hundreds of messages and tool outputs, each clone operation could take tens of milliseconds, and the database read adds more latency on top. Across a typical interaction, this synchronous work could accumulate to 50-200ms of added delay per prompt, which becomes noticeable to the user even though this is supposed to be a background operation. I'm settling on Medium severity, or High if sessions get particularly large.

Now I'm checking the timeout behavior: when readTerminalFor hits its 5-second limit and throws "subc request timeout", that error propagates to the runQueue catch handler. The isConnectionFailure check looks for keywords like "connection", "backoff", or "ECONN" in the error text — but "subc request timeout" doesn't match any of those patterns, so it's treated as a regular failure rather than a connection issue. The socket itself stays open since the timeout doesn't trigger a destroy, which means if a slow response arrives after the 5-second window closes, the correlation ID was already consumed by the timed-out loop.

The next unaryJson call will invoke readTerminalFor again, and it'll read the next frame from the socket — which could be that late response from the previous request. But readTerminalFor matches frames by correlation ID, so it'll skip any frames with the wrong corr and keep reading until it finds the right one or times out. The framing stays aligned because readFrame reads exact byte lengths, so there's no desync risk there. The pending promise chain continues with the next queued item.

The serialization through the pending chain means if one call hangs for 5 seconds, all subsequent calls wait in line — they're not permanently wedged, just slow. When the socket closes mid-request during a readTerminalFor await, the SocketReader's close handler sets closed=true and flushes waiters, causing readExact to throw "connection closed", which bubbles up through unaryJson and gets caught by runQueue. The isConnectionFailure check sees "connection" in the error text and returns true, so state.initialized gets reset to false with requireResetReason set to "route_reopen", and the next cycle re-initializes. That's handled correctly.

On the mismatch path in enqueue, blockedUntilReset gets set to true, the queue clears, and a reset is queued to run through runQueue, which then clears the block. The unresolved path just returns without queuing anything or blocking — it's a skip, not a wedge. But if state_sync throws invalid_params due to the compartment bug in processPass, we hit the runQueue catch without any peer rejection or connection failure, so nothing sets blockedUntilReset and nothing re-resets — the pass is lost and the next pass retries the same failing state_sync. It's not permanently wedged but it's permanently failing, which is the compartment finding's consequence. The question is whether there's a path where blockedUntilReset gets set to true but no reset ever gets queued — in runQueue catch, isPeerReject sets blockedUntilReset=true and requireResetReason, but doesn't queue a reset.

The next pass's processPass checks requireResetReason and triggers an inline reset at line 952, so it self-heals on the next enqueued pass. If the session goes idle after a peer reject, it stays blocked until the next transform, which only happens on user activity — so an idle session stays blocked but that's acceptable since there's no work to do anyway. When activity resumes, it resets. But there's a race condition in enqueue's mismatch branch: it clears the queue and queues a reset via pushWork, which calls schedule, while blockedUntilReset is already true. If a new pass gets enqueued after the reset is queued but before it runs, there's a potential ordering issue.

Actually, the FIFO queue preserves ordering — the reset runs first and clears the blocked state, then the new pass runs after. So that's fine. Now checking isolation: shadow_binding requires is_shadow_session at line 807, and plain transforms reject shadow bindings.  ctx_reduce also rejects shadow operations at line 1711. Shadow operations need shadow bindings, and the session ID gets prefixed with `shadow:` to create isolation. Store writes use this shadow-prefixed ID and the shadow project path, and resets delete compartments and memories using those same identifiers, so the isolation appears properly enforced.

apply_shadow_state_sync upsets compartments and reads/writes cache state all keyed by the shadow-prefixed session ID, meaning shadow writes can never touch non-shadow rows since the session ID is always prefixed. The reset also cleans up pending agent drops using the shadow-prefixed ID, so isolation holds throughout. This looks verified-clean with low risk.

Now checking whether a shadow divergence or panic affects the real lane — handle_shadow_transform_value uses the same store handle...

Examining store contention... The async handle runs on the daemon's single-threaded executor, so a heavy synchronous transform could block other channel operations and stall real transform handling—worth flagging as medium severity. Now I'm reconsidering the watermarks discrepancy: TypeScript sends current watermarks and expected_shadow_seq, while Rust reads the expected_shadow_seq and synthesizes acked_watermarks from compartment/memory/mutation maximums since TypeScript doesn't send that key, storing the result in the shadow metadata.

Since the CAS operation only checks shadow_generation and shadow_seq, not watermarks, this field-name mismatch means Rust stores a different acked_watermarks than TypeScript intends, but the functional impact is limited to diagnostics—I'd rate this as low-medium severity since it's a silent field mismatch the audit specifically asked to flag. There's also the m0_mutations field that TypeScript computes and sends but Rust completely lacks, so M0 mutation state never syncs to the shadow, which could be problematic if the real transform's output depends on pending m0 mutations. The compartment mismatch is unavoidable for any compacted session since that's the only path they take through the wire. This is critical for the soak test because every state_sync with a compartment gets rejected, leaving zero shadow coverage for those sessions and masking real issues. I'm documenting the findings now: the compartment shape mismatch is the same class of bug as the two already found (mocks don't cross the serde boundary), and there's also a watermarks field-name mismatch to note.

Beyond that, m0_mutations are silently dropped since Rust has no field for them, so the shadow store never gets that mutation state and we lose parity. There's also a performance concern on the hot path—enqueuing does a full readRawSessionMessages scan plus two deep JSON clones synchronously on the live prompt path, which adds noticeable latency on large sessions. And there's cross-lane store contention happening in shadow_transform that needs attention. coverage loss under transient slowness. Isolation checks out. I need to verify the connection model for finding 5, but I've spent enough time on it — marking it as needs-verification with medium confidence. The critical issue is the compartment wire-shape mismatch: it means the soak produces zero transform comparisons for any compacted session, which is every relevant session, so running it would give a false green while testing nothing on the actual compacted path. This is the same wire-shape bug class that's already burned twice and slipped past the mocks. I should write this up.
I have gathered strong, cross-referenced evidence across all four audit areas. Let me drop the large raw outputs I've already extracted and finalize.

[assistant] 2026-07-10T11:09:10Z
I'm identifying a critical mismatch between how TypeScript serializes compartment data with nested objects and how Rust expects flat message IDs, which silently corrupts the shadow state without raising errors.

When a state_sync carries compartments, the Rust deserializer fails on missing required fields, throwing an `invalid_params_error` that doesn't match any peer-reject or connection-failure patterns. The error gets logged as a send failure and ignored, so the transform never gets sent — meaning every pass for sessions with compartments fails silently and permanently. The existing test only validates that compartments exist as an array, never checking the internal structure, which is exactly how this wire-shape mismatch slipped through. So this confirms it's a real bug in the wire shape — the payload is missing the required `start_message` and `end_message` fields that Rust's deserializer expects, causing it to reject every compartmentalized session. This defeats the entire purpose of the soak since there are no valid comparisons for real sessions, making it CRITICAL. The `id` field gets silently ignored by serde since `deny_unknown_fields` isn't set, but the missing required fields are the actual blocker.

Now I'm looking at a second issue: the TypeScript side is sending `m0_mutations` and `watermarks` in the state sync payload, but the Rust `ShadowStateSyncWire` struct doesn't have fields for either of those. Since `deny_unknown_fields` isn't enabled, serde just silently drops them instead of erroring, which means M0 mutations from TypeScript never actually make it to the shadow store.

There's also a mismatch on the watermarks side — TypeScript sends `watermarks` but Rust reconstructs `acked_watermarks` from a fallback instead of using what was sent. This means the watermark validation logic is one-sided and never actually validated by Rust. The real concern is whether dropping M0 mutations breaks the byte-compare correctness, since those mutations affect the m[0] materialization and could cause the shadow transform to diverge from TypeScript, leading to either false-positive divergences or masked real ones.

I need to verify whether Rust is actually handling m0_mutations at all — searching the codebase shows zero matches, confirming it's silently dropped. The issue is that TypeScript tracks `m0_mutation_id` and sends the mutations, but if Rust's shadow transform needs them to compute m[0] correctly and they never arrive, the shadow state gets computed incompletely. es m[0] purely from compartments, so I shouldn't overclaim the risk here — I'll mark it as MEDIUM with that caveat.

Now looking at the ordinal fallback basis mismatch issue, which is a higher-priority concern. The shadow store resolution builds ordinal IDs from the raw session messages using a filtered, time-ordered approach with index-based numbering, but the fallback path that reads messages by ID could produce different ordinals, poisoning the shadow store with misaligned values.

The key question is whether both paths compute the same ordinal for the same message. The full reader orders by creation time and ID, filters out summary rows, then assigns ordinals as index plus one — so a message's ordinal equals the count of non-summary rows strictly before it, plus one. The by-ID reader uses a SQL count query with a WHERE clause that checks whether rows come before the target message in the same ordering, also filtering summaries. Checking if these produce matching results... ached ordinal could be stale if messages below the boundary were deleted since the marker was created. The by-id fallback recalculates the ordinal fresh from the current database state, so these two approaches might diverge if the underlying data has changed. based ordinals (tail cache) and others on the fresh-count basis (by-id). The memo drift check only catches per-message drift across passes, not inconsistency within a single pass, so a below-floor deletion silently corrupts the shadow store with mixed-basis ordinals. The full reader recomputes everything fresh from the current database and stays internally consistent, but the tail-primed path is where this risk surfaces, particularly when using the raw session tail read function.

Actually, I'm realizing that `resolveOrdinalsForShadow` creates its own fresh cache scope when it calls the raw session messages read, so it's doing a full read that's internally consistent. The tail-prime optimization only applies during the transform's own scope, not during the shadow enqueue phase, so the ordinalById map built there should be consistent across all messages. So the tail-only cache scenario described in the comment is real, but I need to verify whether `enqueue` actually runs within the transform's `withRawSessionMessageCache` scope. I searched transform.ts and didn't find `withRawSessionMessageCache` being called there directly, which suggests the transform might not wrap its body in that scope — the priming likely happens elsewhere. Yet the shipped code's comment explicitly references this tail-only scenario as something that occurs, so it must be happening somewhere in the actual flow.

The real issue is consistency between the marker's baseOrdinal and the fresh count-based ordinal. When `readRawSessionTailFromDb` sets baseOrdinal to the marker's value (lastCompartmentEnd), and the by-id path computes a fresh count, these only align if no messages below the boundary have been added or removed since the marker was set. The append-only tail prevents additions, but deletions are a real risk — and in OpenCode, message deletion through operations like /undo or message revert does happen. If a revert removes messages below the boundary, the stale marker basis and the fresh-count basis diverge, creating a mixed-basis ordinal problem.

This mixed basis gets written to the shadow store in a single pass, corrupting the shadow lineage and producing false divergences or masking real ones. The memo check won't catch this because it's cross-pass per-id, not intra-pass cross-id validation. This is the exact "silent basis mismatch poisons the shadow store" failure mode. It requires both an active tail-primed cache during enqueue and a below-boundary deletion making the marker stale, so reachability is medium, but the blast radius is significant — silent shadow corruption. I'd rank this HIGH with medium confidence, though the exact reachability of the primed cache during enqueue needs verification.

Now I'm double-checking whether by-id and full-read ordinals actually agree in the normal non-deletion case, since that's the core assumption. The full-read ordinal is the index plus one among filtered non-summary rows sorted by creation time then id. The by-id ordinal is the count of non-summary rows where either the creation time is earlier or the creation time matches but the id is less than or equal. For any message at position k in the sorted list, k+1 equals the count of rows at positions 0 through k, which matches the by-id count. They align in steady state.

The real divergence only happens when the tail cache has a stale baseOrdinal from a deletion below the boundary. When the cache is tail-primed, messages above the floor get their ordinal from the cache's absolute ordinal (baseOrdinal plus offset), while messages below the floor use the by-id count. If baseOrdinal is correct, both groups are consistent. If baseOrdinal is stale due to a deletion, the above-floor ordinals become stale too.

Finding 4 is about the 5-second request timeout potentially leaving the FIFO queue effectively stalled but not fully wedged. In the queue processing loop, when a peer rejection happens, it sets blockedUntilReset and requireResetReason, then continues. On the next pass, if requireResetReason is set, it triggers a performReset, so the queue doesn't actually wedge—it just stalls until that reset fires.

If the reset itself fails (connection down), the error propagates back into the queue's catch handler, which sets initialized=false and requireResetReason="route_reopen", so it retries on the next pass rather than getting permanently stuck. There's a sub-case I'm still examining here. When a mismatch triggers a reset while a pass is already in flight, that pass completes using the old generation's ordinals before the reset runs, but since the mismatch was detected before that pass was queued, it's a different earlier pass and doesn't cause a deadlock.

Now looking at timeout behavior: if `readTerminalFor` hits its 5-second deadline, it throws a timeout error that gets caught as a send failure rather than a connection failure (since the error message doesn't contain the keywords that would classify it as a connection issue). The socket stays open, but the response frame for that timed-out request might arrive later and get picked up by the next `readTerminalFor` call waiting for a different correlation ID — it'll see the mismatch, discard the stale frame, and keep reading. Since frames are length-prefixed, the stream stays aligned and there's no wedging. The real concern is that all sessions share a single transport instance with a serialized `pending` chain, so a 5-second timeout on one request blocks everything else. The ensureConnected backoff throws immediately within the window, so calls fail fast rather than hanging for the full timeout—only unresponsive-but-connected scenarios hit that 5-second stall, which is an edge case. More concerning is the cloneJson overhead in the hot path: both `resolveOrdinalsForShadow` and `denormalizeShadowOutput` perform full deep clones of potentially massive message arrays (up to 400 messages each), meaning two complete serialization cycles per enqueue operation.

These clones happen synchronously on the critical path before the transform returns, and with large tool outputs the cost could easily exceed 100ms—unbounded by message size and directly delaying the user's prompt response. The try-catch wrapping contains errors but doesn't bound the actual performance impact, which directly answers the concern about whether this per-pass cost is acceptable. So shadow mode violates its own design promise—it's supposed to be observe-only without affecting live latency, but these three clones per pass directly impact user-facing performance. That's a HIGH severity issue given the explicit guarantee. I should also check whether the fallback path for below-floor IDs opens a new database connection on each call, which could compound the problem under SQLITE_BUSY conditions. The fallback approach adds hot-path I/O which is acceptable given the bounded frequency, so that's a medium-level concern folding into Finding 5. Now checking whether `withReadOnlySessionDb` caches the handle — though that's not critical to the overall assessment. The key discovery is that enqueue sits inside a try/catch at the outermost level, so any exception thrown during resolveOrdinals, denormalize, or cloneJson gets caught and logged, keeping the live lane protected. This is actually a positive safety property that holds.

But wait — `cloneForShadow(messages)` happens at the top of the transform function, outside that protective try/catch, so if it throws on a circular reference or similar issue, it would propagate up the main transform path rather than being caught by the shadow handler's try/catch. The question is whether the entire transform itself is wrapped in error handling at a higher level.

Messages come from the database as JSON-serializable data, so circular references are unlikely in practice — this is a low-risk edge case worth noting.

The isolation checks look solid: shadow bindings are rejected in the core operations, shadow writes go to separate tables and cache keys, and the binding validation at the library level enforces the separation.

One potential concern though: `apply_shadow_state_sync` writes compartments to the same `mc_compartments` table as real compartments, just namespaced by prefixing the session_id with `shadow:`. The isolation depends entirely on that session_id string prefix — I need to verify there's no query path that could accidentally match both shadow and real compartments, like a LIKE clause or a missing session_id filter.

Actually, looking at this more carefully: real session IDs follow the pattern `ses_...`, so a real session literally named `shadow:xyz` is extremely unlikely and not attacker-controlled. The isolation holds via session_id namespacing. I'll mark this as verified with high confidence.

Now checking whether shadow compartments properly handle...

The shadow transform runs through the shared store's connection lock, which means it contends with real transforms for DB access. Since every real transform also fires a mirrored shadow transform, both go through the same store connection. This adds lock contention to the real lane, but the real and shadow transforms are on different socket routes — I need to figure out if they're actually concurrent.

The request handler is async, so a shadow transform could run concurrently with a real transform, both competing for the store's fenced connection. This would add latency to real store operations, which is a Class 1 issue but only moderate since the store lock is held briefly per operation. The shadow reporting functions also use the fenced connection. A panic in the shadow comparison logic would unwind that async task without affecting the real one, unless it poisons a Mutex — and the store does use `.expect()` calls on mutexes, so if a shadow operation panics while holding a lock, subsequent real-lane operations would fail.

Looking at the code flow, `compare_shadow_outputs` actually runs outside the store transaction, so it won't hold the lock during comparison. But `record_shadow_report` calls `record_shadow_divergence`, which does use the fenced connection. Since rusqlite errors are Results rather than panics, the real risk is if a panic occurs while holding the shared bindings Mutex — that would poison it and take down the real lane too.

This is a genuine cross-lane blast path, though low-probability since it requires a panic to trigger. I'll mark it as medium severity with low confidence.

On the watermarks side, TypeScript sends `params.watermarks` but Rust expects `acked_watermarks` — Rust never actually receives the TS watermarks and instead synthesizes its own from the compartments and memories. The `expected_shadow_seq` is...

The CAS operation works correctly on generation and seq since those are sent flat in the wire format. The real functional loss is `m0_mutations` — the m0 mutation log. The `watermarks` field is just informational since Rust recomputes it anyway. The key insight is that if compartments are present, the entire state_sync rejects at deserialization, so the m0 drop only manifests in early sessions when compartments are empty and state_sync actually parses.

So I'm seeing two failure modes: sessions without compartments lose m0 fidelity silently, while sessions with compartments lose state_sync entirely. Either way the shadow store ends up incomplete. The m0_mutations matter because the module's m[0] materialization consumes the m0_mutation_log, and the TypeScript side explicitly tracks the m0_mutation_id watermark and requests mutations via getM0MutationsAfterId, so the module definitely expects to consume them.

But I'm finding almost no references to m0_mutations in the module code, and I haven't confirmed the shadow store even has an m0 mutation table. Looking at reset_shadow_session, it deletes shadow_memories and shadow_memory_mutation_log, but there's no shadow_m0_mutation table being deleted — which suggests the shadow store might not have one at all. That means the m0_mutations the TypeScript side sends have nowhere to land on the Rust side, so they're just dropped and never modeled.

I also checked the struct definition in lib.rs and the From implementation — no serde flattening or aliases that would map nested compartment fields. It's all straightforward field mapping with no hidden rescue path.

Now looking at the test that should have caught this: shadow-sender.test.ts builds a state_sync with an empty compartments array on a fresh database and only asserts on the compartments field... The soak test becomes completely ineffective for compartmentalized sessions because the wire-shape mismatch causes every state_sync to fail at the Rust deserialization layer, leaving zero valid comparisons to validate parity. This is the same class of bug as the two already fixed, now present in the nested compartment fields. Beyond that, there's the m0_mutations silent drop and the performance cost of cloning in the live path. Let me document this precisely with file and line references, then rank the severity — the compartment wire rejection is critical since it silently defeats the soak's entire purpose while only surfacing as generic send failures. The isolation guards are consistently applied across all code paths, the enqueue operation is protected against exceptions, and the generation/sequence counters are correctly implemented with compare-and-swap logic. Now I'll put together the final assessment.
I have gathered sufficient cross-referenced evidence across the TS sender, Rust wire structs/handlers, store CAS methods, ordinal readers, and existing test coverage. Here is my independent audit.

<COUNCIL_MEMBER_RESPONSE>
# Blind Adversarial Audit — Magic Context Shadow Mode

**False-positive filtering performed:** I read the actual TS builders (`shadow-sender.ts`), every Rust wire struct + handler (`mc-module/src/lib.rs`), the store CAS (`mc-store/src/lib.rs`), both ordinal readers (`read-session-raw.ts`), the enqueue call site (`transform.ts`), the shadow unit tests (`shadow-sender.test.ts`), the sole Rust integration test (`real_daemon.rs`), and `pi-plugin/PARITY.md`. I confirm there is **no `#[serde(flatten)]`, alias, or `deny_unknown_fields`** on the wire structs, and I checked which shapes the tests actually assert before claiming each bug.

---

## Finding 1: Compartment wire-shape mismatch — every state_sync carrying a compartment is rejected by Rust serde
- **Severity**: CRITICAL
- **Location**: `shadow-sender.ts:584-615` (`serializeCompartment`) vs `crates/mc-module/src/lib.rs:266-295` (`ShadowCompartmentWire`)
- **Confidence**: high
- **Issue**: This is the SAME bug class as the two already-fixed integration bugs (wire-shape), still live for compartment inner fields. TS emits each compartment as **nested** objects:
  ```
  { id, sequence, start:{flat_id, bare_message_id, absolute_ordinal}, end:{...}, title, content, ... }
  ```
  But `ShadowCompartmentWire` declares `start_message: i64` and `end_message: i64` as **required, non-defaulted** flat fields (lib.rs:269-270 — no `#[serde(default)]`). The TS payload contains no top-level `start_message`/`end_message`; the nested `start`/`end`/`id` objects are silently ignored (no `deny_unknown_fields`), and serde then fails with *"missing field `start_message`"*. `handle_shadow_state_sync_value` (lib.rs:2149-2151) returns `invalid_params_error` and the whole state_sync rejects.
- **Blast radius**: Any session that has ever formed a compaction boundary (i.e. every real long session — the soak's entire target population) sends ≥1 compartment in state_sync (`buildStateSyncPayload` line 646-657), so **state_sync rejects on every pass**, and because `processPass` calls state_sync *before* the transform (line 986-1007), `shadow_transform` is never reached. The reject code `invalid_params` is neither a peer-reject nor a connection failure, so the TS side merely does `send_failures += 1` + "send failed (ignored)" (runQueue line 889-904) and retries forever. Net effect: **the soak produces zero valid comparisons for any compartmentalized session** — it silently validates nothing while looking healthy. Class (2), defeats the soak's purpose.
- **Evidence — why the mocks miss it**: `shadow-sender.test.ts:350-390` builds state_sync from `basePass` with a **fresh DB and no compartments inserted**, then asserts only `compartments: expect.any(Array)` — it never populates a compartment nor cross-checks the inner shape against the Rust struct. `real_daemon.rs:300-316` constructs `StoredCompartment` **directly in Rust** and calls `replace_compartments`, bypassing the JSON wire entirely. Neither side ever sends a populated compartment across the wire. Exactly the gap the prompt warned about.
- **Suggested Fix**: Make `ShadowCompartmentWire` parse the nested `start`/`end` objects (custom deserialize or a nested `ShadowCompartmentEdgeWire { flat_id, bare_message_id, absolute_ordinal }`), OR have TS emit flat `start_message`/`end_message`. Then add an end-to-end test that serializes a real populated compartment through `toFlatWireBody` and deserializes it with `serde_json::from_value::<ShadowStateSyncWire>` in a shared fixture.

## Finding 2: `m0_mutations` (and `watermarks`) silently dropped at the Rust parser
- **Severity**: HIGH
- **Location**: `shadow-sender.ts:732,735` (emits `params.watermarks`, `params.m0_mutations`) vs `ShadowStateSyncWire` (lib.rs:185-200)
- **Confidence**: high (drop is certain); medium (functional impact magnitude)
- **Issue**: TS sends `m0_mutations` (from `getM0MutationsAfterId`, line 696-706) and `watermarks` (line 732) in state_sync. `ShadowStateSyncWire` has **no `m0_mutations` field and no `watermarks` field** — only `acked_watermarks: Option<Value>` (default). With no `deny_unknown_fields`, both are **silently discarded**. Grep for `m0_mutations` across `crates/mc-module/src` returns **0 matches**, and `reset_shadow_session` (lib.rs:2207-2226) deletes `shadow_memories` and `shadow_memory_mutation_log` but has **no shadow m0-mutation table** — so the Rust side neither receives nor models m0 mutations. The module's shadow transform therefore materializes m[0] without the m0 mutation inputs the live TS lane applied.
- **Blast radius**: On sessions *without* compartments (where Finding 1 doesn't yet fire), every m0-mutation-bearing pass (todo state changes etc.) will produce a **systematic m[0] byte divergence** whose root cause is missing input, not a real transform bug — poisoning the soak's divergence signal with false positives (or masking real ones). Class (2), corrupts shadow comparison silently. The `watermarks` drop is benign (Rust recomputes `acked_watermarks` at lib.rs:2176-2183), but the `m0_mutations` drop is functional.
- **Suggested Fix**: Either add `m0_mutations` to `ShadowStateSyncWire` + a shadow m0 table + reset cleanup, or (if m[0] is intentionally recomputed from compartments only) stop sending `m0_mutations` and document that m[0] divergences from m0 activity are expected/excluded from the compare.

## Finding 3: Ordinal-basis mismatch between tail-primed cache and the by-id COUNT fallback under below-boundary deletion
- **Severity**: MEDIUM-HIGH
- **Location**: `shadow-sender.ts:375-406` (`resolveOrdinalsForShadow`), `read-session-raw.ts:106-117` (full/tail reader, `index+1`) vs `read-session-raw.ts:384-393` (`readRawSessionMessageByIdFromDb`, `COUNT(*)`)
- **Confidence**: medium
- **Issue**: In steady state the two bases agree: full/tail reader assigns `ordinal = index+1` over filtered non-summary rows sorted `(time_created, id)`, and the by-id `COUNT(*) WHERE …(tc<? OR (tc=? AND id<=?))` equals that same count — verified equivalent. **However**, the tail-primed cache (`readRawSessionTailFromDb`, line 158-239) sets absolute ordinals from `baseOrdinal = lastCompartmentEnd` (the *stored compaction marker ordinal*), while the by-id fallback computes the ordinal **fresh from current DB state**. If any non-summary row **below the boundary is deleted** after the marker was written (e.g. an OpenCode message revert/undo), the marker's `baseOrdinal` goes stale: above-floor messages (served from the tail cache) carry stale-basis ordinals while below-floor messages (served by the by-id COUNT) carry fresh-basis ordinals — **mixed bases within a single pass**. The memo drift check (line 400-402) only compares an id against its own prior value across passes; it cannot detect intra-pass cross-message inconsistency. Result: wrong absolute ordinals written to the shadow store → corrupt shadow lineage that emits false divergences.
- **Blast radius**: Class (2), silent shadow corruption. Reachability is conditional (requires the tail-primed cache to be active during `enqueue` AND a below-boundary deletion). I could not confirm the transform's `withRawSessionMessageCache` scope wraps the `enqueue` call (grep found no `withRawSessionMessageCache` in `transform.ts`), which is why confidence is medium — but the shipped comment at `shadow-sender.ts:386-394` explicitly asserts the tail-only-prime scenario is real for this function, so the code itself claims the risky path exists.
- **Suggested Fix**: Have the fallback derive ordinals on the **same basis** as the primed cache (anchor the by-id COUNT to `baseOrdinal` + relative offset from the marker anchor), or invalidate/refuse the tail-prime basis when the marker ordinal cannot be reconciled with a fresh boundary COUNT (fail loud → reset, rather than emit mixed ordinals).

## Finding 4: Shadow mode adds 3 synchronous full-array JSON deep-clones to the LIVE transform path
- **Severity**: HIGH
- **Location**: `transform.ts:459` (`cloneForShadow(messages)`), `shadow-sender.ts:380` (`cloneJson(args.messages)` in `resolveOrdinalsForShadow`), `shadow-sender.ts:288` (`cloneJson(args.outputMessages)` in `denormalizeShadowOutput`)
- **Confidence**: high (clones exist and run inline); medium (ms magnitude)
- **Issue**: The design states the soak is "without affecting the live session," but with `shadow_transform.enabled`, each transform pass runs, **synchronously on the user's critical path**: (1) `cloneForShadow(messages)` at the top of the transform (line 459) capturing the pre-mutation snapshot, then in `enqueue` (line 2205, which runs inside the transform before it returns) (2) `cloneJson(inputMessages)` and (3) `cloneJson(outputMessages)`. Each is a `JSON.parse(JSON.stringify(...))` round-trip over the **entire** message array. On a 400-message tool-heavy session these arrays are multi-MB; three full round-trips per pass is tens-to-hundreds of ms added to the live prompt latency. The `enqueue` clones are inside a try/catch (line 2226) so they are *throw*-safe, but not *cost*-bounded. `cloneForShadow` at line 459 is **outside** that try/catch.
- **Blast radius**: Class (1) — directly affects live user latency, contradicting the "observe-only, no live impact" contract. Not a correctness bug for the live output, but the worst-weighted class because it touches the live session.
- **Suggested Fix**: Defer/observe the clones off the hot path (structured-clone the input once, hand it to the async queue, and clone the output lazily inside `runQueue` rather than in `enqueue`), or gate the capture behind a size cap and drop (with a counter) above it. Measure per-pass added latency on a large session before arming production.

## Finding 5: Cross-lane coupling via the shared store handle and shared std::Mutexes
- **Severity**: MEDIUM
- **Location**: `handle_shadow_transform_value` (lib.rs:2250-2438) using the shared `Arc<McStore>` + `self.bindings.lock().expect(...)` (lib.rs:790), `with_conn_fenced` in `apply_shadow_state_sync`/`reset_shadow_session`
- **Confidence**: medium
- **Issue**: Every real transform in the soak mirrors a `shadow_transform` that runs `transform_with_projection` + `record_shadow_report`/`record_shadow_divergence` against the **same** `McStore` connection (serialized by `with_conn_fenced`) and takes the **same** `self.bindings` / store std::Mutexes the real lane uses. Two coupling risks: (a) DB-lock **contention** — shadow store ops add fenced-connection hold time that can delay real-lane store ops (latency only, short-held). (b) **Mutex poisoning** — the handler uses `.expect("… mutex")` on shared `std::sync::Mutex`es; a panic in shadow-path code while holding a shared lock would poison it, and the next real-lane `.lock().expect(...)` would then panic too. The compare path (`compare_shadow_outputs`, line 2407) runs outside the store txn, and rusqlite is Result-based, so a panic is low-probability — but the poison path is a genuine cross-lane blast channel that the "observe-only" framing assumes cannot exist.
- **Blast radius**: Class (1) if a poison ever triggers; otherwise class (3) latency. Preserve as a concern rather than dismiss.
- **Suggested Fix**: Ensure no shadow code path can panic while holding a mutex shared with the real lane (replace `.expect` with poison-tolerant recovery or `parking_lot` non-poisoning mutexes for the shared bindings), and consider a separate store connection/handle for the shadow lane.

## Verified GOOD (false-positive filter — not findings)
- **Isolation (3a)**: `shadow_binding` (lib.rs:807-812) rejects non-shadow bindings for shadow ops; `ctx_reduce` (lib.rs:1711-1716) and plain `transform` (lib.rs:1944-1950) reject shadow bindings — enforced on every arm. Shadow writes are namespaced by `shadow:` session_id and `shadow_project_path`, and shadow rows live in separate tables (`shadow_memories`, `shadow_memory_mutation_log`) except `mc_compartments`/`pending_agent_drops` which are keyed by the prefixed session_id. String-exact session_id keying keeps lanes disjoint. Confidence high.
- **CAS (3c)**: `apply_shadow_state_sync` gates on `shadow_generation` and `expected_shadow_seq` (lib.rs:2111-2120) inside `with_conn_fenced`; the flat `shadow_generation`/`expected_shadow_seq` ARE sent correctly by `toFlatWireBody`, so the generation/seq CAS works. Compartment upserts are keyed by `(session_id, sequence)` so overlapping sequences are idempotent-upsert, not duplicating.
- **enqueue throw-safety (2a)**: the `enqueue` call is wrapped in try/catch at `transform.ts:2226`, so a throw inside `resolveOrdinalsForShadow`/`denormalizeShadowOutput`/`cloneJson`/the by-id DB read (incl. SQLITE_BUSY) is caught and the live pass proceeds. Live correctness is protected against shadow throws (the remaining live exposure is cost, Finding 4, and the unguarded `cloneForShadow` at line 459).
- **Frame/reader sync (2d)**: corr+channel matching in `readTerminalFor` (line 1392-1405) with full length-prefixed frame reads means a late response after a 5s timeout is fully consumed and discarded by the next call — the stream stays aligned, no permanent wedge. `pending` chain serializes calls correctly. Queue transitions (`blockedUntilReset`/`requireResetReason`) self-heal on the next enqueue via `processPass`→`performReset` (line 952-959).

---

## Summary
**Findings: 1 Critical, 2 High, 1 Medium-High, 1 Medium.**

- **Critical (1)**: Nested-compartment wire shape rejects at Rust serde — the exact wire-shape bug class already burned twice — silently zeroing the soak's comparison coverage for every compartmentalized (i.e. every real target) session.
- **High (2)**: Shadow mode adds 3 synchronous full-array JSON clones to the live path (violates "no live impact"); `m0_mutations` silently dropped at the parser (poisons the divergence signal with false positives).
- **Medium-High (1)**: Tail-prime vs by-id ordinal basis can mix within a pass after a below-boundary deletion, corrupting the shadow lineage silently.
- **Medium (1)**: Shared store handle + shared std::Mutex create a cross-lane latency/poison channel.

**Verdict: HOLD.**

**Single most important reason**: Finding 1 (compartment wire-shape mismatch) is the same class of wire-shape defect that the first armed run already exposed, still uncaught by the mocks, and it causes **every state_sync carrying a compartment to reject at the Rust serde layer** — so arming the soak on production sessions would produce zero valid parity comparisons for the exact population the gate exists to validate, while the dashboards show only generic "send failed" noise. The soak would certify parity it never actually measured. Fix Finding 1 (and re-test with a real populated compartment crossing the JSON wire in a shared TS↔Rust fixture), triage Findings 2 and 4, before arming.
</COUNCIL_MEMBER_RESPONSE>

