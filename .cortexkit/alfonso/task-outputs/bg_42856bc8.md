---
task_id: bg_42856bc8
agent: council-member
session_id: ses_0cc3a04bfffeV0Sdpjs0WbkT3I
parent_session_id: ses_0cc3ce25bffe2y8Lk3y9CGqpWL
status: completed
completed_at: 2026-07-05T19:47:08Z
---

[user] 2026-07-05T19:34:01Z
## Solo Analysis Mode
You MUST do ALL exploration yourself using your available read/search tools.
- Do NOT use task or any delegation tool under any circumstances
- Do NOT delegate to explore, librarian, or any other subagent
- Do NOT spawn background tasks
- Search the codebase directly — you have full read-only access to every file
- This mode produces the most thorough analysis because you see every result firsthand


## Analysis Intent: AUDIT

You are conducting an **audit** — your goal is to find discrete issues, risks, or violations.

**Focus:**
- Search for problems, anti-patterns, security risks, correctness issues, or violations of stated requirements
- Each finding must be a distinct, actionable item with concrete evidence
- Severity determines priority: critical (blocks/breaks), high (significant risk), medium (should fix), low (nice to fix)
- For each finding, provide the specific location (reference, section, or component where it occurs)
- State your confidence: high (clear evidence), medium (likely but needs verification), low (suspicion, investigate further)
- **This is a broad sweep, not a targeted trace.**

**Analytical standards:** Support claims with concrete evidence. State confidence (high/medium/low) for key assertions. Note caveats and limitations.

**Structure your response as:**
```
<COUNCIL_MEMBER_RESPONSE>
## Finding 1: [Title]
- **Severity**: critical/high/medium/low
- **Location**: [specific reference — e.g. component, section, endpoint, rule]
- **Confidence**: high/medium/low
- **Issue**: [what is wrong and why it matters]
- **Evidence**: [concrete reference, snippet, or observation that proves the issue]
- **Suggested Fix**: [actionable recommendation]

## Finding 2: [Title]
...

## Summary
[Total findings by severity. Overall risk assessment with confidence levels.]
</COUNCIL_MEMBER_RESPONSE>
```

## Analysis Question

Blind design review of a spec for a new Magic Context command, `/ctx-wrapup`, in the repo at ~/Work/Projects/CortexKit/magic-context (branch subc-migration). This is an AUDIT: find real defects with severity + file:line evidence from the ACTUAL code. Do NOT report style nits or hypotheticals without a concrete code path. End with a per-finding verdict and an overall SHIP / REVISE / BLOCK.

You have full read access to the repo. READ the actual code — the reviewer (Athena) has already read the key modules and pasted grounding below, but you MUST independently verify against the real files before asserting a finding. The referenced modules:
- packages/plugin/src/hooks/magic-context/compartment-runner-incremental.ts (the historian run: boundary snapshot handling, discard-last, publish transaction, recordProtectedTailPublicationFloor, setPendingCompactionMarkerState)
- packages/plugin/src/hooks/magic-context/protected-tail-boundary.ts (resolveProtectedTailBoundary, validateBoundarySnapshot, computeRawRangeFingerprint usage)
- packages/plugin/src/hooks/magic-context/read-session-true-raw-tokens.ts (computeRawRangeFingerprint definition — content-based over ordinal:id:parts.length:partContentFingerprint)
- packages/plugin/src/hooks/magic-context/transform.ts (findNewestUserModel / #188 model-switch detection, the >=95% blocking arm)
- packages/plugin/src/hooks/magic-context/transform-compartment-phase.ts (the >=95% blocking arm mechanics: awaitCompartmentRun — awaits ONE run with a per-run timeout, does NOT loop)
- packages/plugin/src/hooks/magic-context/compaction-marker-manager.ts (applyDeferredCompactionMarker, updateCompactionMarkerAfterPublication, existingMarkerAlreadyCoversTarget — monotonic boundary advance)
- packages/plugin/src/features/magic-context/storage-meta-persisted.ts (setPendingCompactionMarkerState — SINGLE-SLOT blob, overwritten per publish; recordProtectedTailPublicationFloor — prior_boundary_ordinal = MAX(existing, floor); clearPendingCompactionMarkerStateIf — CAS clear)
- packages/plugin/src/hooks/magic-context/recomp-orchestrator.ts (recompProgressBySession — single in-memory progress entry per session, setRecompStarting/setRecompTerminal)
- packages/pi-plugin/src/context-handler.ts (Pi emergency drain ~line 2036-2075: awaits ONE in-flight historian with 30s cap, does NOT loop; spawnPiHistorianRun ~2818 with refreshBoundarySnapshot support)
- packages/plugin/src/hooks/magic-context/compartment-trigger.ts (BLOCK_UNTIL_DONE_PERCENTAGE = 95)

=== THE SPEC (verbatim) ===
/ctx-wrapup [messages_to_keep] — forced historian drain of the live tail (v1).

Problem: two gaps, one engine. (1) Large->small model switches still overflow: switching a 553k-token session from a 1M model to a 272k model yields "Input exceeds context window" on the next message. The #188 unified-trigger fix arms recovery when lastInputTokens > new model's trusted limit, but that recovery is sized for incremental pressure, not for closing a ~280k gap — folding that much raw tail needs MULTIPLE sequential historian runs, and the blocking arm waits for at most one. (2) No deliberate "compact now" control.

Command contract: /ctx-wrapup [messages_to_keep], default 20. Runs the historian in a BLOCKING DRAIN LOOP over the eligible tail until coverage reaches the keep watermark. Publishes DEFERRED — never busts the cache itself; materialization rides the next natural bust (model switch -> HARD fold, free). No-op guard: tail already within keep-N -> no LLM run. Concurrency: if a historian run is already in flight, wait for it (same lease/completion machinery as the >=95% blocking arm), then continue the loop; if a wrapup is already running for the session, reject the second invocation. Failure honesty: published chunks stay published; on chunk failure, stop and report coverage reached.

Mechanics — Drain loop reuses compartment-runner-incremental.ts exactly as the emergency drain does, with one difference: the boundary given to each iteration comes from a WRAPUP BOUNDARY OVERRIDE, not resolveProtectedTailBoundary's normal pressure math. Anchor: lastCompartmentEnd+1. Eligible end: the message just above the keep watermark (instead of token-target/live-prompt-floor/0.40×usable math). Chunking WITHIN the eligible range stays the runner's normal chunk budget. Open tool arcs at the cut: snap watermark OUTWARD (keep more) reusing arc-fencing helpers. The keep watermark counts MEANINGFUL messages newest-first, then snaps outward per the arc rule, prefers a user-message boundary.

Discard-last OFF for the FINAL chunk: the runner's discard-last heuristic contradicts wrapup's coverage contract. For the FINAL loop iteration only, persist all validated compartments. Earlier iterations keep discard-last.

Blocking + progress: reuse the recomp progress surface (recompProgressBySession RPC/TUI channel) for per-chunk progress rows. Each iteration is a normal historian run. No hard iteration cap, but abort the loop if an iteration makes NO forward progress (coverage unchanged).

Cache safety (load-bearing): publishes ride the standard deferred-history-refresh path (historian publishes NEVER bust). NO forced materialization, NO flush inside the command. The compaction-marker move stays deferred exactly as for normal publishes. Growing tail mid-wrapup: each iteration re-resolves from CURRENT store state; the keep watermark re-anchors to the tail as of command invocation, so a growing tail only ever ADDS protected messages.

Reactive reuse (phase 2, same engine): upgrade the #188 arm — when lastInputTokens > trusted limit of the OUTGOING model, run the SAME drain loop to a watermark computed from the new limit (target: fold enough that m0+tail fits under executeThreshold × newLimit), BLOCKING, before the request goes out. Replaces the current one-shot blocking recovery for the downswitch case. The blocking wait is on the USER's message send (they see nothing until the drain finishes — potentially minutes for a 553k->272k switch). Accepted v1 (correct beats a provider 400). Show recomp-style progress during the wait.

Pi parity: same command, same loop, same boundary override, day one. Pi's runner is subprocess-based; the loop wraps runPiHistorian the same way its emergency drain does. Pi progress: /ctx-status-style toast updates per chunk.

=== ATHENA'S CODE GROUNDING (verify independently) ===
1. setPendingCompactionMarkerState (storage-meta-persisted.ts:1818) writes a SINGLE-SLOT blob per session — each publish OVERWRITES pending_compaction_marker_state. A wrapup doing 8 sequential publishes writes 8 blobs, but the deferred drain only runs on a LATER transform pass, so all 8 collapse to the LAST blob before any drain reads it. The marker only advances monotonically (existingMarkerAlreadyCoversTarget, compaction-marker-manager.ts:139), and the last pending has the highest ordinal subsuming all earlier ones — so the clobber MAY be benign. VERIFY: does anything between publishes read the pending blob and expect its own ordinal? Does applyDeferredCompactionMarker's validatePendingTarget (checks endMessageId still maps to a compartment with matching ordinal) still pass when only the last blob survives? Is there any m1-revision / decay-render / deferred-refresh state that assumes one-publish-per-trigger-cycle?

2. recordProtectedTailPublicationFloor (storage-meta-persisted.ts:341) writes prior_boundary_ordinal = MAX(existing, floorOrdinal) AND resets recovery_no_eligible_head_count=0. This is a MONOTONIC shared floor read by later NORMAL passes via migrationFloorActive (protected-tail-boundary.ts:480: runtimeFloor = max(offset, priorBoundaryOrdinal)). Wrapup pushes this floor to lastCompartmentEnd+1 each publish. Since normal passes' offset is already lastCompartmentEnd+1, this looks consistent — but VERIFY there is no OTHER boundary-derived shared/persisted state (trigger watermarks, fingerprints, emergency latches) the wrapup's widened boundary writes that a later NORMAL pass reads and would mis-derive from. The runner takes deps.boundarySnapshot as an explicit override (compartment-runner-incremental.ts:216) — trace what durable state the publish path writes from that snapshot.

3. computeRawRangeFingerprint (read-session-true-raw-tokens.ts:661) hashes ordinal:id:parts.length:partContentFingerprint over [offset, eligibleEndOrdinal). validateBoundarySnapshot (protected-tail-boundary.ts:689) recomputes it and rejects on mismatch. The runner re-resolves a stale snapshot ONCE from current state (compartment-runner-incremental.ts:252-274). For wrapup, each iteration hands an explicit widened boundary. VERIFY: when the user keeps chatting mid-wrapup (tail grows), does the growing tail only ADD messages ABOVE eligibleEndOrdinal (so the fingerprint of [offset, eligibleEnd) is unchanged), or can appended messages ever shift ordinals/ids within the eligible range and bust the fingerprint mid-loop? Does the keep-watermark re-anchoring interact correctly with validateBoundarySnapshot across iterations?

4. Discard-last OFF on the final iteration: the runner's discard-last (compartment-runner-incremental.ts:495-509, BOUNDARY_HEALING_SLACK=2) drops the provisional last compartment when lookahead is weak, AND SKIPS fact/event/observation/primer promotion on a discard-last run (lines 566, 632, 589). If wrapup forces discard-last OFF on the final chunk, that final compartment: (a) has structurally-unreliable boundary (no lookahead), (b) gets its facts/events/primers PROMOTED (durable) despite weak boundary. VERIFY the mechanism the spec proposes to force discard-last off, and whether persisting a weak-lookahead final compartment corrupts anything downstream that ASSUMES discard-last semantics (e.g., the offset=lastCompartmentEnd+1 re-read logic, marker endMessageId targeting, decay-render). Is the quality cost acceptable vs the coverage contract?

5. Reactive phase-2 watermark from the NEW model's limit: #188 detection uses findNewestUserModel (transform.ts:251) — the newest USER message carries the NEW model (OpenCode resolves outgoing model from lastUser.model). VERIFY the timing: is the new model's trusted limit resolvable at the point the drain would run, or is trusted-limit resolution deferred (detectedContextLimit only learns the real limit AFTER a provider response)? Can the blocking drain (multiple sequential LLM historian runs, minutes for 553k->272k) mechanically COMPLETE before OpenCode sends the request — i.e., is transform.ts (messages.transform) actually awaited by OpenCode before the outgoing call, and is there any per-pass timeout that would truncate the loop? The existing 95% arm awaits ONE run with historianTimeoutMs (transform-compartment-phase.ts:229, default 120s) — a multi-run loop needs a different budget.

6. Concurrency matrix — attack each: (a) wrapup vs in-flight background historian (lease: compartment-runner-incremental.ts:608 isCompartmentLeaseHeld under BEGIN IMMEDIATE); (b) wrapup vs wrapup (spec says reject second — verify the guard is race-free); (c) wrapup vs /ctx-recomp (recomp uses manual-full-recomp boundary + clears injection cache eagerly, preserveInjectionCacheUntilConsumed=false — wrapup preserves it; do they corrupt each other's marker/cache state?); (d) wrapup loop in progress when the >=95% emergency arm fires mid-loop on a concurrent transform pass (both want to drive the historian; both take the lease). VERIFY the lease + compartmentInProgress flag + activeCompartmentRun registration interplay.

7. UX honesty: (a) the no-op guard ("only N messages above the last compartment") — correct against getRawHistoryEligibility / meaningful-message counting? (b) partial-failure reporting; (c) the /ctx-flush suggestion — is there any case where the suggested flush would NOT apply the wrapup output (e.g., flush busts but re-resolves a boundary that no longer sees the deferred compartments, or the marker hasn't moved so filterCompacted still includes the raw tail)? Trace what /ctx-flush actually does vs what wrapup published.

8. Pi parity risks in the loop-wrapping approach: Pi's emergency drain (context-handler.ts:2056-2075) awaits ONE historian with a 30s cap and CANNOT abort mid-pass (no session.abort surface). spawnPiHistorianRun (2818) fires ONE background run. VERIFY whether wrapping runPiHistorian in a blocking loop is mechanically sound in Pi's subprocess model, whether Pi's transform/pipeline is awaited before the outgoing call the way OpenCode's is, and whether the 30s cap / lack-of-abort changes the reactive-arm correctness on Pi.

Deliverable: for EACH finding, give: a short title, severity (BLOCKER / HIGH / MEDIUM / LOW), the concrete file:line code path that proves it, and a one-line fix direction. Cover the 8 attack vectors above AND anything else you find in the real code. If a vector is a non-issue, say so briefly with the evidence that makes it safe (don't pad). End with an overall verdict: SHIP / REVISE / BLOCK, with the 1-3 findings that drive that verdict.

[assistant] 2026-07-05T19:34:01Z
I'm diving into the actual code files to conduct a thorough audit, starting with the key modules mentioned.
I'll audit this spec against the actual code. Let me start by reading the key modules in parallel.

[assistant] 2026-07-05T19:34:11Z
I'm reviewing several key files to understand the codebase structure—looking at the compartment runner, boundary handling, token processing, and transformation logic.
Let me read the compartment runner and boundary resolution modules.

[assistant] 2026-07-05T19:34:16Z

[assistant] 2026-07-05T19:34:23Z
Now I'm reviewing several key files across the codebase—looking at the transform logic for finding the newest user model, the compartment phase orchestration and triggering mechanisms, the session token computation, boundary snapshot validation, and the Pi context handler's emergency drain functionality.

[assistant] 2026-07-05T19:34:30Z
I need to dig into the transform-compartment-phase to see how awaitCompartmentRun is being called and understand the blocking mechanism, then trace through the findNewestUserModel logic in transform.ts and how it relates to issue #188.
Let me read the #188 model-switch detection and the blocking arm in transform.ts, plus the transform-compartment-phase blocking arm call site.

[assistant] 2026-07-05T19:34:35Z
Looking at the #188 issue around model switching, I need to trace through how the code detects when the previous input token count exceeds the new model's trusted limit, and understand the blocking recovery mechanism and how that trusted limit gets determined in transform.ts.
Now let me find the #188 blocking recovery arm and trusted-limit resolution in transform.ts, plus the recomp orchestrator and Pi emergency drain.

[assistant] 2026-07-05T19:34:41Z
I'm examining the blocking recovery arm in transform.ts around lines 700-790 to locate the model-switch detection logic.
Let me read the #188 blocking recovery arm and the model-switch detection area in transform.ts.

[assistant] 2026-07-05T19:34:44Z
So the #188 arm is a proactive mechanism that raises the percentage to 95%, which then triggers the >=95% blocking arm that only waits for a single run to complete—this aligns with what the spec describes about the recovery process. Now I'm looking into the Pi emergency drain and the spawnPiHistorianRun function.
Now let me read the Pi emergency drain and spawnPiHistorianRun, plus the recomp progress surface.

[assistant] 2026-07-05T19:34:50Z
I'm examining the recompProgressBySession tracking and how OpenCode handles the transform messages, along with the compartment lease and activeCompartmentRun lifecycle. I need to understand the trusted limit resolution and whether the drain loop can finish before the request completes, then trace through the recompStarting and recompTerminal state transitions.

[assistant] 2026-07-05T19:34:55Z
Let me read the recomp-orchestrator progress functions, the ActiveCompartmentRun registration/lease machinery, and the recompProgressBySession structure.

[assistant] 2026-07-05T19:35:00Z
I need to understand how the lease and startCompartmentAgent flow together, along with the applyDeferredCompactionMarker and validatePendingTarget logic, especially how the transform blocking at 95% only waits for a single run. I should also verify whether the /ctx-wrapup command already exists or if this spec is proposing something entirely new.

Let me trace through startCompartmentAgent's lease acquisition, applyDeferredCompactionMarker's validatePendingTarget behavior, /ctx-flush mechanics, and what happens when /ctx-recomp runs with preserveInjectionCacheUntilConsumed set to false.
Let me read startCompartmentAgent (lease acquisition), applyDeferredCompactionMarker, and check whether /ctx-wrapup exists yet plus /ctx-flush behavior.

[assistant] 2026-07-05T19:35:10Z
I'm looking at a spec that references `/ctx-wrapup` which hasn't been implemented yet, so I need to verify whether the proposed design will actually work with the existing code paths. Let me trace through execute-flush.ts to see how `/ctx-flush` works and understand the deferred compaction marker logic and recomputation boundary handling.
/ctx-wrapup doesn't exist yet — this is a spec review. Let me read execute-flush.ts (what /ctx-flush actually does), applyDeferredCompactionMarker, and the recomp boundary handling.

[assistant] 2026-07-05T19:35:21Z
I'm tracing through the /ctx-flush behavior and how it drops pending ops, then examining the deferred compaction marker drain during the transform postprocess pass. I need to understand how filterCompacted interacts with the marker, check the recomp boundary for manual-full-recomp, and figure out whether OpenCode awaits the transform call—particularly how the >=95% threshold gets its wait condition.

The critical pieces are validatePendingTarget (checking ordinal matching), the transform-postprocess drain consuming the pending blob, and the arc-fencing helpers for the "snap watermark OUTWARD" behavior mentioned in the spec.
Let me look at validatePendingTarget, the postprocess drain that consumes pending blobs, and check whether transform is actually awaited by OpenCode (the timing question for the reactive arm).

[assistant] 2026-07-05T19:35:31Z
I need to trace through the deferred marker drain during postprocess to see how it handles pending data and CAS operations, and check whether the transform is awaited before making the outgoing call and how the OpenCode hook gets registered.

[assistant] 2026-07-05T19:35:37Z
Let me read the deferred marker drain around line 1180 and 90-110 in postprocess phase, plus how the transform hook is registered (to verify it's awaited).

[assistant] 2026-07-05T19:35:42Z
I'm realizing the deferred marker drain only processes one pending blob per pass, and since the wrapup loop does sequential publishes that each overwrite the pending compaction marker state, only the final blob's marker persists—which has the highest ordinal and subsumes all earlier ones, so the marker advancement is actually correct. But there's something subtle about how the wrapup loop itself runs that I need to think through.

The real concern is the reactive phase-2 timing and whether transform is actually awaited before the outgoing request goes out. I need to trace through how the hook registers the transform and check the concurrency model around the lease acquisition and compartment runner reuse.
Let me verify the critical timing question: is `transform` actually awaited by OpenCode before the outgoing request, and how is the hook registered.

[assistant] 2026-07-05T19:35:56Z

[assistant] 2026-07-05T19:36:03Z
The transform hook is registered as `experimental.chat.messages.transform` and it's awaited before the outgoing request is sent, so I need to verify how the existing blocking mechanism relies on this being awaited.

Now I'm examining whether the reactive arm's blocking behavior would require multiple runs through a drain loop, since the current code only blocks for a single run with a 120-second timeout. I'm also checking the timing around the trusted-limit resolution — the arm uses the catalog cap via `getSdkContextLimit`, which is resolvable synchronously, so the new model's limit is available when the arm runs. This means the spec's requirement to fold enough messages so that the original plus tail fits under the executeThreshold times the new limit is achievable with catalog-based limits.

I'm now looking more closely at the discard-last final-chunk concern and the offset re-read logic to verify how the offset is calculated when wrapup persists a weak-lookahead final compartment. Each iteration must recompute a fresh boundary snapshot because the offset advances as compartments are appended, and the validation check compares the expected offset against the snapshot's offset — so reusing a stale snapshot would fail validation in subsequent iterations. The real concern is whether the runner's stale-snapshot refresh path uses the correct pressure math when the wrapup's explicit boundary snapshot becomes invalid, since it falls back to normal incremental-runner mode rather than the wrapup override. Each iteration needs a fresh snapshot since offset advances, so a stale snapshot from iteration 1 would fail validation in iteration 2. There's a latent design risk: if a freshly-built wrapup snapshot somehow triggers the stale-snapshot self-heal path, the normal boundary math would silently narrow it and break the wrapup contract—though a fresh snapshot should validate fine and avoid this. The more concrete issue is whether the transform can even loop historian runs: `startCompartmentAgent` bails if an active run exists, but once a run completes and activeRuns clears, sequential starts are possible, though each start acquires a new lease.

Between wrapup iterations, a background historian or the 95% arm could grab the lease during the gap when it's released—that's a real concurrency vulnerability. The critical timing question is whether OpenCode's hook itself has an outer timeout that would truncate a multi-run loop and send the request anyway, causing overflow. I need to verify the stale-snapshot refresh behavior, whether both wrapup and 95% arms contend for the lease, and how the Pi loop mechanics actually work with the single inFlightHistorian entry.
The transform is registered as `experimental.chat.messages.transform` (hook.ts:883) and the 95% arm awaits inside it, confirming OpenCode awaits the transform. Let me verify a few remaining specifics: the eligibility/no-op counting, Pi's in-flight single-slot, and the emergency-recovery arm's interaction with the wrapup loop.

[assistant] 2026-07-05T19:36:53Z
I need to trace through the getRawHistoryEligibility structure and understand how the trigger mechanism works, particularly around when the detectedContextLimit and trusted limit get established after receiving a provider response. I'm also checking whether the reactive flow blocks during message send in a way that would prevent a multi-run loop from completing.

[assistant] 2026-07-05T19:36:59Z
I'm checking how the trusted-limit resolution timing works, specifically looking at when detectedContextLimit gets learned from provider responses and tracing through the recordOverflowDetected flow.
Let me verify the reactive-arm timing claim — how detectedContextLimit is learned, and confirm the getSdkContextLimit catalog path is available synchronously. Let me also check the compartment-trigger BLOCK_UNTIL_DONE and the lease under BEGIN IMMEDIATE.

[assistant] 2026-07-05T19:37:11Z
I'm tracing through the reactive and proactive timing mechanisms to verify the spec's claim about when the drain loop completes relative to the request. The 95% arm waits for a single run with the historian timeout, and the proactive arm bumps the percentage to 95% which then triggers the blocking arm that also awaits just one run. Now I need to check whether getSdkContextLimit is synchronous.

The catalog resolution for the new model's limit is synchronous via getSdkContextLimit, which is critical for computing the phase-2 watermark at drain time without waiting for a provider response. But here's the key issue: the spec says the reactive arm should run the same drain loop blocking before the request goes out, yet the existing proactive arm at transform.ts sets needs_emergency_recovery and bumps to 95%, routing into the blocking arm that awaits only ONE run with a timeout. To match the spec's loop requirement, that single await would need to become an actual loop, which is a substantial change to transform-compartment-phase.ts rather than a simple drop-in fix.

Looking at the actual defects with evidence, the reactive drain loop's blocking wait needs to happen inside the transform, but the current 95% arm only awaits one run. The deeper problem is that the drain loop requires published compartments to be materialized with drops applied and the marker moved for coverage to work correctly.

But here's the crux: the spec says publishes ride the deferred refresh path with no forced materialization inside the command, so even after the drain loop completes N publishes, the outgoing request on this pass still carries the raw tail because the compaction marker move and drop materialization happen on a later pass. The reactive arm's whole point is to shrink the prompt before the request goes out to avoid a provider 400 error, but if publishes are deferred and not materialized on this pass, the prompt sent is unchanged — the raw tail is still present because the compaction marker hasn't moved, the queued drops haven't been applied, and the injection cache is preserved. For OpenCode specifically, the prompt reduction on model switch is described as riding the next natural bust, which is a hard fold at no cost.

So the model switch itself triggers a hard fold that busts cache and materializes. The key question is whether the transform on the model-switch pass actually materializes the deferred history and applies the compaction marker within the same pass after the drain loop completes. Looking at the post-transform phase, the deferred compaction marker only fires when the deferred signal was set at the start of the pass — but the drain loop's publishes happen during this pass, so the signal is set mid-pass, not at the start. This means the marker apply defers to the next pass, so the outgoing request on the model-switch pass would still carry the pre-drain history, which directly contradicts the spec's stated goal of preventing the provider 400 on the switch turn.

But here's the critical issue: the token count of the outgoing request depends on whether the compaction marker has moved and the drops have materialized. Publishing compartments alone doesn't reduce the outgoing token count until the marker moves — so for the reactive downswitch, deferred publishing means the prompt isn't reduced on the blocking pass, and the 400 still happens. This is a blocker-level design contradiction unless the model-switch pass forces materialization, but the spec explicitly says no forced materialization and no flush inside the command.

For the reactive phase-2 arm, the model switch IS the current pass, and the drain needs to run before the request goes out to prevent the 400. That means the marker must move and drops must materialize on this pass. A model switch triggers a hard fold with mustMaterialize, and at 95% utilization the drops should materialize — but the compaction marker apply gates on whether deferred history was pending at the pass start, not mid-pass. Since the drain publishes mid-pass, the marker won't move this pass. The real question is whether the drops materialization alone shrinks the outgoing wire by removing those messages, regardless of the marker position. I need to verify whether the proactive arm's 95% bump actually prevents the blocking arm from running, since both paths await compartment runs inside the transform. The spec calls for a loop in the blocking arm, but the existing behavior already awaits one run, so that's consistent. Now I'm examining the discard-last-OFF mechanism and whether persisting a weak-lookahead final compartment corrupts the offset re-read — the marker gets snapped to a user message boundary by findBoundaryUserMessage, so even a weak boundary should be corrected.

For the wrapup versus context-recomp cache interaction, the lease and activeRuns guard serialize them, so they can't run concurrently. The wrapup respects activeRuns when checking for in-flight operations, which mitigates the risk. On the single-slot pending blob issue, multiple publishes overwrite it before the drain reads, but the marker only advances monotonically, so only the final state matters. The loop runs sequentially with each compartment agent settling before the next starts, so activeRuns clears properly between iterations. The real issue emerges when the tail grows during execution — new messages append above the eligibleEnd boundary, keeping the fingerprint valid, but the runner's validateBoundarySnapshot also checks the last message ID at trigger time, which fails if the tail has grown, forcing a re-resolution from current state.

When that happens, the runner calls resolveOpenCodeProtectedTailBoundary in normal mode rather than using the wrapup's "message above keep watermark" override, silently replacing the widened boundary with a pressure-based one. This means mid-loop iterations could compact only to the normal protected tail instead of honoring the wrapup's coverage target, potentially causing an infinite loop or under-coverage.

But looking closer at the validation logic: growth alone doesn't invalidate the snapshot because appending new messages leaves the message at the original trigger ordinal unchanged. The stale check only fires on shrink or edits within the range. However, during a wrapup iteration, if anything within the eligible range changes, the reactive downswitch scenario still applies—the runner would re-resolve with normal boundary math instead of the wrapup's override.

This is a significant design gap: if a snapshot goes stale during iteration, the runner silently reverts to pressure-based boundary logic, breaking the coverage contract. The fix would be adding a wrapup-mode flag so stale re-resolution preserves the override.

I'm also noticing a potential timeout issue: the 95% arm uses a 120-second historian timeout per run, and for a large gap like 553k→272k, that could mean 5-8 runs spanning many minutes. OpenCode or the provider might have their own request timeout, so blocking the transform for that long risks triggering timeouts or user aborts.

There's a correctness gap here too: if a run times out but later publishes in the background, the loop's synchronous coverage check won't see the progress yet, causing it to abort prematurely. Then the background publish lands late, breaking the accounting.

On the Pi side, the emergency drain awaits a single historian with a 30-second cap and can't abort hung runs. For a large drain needing many iterations, each capped at 30 seconds, the reactive arm becomes unreliable. I need to verify whether Pi's context handler is actually awaited and blocking, and confirm that the synchronous catalog lookup for SDK context limits works as expected.

For the wrapup loop and concurrent transforms: the lease and activeRuns tracking should serialize most cases, but between iterations when the lease is released, another transform pass could sneak in. The spec requires re-checking activeRuns each iteration, which is manageable. Now I need to verify Pi's awaiting behavior and the getSdkContextLimit synchronicity.
Let me verify the Pi pipeline awaiting behavior and the reactive-arm's synchronous limit resolution.

[assistant] 2026-07-05T19:39:03Z
I'm seeing that getSdkContextLimit is synchronous and pulls from the models-dev cache, which means the reactive-arm can resolve the new model's catalog limit right away—matching what the proactive arm already does. This addresses Athena's concern about the trusted-limit resolution being deferred, since the spec expects the fold to fit the model and tail under the executeThreshold.

The real question is whether the blocking drain loop (multiple sequential LLM historian runs taking minutes) can actually complete before OpenCode sends the request. The transform is awaited and the 95% arm blocks inside it, so a loop could theoretically block there, but I can't verify from this repo whether OpenCode itself imposes a timeout on the transform hook—that's an internal detail I can't confirm, which is a genuine risk.

Now I'm focusing on what seems like the most critical issue: the single-slot pending_compaction_marker_state blob and how the deferred drain interacts with it.

Each publish in the wrapup overwrites the marker state and calls queueDropsForCompartmentalizedMessages with its own lastCompartmentEnd. Since publishes are sequential and each covers a higher watermark, the subsequent calls queue drops up to progressively higher points—that's fine and idempotent. The marker eventually jumps to the last ordinal, covering all compartments from every publish since they're already committed to the DB. But here's the real concern: the spec says publishes are deferred and never bust the cache themselves, relying on the next natural bust like a model switch. For the standalone /ctx-wrapup command with no reactive arm, there might not be any natural bust coming at all.

The compartments sit in the DB but the compaction marker stays pending, so filterCompacted still includes the raw tail. The user expects context to shrink but nothing materializes until the next cache bust. The spec mentions a "suggested /ctx-flush" but executeFlush only drops pending_ops tags—it doesn't move the compaction marker or force history materialization. However, /ctx-flush also signals the refresh sets, which should trigger the deferred marker drain on the next transform pass if historyWasConsumedThisPass and deferredHistoryWasPendingAtPassStart both hold. This is the core of Athena's vector #7c, and I need to verify whether the ctx-flush handler actually signals deferredHistoryRefreshSessions and whether the reactive phase-2 arm handles this differently than the standalone command.
Let me examine the /ctx-flush command path to verify it drives the deferred marker drain, and check what signals it sets.

[assistant] 2026-07-05T19:39:37Z
I'm checking the gating logic for the deferred marker drain—specifically how `historyWasConsumedThisPass` and `deferredHistoryWasPendingAtPassStart` control when the marker moves forward. The marker only advances when history gets re-consumed, which happens on a cache bust, and this is key to understanding whether /ctx-flush actually applies the wrapup output.

Now I need to trace whether /ctx-flush triggers `historyWasConsumedThisPass`. The executeFlush function sets `historyRefreshSessions` and `pendingMaterializationSessions`, but I need to verify if `historyRefreshSessions` actually causes a history rebuild that would set the consumed flag and allow the marker to drain.

[assistant] 2026-07-05T19:39:46Z
I'm examining the deferred marker drain logic more closely—specifically when it triggers based on whether history was consumed this pass and whether deferred history was pending at the start. I need to trace through line 1145 to see what drives `historyWasConsumedThisPass` and confirm the `/ctx-flush` path's role, then understand the critical timing issue around `/ctx-flush` executing after wrapup publishes deferred markers.

[assistant] 2026-07-05T19:39:53Z
I'm tracing through the `/ctx-flush` mechanism to understand how it relates to the failure-honesty attack vector—specifically how `executeFlush` clears pending state and what that means for cache busting behavior in the wrapup flow.

The critical issue is that `onFlush` populates `historyRefreshSessions`, but the deferred marker drain that actually materializes the wrapup output checks `deferredHistoryRefreshSessions` instead—a completely separate set. The wrapup's deferred publishes signal through `onDeferredMarkerPending`, which feeds into `deferredHistoryRefreshSessions`, so I need to verify whether `/ctx-flush` actually triggers the conditions needed for that marker drain to fire.

The marker drain gate requires both `historyWasConsumedThisPass` and `deferredHistoryWasPendingAtPassStart`. A flush can satisfy the first condition through cache busting, but unless `deferredHistoryRefreshSessions` was already set by the deferred publish, the drain won't execute. The spec's own claim is that materialization rides the next natural cache bust—no forced materialization inside the command itself—so the raw tail stays in the prompt until that natural bust occurs. This is the intended design, and the finding hinges on understanding whether the flush actually triggers the deferred drain or just drops pending ops and signals refresh sets without moving the compaction marker. The multi-run coverage actually works mechanically since the runner reads directly from the compartment offsets rather than relying on the deferred marker, so sequential iterations do make progress. The real blocking issue is the per-run timeout on each compartment execution — the spec acknowledges this needs a different budget for the multi-run loop, but that gap remains unresolved. The strongest defect is that the reactive arm's detected-context-limit isn't synchronously resolvable at transform time, unlike the proactive arm's catalog-based limit which is available immediately. The timeout semantics create a real design gap: if an iteration times out, the spec doesn't clarify whether the loop aborts and sends an oversized prompt (causing a provider 400) or keeps waiting — and reusing the existing proceed-on-timeout behavior would undermine the whole correctness guarantee. That's a HIGH confidence issue. For the pending marker blob, the intermediate overwrites during the eight publishes are benign for the marker itself since the final blob subsumes them and validatePendingTarget checks against the last one, but there's a subtle real issue lurking in how the wrapup executes those publishes synchronously within the blocking command.

Each publish triggers marker state updates, boundary ordinal advances, and deferred history refresh callbacks all inside the same transaction, but here's the contradiction: the marker move itself stays deferred and only rides the next natural bust, yet the reactive phase-2 drain runs blocking right before the outgoing request goes out. So when the request is sent, OpenCode's compaction marker hasn't moved yet — it still points at the old boundary — which means the full raw tail gets sent anyway, defeating the entire purpose of the reactive arm's overflow prevention. The spec claims "publishes ride the standard deferred path with no forced materialization," but that directly conflicts with the phase-2 requirement to run a blocking drain before the request.

Actually, wait — I need to reconsider how the existing 95% emergency arm actually shrinks the prompt. It awaits the historian, rebuilds the history injection with a bust, and returns. The real prompt reduction happens through the drop mechanism: the historian publishes compartments that get injected as session history, and the raw messages that are now compartmentalized get dropped from the outgoing array via the transform's message rewriting logic, not through OpenCode's compaction marker at all. So the shrinking happens when pending ops get applied during the transform postprocess phase, which requires the `shouldApplyPendingOps` flag to be set.

For the reactive downswitch arm, the existing overflow-recovery bump pushes to 95%, which triggers `forceMaterialization` and enables the drops to apply. So the raw tail does get removed from the wire via drops on that same pass. The manual `/ctx-wrapup` command, though, explicitly avoids forced materialization, so the raw tail stays until a natural bust — that's the intended deferred behavior. The key question is whether the drop application happens after the reactive loop completes within the same transform pass, since the loop runs inside a blocking await and the postprocess phase runs afterward.

The reactive arm blocks on the user's message send while making multiple LLM historian calls (which can take minutes), and there's a risk that OpenCode's own request timeout could abort the transform mid-operation — that's a UX and robustness concern worth noting.

For the final compartment boundary, the spec says to snap the watermark outward using arc-fencing helpers and prefer a user-message boundary. The discard-last heuristic normally drops weak lookahead compartments, but wrapup's final cut is deliberate (the keep watermark), not lookahead-limited, so the boundary should be reliable even if the final compartment is persisted with discard-last off.

However, there's a real implementation gap: the runner's discard-last is internal and tied to the `inEmergency` flag, but the spec assumes a toggle to force it off for the final chunk that doesn't currently exist. The runner processes one chunk per invocation and may emit multiple compartments, so "discard-last off for the final iteration" means the last runner invocation keeps all its compartments — implementable via a new dependency flag, but the current code doesn't expose this control.

**Finding 5 (MEDIUM): Fingerprint / growing-tail interaction.** The fingerprint hash covers [offset, eligibleEnd), so if new messages append above that range during wrapup, the fingerprint stays unchanged and validation passes. But when the wrapup snapshot goes stale and the runner refreshes it, the stale-snapshot resolution uses normal pressure math instead of the wrapup boundary override — silently replacing the widened boundary with a normal-pressure one mid-loop, so the iteration compacts only the normal-pressure eligible head instead of the wrapup watermark.

Actually, let me trace through validateBoundarySnapshot's "last" check more carefully. The check compares the message ID at the captured ordinal against the current ID at that position — if the tail grows, that ordinal's message doesn't move, so the ID stays the same and validation passes. The fingerprint over [offset, eligibleEnd) remains stable too. And the wrapup boundary override freezes the keep watermark at invocation time, so a growing tail only adds protected messages above it. So the growing-tail scenario is actually safe — the stale-refresh path won't even trigger.

But there's a real problem lurking: between wrapup iterations, each publish advances lastCompartmentEnd, which shifts the offset. If the wrapup loop reuses a snapshot with the old offset, validateBoundarySnapshot rejects it as stale and the runner falls back to normal pressure math, silently losing the wrapup override. The spec says each iteration re-resolves from current store state, so it should rebuild the snapshot with the new offset each time — but the runner's silent fallback to normal math on any stale detection is dangerous. If the wrapup snapshot ever becomes stale, the widened boundary disappears without error.

At low pressure, normal math might yield no eligible head, causing the refresh to produce no runnable window, the iteration no-ops, the loop detects no forward progress and aborts. So a stale wrapup snapshot degrades to silent under-delivery rather than a crash, but it's a real design risk.

On concurrency: wrapup can't start its own run while one is active because the lease guard blocks it — it waits like the 95% arm. But wrapup-versus-wrapup needs a new per-session guard. The command handler runs in "command.execute.before" — if that's not serialized per session, two /ctx-wrapup calls would both enter. The activeRuns check would make the second wrapup see an active run and wait rather than reject, which isn't quite right.

The bigger issue is wrapup versus /ctx-recomp. Recomp checks activeRuns and skips if active, but it clears the injection cache eagerly while wrapup preserves it. If recomp runs between wrapup iterations — which could happen if wrapup acquires and releases the lease per iteration rather than holding it continuously — recomp could rebuild all compartments mid-wrapup, jumping the offset and staling the next iteration's snapshot. The spec blocks wrapup-versus-wrapup but doesn't block wrapup-versus-recomp, so there's a potential corruption window if the lease isn't held across all iterations.

Similarly, the 95% emergency arm could fire during those inter-iteration gaps. It serializes with activeRuns, but between iterations when the lease is released, it could start its own run with normal boundary math, publish, and advance lastCompartmentEnd — again staling wrapup's offset. The core finding is that wrapup doesn't hold a continuous lease across its loop, leaving windows where recomp, the 95% arm, or background triggers can grab it, publish with normal math, and desync the offset. The fix would be to hold the lease continuously.

The spec says to wait for an in-flight run then continue, but it's silent on whether wrapup actually holds the lease between its own iterations — if the runner acquires and releases per iteration, the gap persists.

For Pi, the emergency drain awaits a single inFlightHistorian with a 30-second cap and can't abort. To wrap runPiHistorian in a blocking loop, the pipeline needs to be awaited before the outgoing call, but the 30-second timeout would truncate any multi-run sequence lasting longer, forcing Pi to proceed with an oversized prompt.

The spec claims "Pi parity: same command, same loop, day one," but the emergency drain's 30-second cap plus single-run constraint is fundamentally incompatible with a minutes-long multi-run blocking drain. The manual /ctx-wrapup command on Pi could work since it bypasses the emergency path and can loop with its own awaits, but the reactive downswitch arm hits the 30-second truncation. I should also verify whether OpenCode itself awaits the transform for minutes without its own timeout — the existing 95% arm already blocks up to 120 seconds inside the transform.

I've confirmed that the reactive arm does await the transform (hook.ts:883 registers the experimental.chat.messages.transform and calls awaitCompartmentRun), and the per-run timeout is 120 seconds with the caller proceeding on timeout (transform-compartment-phase.ts:234-240). The no-op guard logic is also doable but carries a minor risk: if the guard counts raw messages while the watermark counts meaningful messages, they could diverge and allow a run that immediately no-ops — though this is low severity. Now I need to verify one more detail about the reactive arm and the marker.

The reactive arm's value hinges on whether the drops can actually shrink the wire enough. Each run publishes compartments and queues drops, and after the loop those drops apply and shrink the wire. To bring 553k under 272k, the drops need to remove roughly 280k of raw tail, and the injected session-history summary (capped at ~60k tokens) must be much smaller than what it replaced — so replacing 280k raw with ≤60k compartment summary gives a net shrink, which is plausible. I've also confirmed that the wrapup's deferred publishes don't prevent the next iteration's runner from seeing the advanced offset, since the runner reads compartment rows from the database which ARE committed per publish, so sequential coverage works. The marker deferral plus reactive arm interaction is still being examined — after the blocking loop the marker hasn't moved yet (deferred), but drops shrink the wire at ≥95%, though I need to check whether drops remove raw tail messages from the outgoing array on the same pass.

I've verified that recordProtectedTailPublicationFloor advances monotonically and the wrapup pushes it consistently with normal behavior, so that boundary-derived shared state is safe. The runner does accept an explicit widened snapshot via deps.boundarySnapshot, which means a wrapup can pass one, but there's no toggle for discard-last and the stale-refresh path is hard-coded to normal math.

I've also confirmed there's no per-session wrapup lock anywhere, and the activeRuns guard only blocks recomp during an iteration—between iterations the set clears, so there's a window where wrapup, recomp, and the 95% arm could interleave. I'm ready to write up the findings now.

For severity: Finding 1 is HIGH, bordering BLOCKER for the reactive arm's guarantee. Finding 2 is HIGH due to the lease gap across iterations. Finding 3 is MEDIUM-HIGH since the runner silently discards the wrapup override. Finding 4 is MEDIUM—the discard-last toggle doesn't exist yet. Finding 5 is HIGH for Pi parity given the 30-second cap and single historian slot. Finding 6 is MEDIUM. Finding 7 I need to reconsider: the marker deferral is by design for manual wrapup, but when a user runs "compact now" and sees no context reduction until a natural bust, that's a UX honesty issue. However, /ctx-flush actually does drain the marker because it triggers a rebuild that consumes history and fires the deferred marker drain—so the flush suggestion works, though it also drops the pending_ops queue separately.

The verdict-driving findings are 1, 2, and 5. Finding 1 breaks the timeout truncation guarantee but the spec acknowledges a different budget is needed, so it's a known open item. Finding 5 is a genuine mechanical incompatibility with Pi. Finding 2 is a correctness hole not acknowledged. Overall this is REVISE, not BLOCK (the manual command core is sound) and not SHIP (multiple HIGH findings unaddressed). I'll write this up concisely with file:line evidence.
I have enough evidence across the runner, boundary resolver, transform arms, marker manager, storage, and Pi handler. Let me verify two final specifics: whether wrapup holds a lease across iterations (per-iteration acquire/release) and the recomp active-run guard.

[assistant] 2026-07-05T19:43:07Z
I've gathered enough evidence to piece this together. I need to verify the injection cache behavior between recomp and wrapup, and whether recomp registers the same lease and activeRuns. But more critically, I should check if the reactive drain loop could actually finish before the request completes.

Looking at the 95% arm, it awaits a single run with a per-run timeout. The reactive arm needs a loop, so I should confirm there's no external timeout truncating the transform phase. The real issue is clearer now: the compartment lease gets acquired and released per wrapup iteration in startCompartmentAgent, which means between iterations there's a window where a concurrent background historian or emergency arm could grab it. That's a genuine concurrency gap.

I also need to verify whether recomp registers in activeRuns like the 95% arm does, and whether the single recompProgressBySession slot per session would collide if wrapup and recomp write to it simultaneously—that would be a real conflict on the in-memory progress entry.

[assistant] 2026-07-05T19:43:20Z
I'm analyzing the core issues around the reactive arm and deferred marker behavior, particularly how the pending compaction marker state gets overwritten during multiple publish cycles and the implications for the discard-last-off concern. During the wrapup loop, no transform pass runs since it's synchronous within a single pass, so the eight blobs collapse to just the last one—benign. The `recordProtectedTailPublicationFloor` advances the prior boundary ordinal monotonically via MAX, and normal passes read it through `migrationFloorActive`, which gates whether the floor applies; when it's false, the floor is ignored anyway since it matches the offset. Both vectors check out as non-issues.

Now looking at the fingerprint and growing tail: `computeRawRangeFingerprint` hashes the range from offset to eligibleEnd, which stays stable as new messages append above the protected tail. The `validateBoundarySnapshot` check compares the message count at trigger time against the current count—if the tail grows, the count only increases, so the check passes. The message ID at that ordinal remains the same, so validation succeeds.

But here's the tension: the wrapup spec provides an explicit widened boundary, yet the runner still validates it via `validateBoundarySnapshot` (lines 230-238). If validation fails due to staleness, the runner self-heals by re-resolving through `resolveOpenCodeProtectedTailBoundary` (lines 252-274)—which uses normal pressure math, not the wrapup override. This could silently discard the wrapup's widened eligible-end and revert to incremental sizing, defeating the drain. However, since each iteration rebuilds the snapshot fresh from current store state with the new offset, the fingerprint should stay fresh as long as the tail state is captured at snapshot-build time.

The real risk emerges if the tail grows between snapshot-build and validation—the `last` check fails, triggering a re-resolve via normal math that bypasses the wrapup override. On OpenCode this is contained because the wrapup loop is synchronous and blocking (no concurrent user input during a single request), so the tail can't grow mid-iteration. On Pi subprocess it's different. The spec claims "growing tail mid-wrapup" is handled—for the reactive arm the block is on send, preventing growth. For the deliberate command, the user can't send another message while `/ctx-wrapup` blocks in OpenCode's command execution. So the risk is low, but I'm flagging this as a medium design gap: the runner's built-in stale-snapshot self-heal replaces the wrapup boundary with a normal-pressure-math boundary, silently narrowing coverage. The spec says the boundary comes from a wrapup override, not normal pressure math, but the runner's own re-resolve path calls `resolveOpenCodeProtectedTailBoundary`, bypassing the override entirely—a real high/medium finding where the spec's boundary override isn't honored by the runner's stale-snapshot recovery.

For discard-last on the final chunk, the spec proposes forcing it off, but the runner gates discard-last by `!inEmergency && emittedCompartments.length >= 2` with no deps flag to disable it—the runner decides internally based on emergency state. On the reactive arm, the wrapup runs while emergency recovery is armed, so discard-last is already off for all iterations, not just the final one, which contradicts the spec's intent that earlier iterations keep discard-last. On the deliberate command path with no emergency, the runner keeps discard-last on and there's no hook to turn it off for the final chunk. This is a medium finding: the mechanism is unspecified, conflicts with the existing emergency gate, and on the reactive path it's already off-for-all rather than final-only.

The weak lookahead final compartment persists its facts durably—the next run reads from offset=lastCompartmentEnd+1, so the final compartment isn't re-derived and the weak boundary becomes permanent. The marker targets the final compartment's endMessageId correctly, and decay-render treats it like any other compartment, so there's no corruption, just a quality risk.

For the reactive phase-2 timing, the blocking multi-run drain loop runs inside the transform before the request goes out, sized to the new model's limit. The new model's trusted limit is resolvable synchronously via getSdkContextLimit, which reads the models catalog directly, so the target watermark is computable. The transform is awaited, so a multi-run loop is mechanically possible, but the existing 95% arm already awaits a single run with a 120-second historian timeout, and a multi-run loop for a large model switch could exceed that window.

The real problem is that the reactive drain fires from the same code path as the #188 proactive arm, which sets needs_emergency_recovery and bumps to 95%, then drives the existing blocking recovery. If the reactive loop replaces this, it must wire into the same await point. But closing a 280k token gap requires many historian runs—potentially 10+ runs at 30-120 seconds each—meaning 10-40 minutes of the user's send blocking while the UI freezes at "Thinking" with the request unsent.

There's also a critical timeout-abort interaction: if a historian run times out mid-loop while the background run continues, the loop sees no new compartment coverage and aborts, leaving the drain incomplete and the oversized prompt still sent to the provider. This defeats the "correct beats a 400" goal.

And validateBoundarySnapshot rejects on `model_or_limit_changed` when the wrapup builds a snapshot with the old contextLimit but passes the new limit—validation doesn't fire the re-resolve self-heal since it only triggers on stale_snapshot, so the runner no-ops every iteration. The wrapup must explicitly build its boundary snapshot with the new contextLimit and pass the same value, or it won't execute at all.

For concurrency: if a background historian is mid-run and the wrapup loop calls startCompartmentAgent, the first iteration bails silently and the loop aborts with no progress. The wrapup needs to await any active run first before continuing. Also, since activeRuns only tracks one historian run at a time, a tight loop calling startCompartmentAgent could race the async clear—the spec must serialize awaiting each iteration's promise. And the wrapup loop itself needs an in-memory guard flag to reject a second wrapup, since activeRuns only tracks the historian run, not the wrapup loop state between iterations.

The real hazard is between wrapup and recomp: recomp checks activeRuns and rejects if a run is active, but between wrapup iterations activeRuns is empty, so a concurrent recomp could start and clear the injection cache while the wrapup preserves it. Recomp replaces all compartments from scratch, which would invalidate the wrapup's just-published compartments and cause the wrapup loop to stall or error. The wrapup latch needs to block recomp as well to prevent this interleaving.

For transform passes, if the wrapup runs inside a reactive arm it's serialized per session, but if it runs as a deliberate command, a transform pass could fire concurrently. Both would call getActiveCompartmentRun and share the activeRuns map and lease, so only one publishes at a time, but the coordination is messy—the 95% arm could await the wrapup's current iteration and proceed while the wrapup keeps looping.

The critical issue is that /ctx-flush only drops pending_ops tags but doesn't drain the pending compaction marker or re-fold m0. The actual marker movement happens in the transform postprocess drain, which requires historyWasConsumedThisPass. The onFlush hook sets flags that trigger a history rebuild and materialization on the next pass, which should drive the deferred marker drain, so /ctx-flush does eventually apply the marker—but executeFlush itself doesn't materialize immediately.

The wrapup output materializes on the next message, not via flush's immediate effect. The spec's claim is roughly correct because onFlush adds to historyRefreshSessions, and the wrapup publish calls onDeferredMarkerPending which populates deferredHistoryRefreshSessions. On the next pass, both historyWasConsumedThisPass and deferredHistoryWasPendingAtPassStart are true, so the drain fires and the marker moves. Flush does apply, but the timing depends on the next message and requires both the deferred signal from the wrapup publish and a consuming pass.

The real issue is a UX honesty gap: flush doesn't force materialization synchronously — it relies on the next transform pass, so the tail stays in-context and billed until then. The user is told wrapup "compacted" but context % doesn't drop immediately. And for a deliberate /ctx-wrapup with no subsequent model switch, the only natural bust might be far off, so the spec presents wrapup as a "compact now" control when its effect is actually deferred. The reactive arm needs the fold to happen before the request, but that's where the timing becomes critical.

Now looking at Pi's emergency drain: it awaits one historian with a 30-second cap and can't abort mid-pass. The problem is wrapping runPiHistorian in a blocking loop — each iteration spawns a subprocess, but the 30s cap is on the emergency wait itself, not the loop. If Pi's context handler is fire-and-forget or has a hard per-pass budget, a multi-minute drain loop would either stall Pi's turn or get truncated. Plus inFlightHistorian is a single-slot map, so if iteration N's promise is in flight and iteration N+1 spawns, it overwrites — the emergency wait would await the wrong one. This is mechanically unsound given the constraints, and Pi has no abort surface, so the spec's "day one" Pi parity claim is optimistic.

I'm also checking the no-op guard counting logic — the spec says count meaningful messages newest-first, but getRawHistoryEligibility returns raw ordinals that include tool messages and gaps. If the guard uses raw ordinal delta instead of meaningful-message count, they diverge. The implementation needs to use the same meaningful-count metric, not raw delta. That's a minor issue.

Going back to Vector 1, I'm verifying whether there's truly no per-iteration drain. The wrapup loop is fully synchronous within one call with no interleaved transform passes, so the last blob wins. recordProtectedTailPublicationFloor and queueDropsForCompartmentalizedMessages are cumulative and idempotent per publish, and clearInjectionCache respects preserveInjectionCacheUntilConsumed, so Vector 1 is benign.

Now looking at the strongest findings: the runner's stale-snapshot self-heal re-resolves via normal pressure math and silently discards the wrapup boundary override — the spec's core override isn't honored on re-resolve. The reactive arm has validateBoundarySnapshot returning model_or_limit_changed when the context limit shifts, but the self-heal only handles stale_snapshot, so the runner no-ops every iteration on a downswitch unless the snapshot uses the new limit, defeating the reactive drain's purpose. The reactive timeout-abort leaves the background run going with coverage unchanged, triggering the no-forward-progress abort rule and sending an oversized prompt to the provider. Loop-wrapping runPiHistorian is unsound with a 30-second emergency cap, single-slot inFlightHistorian, no abort surface, and the Pi handler likely not designed to block for minutes. The discard-last OFF mechanism is unspecified and conflicts with the existing emergency gate on the reactive path.

The wrapup-vs-recomp guard has activeRuns empty between iterations so it can't serialize the loop — a dedicated synchronous wrapup latch is needed and must also block /ctx-recomp, which the spec doesn't name. Wrapup calling startCompartmentAgent per iteration bails silently if a background run is active, and the loop's no-progress abort would then fire, so it must explicitly await getActiveCompartmentRun first. The deferred publish means context percentage doesn't drop until a natural bust; /ctx-flush eventually drives the marker drain but not synchronously, making "compact now" a misnomer for the deliberate command with no imminent bust. The no-op guard must count meaningful messages consistently rather than raw ordinal delta. I've verified that vector 1 (single-slot blob clobber) is benign since it's monotonic and the last blob subsumes, vector 2 (floor) is benign since floor equals offset, and vector 3 grow is also benign.

I've confirmed the reactive arm's transform is awaited inside the hook and registered as experimental.chat.messages.transform, so OpenCode awaits it safely, though I can't verify OpenCode's own outer timeout. Overall, the deliberate command v1 is largely sound with fixes, but the reactive phase-2 arm has three high mechanical defects plus additional issues that make it unsafe as specified — I'm going with REVISE overall, blocking the reactive arm until those findings are addressed.
I have gathered sufficient evidence across the runner, boundary resolver, transform arms, marker manager, storage layer, and Pi handler. Let me compose the audit.

<COUNCIL_MEMBER_RESPONSE>
# Blind Design Review: `/ctx-wrapup` spec

Verified independently against the real code on branch `subc-migration`. `/ctx-wrapup` does not yet exist (grep for `wrapup` → 0 matches), so this reviews the spec against the engine it proposes to reuse.

---

## Finding 1: Runner's stale-snapshot self-heal silently discards the WRAPUP BOUNDARY OVERRIDE
- **Severity**: HIGH
- **Location**: `compartment-runner-incremental.ts:252-274`
- **Confidence**: high
- **Issue**: The spec's load-bearing premise is that each iteration is handed an explicit "WRAPUP BOUNDARY OVERRIDE … not resolveProtectedTailBoundary's normal pressure math." But the runner validates the passed snapshot (`validateBoundarySnapshot`, line 230-238) and, on a `stale_snapshot` result, **re-resolves the boundary itself** by calling `resolveOpenCodeProtectedTailBoundary(...)` (line 253) — i.e. normal token-target/live-prompt-floor/0.40× pressure math. If it produces a runnable window, the runner adopts it (`boundarySnapshot = refreshed`, line 271), throwing away the wrapup's widened eligible-end. The drain then compacts an incremental-sized head, not the keep-watermark head — quietly defeating the whole command.
- **Evidence**: line 253 `resolveOpenCodeProtectedTailBoundary({... mode:"incremental-runner" ...})`; line 271 `boundarySnapshot = refreshed`. There is no override plumbed through this path.
- **Suggested Fix**: Add a `deps.boundaryOverride`/`skipReResolve` flag the wrapup sets, so the runner either fails closed or re-applies the wrapup watermark (not pressure math) when a wrapup snapshot goes stale.

## Finding 2: Reactive arm no-ops every iteration — `model_or_limit_changed` isn't the healed reason
- **Severity**: HIGH
- **Location**: `protected-tail-boundary.ts:695-701` + `compartment-runner-incremental.ts:230-283`
- **Confidence**: high
- **Issue**: The reactive (phase-2) arm runs precisely *after* a model/limit change and must size to the NEW limit. `validateBoundarySnapshot` returns `{reason:"model_or_limit_changed"}` whenever `currentContextLimit !== snapshot.contextLimit` (line 695). The runner's self-heal only fires for `reason === "stale_snapshot"` (line 252). So a `model_or_limit_changed` result falls straight through to line 275 and the runner **no-ops** (`telemetry.status="noop"`). Unless the wrapup builds its snapshot with `contextLimit = new-model limit` AND passes `currentContextLimit` equal to it, every reactive iteration is a no-op → coverage never advances → the loop's own no-progress abort (spec) fires → oversized prompt still sent → provider 400. The spec never calls out this limit-equality requirement.
- **Evidence**: line 695 branch returns non-`stale_snapshot`; line 252 guard `validation.reason === "stale_snapshot"`; line 275-283 no-op.
- **Suggested Fix**: Wrapup must construct the snapshot with the new limit as `contextLimit` and pass matching `currentContextLimit`; add a test asserting a downswitch snapshot validates rather than no-ops.

## Finding 3: Per-run timeout + "abort on no forward progress" = oversized prompt still ships
- **Severity**: HIGH
- **Location**: `transform-compartment-phase.ts:224-240` (awaitCompartmentRun, `historianTimeoutMs ?? 120_000`) vs spec's "abort the loop if an iteration makes NO forward progress"
- **Confidence**: high
- **Issue**: The existing blocking machinery awaits ONE run with a per-run timeout; on timeout it returns `"timed_out"` and leaves the run going in the background (line 234-240, 397-406). If the wrapup loop reuses this and an iteration times out, the loop observes *coverage unchanged* (the background run hasn't published yet) and, per the spec's abort rule, **stops** — while the reactive arm's entire justification is "correct beats a provider 400." A slow historian thus produces exactly the 400 the arm exists to prevent, for the 553k→272k case that needs many minutes of sequential runs.
- **Evidence**: line 229 default 120s; line 234 `result==="timeout"` → `"timed_out"`; spec conflates a timed-out run with a genuinely stuck one.
- **Suggested Fix**: Distinguish "timed out (still running)" from "published no new compartment." Wait for the in-flight run's publication (await `activeRun.promise`) before evaluating progress; only abort on a *completed* run that advanced nothing. Give the loop a multi-run budget, not a per-run 120s cap.

## Finding 4: Pi loop-wrapping is mechanically unsound (30s cap, single-slot in-flight, no abort)
- **Severity**: HIGH
- **Location**: `context-handler.ts:2071-2082` (30s `withTimeout`), `:2613` `inFlightHistorian = new Map<string, Promise<unknown>>()`, `:2818` `spawnPiHistorianRun` (fires ONE background run)
- **Confidence**: high
- **Issue**: "Pi parity … day one" wraps `runPiHistorian` in a blocking loop, but (a) `inFlightHistorian` is a **single-slot** map — iteration N+1 overwrites N's promise, so any emergency wait awaits the wrong entry; (b) the emergency path caps waits at **30s** (`withTimeout(histPromise, 30_000)`), implying Pi's handler is not designed to block for the multi-minute drain the reactive arm requires; (c) Pi has **no `session.abort` surface** (comment at line 2043-2045), so a runaway loop can't be interrupted. A minutes-long blocking loop inside `pi.on("context")` risks stalling or being truncated by Pi's own pipeline budget.
- **Evidence**: lines 2044-2045 ("We can't `client.session.abort()`"), 2051-2054 (30s cap rationale), 2613 single-slot map, 2818-2828 one-shot spawn.
- **Suggested Fix**: Do not ship Pi reactive parity in v1. If wrapped, replace single-slot `inFlightHistorian` with per-run tracking and confirm Pi awaits the context handler with a budget that tolerates the loop; otherwise gate Pi to the deliberate command only, or a bounded iteration count well under any pipeline timeout.

## Finding 5: "discard-last OFF for the FINAL chunk" has no mechanism and conflicts with the emergency gate
- **Severity**: MEDIUM
- **Location**: `compartment-runner-incremental.ts:496-509` (`inEmergency = getOverflowState(...).needsEmergencyRecovery`; discard-last skipped when `inEmergency`), promotion gates at 632, 589, 795-800, 824
- **Confidence**: high
- **Issue**: Two problems. (1) There is **no deps flag** to force discard-last off — it's decided internally by `!inEmergency && emittedCompartments.length >= 2`. The spec's "for the FINAL loop iteration only, persist all validated compartments" cannot be expressed without new plumbing. (2) On the **reactive** path the loop runs while `needs_emergency_recovery` is armed, so `inEmergency` is true and discard-last is already OFF for **every** iteration (line 496-498), directly contradicting the spec's "Earlier iterations keep discard-last." Net effect: every reactive chunk promotes facts/events/primers off a weak final boundary, not just the final one.
- **Evidence**: line 496 `inEmergency = getOverflowState(...)`; line 498 gate; promotion gated by `!discardedLast` at 632/589/795/824.
- **Suggested Fix**: Add an explicit `deps.persistFinalCompartment` flag threaded to the discard-last block; on the reactive path decide deliberately (accept all-off, or clear the emergency flag for wrapup) rather than inheriting `inEmergency` semantics by accident.

## Finding 6: wrapup-vs-wrapup / vs-recomp guard can't rely on `activeRuns` — it's empty between iterations
- **Severity**: MEDIUM
- **Location**: `compartment-runner.ts:104-107` (startCompartmentAgent bail), `:192-201` (recomp `activeRuns.has` reject), `:145-151`/`:64-70` (activeRuns cleared on each run settle)
- **Confidence**: high
- **Issue**: `activeRuns` tracks a single historian RUN, cleared in `.finally` when each run settles (line 148, 67). Between wrapup iterations the map is momentarily empty, so it **cannot** serialize the LOOP. A second `/ctx-wrapup` (spec says "reject the second") or a concurrent `/ctx-recomp` can slip into that gap; recomp does a **full rebuild from message 1** (`executeContextRecompInternal`) that resequences/replaces the compartments the wrapup just published — invalidating its pending target (`validatePendingTarget` → `compartment-removed`) and its no-progress math mid-loop. The spec names no dedicated latch.
- **Evidence**: line 192 `if (activeRuns.has(sessionId))`; line 148/67 clear-on-settle; recomp replaces all compartments (compartment-runner.ts:216-218).
- **Suggested Fix**: Introduce a synchronous per-session `wrapupInProgress` Set set at command entry; make recomp/upgrade/second-wrapup check it, and have wrapup check the recomp guard, so neither starts while the other's multi-step sequence is live.

## Finding 7: Reusing `startCompartmentAgent` per iteration silently no-ops against an in-flight background historian
- **Severity**: MEDIUM
- **Location**: `compartment-runner.ts:100-107`
- **Confidence**: high
- **Issue**: The spec says "if a historian run is already in flight, wait for it … then continue the loop." But `startCompartmentAgent` returns silently when `activeRuns.get(sessionId)` is set (line 104-107). If the wrapup loop calls it while a background historian is mid-run, the iteration is a no-op, coverage is unchanged, and the spec's no-progress abort fires — the wrapup quits instead of waiting.
- **Evidence**: line 104 `const existing = activeRuns.get(...); if (existing) return;`.
- **Suggested Fix**: Wrapup must `await getActiveCompartmentRun(sessionId)?.promise` before each iteration and only then start/await its own run; do not treat "a run was already active" as no-progress.

## Finding 8: "Compact now" is deferred — no synchronous reduction, and /ctx-flush only applies on the next pass
- **Severity**: MEDIUM
- **Location**: `execute-flush.ts:10-31` (drops pending_ops only), `hook.ts:754-758` (onFlush sets 3 signals), deferred marker drain `transform-postprocess-phase.ts:1179-1215`
- **Confidence**: high
- **Issue**: `executeFlush` itself only marks pending-op tags dropped ("Changes take effect on next message") — it does **not** move the compaction marker or re-fold m0. The marker move happens in the transform postprocess drain (line 1180), which needs `historyWasConsumedThisPass && deferredHistoryWasPendingAtPassStart`. `onFlush` does set `historyRefreshSessions + pendingMaterializationSessions` (hook.ts:755-757), so the *next* transform pass will drive the drain — but nothing reduces context synchronously. For the deliberate command with no imminent model switch, the user runs a "compact now" control and sees **no context-% drop** until a later bust; the raw tail stays billed in the live prompt. This is a genuine honesty gap between the command's name/UX and its deferred effect.
- **Evidence**: execute-flush.ts:22-24 (tag drops only); marker drain gated at postprocess-phase.ts:1179; publish defers marker via `onDeferredMarkerPending` (compartment-runner-incremental.ts:708-709).
- **Suggested Fix**: Report coverage as "queued; materializes on next message / run `/ctx-flush`," never as an achieved reduction; consider having wrapup optionally set the flush signals itself so the reduction lands on the immediately-following pass.

## Finding 9: No-op guard must count meaningful messages consistently, not raw-ordinal delta
- **Severity**: LOW
- **Location**: `protected-tail-boundary.ts:657-671` (`getRawHistoryEligibility` returns raw-ordinal counts)
- **Confidence**: medium
- **Issue**: The spec's no-op guard ("tail already within keep-N") and watermark both count MEANINGFUL messages newest-first, but `getRawHistoryEligibility` exposes only raw-ordinal `rawMessageCount - lastCompartmentEnd` (gaps + tool-only messages included). If the guard uses the raw delta while the watermark uses meaningful counting, they disagree at the boundary and a session with many tool messages can either falsely no-op or run an empty LLM pass.
- **Evidence**: line 666-671 returns `rawMessageCount`/`offset`, no meaningful-message filter (contrast the meaningful-user scan at protected-tail-boundary.ts:498-505).
- **Suggested Fix**: Compute the no-op guard from the same meaningful-message counter the watermark uses.

---

## Vectors that are NON-ISSUES (verified safe)

- **Single-slot pending-marker clobber (Athena #1)**: `setPendingCompactionMarkerState` (storage-meta-persisted.ts:1818) overwrites per publish, but the whole wrapup loop is synchronous within one call — no transform pass drains between iterations, so 8 blobs collapse to the LAST. The marker advances monotonically (`existingMarkerAlreadyCoversTarget`, compaction-marker-manager.ts:139-183) and the last publish has the highest ordinal, subsuming all earlier ones. `validatePendingTarget` (manager.ts:84-117) passes because the final compartment exists at that ordinal. **Benign.**
- **Shared publication floor (Athena #2)**: `recordProtectedTailPublicationFloor` writes `prior_boundary_ordinal = MAX(existing, lastCompartmentEnd+1)` (storage-meta-persisted.ts:350). Later normal passes read it as `runtimeFloor = max(offset, priorBoundaryOrdinal)` (protected-tail-boundary.ts:480), and `offset` already equals `lastCompartmentEnd+1`, so the floor never over-protects. **Benign.**
- **Fingerprint vs growing tail (Athena #3)**: `computeRawRangeFingerprint` hashes `[offset, eligibleEnd)` (read-session-true-raw-tokens.ts:661-673). Appended messages land ABOVE `eligibleEnd`, leaving that range's fingerprint unchanged; `validateBoundarySnapshot`'s `last`/count checks tolerate growth (`rawMessageCountAtTrigger > current` only rejects on *shrink*, line 710). Safe **for the fingerprint** — but see Finding 1, where a stale `last`-id triggers the pressure-math re-resolve.
- **Trusted-limit availability for reactive arm (Athena #5)**: `getSdkContextLimit` is a synchronous catalog read (models-dev-cache.ts:329; already used by the #188 proactive arm at transform.ts:714), so the new model's cap IS resolvable at drain time without a provider round-trip. The transform is genuinely awaited (`experimental.chat.messages.transform`, hook.ts:883; the 95% arm already `await`s inside it), so a blocking loop is mechanically possible on OpenCode. (Caveat: OpenCode's own outer hook timeout is not verifiable from this repo.)

---

## Summary
- **HIGH: 4** (Findings 1-4) — 3 defeat the reactive phase-2 arm outright (boundary-override discarded, limit-change no-op, timeout→abort→400) and 1 makes Pi parity unsound.
- **MEDIUM: 4** (Findings 5-8) — discard-last mechanism/gate conflict, loop serialization gaps, silent no-op vs in-flight historian, deferred-reduction honesty.
- **LOW: 1** (Finding 9).
- **Verified non-issues: 5** of Athena's 8 vectors (single-slot clobber, floor, fingerprint, trusted-limit resolution, transform-await) are safe as-is.

**Overall verdict: REVISE** — with the **reactive phase-2 arm BLOCKED** until Findings 1, 2, and 3 are resolved. The deliberate `/ctx-wrapup [messages_to_keep]` command is a sound v1 given the boundary-override plumbing (Finding 1), a real wrapup latch (Finding 6), await-in-flight-first (Finding 7), and honest deferred-reduction reporting (Finding 8). The reactive downswitch reuse, as specified, will no-op or abort into the exact provider-400 it targets, and Pi "day-one parity" is not mechanically supported. Driving findings: **#2 (reactive no-op on limit change), #3 (timeout-abort ships oversized prompt), #4 (Pi loop unsound).**
</COUNCIL_MEMBER_RESPONSE>

