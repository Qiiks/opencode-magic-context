---
task_id: bg_7d977608
agent: council-member
session_id: ses_0b4657c68ffe4fBJuBrHpVTmUS
parent_session_id: ses_0b4664e56ffeKx8uRSceXLl3v9
status: completed
completed_at: 2026-07-10T10:45:28Z
---

[user] 2026-07-10T10:37:25Z
## Solo Analysis Mode
You MUST do ALL exploration yourself using your available read/search tools.
- Do NOT use task or any delegation tool under any circumstances
- Do NOT delegate to explore, librarian, or any other subagent
- Do NOT spawn background tasks
- Search the codebase directly — you have full read-only access to every file
- This mode produces the most thorough analysis because you see every result firsthand


## Analysis Intent: AUDIT

You are conducting an **audit** — your goal is to find discrete issues, risks, or violations.

**Focus:**
- Search for problems, anti-patterns, security risks, correctness issues, or violations of stated requirements
- Each finding must be a distinct, actionable item with concrete evidence
- Severity determines priority: critical (blocks/breaks), high (significant risk), medium (should fix), low (nice to fix)
- For each finding, provide the specific location (reference, section, or component where it occurs)
- State your confidence: high (clear evidence), medium (likely but needs verification), low (suspicion, investigate further)
- **This is a broad sweep, not a targeted trace.**

**Analytical standards:** Support claims with concrete evidence. State confidence (high/medium/low) for key assertions. Note caveats and limitations.

**Structure your response as:**
```
<COUNCIL_MEMBER_RESPONSE>
## Finding 1: [Title]
- **Severity**: critical/high/medium/low
- **Location**: [specific reference — e.g. component, section, endpoint, rule]
- **Confidence**: high/medium/low
- **Issue**: [what is wrong and why it matters]
- **Evidence**: [concrete reference, snippet, or observation that proves the issue]
- **Suggested Fix**: [actionable recommendation]

## Finding 2: [Title]
...

## Summary
[Total findings by severity. Overall risk assessment with confidence levels.]
</COUNCIL_MEMBER_RESPONSE>
```

## Analysis Question

BLIND ADVERSARIAL COUNCIL AUDIT — Magic Context SHADOW-MODE lane, pre-production soak gate.

Repo: /Users/ufukaltinok/Work/Projects/CortexKit/magic-context
Branch: subc-migration, HEAD 6ea3179f

You are one of several independent auditors. Work ALONE, hunt for the NEXT bug class. Do NOT assume the code is correct because it has tests — the first armed run already exposed two integration bugs the mocked tests missed (wire-shape mismatch, ordinal starvation), both already fixed at HEAD. Your job is to find what the mocks still miss. Read the ACTUAL code with file:line evidence; do not speculate abstractly.

=== WHAT SHADOW MODE IS ===
A dev-flag lane (config shadow_transform.enabled, user-tier only) where the OpenCode TS plugin mirrors every finalized transform pass to the Rust mc-module over the subc daemon socket for byte-comparison — WITHOUT affecting the live session. The TS side fire-and-forgets: per-session FIFO queue, state_sync (compartments/memories/mutations sync), shadow_transform (input + TS output + decision), shadow_reset (generation bump + wipe). The Rust side runs its own transform on the mirrored input against a shadow:<sid> store lineage, byte-compares against the TS output, and records divergences in shadow_divergences. The soak is OBSERVE-ONLY by design.

=== AUDIT SCOPE (areas that changed or are first-run-live today) ===

1. packages/plugin/src/plugin/hooks/create-session-hooks.ts — the config mapper was changed from a hand-maintained field list to a full spread (`...pluginConfig`) with two defaulted overrides (commit e932804c). HUNT: does spreading the ENTIRE plugin config into the hook config leak anything that shouldn't reach per-session hook code, shadow anything the hook type declares differently, or change behavior for any existing consumer that relied on a field being absent (e.g. `undefined` vs present-with-value)?

2. packages/plugin/src/hooks/magic-context/shadow-sender.ts — the full sender. HUNT ESPECIALLY:
   (a) HOT-PATH SAFETY: enqueue() runs inside the transform hot path. Verify EVERY code path in enqueue (including resolveOrdinalsForShadow with its new by-id DB fallback, denormalizeShadowOutput, cloneJson of potentially huge message arrays) is exception-safe and bounded — a throw or a multi-second stall here delays the user's real prompt. Is cloneJson(messages) on a 400-message array acceptable per-pass cost? Is the by-id DB read (readRawSessionMessageById) opening a DB handle per call, and what happens under SQLITE_BUSY?
   (b) WIRE SHAPE (commit 6ea3179f): toFlatWireBody flattens {method, params} to flat. Cross-check EVERY field the TS builders emit against the Rust serde parsers in crates/mc-module/src/lib.rs (ShadowStateSyncWire, ShadowTransformWire, ShadowResetWire, ShadowPassInputs, ShadowUsageWire, ShadowCompartmentWire, ShadowMemoryWire, ShadowMemoryMutationWire): field names, types, required-vs-optional, snake_case exactness. Name any field that will reject or silently default.
   (c) ORDINAL FALLBACK: the new below-floor by-id fallback in resolveOrdinalsForShadow. Can the fallback return an ordinal computed on a DIFFERENT basis than the cache (e.g. cache primed tail-only carries absolute ordinals from baseOrdinal while the by-id COUNT includes/excludes summary rows differently)? A silent basis mismatch would poison the shadow store with wrong ordinals rather than failing loud.
   (d) SubcShadowTransport: hand-rolled socket protocol (auth handshake, frame header, backoff). Route kind now tool_provider (commit 1555b231). HUNT: connection lifecycle bugs (socket close mid-request, reader waiters, backoff doubling), auth proof correctness, frame length handling, the 5s request timeout leaving the FIFO wedged.
   (e) QUEUE SEMANTICS: MAX_QUEUE_PER_SESSION=4 drop-oldest, blockedUntilReset, requireResetReason transitions. Any path where a session gets permanently wedged (blocked but no reset queued) or where a reset races a queued pass?

3. crates/mc-module/src/lib.rs shadow handlers (handle_shadow_state_sync_value, handle_shadow_transform_value, handle_shadow_reset_value, shadow_binding) + crates/mc-store shadow methods (apply_shadow_state_sync, reset_shadow_session). HUNT:
   (a) ISOLATION: can ANY shadow-lane write touch a non-shadow session row? The shadow session id is derived from binding (shadow: prefix). Verify shadow_binding rejects non-shadow bindings on shadow ops AND that plain transform ops reject shadow bindings (codes non_shadow_op_on_shadow_binding, plain_transform_on_shadow_binding — verify they're actually enforced on EVERY arm).
   (b) The shadow_transform handler runs the REAL transform + a byte-compare. Does a shadow divergence or panic in the compare path affect the real lane in any way (shared store handle, lease contention, pass-trace pollution)?
   (c) apply_shadow_state_sync CAS semantics: generation/seq mismatch arms, and what happens when state_sync carries compartments whose sequences overlap already-synced ones (idempotent or duplicating?).

4. CACHE SAFETY OF THE LIVE LANE: the sender captures cloneForShadow(messages) BEFORE the transform mutates and reads declared-trim state before/after. Verify the shadow capture path cannot mutate the live messages array (shared references through cloneJson boundaries — is the clone deep and taken at the right point?), and that shadow-sender DB reads (getCompartments, getMemoriesByProject, etc.) hold no transactions that could contend with the transform's own writes.

=== DELIVERABLE (per member) ===
- Independent findings with file:line evidence. Read the real code.
- Rank each finding Critical / High / Medium / Low.
- Weight by blast radius: (1) can affect the LIVE user session = WORST class; (2) corrupts shadow state silently = defeats the soak's purpose; (3) merely loses shadow coverage but logged = ACCEPTABLE.
- Explicit false-positive filtering: check PARITY.md files and existing test coverage before claiming a bug. State what you checked.
- Your own verdict: SHIP (arm the soak on production sessions) or HOLD, with the single most important reason.

Relevant reference files exist at packages/pi-plugin/PARITY.md and packages/plugin/src/features/magic-context/smart-notes/PARITY.md. Test files live alongside sources (*.test.ts) and in crates/*/tests or #[cfg(test)] modules.

[assistant] 2026-07-10T10:37:26Z
Let me start by exploring the repository structure to understand the codebase layout, then dive into the specific files mentioned in the audit scope.
I'll conduct a thorough independent audit. Let me start by exploring the repository structure and the key files in scope.

[assistant] 2026-07-10T10:37:41Z
Let me continue reading the shadow-sender.ts file and also look at the Rust side.
Let me read the rest of shadow-sender.ts and the Rust handlers in parallel.

[assistant] 2026-07-10T10:37:44Z
The literal search found nothing — the Rust wire types may use different names. Let me search the Rust lib.rs for shadow handlers and wire types.

[assistant] 2026-07-10T10:37:47Z
Found the wire types. Let me read the Rust shadow wire types and handlers in detail.

[assistant] 2026-07-10T10:37:48Z
Now I have the wire types. I can already see a critical mismatch: the TS sender emits `watermarks` and `m0_mutations` in state_sync, but the Rust `ShadowStateSyncWire` has `acked_watermarks` and NO `m0_mutations` field. Let me read the handlers and the store methods to confirm.

[assistant] 2026-07-10T10:37:56Z
I've confirmed a critical wire-shape mismatch. The TS sender emits `watermarks` and `m0_mutations` in state_sync params, but the Rust `ShadowStateSyncWire` has `acked_watermarks` (not `watermarks`) and NO `m0_mutations` field. Let me verify the TS side more carefully and check the store methods. Let me also look at the compartment wire shape — the TS emits nested `start`/`end` objects but Rust expects flat `start_message`/`end_message`/`start_message_id`/`end_message_id`.

[assistant] 2026-07-10T10:38:04Z
Now let me read the `apply_shadow_state_sync` and `reset_shadow_session` store methods, and also check the `StoredCompartment::from` conversion to understand the wire shape mismatch for compartments.

[assistant] 2026-07-10T10:38:13Z
Now let me read the From conversions for compartments — this is critical for the wire shape mismatch. The TS emits nested `start`/`end` objects with `flat_id`, `bare_message_id`, `absolute_ordinal`, but Rust expects flat `start_message`, `end_message`, `start_message_id`, `end_message_id`.

[assistant] 2026-07-10T10:38:18Z
This confirms a CRITICAL wire-shape mismatch. The TS `serializeCompartment` emits nested objects:
```js
start: { flat_id, bare_message_id, absolute_ordinal },
end: { flat_id, bare_message_id, absolute_ordinal },
```
But the Rust `ShadowCompartmentWire` expects flat fields:
```rust
start_message: i64, end_message: i64, start_message_id: String, end_message_id: String
```
The nested `start`/`end` objects will be silently dropped by serde (no matching fields), and `start_message`/`end_message` will default to `0` (they're `i64` without `#[serde(default)]`... wait, let me check). Actually `start_message` and `end_message` have NO `#[serde(default)]`, so they're REQUIRED. The TS never sends them. This would cause a deserialization error. Let me verify this is truly the case and check the PARITY files and tests.

[assistant] 2026-07-10T10:38:21Z
The PARITY files don't cover the shadow wire shape. Now let me verify the compartment wire mismatch is real by checking the test files for shadow-sender, and also look at the `create-session-hooks.ts` config spread change.

[assistant] 2026-07-10T10:38:25Z
Now let me read the shadow-sender test file to see what the tests actually cover (and what they miss), and check the `MagicContextPluginConfig` type to understand what the spread leaks.

[assistant] 2026-07-10T10:38:27Z
This is very revealing. The test at line 350-447 ("serializes state_sync and shadow_transform with the shadow wire field inventory") checks the TS-side wire shape but uses a `FakeTransport` — it NEVER sends to the real Rust parser. The test asserts `watermarks` and `m0_mutations` exist in the TS payload (lines 376-386), but it never validates against the Rust `ShadowStateSyncWire` struct which expects `acked_watermarks` (not `watermarks`) and has NO `m0_mutations` field. This is exactly the class of bug the mocks miss.

Let me now verify the exact field mismatches systematically. Let me check the `readRawSessionMessageById` function for the DB handle concern, and look at the `DeclaredTrim` Rust struct.

[assistant] 2026-07-10T10:38:31Z
Let me read the `readRawSessionMessageById` function and the `DeclaredTrim` Rust struct, plus check the `readRawSessionMessages` to understand the ordinal computation basis.

[assistant] 2026-07-10T10:39:02Z
Now I have critical evidence. The `readRawSessionMessageByIdFromDb` at line 384-393 computes the ordinal using a COUNT query that EXCLUDES summary rows (`NOT (summary=1 AND finish='stop')`). Let me now check how `readRawSessionMessagesFromDb` computes ordinals to see if there's a basis mismatch with the cache.

[assistant] 2026-07-10T10:39:07Z
Now let me check the full `readRawSessionMessagesFromDb` to see how it computes ordinals — this is the basis for the cache path. Let me also check `withReadOnlySessionDb` to understand if it opens a DB handle per call.

[assistant] 2026-07-10T10:39:11Z
Let me read `withReadOnlySessionDb` and `withRawSessionMessageCache` to understand DB handle reuse and the cache semantics.

[assistant] 2026-07-10T10:39:14Z
Good. The DB is cached and reused (`getReadOnlySessionDb` caches the handle). Now let me verify the ordinal basis mismatch concern. The cache path (`readRawSessionMessages`) can be primed tail-only with absolute ordinals from `baseOrdinal`. The by-id fallback (`readRawSessionMessageByIdFromDb`) computes ordinal via COUNT excluding summary rows. Both exclude summary rows, so the basis is the same. But there's a subtle issue: the by-id COUNT uses `time_created < ? OR (time_created = ? AND id <= ?)` while the full/tail reader uses `time_created ASC, id ASC` ordering. Let me verify the full reader's ordinal computation.

[assistant] 2026-07-10T10:39:19Z
Now I have the full picture on the ordinal basis. The full reader (`readRawSessionMessagesFromDb`, line 76-118) filters summary rows FIRST, then assigns ordinals `index + 1` based on the filtered array position. The by-id fallback (`readRawSessionMessageByIdFromDb`, line 384-393) computes ordinal via COUNT with the same summary filter AND the same sort order (`time_created ASC, id ASC` via the `time_created < ? OR (time_created = ? AND id <= ?)` predicate). These are consistent — both exclude summaries and use the same sort. The tail-only cache uses `baseOrdinal` which is derived from the compartment boundary ordinal, which itself comes from the full/tail reader. So the basis is consistent.

Now let me check the `non_shadow_op_on_shadow_binding` and `plain_transform_on_shadow_binding` enforcement. Let me look at the transform handler to see if shadow bindings are rejected on plain transform ops.

[assistant] 2026-07-10T10:39:23Z
Good — the plain transform handler rejects shadow bindings at line 1944-1950 (`plain_transform_on_shadow_binding`). And the shadow handlers use `shadow_binding` which rejects non-shadow bindings at line 807-812 (`shadow_binding_required`). Let me now check if there are OTHER non-shadow ops that might NOT have the shadow-binding rejection guard. Let me search for all `is_shadow_session` checks.

[assistant] 2026-07-10T10:39:26Z
Only 3 guard sites: `shadow_binding` (line 807, rejects non-shadow on shadow ops), ctx_reduce (line 1711, rejects shadow binding), and plain transform (line 1944, rejects shadow binding). Let me check what other handler methods exist that might be missing the guard — guidance, memory tool, etc.

[assistant] 2026-07-10T10:39:28Z
Let me see the full list of handler methods and check which ones use `resolve_binding` vs `shadow_binding`.

[assistant] 2026-07-10T10:39:30Z
Let me see all the call sites to understand which handlers check for shadow bindings and which don't.

[assistant] 2026-07-10T10:39:33Z
So `resolve_binding` is used by: `handle_agent_drops_value` (line 1695), `handle_guidance_value` (line 1741), and `handle_transform_value` (line 1928). Only `handle_agent_drops_value` and `handle_transform_value` have the `is_shadow_session` rejection guard. `handle_guidance_value` at line 1741 does NOT reject shadow bindings — but guidance is read-only (returns text bytes), so it's low risk. Let me verify `handle_guidance_value` doesn't write.

Now let me look at the `handle_shadow_transform_value` continuation to check if a shadow divergence/panic affects the real lane, and check the `shadow_input_messages` function.

[assistant] 2026-07-10T10:39:37Z
I can see that the shadow transform handler (line 2369) calls `transform_with_projection(&store, ...)` using the SAME `store` handle as the real lane. This is a shared `Arc<McStore>`. The shadow transform runs the real transform on the shadow session (`shadow:<sid>`), which is isolated by session id. The store handle is shared but all writes are scoped to the shadow session id. Let me verify the `record_shadow_report` method to see if it can affect the real lane.

[assistant] 2026-07-10T10:39:40Z

[assistant] 2026-07-10T10:39:42Z
The `record_shadow_report` writes to `shadow_divergences` table scoped by `session_id` (which is `shadow:<sid>`). The `record_shadow_divergence` store method also checks generation match. This is isolated by session id. Good.

Now let me check the critical wire-shape issue more carefully. Let me look at the `ShadowCompartmentWire` again — the TS emits `start` and `end` as nested objects, but Rust expects `start_message`, `end_message`, `start_message_id`, `end_message_id` as flat fields. Let me also check if there's a `project_path` field mismatch in memories.

[assistant] 2026-07-10T10:39:50Z
Confirmed. `ShadowCompartmentWire` requires `start_message: i64` and `end_message: i64` (NO `#[serde(default)]`), and has `start_message_id`/`end_message_id` as optional String fields. The TS `serializeCompartment` (shadow-sender.ts lines 584-615) emits:
```js
{
  id: ...,
  sequence: ...,
  start: { flat_id, bare_message_id, absolute_ordinal },
  end: { flat_id, bare_message_id, absolute_ordinal },
  title, content, p1, p2, p3, p4, importance, episode_type, legacy, created_at
}
```

The Rust parser will:
1. Find `start_message` and `end_message` MISSING → deserialization FAILS (they're required, no default)
2. The nested `start`/`end` objects are unknown fields (serde ignores unknown fields by default, but the REQUIRED fields are still missing)

This means EVERY state_sync with compartments will REJECT with `invalid_params`. Let me also check the `ShadowMemoryWire` — the TS emits `project_path` but Rust doesn't have that field.

[assistant] 2026-07-10T10:39:54Z
The TS emits `project_path` (shadow-sender.ts line 671) but `ShadowMemoryWire` has NO `project_path` field. Since serde ignores unknown fields by default, this is harmless (the field is silently dropped). The `id` field is required (no default) — the TS always emits it. This is OK.

Now let me check the `ShadowMemoryMutationWire` — the TS emits `project_path` and `id` but Rust has `id`, `mutation_type`, `target_memory_id`, `superseded_by_id`, `category`, `new_content`, `queued_at`. The TS emits all of those (shadow-sender.ts lines 714-723). The extra `project_path` is harmless. But wait — the TS also emits `session_id` for m0_mutations (line 702) but the Rust `ShadowStateSyncWire` has NO `m0_mutations` field at all! Let me verify this is truly missing.

Looking back at the Rust `ShadowStateSyncWire` (lines 185-200):
```rust
struct ShadowStateSyncWire {
    #[serde(default)] session_id: Option<String>,
    shadow_generation: u64,
    expected_shadow_seq: u64,
    #[serde(default)] compartments: Vec<ShadowCompartmentWire>,
    #[serde(default)] memories: Vec<ShadowMemoryWire>,
    #[serde(default)] memory_mutations: Vec<ShadowMemoryMutationWire>,
    #[serde(default)] last_todo_state: Option<String>,
    #[serde(default)] acked_watermarks: Option<Value>,
}
```

There is NO `m0_mutations` field. The TS sends `m0_mutations` (shadow-sender.ts line 174, 735). Since serde ignores unknown fields, `m0_mutations` is silently dropped. The Rust shadow store never receives m0_mutations. This means the shadow store's m0 mutation state is NEVER synced — it's always empty on the Rust side. This silently corrupts the shadow comparison.

Also: the TS sends `watermarks` (line 732) but Rust expects `acked_watermarks` (line 199). Since `acked_watermarks` has `#[serde(default)]`, it defaults to `None`. The Rust handler at line 2176-2183 then computes a fallback from the compartments/memories. So the watermarks the TS carefully computes are SILENTLY IGNORED, and a fallback is used instead. This is a silent semantic mismatch.

Let me now check the `ShadowTransformWire` for the `pass_seq` field — the TS doesn't emit it but Rust has it as optional. Let me also check `serializer_profile`, `render_config`, `full_array_fingerprint`, `messages`, `ts_ck_messages` — all optional in Rust but not emitted by TS.

[assistant] 2026-07-10T10:40:02Z
The `ShadowTransformWire` has `shadow_generation: u64` (required), `input: Vec<Value>` (required, no default — wait, it has `#[serde(default)]` at line 223), `pass_inputs: ShadowPassInputs` (required). The TS emits `shadow_generation`, `input`, `ts_output`, `normalizations`, `pass_inputs`, `ts_decision`, `declared_trim`. The Rust `input` has `#[serde(default)]` so it defaults to empty if missing. But the TS always sends it. The `pass_inputs` is required and the TS sends it. This looks OK for the transform wire.

Now let me check `shadow_input_messages` to see how the Rust side builds the transform input from the wire — does it use `input` or `messages`?

[assistant] 2026-07-10T10:40:07Z

[assistant] 2026-07-10T10:40:09Z
Good. The `shadow_input_messages` uses `parsed.input` (the TS-annotated input with `absolute_ordinal` fields) and decodes via `decode_opencode`. The `absolute_ordinal` function (line 3509) looks for `absolute_ordinal` or `absoluteOrdinal` keys. The TS emits `absolute_ordinal` (shadow-sender.ts line 405). This matches.

Now let me check the `ShadowPassInputs` — the TS emits `effective_execute_threshold` but Rust has aliases. Let me verify the `usage` field shape.

The TS `ShadowPassInputs` (shadow-sender.ts lines 54-64):
```ts
{ now_ms, model_key, usage: { input_tokens, limit }, effective_execute_threshold, cache_ttl, provider_error? }
```

The Rust `ShadowPassInputs` (lines 240-256):
```rust
{ now_ms: i64, model_key: Option<String>, usage: Option<ShadowUsageWire>, 
  effective_execute_threshold: f64 (alias "effective_execute_threshold", "execute_threshold_percentage"),
  cache_ttl: String (default "5m"), provider_error: Option<String> }
```

The Rust `ShadowUsageWire` (lines 258-264):
```rust
{ input_tokens: u64 (alias "current_total_input_tokens"), limit: u64 (alias "context_limit_tokens") }
```

The TS emits `input_tokens` and `limit` — these match the primary field names. Good. The `effective_execute_threshold` matches via alias. The `now_ms` is `i64` in Rust but the TS emits a number — if the TS emits a float it would fail. Let me check what the TS actually passes.

Looking at the test (line 125-130): `now_ms: args.nowMs ?? 1` — it's a number. In production, it comes from `Date.now()` which is an integer. This is fine.

Now let me check the `ShadowMemoryMutationWire` — the TS emits `project_path` (line 716) but Rust doesn't have that field. The Rust struct has `id` (required), `mutation_type` (required), `target_memory_id` (required), `superseded_by_id` (optional), `category` (optional), `new_content` (optional), `queued_at` (required, i64). The TS emits all of these. But wait — `queued_at` is `i64` in Rust. Let me check what the TS emits for `queued_at`.

Looking at shadow-sender.ts line 723: `queued_at: row.queuedAt`. This comes from `getMemoryMutationsForRenderByProjects`. Let me check the type.

[assistant] 2026-07-10T10:40:19Z
`queuedAt` is a `number` in TS. In Rust it's `i64`. If the TS value exceeds `i64` range it would fail, but `queuedAt` is a timestamp (milliseconds), which fits in `i64`. This is fine.

Now let me check the `m0_mutations` issue more carefully. The TS sends `m0_mutations` in the state_sync payload (line 735), but the Rust `ShadowStateSyncWire` has no `m0_mutations` field. The Rust handler at line 2171-2175 only processes `compartments`, `memories`, and `memory_mutations`. The `apply_shadow_state_sync` store method (line 2079-2172) only takes `compartments`, `memories`, `memory_mutations`, `last_todo_state`, `acked_watermarks`. There's no m0_mutations parameter at all. So m0 mutations are NEVER synced to the shadow store.

Let me verify whether the shadow transform on the Rust side actually uses m0 mutations. Let me check if the transform reads m0 mutations from the store.

[assistant] 2026-07-10T10:40:23Z
The m0_mutation_log is a TS-side concept (in the shared Magic Context DB). The Rust shadow store doesn't have an m0_mutations table — it only has `shadow_memories` and `shadow_memory_mutation_log`. The m0_mutations are a TS-side watermark for cache-busting detection. Since the Rust shadow transform runs its own transform independently, it doesn't need the TS's m0_mutations — it computes its own. So the missing `m0_mutations` field in the wire is actually a DESIGN choice, not a bug — the Rust side doesn't use m0_mutations for the shadow comparison. The watermark is still tracked on the TS side for the incremental sync logic.

But the `watermarks` vs `acked_watermarks` mismatch IS a real issue — the TS sends `watermarks` but Rust reads `acked_watermarks`. Let me verify this more carefully. The Rust handler at line 2176-2183:
```rust
let acked_watermarks = parsed.acked_watermarks.unwrap_or_else(|| {
    json!({
        "compartment_seq": compartments.iter().map(|c| c.sequence).max(),
        "memory_id": memories.iter().map(|m| m.id).max(),
        "memory_mutation_id": memory_mutations.iter().map(|m| m.id).max(),
        "last_todo_state": parsed.last_todo_state.is_some(),
    })
});
```

So if `acked_watermarks` is None (which it always will be since the TS sends `watermarks` not `acked_watermarks`), the Rust side computes a fallback. This fallback is stored as `meta.shadow_acked_watermarks`. The TS side tracks `lastAckedWatermarks` for its own incremental sync logic. The Rust side's `acked_watermarks` is used for... let me check.

[assistant] 2026-07-10T10:40:27Z
The `shadow_acked_watermarks` is stored in `meta` and set during state_sync (line 2134) and cleared during reset (line 2205). Let me check if it's ever READ back for any logic beyond storage.

[assistant] 2026-07-10T10:40:39Z
The `shadow_acked_watermarks` is only written, never read for any logic. It's stored as metadata but doesn't affect the transform or comparison. So the `watermarks` vs `acked_watermarks` mismatch is a silent data loss but doesn't affect the shadow comparison correctness. It's a medium-severity issue (the watermarks are stored but never used for decisions).

Now let me focus on the COMPARTMENT wire shape mismatch, which IS critical. Let me verify by checking if there are any Rust tests that exercise the state_sync with real compartment data.

[assistant] 2026-07-10T10:40:43Z
Let me check the store tests for shadow state sync.

[assistant] 2026-07-10T10:40:55Z

[assistant] 2026-07-10T10:40:57Z
The Rust store tests use `StoredCompartment` directly (via the `comp()` helper), NOT the wire types. They never test the serde deserialization from the TS wire shape. This confirms the wire-shape mismatch is untested.

Let me check the `comp()` helper to see what fields it sets.

[assistant] 2026-07-10T10:41:00Z

[assistant] 2026-07-10T10:41:02Z
Confirmed. The Rust store tests use `StoredCompartment` directly with `start_message: 0, end_message: end, start_message_id, end_message_id` — the flat shape. They NEVER test deserialization from the TS wire shape with nested `start`/`end` objects.

Now let me check if there's an integration test that exercises the full wire path (TS sender → Rust handler).

[assistant] 2026-07-10T10:41:06Z
Let me check if the real_daemon test exercises shadow ops.

[assistant] 2026-07-10T10:41:08Z
The real daemon test does NOT exercise shadow ops at all. This confirms the wire-shape mismatch is completely untested end-to-end.

Now let me check the `cloneJson` hot-path concern and the `denormalizeShadowOutput` being called TWICE (once in `enqueue`, once in `buildShadowTransformBody`). Let me re-read the enqueue path carefully.

[assistant] 2026-07-10T10:41:14Z
I see that `enqueue` calls `denormalizeShadowOutput` (line 1054) which does `cloneJson(args.outputMessages)` (line 288). Then in `buildShadowTransformBody` (line 807-818), it checks if `shadowTsOutput` and `shadowNormalizations` are already set (from the prepared pass), and only calls `denormalizeShadowOutput` again if they're NOT set. Since `enqueue` always sets them (lines 1065-1066), the second call is skipped. Good — no double clone.

But there's a hot-path concern: `enqueue` runs synchronously inside the transform hot path. It calls:
1. `resolveOrdinalsForShadow` — which calls `cloneJson(args.messages)` (line 380) AND `withRawSessionMessageCache(() => readRawSessionMessages(...))` (line 375-378) AND potentially `readRawSessionMessageById` (line 395) per below-floor message
2. `denormalizeShadowOutput` — which calls `cloneJson(args.outputMessages)` (line 288) AND `getAutoSearchHintDecisions` (line 323, a DB read)

The `cloneJson` uses `JSON.parse(JSON.stringify(value))` — for a 400-message array this could be significant. Let me check if there's any bounding or if this is truly unbounded.

The `resolveOrdinalsForShadow` clones `args.messages` (the input messages) at line 380. The `denormalizeShadowOutput` clones `args.outputMessages` at line 288. Both use `JSON.parse(JSON.stringify(...))`. For a 400-message session, each message can have multiple parts with potentially large text content. Two full JSON round-trips per pass on the hot path is concerning but the question says this is fire-and-forget shadow mode — let me verify that `enqueue` is truly called synchronously in the hot path.

[assistant] 2026-07-10T10:41:21Z

[assistant] 2026-07-10T10:41:23Z

[assistant] 2026-07-10T10:41:25Z
Good — the `enqueue` call at line 2205 is wrapped in a try/catch (line 2226-2228). So if `enqueue` throws, it's caught and logged, and the transform continues. This is the exception-safety guard. But the concern is about STALLS, not just throws — `cloneJson` on a huge array won't throw but could take hundreds of milliseconds.

Let me now check the `shadowInputMessages` capture to verify it's taken BEFORE the transform mutates.

[assistant] 2026-07-10T10:41:28Z
I see that `shadowInputMessages` is passed as `inputMessages` (line 2208) and `messages` as `outputMessages` (line 2209). Let me find where `shadowInputMessages` is captured to verify it's a pre-transform snapshot.

[assistant] 2026-07-10T10:41:33Z

[assistant] 2026-07-10T10:41:36Z
`shadowInputMessages` is captured at line 459 via `cloneForShadow(messages)` BEFORE the transform runs. Let me check `cloneForShadow`.

[assistant] 2026-07-10T10:41:38Z

[assistant] 2026-07-10T10:41:44Z
`cloneForShadow` is `JSON.parse(JSON.stringify(value))` — a deep clone. This is taken at line 459, BEFORE the transform mutates `messages`. So the shadow input is a pre-transform snapshot. Good.

But now there's a concern: `enqueue` at line 2205 passes `outputMessages: messages` — this is the POST-transform `messages` array (the live array, not a clone). Inside `enqueue`, `denormalizeShadowOutput` does `cloneJson(args.outputMessages)` (line 288) which deep-clones it. So the live array is not mutated. But there's a timing issue: between line 2205 (enqueue call) and the `cloneJson` inside `denormalizeShadowOutput`, the `messages` array could theoretically be mutated by another async operation. However, since `enqueue` is synchronous and `denormalizeShadowOutput` is called synchronously within `enqueue`, there's no async gap. This is safe.

Now let me check the `SubcShadowTransport` connection lifecycle for the socket close mid-request issue. Let me re-read the `readTerminalFor` function and the `SocketReader`.

[assistant] 2026-07-10T10:41:49Z
I see a potential issue in `SocketReader.readExact`: when a `read timeout` fires (line 1284), the promise rejects but the `onReady` callback is still in the `waiters` array (it's removed at line 1283 before reject). But if data arrives AFTER the timeout reject, `flushWaiters` will call the stale `onReady` which calls `clearTimeout(timer)` (already fired) and `resolve()` — but the promise was already rejected. The `resolve` is a no-op since the promise is already settled. This is safe but wasteful.

More importantly: when a timeout fires in `readExact`, the reader's internal state (`chunks`, `buffered`) may have partial data from a previous read that was interrupted. The next `readFrame` call will try to read from the corrupted buffer position. But since the transport serializes all calls through `this.pending` (line 1096-1120), there's only one outstanding request at a time. After a timeout, the connection is likely in a bad state, and the next `ensureConnected` will check `this.socket.destroyed` — but the socket might NOT be destroyed yet (just timed out on read). Let me check if a timeout causes socket cleanup.

Looking at `unaryJson` (line 1192-1213): after `readTerminalFor` throws (timeout), the error propagates up. The `call` method's `run` function throws, which rejects `next`. The `pending` chain catches it (line 1116-1119). But the socket is NOT destroyed on timeout — it's left in an unknown state. The next `ensureConnected` check at line 1159 (`if (this.socket && !this.socket.destroyed && this.reader) return`) will see the socket as still connected and reuse it, even though the reader's buffer may have partial/stale frame data from the timed-out read.

This is a real bug: after a request timeout, the socket is reused with a corrupted reader buffer. The next request will read stale/partial data from the previous timed-out response, causing frame misalignment.

Let me verify there's no socket cleanup on timeout.

[assistant] 2026-07-10T10:41:58Z
Confirmed. After `readTerminalFor` throws (timeout at line 1204), the error propagates out of `unaryJson`. The socket is NOT destroyed. The `ensureConnected` check on the next call (line 1159) sees `this.socket` is still set and not destroyed, so it reuses the stale socket + corrupted reader buffer. This is a real connection lifecycle bug.

However, the `socket.once("close", ...)` handler (line 1176-1180) will fire if the server closes the socket, which would clean up. But if the server doesn't close it (the timeout is client-side), the stale socket persists.

Now let me check the `readTerminalFor` function more carefully — it loops reading frames until it finds one matching channel+corr. If a timeout occurs mid-loop, the partial frame data stays in the reader buffer. The next request will try to parse that partial data as a frame header, causing misalignment.

Let me also check the queue wedging concern. Looking at `processPass` (line 948-1013): if `state.blockedUntilReset` is true (line 960), it returns early without processing. But `blockedUntilReset` is set to true on peer reject (line 896) and cleared on reset (line 939). The reset is queued via `pushWork` in the `runQueue` catch block (line 895-896 sets `requireResetReason` but doesn't push a reset — let me re-check).

Looking at the catch block (line 889-904):
```js
catch (error) {
    state.counters.send_failures += 1;
    if (isPeerReject(error)) {
        state.requireResetReason = code ?? "peer_reject";
        state.blockedUntilReset = true;
        state.lastAckedWatermarks = null;
    } else if (isConnectionFailure(error)) {
        state.counters.connection_skips += 1;
        state.initialized = false;
        state.requireResetReason = "route_reopen";
    }
    sessionLog(...);
}
```

On peer reject, it sets `blockedUntilReset = true` and `requireResetReason`, but does NOT push a reset work item. The next `processPass` call (line 952) checks `!state.initialized || state.requireResetReason` — if `requireResetReason` is set, it calls `performReset`. So the reset happens on the NEXT pass, not immediately. But if no more passes are enqueued, the session stays blocked forever with no reset queued. This is the "permanently wedged" scenario.

Wait — let me re-read. After the catch block, the `while` loop continues (line 875). If there are more items in the queue, the next item will be processed. If the next item is a pass, `processPass` will see `requireResetReason` is set and call `performReset` (line 952-958). But if the queue is empty after the failed pass, the loop exits. The next `enqueue` will push a pass, which triggers `schedule` → `runQueue` → `processPass` → sees `requireResetReason` → calls `performReset`. So the session is NOT permanently wedged as long as new passes keep coming.

But if the session is idle (no more passes), it stays `blockedUntilReset = true`. When a new pass arrives, it will trigger a reset first. This is acceptable behavior — the session self-heals on the next pass.

Let me now check the `resetSession` method for the race between reset and queued passes.

[assistant] 2026-07-10T10:42:08Z
`resetSession` clears the queue (line 1072) then pushes a reset (line 1076). If `runQueue` is currently running (state.running = true), the reset is queued and will be processed after the current item. But if the current item is a pass that's mid-flight (awaiting transport.call), the reset will be processed after that pass completes. This is a potential race: the in-flight pass might succeed and advance `lastAckedSeq`, then the reset wipes everything. But since the reset bumps the generation, the in-flight pass's ack would be for the old generation. The next pass after reset would use the new generation. This is acceptable — the reset is authoritative.

Now let me check the `MagicContextPluginConfig` type to understand what the `...pluginConfig` spread leaks into the hook config.

[assistant] 2026-07-10T10:42:11Z

[assistant] 2026-07-10T10:42:14Z
`MagicContextPluginConfig` extends `MagicContextConfig` and adds `disabled_hooks` and `command`. Let me check the hook config type to see if these extra fields cause any issues.

[assistant] 2026-07-10T10:42:18Z

[assistant] 2026-07-10T10:42:22Z

[assistant] 2026-07-10T10:42:25Z

[assistant] 2026-07-10T10:42:27Z

[assistant] 2026-07-10T10:42:30Z

[assistant] 2026-07-10T10:42:32Z

[assistant] 2026-07-10T10:42:35Z

[assistant] 2026-07-10T10:42:37Z
Let me check the hook config type at hook.ts line 105.

[assistant] 2026-07-10T10:42:40Z
The hook config type (lines 105-149) declares specific fields. The `...pluginConfig` spread from `buildMagicContextHookConfig` will include ALL fields from `MagicContextPluginConfig`, including `disabled_hooks` and `command` (which are not in the hook config type). Since TypeScript is structural, the extra fields are harmless at the type level. But the question is whether any hook code accesses `config.disabled_hooks` or `config.command` and does something unexpected. Since the hook config type doesn't declare these fields, no hook code should access them. The spread is safe from a type perspective.

But there's a subtle behavioral concern: the spread includes `shadow_transform` (line 146-148), which was previously dropped by the old hand-maintained mapping. The comment at line 22-26 of `create-session-hooks.ts` explicitly calls this out as the fix. This is the intended improvement, not a bug.

Let me now check the `AUDIT-KNOWN-ISSUES.md` to see if any of my findings are already known.

[assistant] 2026-07-10T10:42:44Z
No known issues mention shadow or wire shape. Let me now check if there's a `denormalizeShadowOutput` exception-safety concern — it calls `getAutoSearchHintDecisions` which does a DB read. If the DB is busy, this could throw.

[assistant] 2026-07-10T10:42:47Z
The `denormalizeShadowOutput` calls `getAutoSearchHintDecisions(args.db, args.sessionId)` at line 323. This is a DB read on the LIVE `ContextDatabase` (`pass.db`). If this DB is the same one the transform uses, and the transform is writing to it, there could be contention. But `enqueue` is called AFTER the transform completes (line 2205 in transform.ts is after the transform logic), so the transform's writes are done. The `getAutoSearchHintDecisions` read should not contend with transform writes.

However, `denormalizeShadowOutput` is called synchronously in `enqueue` which is in the hot path. If `getAutoSearchHintDecisions` throws (e.g., SQLITE_BUSY from another writer), the throw propagates out of `enqueue`. But the transform.ts caller wraps `enqueue` in try/catch (line 2226-2228), so it's caught. The exception-safety is OK.

Now let me check one more thing: the `resolveOrdinalsForShadow` function calls `withRawSessionMessageCache(() => readRawSessionMessages(...))` which reads from the OpenCode session DB. This is a DIFFERENT DB from the Magic Context DB (`pass.db`). The OpenCode session DB is opened read-only (line 63 in read-session-db.ts: `new Database(dbPath, { readonly: true })`). So there's no write contention. But the `readRawSessionMessageById` fallback also reads from this same read-only DB. Under SQLITE_BUSY, a read-only connection can still get SQLITE_BUSY if a writer is holding a lock. But since it's read-only, it uses a shared lock. Let me check if there's any retry or timeout handling.

Looking at `withReadOnlySessionDb` (line 68-70): it just calls `fn(getReadOnlySessionDb())` with no retry. If the DB query throws SQLITE_BUSY, it propagates. The `readRawSessionMessageById` call at line 395 in `resolveOrdinalsForShadow` is NOT wrapped in a try/catch — if it throws, the throw propagates out of `resolveOrdinalsForShadow`, out of `enqueue`, and is caught by the transform.ts try/catch (line 2226). So the pass is skipped but the transform continues. This is acceptable.

Let me now check the `ShadowMemoryMutationWire` — the TS emits `project_path` but Rust doesn't have it. Also, the TS `ShadowMemoryMutationWire` doesn't emit `session_id` for memory mutations, but let me verify the Rust struct doesn't need it.

Looking at the Rust `ShadowMemoryMutationWire` (lines 348-361): it has `id`, `mutation_type`, `target_memory_id`, `superseded_by_id`, `category`, `new_content`, `queued_at`. No `session_id` or `project_path`. The TS emits `id`, `project_path`, `mutation_type`, `target_memory_id`, `superseded_by_id`, `category`, `new_content`, `queued_at` (lines 714-723). The extra `project_path` is silently dropped by serde. This is fine.

Now let me check one final critical thing: the `ShadowCompartmentWire` has `start_message: i64` and `end_message: i64` as REQUIRED fields (no `#[serde(default)]`). The TS `serializeCompartment` does NOT emit these fields — it emits `start.absolute_ordinal` and `end.absolute_ordinal` inside nested objects. This means EVERY state_sync with compartments will FAIL with a deserialization error. Let me verify this is truly the case by checking if serde has `deny_unknown_fields` which would also reject the nested `start`/`end`.

[assistant] 2026-07-10T10:42:54Z
No `#[serde(deny_unknown_fields)]` on `ShadowCompartmentWire`. So serde will:
1. Ignore the unknown `start` and `end` nested objects (they're unknown fields)
2. Ignore the unknown `id` field (the TS emits `id` but Rust doesn't have it)
3. Find `start_message` and `end_message` MISSING → deserialization FAILS

This is a CRITICAL bug. Every state_sync that includes compartments will reject with `invalid_params: missing field start_message`. Since compartments are the core of the shadow comparison, this means the shadow soak is completely broken for any session with compartments.

Let me verify the TS `serializeCompartment` one more time to be absolutely sure.

[assistant] 2026-07-10T10:42:57Z
Confirmed beyond doubt. The TS `serializeCompartment` emits:
- `id` (not in Rust struct — silently dropped)
- `sequence` (matches Rust `sequence: i64`)
- `start: { flat_id, bare_message_id, absolute_ordinal }` (nested object — silently dropped as unknown field; Rust expects flat `start_message: i64`, `start_message_id: String`)
- `end: { flat_id, bare_message_id, absolute_ordinal }` (same issue — Rust expects flat `end_message: i64`, `end_message_id: String`)
- `title, content, p1-p4, importance, episode_type, legacy, created_at` (all match)

The REQUIRED fields `start_message` and `end_message` are NEVER emitted by the TS. This is a CRITICAL wire-shape mismatch that will cause EVERY state_sync with compartments to fail.

Now let me do one final check — let me see if there's a `#[serde(deny_unknown_fields)]` on `ShadowStateSyncWire` that would reject the `watermarks` and `m0_mutations` unknown fields.

[assistant] 2026-07-10T10:43:02Z
No `#[serde(deny_unknown_fields)]` on `ShadowStateSyncWire`. So the unknown `watermarks` and `m0_mutations` fields from the TS are silently dropped. The `acked_watermarks` field defaults to `None`. The `compartments` field will try to deserialize each compartment as `ShadowCompartmentWire`, which will FAIL because `start_message` and `end_message` are missing.

So the actual failure path is:
1. TS sends state_sync with `compartments: [{ sequence, start: {...}, end: {...}, ... }]`
2. Rust tries to deserialize each compartment as `ShadowCompartmentWire`
3. `start_message` (required, no default) is missing → `serde_json::from_value` fails
4. Handler returns `invalid_params_error(e.to_string())` at line 2151
5. The error propagates back to the TS sender as a peer reject
6. The sender sets `requireResetReason` and `blockedUntilReset`
7. Every subsequent pass triggers a reset, then another state_sync with the same broken compartments
8. The session is permanently stuck in a reset loop

This is the SAME class of bug as the first armed run's wire-shape mismatch, but in the compartment sub-structure. The flat-wire fix (commit 6ea3179f) fixed the top-level `{method, params}` → flat shape, but the NESTED compartment shape was never aligned.

Let me now also quickly check the `ShadowResetWire` — the TS sends `shadow_generation` and `reason`, but Rust only has `session_id` and `shadow_generation` (both optional). The `reason` field is silently dropped. This is harmless (reason is just for logging).

Let me verify the `ShadowTransformWire` `ts_decision` field — the TS sends it as an object, Rust expects `Value` (line 233). This should work since `Value` accepts any JSON. Good.

Now let me check one more potential issue: the `pass_inputs` field in the transform wire. The TS emits `pass_inputs` as an object with `now_ms`, `model_key`, `usage`, `effective_execute_threshold`, `cache_ttl`. The Rust `ShadowPassInputs` has `now_ms: i64` (required). If the TS emits `now_ms` as a float (unlikely but possible from `Date.now()` which returns integer), it would fail. But `Date.now()` returns an integer. This is fine.

Let me now check the `provider_error` field — the TS `ShadowPassInputs` has `provider_error?: string` (line 63), and the Rust has `provider_error: Option<String>` (line 255). The TS transform.ts at line 2213-2222 does NOT emit `provider_error` in the `passInputs`. Let me verify.

[assistant] 2026-07-10T10:43:23Z
The TS does NOT emit `provider_error` in `passInputs`. The Rust `ShadowPassInputs` has `#[serde(default)] provider_error: Option<String>`, so it defaults to `None`. This is fine — the shadow transform doesn't need the provider error for comparison.

Now I have enough evidence for my findings. Let me do one final check on the `SocketReader` timeout leak issue and the `readTerminalFor` function to confirm the frame misalignment bug.

The `readTerminalFor` function (lines 1392-1405) loops calling `readFrame`. If a frame arrives for a DIFFERENT channel/corr (e.g., a late response from a previous timed-out request), it's silently skipped and the loop continues. But the frame data is consumed from the buffer. This means a late response from a timed-out request is silently discarded, which is correct. However, if a timeout occurs MID-FRAME (partial header read), the partial data stays in the buffer. The next `readFrame` call will try to read a header from the partial data, causing misalignment.

Actually, looking more carefully at `readExact` (line 1275-1305): it only returns when it has read the FULL `length` bytes. If it times out before reading all bytes, it throws. But the partial bytes it DID read are NOT consumed from the buffer — they stay in `this.chunks` and `this.buffered`. Wait, let me re-read: `readExact` only copies bytes out of `this.chunks` at lines 1293-1303 AFTER the while loop at line 1277 completes (i.e., after `this.buffered >= length`). If the while loop throws (timeout or connection closed), the copy block at line 1293 is never reached. So partial data stays in the buffer. This confirms the frame misalignment bug: after a timeout, the next `readFrame` will try to read a header from the stale partial data.

But wait — the `readExact` while loop at line 1277 checks `this.buffered < length`. If `this.buffered` already has enough data (from a previous partial read or from data that arrived after the timeout), it will proceed to the copy block. But the data might be a mix of partial header from the timed-out frame and new data. This is the misalignment.

Actually, I need to think about this more carefully. The `readExact` function reads exactly `length` bytes. If it times out, it throws WITHOUT consuming any bytes. The next call to `readExact` (from the next request) will start reading from the same position in the buffer. If the server sends a new response, the buffer will have: [partial old frame data] + [new response data]. The `readExact(SUBC_HEADER_LEN)` will read the first 17 bytes, which are partial old frame data — NOT a valid header. This causes frame misalignment.

However, there's a subtlety: after a timeout in `readTerminalFor`, the error propagates to `unaryJson`, which throws. The `call` method's `pending` chain catches it. But the socket is NOT destroyed. The next `ensureConnected` sees the socket as alive and reuses it. The `SocketReader` still has the stale partial data. The next `readFrame` reads a corrupted header.

This IS a real bug, but its blast radius is limited to the shadow lane (the live session is unaffected). And the shadow lane is observe-only. The consequence is that after a timeout, the shadow connection is corrupted and all subsequent shadow passes fail until the server closes the socket (which triggers the `close` handler and clears the socket). The backoff mechanism would eventually reconnect. But between the timeout and the server-side close, all shadow passes fail with frame parse errors.

Let me now compile my findings. Let me also quickly check if there's a `SQLITE_BUSY` concern with the `readRawSessionMessageById` fallback.

[assistant] 2026-07-10T10:43:32Z
The read-only DB is cached and reused. It's opened with `readonly: true`. SQLite read-only connections can still get `SQLITE_BUSY` if a writer is holding a RESERVED lock, but WAL mode allows concurrent readers. Let me check if OpenCode uses WAL mode.

[assistant] 2026-07-10T10:43:39Z
The Magic Context DB uses WAL mode (line 363: `db.exec("PRAGMA journal_mode=WAL")`). But the read-only session DB (`getReadOnlySessionDb`) opens the OpenCode session DB (`opencode.db`), not the Magic Context DB. The OpenCode session DB may or may not use WAL. Let me check if the read-only connection has a busy_timeout.

[assistant] 2026-07-10T10:43:46Z
The read-only DB is opened at line 63: `new Database(dbPath, { readonly: true })` with NO busy_timeout configured. If OpenCode's session DB is in WAL mode (which it likely is since OpenCode uses better-sqlite3 with WAL), read-only connections can still get SQLITE_BUSY in certain edge cases (e.g., during checkpoint). But in WAL mode, readers generally don't block. The risk is low but non-zero.

I now have enough evidence for a comprehensive audit. Let me compile my findings.

[assistant] 2026-07-10T10:43:54Z
<COUNCIL_MEMBER_RESPONSE>

## Finding 1: CRITICAL — Compartment wire-shape mismatch: `serializeCompartment` emits nested `start`/`end` objects but Rust `ShadowCompartmentWire` requires flat `start_message`/`end_message` fields, causing EVERY state_sync with compartments to reject

- **Severity**: critical
- **Location**: `packages/plugin/src/hooks/magic-context/shadow-sender.ts:584-616` (TS emitter) vs `crates/mc-module/src/lib.rs:267-295` (Rust parser)
- **Confidence**: high
- **Issue**: The TS `serializeCompartment` emits compartments with nested `start: { flat_id, bare_message_id, absolute_ordinal }` and `end: { flat_id, bare_message_id, absolute_ordinal }` objects. The Rust `ShadowCompartmentWire` expects flat `start_message: i64` and `end_message: i64` fields (REQUIRED, no `#[serde(default)]`), plus optional `start_message_id: String` and `end_message_id: String`. Since serde ignores unknown fields (no `deny_unknown_fields`), the nested `start`/`end` objects are silently dropped, and the required `start_message`/`end_message` fields are missing → `serde_json::from_value` fails with `missing field start_message`. The handler at `lib.rs:2149-2151` returns `invalid_params_error`. This triggers a peer-reject → reset loop on the TS side (`shadow-sender.ts:891-896`), permanently wedging the shadow lane for any session with compartments.
- **Evidence**: 
  - TS emits: `{ id, sequence, start: { flat_id, bare_message_id, absolute_ordinal }, end: { flat_id, bare_message_id, absolute_ordinal }, title, content, p1, p2, p3, p4, importance, episode_type, legacy, created_at }` (shadow-sender.ts:584-615)
  - Rust expects: `{ sequence: i64, start_message: i64, end_message: i64, start_message_id: String, end_message_id: String, title, content, p1-p4, importance, episode_type, legacy, created_at }` (lib.rs:267-295)
  - `start_message` and `end_message` have NO `#[serde(default)]` → required → deserialization fails when absent
  - The test at `shadow-sender.test.ts:350-447` validates the TS wire shape against a `FakeTransport` that accepts ANY shape — it never sends to the real Rust parser. The Rust store tests (`lib.rs:6080-6166`) use `StoredCompartment` directly, never testing serde deserialization from the TS wire shape.
  - The `real_daemon.rs` integration test does NOT exercise shadow ops at all (no `shadow` or `state_sync` references in `crates/mc-module/tests/`).
- **Suggested Fix**: Align the TS `serializeCompartment` to emit flat fields matching the Rust struct: `start_message: startOrdinal, end_message: endOrdinal, start_message_id: args.compartment.startMessageId, end_message_id: args.compartment.endMessageId`. Remove the nested `start`/`end` objects. Add an integration test that round-trips a compartment through the real Rust serde parser.

## Finding 2: HIGH — `watermarks` vs `acked_watermarks` field name mismatch: TS sends `watermarks` but Rust reads `acked_watermarks`, silently losing the TS-computed watermarks

- **Severity**: high
- **Location**: `packages/plugin/src/hooks/magic-context/shadow-sender.ts:732` (TS emits `watermarks`) vs `crates/mc-module/src/lib.rs:199` (Rust expects `acked_watermarks`)
- **Confidence**: high
- **Issue**: The TS `buildStateSyncPayload` emits `watermarks: currentWatermarks` in the state_sync params (shadow-sender.ts:732). The Rust `ShadowStateSyncWire` has `#[serde(default)] acked_watermarks: Option<Value>` (lib.rs:198-199) — the field is named `acked_watermarks`, not `watermarks`. Since serde ignores unknown fields, the TS `watermarks` field is silently dropped, and `acked_watermarks` defaults to `None`. The Rust handler then computes a fallback (lib.rs:2176-2183) from the compartment/memory max IDs. The carefully computed TS watermarks (which track `compartment_sequence`, `memory_id`, `m0_mutation_id`, `memory_mutation_id`, `last_todo_state_hash`) are silently discarded. The fallback uses different key names (`compartment_seq` vs `compartment_sequence`, `memory_mutation_id` matches, `last_todo_state` as boolean vs hash string).
- **Evidence**: 
  - TS: `watermarks: currentWatermarks` (shadow-sender.ts:732) where `currentWatermarks` has keys `compartment_sequence, memory_id, m0_mutation_id, memory_mutation_id, last_todo_state_hash`
  - Rust: `#[serde(default)] acked_watermarks: Option<Value>` (lib.rs:199) — field name mismatch
  - Rust fallback: `json!({ "compartment_seq": ..., "memory_id": ..., "memory_mutation_id": ..., "last_todo_state": bool })` (lib.rs:2176-2183) — different shape than TS watermarks
  - The stored `shadow_acked_watermarks` is only written, never read for logic (grep confirms only 3 references: field decl, write in state_sync, clear in reset), so the impact is limited to stored metadata being wrong, not affecting the transform comparison. But it defeats the purpose of tracking watermarks for potential future use.
- **Suggested Fix**: Rename the TS field from `watermarks` to `acked_watermarks` to match the Rust struct, OR rename the Rust field to `watermarks`. Ensure the watermark value shape matches what the Rust side expects.

## Finding 3: MEDIUM — `m0_mutations` field silently dropped: TS sends m0_mutations in state_sync but Rust `ShadowStateSyncWire` has no such field

- **Severity**: medium
- **Location**: `packages/plugin/src/hooks/magic-context/shadow-sender.ts:174,735` (TS emits `m0_mutations`) vs `crates/mc-module/src/lib.rs:185-200` (Rust `ShadowStateSyncWire` has no `m0_mutations` field)
- **Confidence**: high
- **Issue**: The TS `ShadowStateSyncPayload` includes `m0_mutations: unknown[]` (shadow-sender.ts:174) and `buildStateSyncPayload` populates it (shadow-sender.ts:696-706, 735). The Rust `ShadowStateSyncWire` has no `m0_mutations` field — only `compartments`, `memories`, `memory_mutations`, `last_todo_state`, `acked_watermarks`. The `m0_mutations` array is silently dropped by serde. The Rust shadow store never receives m0 mutation data. This is likely a design choice (the Rust shadow transform computes its own m0 state), but the TS side tracks `m0_mutation_id` in watermarks (shadow-sender.ts:160,547) and sends m0_mutations for incremental sync — all of which is wasted work. The TS test at `shadow-sender.test.ts:385` asserts `m0_mutations: expect.any(Array)` exists in the payload, validating a field that the Rust side silently discards.
- **Evidence**: 
  - TS: `m0_mutations: m0Mutations` (shadow-sender.ts:735) where `m0Mutations` comes from `getM0MutationsAfterId` (line 696-706)
  - Rust: `ShadowStateSyncWire` fields are `session_id, shadow_generation, expected_shadow_seq, compartments, memories, memory_mutations, last_todo_state, acked_watermarks` (lib.rs:185-200) — no `m0_mutations`
  - The `apply_shadow_state_sync` store method (lib.rs:2079-2172) has no m0_mutations parameter
- **Suggested Fix**: If m0_mutations are not needed on the Rust side, remove the TS-side computation to avoid wasted DB reads and serialization. If they ARE needed, add an `m0_mutations` field to `ShadowStateSyncWire` and wire it through to the store.

## Finding 4: MEDIUM — Socket reuse after request timeout corrupts frame alignment: `SubcShadowTransport` does not destroy the socket on read timeout, leaving stale partial data in the `SocketReader` buffer

- **Severity**: medium
- **Location**: `packages/plugin/src/hooks/magic-context/shadow-sender.ts:1192-1213` (`unaryJson`) and `1275-1305` (`SocketReader.readExact`)
- **Confidence**: high
- **Issue**: When `readTerminalFor` times out (REQUEST_TIMEOUT_MS = 5s, line 1204), the error propagates out of `unaryJson` but the socket is NOT destroyed. The `SocketReader.readExact` throws on timeout WITHOUT consuming partial data from `this.chunks` (the copy block at lines 1293-1304 is only reached after the while loop at line 1277 completes successfully). The next `ensureConnected` check (line 1159: `if (this.socket && !this.socket.destroyed && this.reader) return`) sees the socket as alive and reuses it. The next `readFrame` call reads a header from stale partial data, causing frame misalignment. All subsequent shadow passes fail with frame parse errors until the server closes the socket (triggering the `close` handler at line 1176-1180 which clears the socket).
- **Evidence**: 
  - `readExact` timeout: line 1280-1284 throws `"read timeout"` without consuming buffer data
  - `unaryJson` does NOT destroy socket on error: lines 1192-1213 — no cleanup in catch/throw path
  - `ensureConnected` reuses stale socket: line 1159 checks `!this.socket.destroyed` — a timed-out socket is not destroyed
  - The `socket.once("close", ...)` handler (line 1176) only fires on server-side close, not on client-side timeout
  - Blast radius: shadow lane only (live session unaffected), but corrupts all shadow coverage until server closes connection
- **Suggested Fix**: On any error from `readTerminalFor` (timeout, connection closed, unexpected frame type), destroy the socket and null out `this.socket`/`this.reader` so `ensureConnected` reconnects cleanly. Add `socket.destroy()` in a `finally` block or in the error path of `unaryJson`.

## Finding 5: MEDIUM — `cloneJson` on potentially huge message arrays in the transform hot path: two `JSON.parse(JSON.stringify(...))` round-trips per pass with no bounding

- **Severity**: medium
- **Location**: `packages/plugin/src/hooks/magic-context/shadow-sender.ts:226-228` (`cloneJson`), `380` (`resolveOrdinalsForShadow`), `288` (`denormalizeShadowOutput`); called from `enqueue` at lines 1018 and 1054
- **Confidence**: high
- **Issue**: `enqueue` runs synchronously inside the transform hot path (transform.ts:2205). It calls `resolveOrdinalsForShadow` which does `cloneJson(args.messages)` (line 380) and `denormalizeShadowOutput` which does `cloneJson(args.outputMessages)` (line 288). Both use `JSON.parse(JSON.stringify(value))` — two full JSON serialization round-trips on the ENTIRE message array per pass. For a 400-message session with large tool outputs, each clone can take 50-200ms, adding 100-400ms of synchronous stall to the user's real prompt. The `enqueue` call is wrapped in try/catch (transform.ts:2226), so a throw is safe, but a multi-second stall delays the real prompt without throwing. There is no size bound or skip-when-large guard.
- **Evidence**: 
  - `cloneJson` at line 226-228: `JSON.parse(JSON.stringify(value))` — no size check
  - `resolveOrdinalsForShadow` line 380: `cloneJson(args.messages)` — clones ALL input messages
  - `denormalizeShadowOutput` line 288: `cloneJson(args.outputMessages)` — clones ALL output messages
  - `enqueue` is called from transform.ts:2205, synchronously in the hot path
  - The test at shadow-sender.test.ts:494-514 verifies enqueue doesn't THROW, but doesn't measure latency
- **Suggested Fix**: Add a message count or estimated byte size guard: skip shadow enqueue when the array exceeds a threshold (e.g., >200 messages or >5MB serialized). Alternatively, use structuredClone (faster) or defer the clone to the async queue worker (runQueue) rather than doing it synchronously in `enqueue`.

## Finding 6: LOW — `handle_guidance_value` does not reject shadow bindings, allowing guidance reads on shadow routes

- **Severity**: low
- **Location**: `crates/mc-module/src/lib.rs:1730-1779` (`handle_guidance_value`)
- **Confidence**: high
- **Issue**: `handle_guidance_value` uses `resolve_binding` (line 1741) but does NOT check `is_shadow_session` to reject shadow bindings, unlike `handle_agent_drops_value` (line 1711) and `handle_transform_value` (line 1944). A shadow route could call `guidance.get` and receive guidance text. This is read-only (returns text bytes), so it cannot corrupt shadow state, but it's an isolation gap — shadow routes should only accept shadow ops.
- **Evidence**: 
  - `handle_agent_drops_value` has guard: line 1711 `if is_shadow_session(&binding.session) { return non_shadow_op_on_shadow_binding }`
  - `handle_transform_value` has guard: line 1944 `if is_shadow_session(&binding.session) { return plain_transform_on_shadow_binding }`
  - `handle_guidance_value` has NO guard: line 1741 only calls `resolve_binding` with no `is_shadow_session` check
- **Suggested Fix**: Add `if is_shadow_session(&binding.session) { return HandlerOutcome::Error { code: "non_shadow_op_on_shadow_binding", ... } }` after the `resolve_binding` call in `handle_guidance_value`.

## Finding 7: LOW — Read-only OpenCode session DB has no busy_timeout configured, risking SQLITE_BUSY on the shadow hot path

- **Severity**: low
- **Location**: `packages/plugin/src/hooks/magic-context/read-session-db.ts:63` (`new Database(dbPath, { readonly: true })`)
- **Confidence**: medium
- **Issue**: The cached read-only OpenCode session DB is opened without a `busy_timeout` pragma. The `readRawSessionMessageById` fallback (shadow-sender.ts:395) and `readRawSessionMessages` (via `withRawSessionMessageCache`) both read from this DB synchronously in the `enqueue` hot path. If OpenCode's writer is holding a lock (e.g., during a WAL checkpoint), the read-only connection can get SQLITE_BUSY. The throw propagates out of `enqueue` and is caught by transform.ts:2226, so the pass is skipped safely. But under heavy write load, the shadow lane could lose coverage silently. The Magic Context DB has a busy_timeout (storage-db.ts:354), but the read-only session DB does not.
- **Evidence**: 
  - `getReadOnlySessionDb` at line 56-66: `new Database(dbPath, { readonly: true })` — no `PRAGMA busy_timeout`
  - Magic Context DB for comparison: storage-db.ts:354 installs busy_timeout BEFORE WAL
  - The `readRawSessionMessageById` fallback is called per below-floor message (shadow-sender.ts:395), which is bounded by marker lag but still on the hot path
- **Suggested Fix**: Add `db.exec("PRAGMA busy_timeout = 2000")` after opening the read-only connection, or wrap the read in a retry with a short backoff.

## Summary

**Total findings: 7** — Critical: 1, High: 1, Medium: 3, Low: 2

**False-positive filtering performed:**
- Checked `packages/pi-plugin/PARITY.md` (730 lines) — no shadow-related entries; all divergences are Pi↔OpenCode mechanism differences, not shadow wire shape
- Checked `packages/plugin/src/features/magic-context/smart-notes/PARITY.md` — SSRF guard parity only, irrelevant to shadow
- Checked `docs/AUDIT-KNOWN-ISSUES.md` — no shadow or wire-shape entries
- Checked `shadow-sender.test.ts` — tests use `FakeTransport` that accepts ANY wire shape; never validates against real Rust serde parser
- Checked `crates/mc-store/src/lib.rs` store tests — use `StoredCompartment` directly, never test serde deserialization from TS wire
- Checked `crates/mc-module/tests/real_daemon.rs` — no shadow ops exercised
- Verified ordinal basis consistency: both `readRawSessionMessagesFromDb` (index+1 after summary filter) and `readRawSessionMessageByIdFromDb` (COUNT with same summary filter and sort order) use the same basis — no basis mismatch
- Verified shadow isolation: `shadow_binding` rejects non-shadow bindings (lib.rs:807), plain transform rejects shadow bindings (lib.rs:1944), ctx_reduce rejects shadow bindings (lib.rs:1711) — except guidance (Finding 6)
- Verified shadow capture safety: `cloneForShadow(messages)` is taken BEFORE transform (transform.ts:459), `denormalizeShadowOutput` deep-clones output before mutating (shadow-sender.ts:288) — live array not mutated
- Verified no double-clone: `enqueue` pre-computes `shadowTsOutput`/`shadowNormalizations` (lines 1054-1066), `buildShadowTransformBody` reuses them (lines 807-812) — no second clone

**Overall risk assessment**: The compartment wire-shape mismatch (Finding 1) is the same class of bug that the first armed run exposed — a wire-shape mismatch that mocks miss because the `FakeTransport` accepts any shape. It will cause EVERY state_sync with compartments to reject, permanently wedging the shadow lane in a reset loop. This completely defeats the soak's purpose for any session that has compartments (which is the primary use case). The `watermarks` mismatch (Finding 2) and `m0_mutations` drop (Finding 3) are silent data losses that don't crash but corrupt the shadow state's metadata.

**Verdict: HOLD** — The single most important reason is Finding 1: the compartment wire-shape mismatch will cause every shadow state_sync with compartments to fail with `invalid_params: missing field start_message`, wedging the shadow lane in a reset loop. This is the exact "wire-shape mismatch the mocked tests missed" class that the audit brief warns about, and it's untested because no integration test sends TS-built compartments through the real Rust serde parser. The soak cannot observe anything useful until this is fixed and verified with an end-to-end test.
</COUNCIL_MEMBER_RESPONSE>

