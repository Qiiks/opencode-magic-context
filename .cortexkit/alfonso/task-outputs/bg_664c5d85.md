---
task_id: bg_664c5d85
agent: council-member
session_id: ses_0b4657e09ffecigi10TSTYW3lY
parent_session_id: ses_0b4664e56ffeKx8uRSceXLl3v9
status: completed
completed_at: 2026-07-10T10:39:37Z
---

[user] 2026-07-10T10:37:25Z
## Solo Analysis Mode
You MUST do ALL exploration yourself using your available read/search tools.
- Do NOT use task or any delegation tool under any circumstances
- Do NOT delegate to explore, librarian, or any other subagent
- Do NOT spawn background tasks
- Search the codebase directly — you have full read-only access to every file
- This mode produces the most thorough analysis because you see every result firsthand


## Analysis Intent: AUDIT

You are conducting an **audit** — your goal is to find discrete issues, risks, or violations.

**Focus:**
- Search for problems, anti-patterns, security risks, correctness issues, or violations of stated requirements
- Each finding must be a distinct, actionable item with concrete evidence
- Severity determines priority: critical (blocks/breaks), high (significant risk), medium (should fix), low (nice to fix)
- For each finding, provide the specific location (reference, section, or component where it occurs)
- State your confidence: high (clear evidence), medium (likely but needs verification), low (suspicion, investigate further)
- **This is a broad sweep, not a targeted trace.**

**Analytical standards:** Support claims with concrete evidence. State confidence (high/medium/low) for key assertions. Note caveats and limitations.

**Structure your response as:**
```
<COUNCIL_MEMBER_RESPONSE>
## Finding 1: [Title]
- **Severity**: critical/high/medium/low
- **Location**: [specific reference — e.g. component, section, endpoint, rule]
- **Confidence**: high/medium/low
- **Issue**: [what is wrong and why it matters]
- **Evidence**: [concrete reference, snippet, or observation that proves the issue]
- **Suggested Fix**: [actionable recommendation]

## Finding 2: [Title]
...

## Summary
[Total findings by severity. Overall risk assessment with confidence levels.]
</COUNCIL_MEMBER_RESPONSE>
```

## Analysis Question

BLIND ADVERSARIAL COUNCIL AUDIT — Magic Context SHADOW-MODE lane, pre-production soak gate.

Repo: /Users/ufukaltinok/Work/Projects/CortexKit/magic-context
Branch: subc-migration, HEAD 6ea3179f

You are one of several independent auditors. Work ALONE, hunt for the NEXT bug class. Do NOT assume the code is correct because it has tests — the first armed run already exposed two integration bugs the mocked tests missed (wire-shape mismatch, ordinal starvation), both already fixed at HEAD. Your job is to find what the mocks still miss. Read the ACTUAL code with file:line evidence; do not speculate abstractly.

=== WHAT SHADOW MODE IS ===
A dev-flag lane (config shadow_transform.enabled, user-tier only) where the OpenCode TS plugin mirrors every finalized transform pass to the Rust mc-module over the subc daemon socket for byte-comparison — WITHOUT affecting the live session. The TS side fire-and-forgets: per-session FIFO queue, state_sync (compartments/memories/mutations sync), shadow_transform (input + TS output + decision), shadow_reset (generation bump + wipe). The Rust side runs its own transform on the mirrored input against a shadow:<sid> store lineage, byte-compares against the TS output, and records divergences in shadow_divergences. The soak is OBSERVE-ONLY by design.

=== AUDIT SCOPE (areas that changed or are first-run-live today) ===

1. packages/plugin/src/plugin/hooks/create-session-hooks.ts — the config mapper was changed from a hand-maintained field list to a full spread (`...pluginConfig`) with two defaulted overrides (commit e932804c). HUNT: does spreading the ENTIRE plugin config into the hook config leak anything that shouldn't reach per-session hook code, shadow anything the hook type declares differently, or change behavior for any existing consumer that relied on a field being absent (e.g. `undefined` vs present-with-value)?

2. packages/plugin/src/hooks/magic-context/shadow-sender.ts — the full sender. HUNT ESPECIALLY:
   (a) HOT-PATH SAFETY: enqueue() runs inside the transform hot path. Verify EVERY code path in enqueue (including resolveOrdinalsForShadow with its new by-id DB fallback, denormalizeShadowOutput, cloneJson of potentially huge message arrays) is exception-safe and bounded — a throw or a multi-second stall here delays the user's real prompt. Is cloneJson(messages) on a 400-message array acceptable per-pass cost? Is the by-id DB read (readRawSessionMessageById) opening a DB handle per call, and what happens under SQLITE_BUSY?
   (b) WIRE SHAPE (commit 6ea3179f): toFlatWireBody flattens {method, params} to flat. Cross-check EVERY field the TS builders emit against the Rust serde parsers in crates/mc-module/src/lib.rs (ShadowStateSyncWire, ShadowTransformWire, ShadowResetWire, ShadowPassInputs, ShadowUsageWire, ShadowCompartmentWire, ShadowMemoryWire, ShadowMemoryMutationWire): field names, types, required-vs-optional, snake_case exactness. Name any field that will reject or silently default.
   (c) ORDINAL FALLBACK: the new below-floor by-id fallback in resolveOrdinalsForShadow. Can the fallback return an ordinal computed on a DIFFERENT basis than the cache (e.g. cache primed tail-only carries absolute ordinals from baseOrdinal while the by-id COUNT includes/excludes summary rows differently)? A silent basis mismatch would poison the shadow store with wrong ordinals rather than failing loud.
   (d) SubcShadowTransport: hand-rolled socket protocol (auth handshake, frame header, backoff). Route kind now tool_provider (commit 1555b231). HUNT: connection lifecycle bugs (socket close mid-request, reader waiters, backoff doubling), auth proof correctness, frame length handling, the 5s request timeout leaving the FIFO wedged.
   (e) QUEUE SEMANTICS: MAX_QUEUE_PER_SESSION=4 drop-oldest, blockedUntilReset, requireResetReason transitions. Any path where a session gets permanently wedged (blocked but no reset queued) or where a reset races a queued pass?

3. crates/mc-module/src/lib.rs shadow handlers (handle_shadow_state_sync_value, handle_shadow_transform_value, handle_shadow_reset_value, shadow_binding) + crates/mc-store shadow methods (apply_shadow_state_sync, reset_shadow_session). HUNT:
   (a) ISOLATION: can ANY shadow-lane write touch a non-shadow session row? The shadow session id is derived from binding (shadow: prefix). Verify shadow_binding rejects non-shadow bindings on shadow ops AND that plain transform ops reject shadow bindings (codes non_shadow_op_on_shadow_binding, plain_transform_on_shadow_binding — verify they're actually enforced on EVERY arm).
   (b) The shadow_transform handler runs the REAL transform + a byte-compare. Does a shadow divergence or panic in the compare path affect the real lane in any way (shared store handle, lease contention, pass-trace pollution)?
   (c) apply_shadow_state_sync CAS semantics: generation/seq mismatch arms, and what happens when state_sync carries compartments whose sequences overlap already-synced ones (idempotent or duplicating?).

4. CACHE SAFETY OF THE LIVE LANE: the sender captures cloneForShadow(messages) BEFORE the transform mutates and reads declared-trim state before/after. Verify the shadow capture path cannot mutate the live messages array (shared references through cloneJson boundaries — is the clone deep and taken at the right point?), and that shadow-sender DB reads (getCompartments, getMemoriesByProject, etc.) hold no transactions that could contend with the transform's own writes.

=== DELIVERABLE (per member) ===
- Independent findings with file:line evidence. Read the real code.
- Rank each finding Critical / High / Medium / Low.
- Weight by blast radius: (1) can affect the LIVE user session = WORST class; (2) corrupts shadow state silently = defeats the soak's purpose; (3) merely loses shadow coverage but logged = ACCEPTABLE.
- Explicit false-positive filtering: check PARITY.md files and existing test coverage before claiming a bug. State what you checked.
- Your own verdict: SHIP (arm the soak on production sessions) or HOLD, with the single most important reason.

Relevant reference files exist at packages/pi-plugin/PARITY.md and packages/plugin/src/features/magic-context/smart-notes/PARITY.md. Test files live alongside sources (*.test.ts) and in crates/*/tests or #[cfg(test)] modules.

[assistant] 2026-07-10T10:37:25Z
The user wants me to conduct a blind adversarial council audit on the Magic Context SHADOW-MODE lane. I need to work alone, explore the codebase directly, and find issues with concrete evidence. Let me systematically audit the areas mentioned:

1. create-session-hooks.ts - config mapper spread
2. shadow-sender.ts - full sender, hot path, wire shape, ordinals, transport, queue
3. mc-module shadow handlers and mc-store
4. Cache safety of live lane

I'll search and read the actual code files to gather evidence.
Conducting an independent audit: reading the cited code paths and cross-checking wire shapes, isolation, and hot-path behavior.

[assistant] 2026-07-10T10:37:41Z
I need to continue reading shadow-sender.ts (truncated part), mc-module lib.rs shadow handlers, readRawSessionMessageById, hook config types, enqueue call site in magic context hook, mc-store shadow methods.

[assistant] 2026-07-10T10:37:43Z
I need to read the Rust wire structs in full, shadow handlers, mc-store methods, transform.ts enqueue path, readRawSessionMessageById, hook config type, and tests for create-session-hooks and shadow-sender.

[assistant] 2026-07-10T10:37:45Z
I need more details:
1. Rust ShadowCompartmentWire - TS sends nested start/end with flat_id - need to see if Rust expects flat structure
2. handle_shadow_state_sync and transform handlers
3. non_shadow_op and plain_transform guards on every arm
4. readRawSessionMessageById vs cache ordinal semantics
5. MagicContextHookConfig type vs spread
6. blockedUntilReset without reset queued path
7. enqueue is NOT wrapped in try on transform - only shadow block has try at 2187

Let me read more Rust wire types and handlers, mc-store shadow sync, read-session-chunk ordinal logic.

[assistant] 2026-07-10T10:37:48Z
I need to dig deeper into:
1. Wire shape mismatch - TS sends nested compartment start/end objects but Rust expects start_message/end_message i64 and optional IDs
2. m0_mutations in state_sync - does Rust parse them?
3. ShadowCompartmentWire conversion From trait
4. buildMagicContextHookConfig tests and MagicContextHookConfig type
5. transform enqueue try/catch - if enqueue throws?
6. resolveDeclaredTrimForShadow in hot path via enqueue

[assistant] 2026-07-10T10:37:51Z
The first grep for StoredCompartment in mc-module - I need the From impl at line 438 in lib.rs

Also check:
- watermarks on wire - Rust ShadowStateSyncWire doesn't seem to have watermarks field!
- m0_mutations not in Rust struct

Let me read the From impl and check if compartment wire uses nested start/end from TS

[assistant] 2026-07-10T10:37:56Z
This is critical - TS sends compartment with nested `start`/`end` with `absolute_ordinal` but Rust expects `start_message` and `end_message` as i64 at top level of compartment. Let me read the test and verify what TS actually sends vs Rust expects.

Also m0_mutations - TS sends in state_sync but Rust ShadowStateSyncWire doesn't have m0_mutations field!

Let me read shadow-sender.test.ts wire test and search for m0 in rust wire struct.

[assistant] 2026-07-10T10:37:59Z
Critical wire mismatch found:

TS `serializeCompartment` sends:
```javascript
{
  id: args.compartment.id,
  sequence: ...,
  start: { flat_id, bare_message_id, absolute_ordinal },
  end: { flat_id, bare_message_id, absolute_ordinal },
  ...
}
```

Rust `ShadowCompartmentWire` expects:
- start_message: i64 (required)
- end_message: i64 (required)
- start_message_id, end_message_id with serde(default) -> empty string if missing

The nested `start`/`end` objects won't map to start_message/end_message - serde will use default 0 for missing fields!

So compartments sync with start_message=0, end_message=0 - SILENT corruption of shadow store!

Let me verify there's no custom deserializer and check shadow transform input handling for absolute_ordinal.

Also m0_mutations - TS sends them in params but Rust ShadowStateSyncWire doesn't have m0_mutations field - serde will ignore extra fields by default... actually serde ignores unknown fields by default for struct deserialize. So m0 mutations never mirror to Rust - silent loss of m0 sync!

watermarks - TS sends in params, Rust doesn't have watermarks field - ignored. Only used client-side for skip logic.

Let me read handle_shadow_transform for ingress and ck wire

[assistant] 2026-07-10T10:38:02Z
Critical finding: TS sends compartments with nested `start`/`end` objects containing `absolute_ordinal`, `flat_id`, etc. Rust `ShadowCompartmentWire` expects `start_message: i64`, `end_message: i64` at top level - NOT nested `start`/`end`.

When serde deserializes, nested `start`/`end` would be ignored (unknown fields?) - actually serde by default denies unknown fields only if configured - default is to ignore unknown fields. So `start_message` and `end_message` would default to 0 (required i64 without default might fail...)

ShadowCompartmentWire:
```
sequence: i64,  // required
start_message: i64,  // required
end_message: i64,  // required
```

If JSON has `start: { absolute_ordinal: 5 }` but no `start_message`, deserialization FAILS for missing field start_message.

This is a HIGH severity wire mismatch - state_sync compartments would fail invalid_params OR deserialize with wrong data if serde has defaults... start_message has no default, so invalid_params on entire state_sync.

Wait - would the whole ShadowStateSyncWire fail? compartments is Vec<ShadowCompartmentWire> - each item fails if missing start_message.

This is huge - unless there's a custom deserializer or the test doesn't catch it because buildStateSyncPayload returns empty compartments in test.

Let me check shadow test with actual compartments and integration tests in Rust.

[assistant] 2026-07-10T10:38:06Z
The compartment wire mismatch is critical - TS sends nested `start`/`end` objects but Rust expects flat `start_message`, `end_message`. Serde would ignore unknown nested objects? Actually serde by default ignores unknown fields - but it would leave start_message/end_message as 0 (default for i64)!

Let me verify serde behavior - for ShadowCompartmentWire:
- sequence: required i64 - OK from TS
- start_message: default 0 if missing
- end_message: default 0
- TS sends: id, start: { flat_id, bare_message_id, absolute_ordinal }, end: {...}

So compartments would sync with sequence and titles but start_message=0, end_message=0 - **silent corruption** of shadow store.

Also m0_mutations - not in Rust struct at all - dropped.

watermarks - not in Rust struct - dropped. TS sends watermarks in params but Rust uses acked_watermarks optional and builds default from compartments max.

Let me read StoredCompartment from and upsert - does it use 0 ordinals?

Also check shadow_reset reason field - Rust ShadowResetWire doesn't have reason.

Check marker_state in ts_decision - probably fine as Value.

For enqueue hot path - resolveDeclaredTrimForShadow at transform START (line 460) - that's BEFORE transform, on hot path start. It does getCompartmentsByEndMessageId, readRawSessionMessages - significant!

shadow enqueue at END calls resolveDeclaredTrimForShadow AGAIN (line 2188).

readRawSessionMessages in resolveOrdinals - full read or cache tail?

primeInMemoryTailRawMessageCache - when primed, readRawSessionMessages returns tail only. ordinalById map missing below-floor ids → by-id fallback.

Basis mismatch: tail cache uses baseOrdinal from in-memory build (read-session-raw buildInMemoryTailRawMessages) vs DB COUNT query. Need to compare filter rules.

readRawSessionMessagesFromDb - full list with ordinals
buildInMemoryTailRawMessages - filters summary rows similarly?

I'll read readRawSessionMessages and compare ordinal computation.

[assistant] 2026-07-10T10:38:13Z
Critical finding: TS sends compartments as:
```
{
  id, sequence,
  start: { flat_id, bare_message_id, absolute_ordinal },
  end: { ... },
  title, content, ...
}
```

Rust ShadowCompartmentWire expects:
```
sequence, start_message, end_message, start_message_id, end_message_id, ...
```

Serde with default will deserialize nested `start` object - if it's not a field, it's IGNORED. So start_message and end_message will be 0 (default for i64), start_message_id and end_message_id will be empty string.

This is a CRITICAL silent corruption bug for state_sync compartments!

Let me verify - does serde deny_unknown_fields? Default serde ignores unknown fields and missing fields get defaults.

So compartments sync would write sequence N with start_message=0, end_message=0 - breaking shadow transform parity.

Also m0_mutations - TS sends in state_sync params, Rust ShadowStateSyncWire doesn't have m0_mutations field - dropped silently.

watermarks - TS sends, Rust doesn't have watermarks in ShadowStateSyncWire - ignored (TS uses for its own logic, Rust doesn't need it on wire maybe)

Let me read shadow_reset_and_state_sync test to see what compartment shape they use

[assistant] 2026-07-10T10:38:17Z
Critical finding: TS sends compartments with nested structure:
```javascript
return {
    id: args.compartment.id,
    sequence: args.compartment.sequence,
    start: { flat_id, bare_message_id, absolute_ordinal },
    end: { ... },
    ...
};
```

Rust ShadowCompartmentWire expects:
- start_message: i64 (required, no default)
- end_message: i64
- start_message_id, end_message_id as strings

Serde will NOT deserialize `start: { absolute_ordinal }` into `start_message` - it will default start_message/end_message to 0 (wait, i64 without default is required - actually in serde, missing fields for i64 without #[serde(default)] cause deserialization error for the struct, but for each compartment in Vec, failed items...)

Actually for Vec<ShadowCompartmentWire>, if a compartment fails to deserialize, the whole from_value fails. So compartments with only `start` nested object would fail deserialization entirely when non-empty.

When compartments array is empty (common case at start), state_sync works. First compartment sync would fail invalid_params.

Let me verify - does serde deny unknown fields? Default is to ignore unknown fields. So `start` and `end` are ignored, and `start_message` and `end_message` are MISSING - for Deserialize on struct without default on i64, serde returns error "missing field `start_message`".

So state_sync with any compartment would fail at Rust parse time!

Unless... there's a custom deserializer. Let me read full ShadowCompartmentWire again - sequence is i64 without default too.

Also m0_mutations: TS sends in params but Rust ShadowStateSyncWire doesn't have m0_mutations field - dropped silently. Shadow mirror won't have m0 mutation log synced - might cause divergent transform but not necessarily invalid_params.

watermarks: TS sends in params but Rust doesn't have watermarks field - only acked_watermarks optional. TS watermarks are dropped on wire. OK for parsing.

Let me also check blockedUntilReset path when reset fails - if performReset throws, catch in runQueue sets requireResetReason but blockedUntilReset is only cleared in performReset success. If reset fails repeatedly, passes dropped?

When ordinal mismatch in enqueue, blockedUntilReset=true and reset queued. processPass: if blockedUntilReset return early at line 960 - but reset should run first from queue. Good.

When peer reject: blockedUntilReset=true, requireResetReason set. Next enqueue might push pass but processPass sees blockedUntilReset and returns without processing - AND no reset in queue unless user calls resetSession. **WEDGE**: After peer reject in processPass catch, the failing pass is already consumed from queue. blockedUntilReset=true but requireResetReason is set. Next pass item: processPass checks initialized/requireResetReason and calls performReset at start - but then blockedUntilReset at 960 returns before doing work!

Look at processPass:
```
if (!state.initialized || state.requireResetReason) {
    await performReset(...);
}
if (state.blockedUntilReset) return;
```

So it performs reset first, which clears blockedUntilReset on success. Good.

If performReset fails (connection), catch doesn't set blockedUntilReset from peer reject path for reset item... reset is in performReset directly awaited from runQueue - if reset throws, catch in runQueue handles it - for peer reject sets blockedUntilReset true again?

Actually for reset item kind, it's awaited in runQueue without try/catch wrapping performReset separately - it's in the reset branch:
```
if (item.kind === "reset") {
    await performReset(...);
    continue;
}
```
performReset is NOT in try/catch for reset-only items! If performReset throws on reset item, the exception propagates from runQueue's while loop - only processPass is in try/catch.

**Finding**: reset work items that throw will crash runQueue's finally and state.running=false, but blocked state may be inconsistent.

Let me read runQueue again...

```javascript
if (item.kind === "reset") {
    await performReset({...});
    continue;
}
try {
    await processPass(state, item.pass);
} catch (error) {
```

So reset failures are NOT caught - they'll reject runQueue promise, schedule's finally still runs. Queue may have remaining items. requireResetReason might still be set from earlier peer reject. **Uncaught reset failure** could leave queue stuck.

Hot path: enqueue runs synchronously on transform completion - includes resolveOrdinalsForShadow (may call readRawSessionMessageById per missing id - opens RO DB each time via withReadOnlySessionDb), denormalizeShadowOutput (cloneJson output), pushWork. transform already finished so user got response - but wait, enqueue is AFTER transform completes at end of transform function. So it's not delaying the transform itself but still on the same synchronous stack before transform returns to caller. So it CAN delay returning from transform hook to OpenCode.

That's significant - cloneJson(messages) at start is early in transform; enqueue at end does more cloneJson on output + ordinal resolution + DB reads.

resolveDeclaredTrimForShadow in enqueue path - called from transform before and during shadow - at enqueue time declaredTrim is already computed.

For primeTail cache vs by-id: withRawSessionMessageCache during enqueue - if transform left cache primed with tail only, ordinalById from readRawSessionMessages only has tail messages. Below-floor ids use readRawSessionMessageByIdFromDb with COUNT query - same summary filter as full read. Tail cache assigns ordinals baseOrdinal+i - should match absolute ordinals for messages in tail. For below-floor, by-id COUNT should match full read semantics (verified in comments). **Risk**: primeInMemoryTailRawMessages uses different baseOrdinal logic - if that's what's primed during transform, ordinals in cache for tail should still match DB COUNT for each id.

Pi provider path: readRawSessionMessageById uses provider - different semantics possible.

SubcShadowTransport: pending chain serializes all calls globally across sessions on one transport instance - good. Request timeout 5s - if timeout, throw, processPass catch may set connection skip. Reader loop in readTerminalFor discards non-matching frames - could accumulate if server sends garbage?

Connection close mid-request: readExact throws connection closed.

MagicContextPluginConfig spread: project-security strips shadow_transform from project config - user tier only. Extra keys in hook config - need hook.ts config type.

I'll check hook config consumption for dangerous fields.

Also verify compartment wire - read serializeCompartment output again - definitely nested start/end.

Rust test uses flat start_message/end_message - confirms expected wire shape differs from TS production serializer.

# Finding: state_sync compartment wire shape mismatch - CRITICAL for soak when compartments exist

# Finding: m0_mutations not applied on Rust - HIGH for divergence

# Finding: enqueue synchronous post-transform cost - MEDIUM for live latency

# Finding: reset work item uncaught errors - MEDIUM wedge

# Finding: ts_decision marker_state uses camelCase in TS interface but JSON.stringify typically keeps object keys as defined - marker_message_id vs marker_message_id in interface ShadowTransformDecision has marker_state.marker_message_id - OK snake_case in nested? It has marker_message_id with underscore. advanced_this_pass - good.

ShadowTransformDecision in TS uses marker_message_id - good.

Check serde for ts_decision - Value type, any shape OK.

provider_error optional in TS, optional in Rust.

declared_trim DeclaredTrim type on Rust side.

I'll grep DeclaredTrim and flat_boundary

[assistant] 2026-07-10T10:38:31Z
The test "keeps sender exceptions off the transform hot path" uses a throwing transport on async path - but enqueue itself shouldn't throw.

Key finding: **state_sync compartment wire shape mismatch** - TS sends nested `start`/`end` objects, Rust expects flat `start_message`, `end_message`, `start_message_id`, `end_message_id`. Serde will ignore unknown fields `start`, `end`, `id` and default ordinals to 0.

Test at 7071 uses flat format - proving Rust expects flat. TS serializeCompartment sends wrong shape. This is CRITICAL for shadow soak - compartments sync with wrong geometry.

**m0_mutations** - not in ShadowStateSyncWire - dropped silently on Rust side. TS still sends them. Shadow lane might miss m0 mutation mirror - medium for soak completeness.

**watermarks** in TS params but Rust uses acked_watermarks optional - TS sends `watermarks` which Rust ignores (extra field). Dedup logic is client-side only via lastAckedWatermarks - OK.

**shadow reset reason** - TS sends but Rust ShadowResetWire doesn't have reason field - ignored, OK.

Let me verify serde behavior for unknown fields - default deny_unknown_fields? ShadowCompartmentWire doesn't have deny_unknown_fields so `start` and `end` are ignored.

**enqueue hot path** - resolveDeclaredTrimForShadow at transform START (line 460) - that's on hot path BEFORE transform! And resolveDeclaredTrim does DB + readRawSessionMessages. That's significant.

Also at end enqueue does resolveOrdinals, denormalize, buildStateSync data only async - but enqueue synchronous work:
- resolveOrdinalsForShadow: readRawSessionMessages (cache or full), cloneJson messages, possibly N readRawSessionMessageById each opening withReadOnlySessionDb
- denormalizeShadowOutput: cloneJson output + getAutoSearchHintDecisions DB read

transform.ts 460: shadowDeclaredTrimBefore = resolveDeclaredTrimForShadow - ON HOT PATH during transform, not just enqueue end.

**blockedUntilReset** - ordinal mismatch queues reset with pushWork - performReset clears blocked. Connection failure sets requireResetReason but NOT blockedUntilReset - passes can proceed and fail again - acceptable.

**resetSession** clears queue then push reset - good.

**Race**: reset and pass in queue - reset shifts first when at front - if pass blocked mid-queue after peer reject, reset at end of queue from enqueue mismatch - good.

**Permanent wedge**: blockedUntilReset true + ordinal mismatch pushes reset - reset clears blocked. If reset fails repeatedly, blocked cleared on successful reset only - if reset never succeeds, next pass tries performReset at start of processPass - if blockedUntilReset, return early without reset attempt! 

processPass:
```
if (!state.initialized || state.requireResetReason) {
    await performReset(...);
}
if (state.blockedUntilReset) return;
```

So order: if blocked from ordinal mismatch, requireResetReason is set AND blockedUntilReset true. enqueue pushes reset work. When processPass runs for a pass item - first it checks requireResetReason and calls performReset - but blockedUntilReset is still true until performReset completes! After performReset, blockedUntilReset = false.

But if the queue item is a pass (not reset), and blockedUntilReset is true from PREVIOUS failure before reset work runs...

enqueue mismatch: clears queue, blockedUntilReset=true, pushes ONLY reset.

runQueue processes reset first - performReset clears blocked.

If peer reject on transform: blockedUntilReset=true, requireResetReason set, but NO reset auto-queued! Only counters. Next pass in queue will hit processPass: requireResetReason triggers performReset at start - good. blockedUntilReset - if still true from previous transform fail, after performReset in same processPass... actually on peer reject catch doesn't set blockedUntilReset - only generation reject sets blockedUntilReset true!

Look at catch in runQueue:
```
if (isPeerReject(error)) {
    ...
    state.blockedUntilReset = true;
```

Yes blocked on peer reject. requireResetReason set. Next queued pass: processPass calls performReset first because requireResetReason - then blocked cleared on success. If reset fails, blocked stays? performReset throws - catch doesn't clear queue item already consumed. Stuck?

If performReset throws, catch in runQueue increments send_failures, may set connection skip - blockedUntilReset already true from previous - requireResetReason route_reopen or peer - pass item lost (shifted already). Subsequent items may all hit blockedUntilReset at line 960 return without resetting if requireResetReason cleared incorrectly?

performReset on success clears blocked and requireResetReason. On throw, state unchanged - blocked still true, requireResetReason still set. Next queue item: processPass tries performReset again at line 952 - good.

Unless queue empty and no more passes - session wedged until new enqueue triggers reset via requireResetReason in processPass - new enqueue will processPass which resets first. OK.

**SubcShadowTransport** global pending chain - if request times out at readTerminalFor, unaryJson throws, socket may still have unread frame - next read may get wrong corr - protocol desync. Critical for shadow not live.

**create-session-hooks spread** - project-security strips shadow_transform from project - user tier only. Extra keys like enabled at top level already in hook type. Sidekick/dreamer in config passed to hook - hook may only read declared fields - inert. Risk: if hook destructures entire config into something that serializes secrets? Unlikely. Medium if unknown future key changes behavior.

Check hook uses config fields only.

**PARITY.md** - smart-notes and pi-plugin - not shadow specific; checked for false positives.

**Tests missed compartment wire** - shadow-sender.test checks compartments is array but not shape vs Rust.

**absolute_ordinal in compartments** - Rust uses start_message i64 not from nested start.absolute_ordinal.

I'll also note **double denormalize** in enqueue - denormalize twice if processPass buildShadowTransformBody runs again - enqueue caches shadowTsOutput on pass so buildShadowTransformBody skips second denormalize - good.

**Hot path resolveDeclaredTrimForShadow** at transform start - reads compartments, raw messages - DB heavy on every transform when shadow enabled.

**cloneForShadow** at transform start - full JSON clone of messages at start - separate from enqueue clones.

Verdict HOLD due to compartment wire mismatch silently poisoning shadow store - defeats soak purpose.

Also check shadow_input_messages uses absolute_ordinal from input - yes lib.rs 3509.

Secondary: m0 not synced to Rust.

Check upsert_compartment with start_message 0 - corrupts shadow historian?

I'll verify StoredCompartment from wire with zeros.

[assistant] 2026-07-10T10:38:43Z
Critical finding: resolveDeclaredTrimForShadow runs at transform START (line 461) on the hot path when shadow is enabled - not just at enqueue end. Let me read that section again - shadowDeclaredTrimBefore uses resolveDeclaredTrimForShadow which does readRawSessionMessages, getCompartmentsByEndMessageId, etc.

That's synchronous work on EVERY transform pass when shadow enabled, before the main transform work. That could be significant!

Lines 457-462:
```
const shadowSender = deps.shadowSender;
const shadowNowMs = shadowSender ? Date.now() : 0;
const shadowInputMessages = shadowSender ? cloneForShadow(messages) : undefined;
const shadowDeclaredTrimBefore = shadowSender
    ? resolveDeclaredTrimForShadow({ db, sessionId })
    : null;
```

So every transform with shadow:
1. cloneForShadow(messages) - full JSON clone at START
2. resolveDeclaredTrimForShadow - DB + cache reads at START

Then at end:
- enqueue does another clone + denormalize + ordinals

That's DOUBLE clone of messages (start and inside enqueue for ordinals/denormalize). High impact on live path!

Finding: shadow enablement adds synchronous cloneForShadow + resolveDeclaredTrimForShadow on every transform pass entry, not just fire-and-forget at end.

Confidence: high from transform.ts:457-462

For compartment wire - I should verify serde ignores unknown fields. In serde, extra fields in JSON are ignored by default for structs. So TS nested start/end might be dropped and start_message defaults to 0.

Read serializeCompartment output again - only nested structure, no start_message field.

This is CRITICAL for soak - compartments would sync with wrong ordinals (0).

Let me verify StoredCompartment upsert uses start_message

[assistant] 2026-07-10T10:38:48Z
Confirmed - serializeCompartment uses start/end with flat_id, bare_message_id, absolute_ordinal - NOT start_message/end_message integers at top level.

Rust ShadowCompartmentWire requires:
- sequence (no default on required fields - serde will error if missing? Actually sequence: i64 without default means required)
- start_message: i64 - REQUIRED, no default
- end_message: i64 - REQUIRED

TS sends nested structure without start_message/end_message at top level - **serde will fail deserialization** for each compartment!

Unless... serde flatten? Let me look at ShadowCompartmentWire again - no flatten on start/end objects.

This is a **Critical** soak-killer: state_sync with any compartment will fail invalid_params OR deserialize with default 0 for start_message if there's unknown field handling - actually unknown fields are ignored by default in serde, but required start_message and end_message would be **missing** → deserialization error for the whole ShadowStateSyncWire? 

For Vec<ShadowCompartmentWire>, if one compartment fails, whole parse fails.

So first state_sync with compartments → invalid_params.

Tests use buildStateSyncPayload but don't assert Rust round-trip - they only check TS structure. Rust test uses flat start_message/end_message format.

**Finding 1: Critical wire shape mismatch for compartments**

Also m0_mutations and watermarks dropped on wire.

**pass_inputs.usage**: TS always sends usage object. Rust Option - OK.

**provider_error**: optional OK.

**ts_decision**: TS sends structured object; Rust Value - OK.

**materialize_reason, emergency, marker_state**: in TS ShadowTransformDecision - Rust ts_decision is Value - OK.

Let me verify serde behavior for missing fields on struct - yes deserialize error.

**Hot path**: resolveDeclaredTrimForShadow at transform START (before transform body) - does DB reads and readRawSessionMessages - that's on live path when shadow enabled!

transform.ts:459-461 shadowDeclaredTrimBefore = resolveDeclaredTrimForShadow - ON HOT PATH at start
2188 shadowDeclaredTrimAfter - after transform

resolveDeclaredTrimForShadow calls getPersistedCompactionMarkerState, readRawSessionMessages (can be full session if cache not primed), getCompartmentsByEndMessageId

That's significant hot path cost for shadow enabled users!

enqueue at end also heavy.

**Finding: shadow trim resolution on transform hot path start**

Confidence high for compartment wire - read serializeCompartment lines 584-615 again

Double-check Rust doesn't have alternate deserializer for compartments...

Maybe there's serde alias on ShadowCompartmentWire for "start" object?

Read full ShadowCompartmentWire struct - lines 267-295 only flat fields.

**m0 mutations**: handle_shadow_state_sync doesn't pass m0 - shadow store missing m0 mirror - could cause false divergences in transform if Rust uses m0 from store.

Search m0 in shadow transform path...

**Hot path exception**: enqueue wrapped in try in transform but enqueue itself not try-catched internally for resolveOrdinals - throws would propagate to transform catch - good.

denormalizeShadowOutput, resolveOrdinalsForShadow - can throw JSON.parse errors on circular refs? Unlikely on messages.

readRawSessionMessageById - withReadOnlySessionDb per missing id - N queries.

**Queue wedge**: resetSession sets blockedUntilReset true and clears queue then pushWork reset. performReset sets blockedUntilReset false. processPass after peer reject sets blockedUntilReset true but doesn't auto-queue reset unless requireResetReason - next enqueue with mismatch queues reset. If only connection failure: requireResetReason = "route_reopen", blockedUntilReset NOT set in catch - only initialized = false. So passes can continue!

connection failure path lines 898-902: no blockedUntilReset = true

Peer reject: blockedUntilReset = true

processPass line 952: if requireResetReason performs reset at start - good

If blockedUntilReset after peer reject, processPass returns early at 960 without processing - but reset should have been queued on mismatch at enqueue. For peer reject during processPass, blockedUntilReset set but is reset queued? catch doesn't push reset - **wedge**?

After send failure with peer reject: blockedUntilReset=true, requireResetReason set, but no reset work item queued. Queue might be empty. Next enqueue would... need user to trigger another transform. Session wedged until next pass?

Actually next transform enqueue: processPass not called until work in queue. If queue empty and blockedUntilReset true, no automatic recovery until resetSession or new pass?

When new pass enqueued: pushWork adds pass, runQueue processes - processPass checks blockedUntilReset at 960 AFTER performReset if requireResetReason. Line 952-958: if requireResetReason, performReset first. So requireResetReason triggers reset at beginning of next pass. blockedUntilReset - if true and requireResetReason was cleared by... performReset clears both. So order: if !initialized || requireResetReason -> reset. Then if blockedUntilReset return - performReset sets blockedUntilReset false. OK.

If peer reject during processPass: requireResetReason set, blockedUntilReset true, queue empty. No new pass until user transforms again. Next transform enqueues pass -> performReset because requireResetReason -> unblocks. OK not permanent wedge.

**Subc transport**: pending chain serializes all calls globally across sessions on one transport instance - one slow 5s timeout blocks all shadow sessions in the process.

**socket close mid-read**: readTerminalFor loops forever discarding unrelated frames until timeout - could be OK.

**Global transport singleton** in createShadowSender - one SubcShadowTransport for all sessions - pending queue is global.

I'll note read_frame doesn't validate max body length - DoS? shadow only dev.

**Isolation**: shadow uses binding.session shadow:xxx, store writes to shadow session id - upsert_compartment_tx uses request.session_id from binding - good. reset deletes mc_compartments for shadow session only.

Plain transform rejects shadow binding - tested.

ctx_reduce rejects shadow - only one non_shadow op checked? Other ops might still run on shadow?

grep is_shadow_session in lib.rs for all handlers

**apply_shadow_state_sync** upsert ON CONFLICT updates - resync same sequence idempotent at row level. Duplicate seq rejected by CAS - test at 7105.

**False positive check**: PARITY.md not about shadow wire. shadow-sender.test.ts documents flat wire but doesn't test Rust parse of compartment shape. Rust test uses correct flat compartment shape.

**create-session-hooks spread**: test passes through fields; extra plugin keys like auto_update might leak to hook - hook might pass config to something that spreads again?

Search config usage in hook for unexpected fields...

Likely low risk if hook only reads known fields.

**config.shadow_transform user tier**: project stripped - good.

Verdict HOLD due to compartment wire mismatch - soak will fail state_sync whenever compartments sync.

Also trim resolution at transform start - live latency concern for shadow enabled.

Let me verify compartment serde - could start_message be extracted from nested via custom deserializer?

Read ShadowCompartmentWire full - no custom impl.

**Optional finding**: watermarks and m0 not in Rust - TS sends watermarks in params but flatten puts watermarks in body - Rust ShadowStateSyncWire has no watermarks field - **ignored silently**. Only used for TS-side skip logic. OK.

m0_mutations in TS body - Rust ignores - **shadow m0 state diverges**.

Search m0 in mc-module transform for shadow...

Actually shadow runs transform_with_projection on shadow store - m0 mutations might matter for parity.

I'll rank m0 sync gap as High for false divergences.

**pass shadow_generation on reset**: ShadowResetWire has optional shadow_generation - TS sends it. OK.

Double-check: does flatten include `id` on compartment? serializeCompartment has id: args.compartment.id - Rust ShadowCompartmentWire has no id field - ignored.

episode_type, legacy, created_at sent - Rust has them. Good.

p1-p4, importance - good.

Only start_message/end_message missing!

**readRawSessionMessageById ordinal vs tail cache**: Tail cache assigns ordinals starting at baseOrdinal. Full read uses index+1. By-id uses COUNT query with summary filter - matches full read. Tail cache messages have absolute ordinals. ordinalById from readRawSessionMessages during enqueue uses cache if primed - **tail only entries have ordinals baseOrdinal+** but messages BELOW floor not in cache map - fallback by-id uses absolute COUNT semantics - **consistent**. Messages IN cache from tail have correct absolute ordinals. Good.

**Mismatch risk**: if provider returns different ordinals than DB for Pi - shadow only for OpenCode user tier likely.

**resolveOrdinals uses withRawSessionMessageCache** wrapping readRawSessionMessages - if tail cache primed, map only has tail ids; below-floor uses by-id. OK.

I'll also note: performReset on connection error doesn't set blockedUntilReset - passes retry - OK.

SubcShadowTransport: ensureConnected on socket close clears routes - next call reopens route - OK.

Document what I checked: create-session-hooks.test.ts, shadow-sender.test.ts (wire inventory, not Rust compartment), mc-module shadow_reset_and_state_sync test (flat compartments), PARITY.md (unrelated to shadow), read-session-raw ordinal semantics.

Final verdict HOLD - compartment wire mismatch primary.
Checking whether compartment JSON uses serde aliases and whether the transform hot path calls trim resolution at start:

[assistant] 2026-07-10T10:39:08Z
m0_mutations completely absent from Rust mc-module - confirmed silent drop

For compartment - serde without deny_unknown_fields - extra "id", "start": {...} would be ignored, but start_message/end_message would be missing from JSON and deserialize would FAIL because they're required i64 without default.

So state_sync with compartments from TS would fail entire ShadowStateSyncWire deserialize!

Unless... compartments array is empty on first passes. When compartments need sync, it breaks.

Let me verify - if compartments is empty vec, sync works. When historian creates compartment and state sync tries to push - deserialize fails on each compartment object → entire request invalid_params.

Actually serde deserializing Vec - each element must deserialize. One bad element fails whole thing.

HIGH confidence bug for soak with compartments.

Pi parity - check PARITY for shadow? unlikely in PARITY.md

False positive check: Rust tests use flat start_message at 7071-7079. TS serializeCompartment uses nested - no test asserts round-trip TS→Rust for compartment shape.

Also memory wire - TS sends project_path on memories; Rust ShadowMemoryWire doesn't have project_path field - extra field ignored by serde default.

TS memories include many fields - need to check project_path - not in Rust struct - ignored OK.

ShadowStateSync - TS sends m0_mutations in params but Rust ShadowStateSyncWire has no m0_mutations - **entire field dropped on deserialize** - shadow store never gets m0 mutations. Silent.

watermarks - TS sends in params, Rust has no watermarks field - dropped. TS uses watermarks only client-side for skip logic - OK.

I'll also note: first transform path calls resolveDeclaredTrimForShadow at START (line 460-461) which can readRawSessionMessages via cache - that's on hot path START not just post-transform enqueue.

Finding on hot path: resolveDeclaredTrimForShadow at transform start - uses getPersistedCompactionMarkerState + readRawSessionMessages - additional DB/cache work per transform when shadow enabled.

From shadow-sender resolveDeclaredTrimForShadow - getCompartmentsByEndMessageId, readRawSessionMessages - on EVERY transform at beginning.

That's live session impact when shadow_transform enabled!

transform.ts 459-462
<COUNCIL_MEMBER_RESPONSE>

## Finding 1: `state_sync` compartment wire shape mismatch (TS nested `start`/`end` vs Rust flat `start_message`/`end_message`)
- **Severity**: critical
- **Location**: `packages/plugin/src/hooks/magic-context/shadow-sender.ts` (`serializeCompartment` ~584–615) vs `crates/mc-module/src/lib.rs` (`ShadowCompartmentWire` ~267–295, `handle_shadow_state_sync_value` ~2148–2151)
- **Confidence**: high
- **Issue**: TS emits compartments as `{ id, sequence, start: { flat_id, bare_message_id, absolute_ordinal }, end: { ... }, title, ... }`. Rust expects flat `start_message`, `end_message`, `start_message_id`, `end_message_id` (required `i64` for start/end). Serde has no aliases for nested `start`/`end`. Any `state_sync` that includes non-empty `compartments` fails whole-request deserialization (`invalid_params`) or, if tests only used empty arrays, the soak never mirrors compartment history into the shadow store.
- **Evidence**: TS builder at 584–615; Rust struct at 267–295; module integration test uses flat JSON at `lib.rs` 7071–7080. `shadow-sender.test.ts` (~350–447) asserts flat top-level sync fields and `compartments: expect.any(Array)` but does not assert per-compartment Rust shape.
- **Suggested Fix**: Align TS serialization to `ShadowCompartmentWire` (map `start.absolute_ordinal` → `start_message`, `end.absolute_ordinal` → `end_message`, ids to `*_message_id`, drop or map `id`/`flat_id`), or add `#[serde(alias)]`/custom deserializer on Rust. Add a round-trip test with one real compartment.

## Finding 2: `m0_mutations` mirrored on TS wire but absent from Rust `ShadowStateSyncWire`
- **Severity**: high
- **Location**: `shadow-sender.ts` (`buildStateSyncPayload` ~696–706, ~735) vs `crates/mc-module/src/lib.rs` (`ShadowStateSyncWire` ~185–200)
- **Confidence**: high
- **Issue**: TS includes `m0_mutations` in every state sync payload and tests document it (`shadow-sender.test.ts` ~385). Rust wire struct has no `m0_mutations` field; serde silently ignores it. Shadow Rust transform never sees M0 mutation log state TS uses, causing systematic byte/decision divergence once M0 paths matter—not a logged “coverage loss,” a silent wrong mirror.
- **Evidence**: TS `m0_mutations: m0Mutations` at 735; grep shows zero `m0_mutation` references under `crates/mc-module`. `apply_shadow_state_sync` (~2122–2130) only upserts compartments, memories, memory_mutations.
- **Suggested Fix**: Extend `ShadowStateSyncWire` + `apply_shadow_state_sync` to apply M0 mutations (parity with TS watermarks `m0_mutation_id`), or stop claiming parity and gate shadow compare until implemented.

## Finding 3: Post-transform `enqueue()` still does synchronous JSON clone + ordinal/DB work on the transform completion path
- **Severity**: high
- **Location**: `packages/plugin/src/hooks/magic-context/transform.ts` (~2186–2228); `shadow-sender.ts` `enqueue` (~1016–1068), `resolveOrdinalsForShadow` (~361–408), `denormalizeShadowOutput` (~282–358)
- **Confidence**: high
- **Issue**: Shadow is “fire-and-forget” for subc I/O, but `enqueue()` runs synchronously after transform completes (still before the hook returns). Per pass it can: `cloneJson` input (~380), `readRawSessionMessages` / tail cache (~375–378), per-missing-id `readRawSessionMessageById` → `withReadOnlySessionDb` (~395–314), and another full `cloneJson` on output in `denormalizeShadowOutput` (~288, ~1054–1058). On large sessions (hundreds of messages, huge tool bodies) this is multi‑millisecond CPU + SQLite opens on the same thread that just finished transform—blast radius to live latency when `shadow_transform.enabled` is on.
- **Evidence**: `transform.ts` 2205 `shadowSender.enqueue`; `cloneForShadow` only covers input at start (459), not enqueue work; test “keeps sender exceptions off the transform hot path” (~494–514) only covers transport throw after enqueue returns, not cost/stall.
- **Suggested Fix**: Defer heavy prep to `setImmediate`/microtask queue item (keep enqueue O(1)), or precompute annotated input during transform behind the existing clone; cap/lazy denormalize; batch by-id ordinal reads.

## Finding 4: `resolveDeclaredTrimForShadow` + marker read at transform **start** when shadow is enabled
- **Severity**: medium
- **Location**: `transform.ts` (~457–462, ~2188); `shadow-sender.ts` `resolveDeclaredTrimForShadow` (~483–511)
- **Confidence**: high
- **Issue**: Besides post-pass enqueue, enabling shadow adds `resolveDeclaredTrimForShadow` at the beginning of every transform (before main work). That hits session meta, compartment-by-end-message, and `readRawSessionMessages` (cache or full DB). Live sessions pay extra I/O on every pass, not only when a shadow pass is actually queued.
- **Evidence**: `transform.ts` 460–461; `resolveDeclaredTrimForShadow` 495–499.
- **Suggested Fix**: Move “before” trim capture to post-pass only (compare using persisted marker delta) or cache trim with invalidation on compaction marker updates only.

## Finding 5: Ordinal fallback vs tail-primed cache — basis mismatch risk for compartment `state_sync` (not input ordinals)
- **Severity**: medium
- **Location**: `resolveOrdinalsForShadow` (~385–396) vs `serializeCompartment` / `ordinalForMessageId` (~639–644, ~570–581) in `shadow-sender.ts`; `read-session-chunk.ts` tail prime (~242–264)
- **Confidence**: medium
- **Issue**: Input ordinals use by-id DB fallback when cache is tail-only; comment claims same COUNT semantics as full read (~389–393). **Compartment** serialization uses `ordinalForMessageId` with `rawById` from `readRawSessionMessages` only—no by-id fallback. If compartment boundary message IDs sit below the tail-prime floor, `serializeCompartment` returns `null` → `buildStateSyncPayload` returns `"unresolved"` (~655–656) and pass skips sync (~978–984) without reset. Shadow store drifts from TS silently (logged skip), defeating soak on long sessions with new compartments.
- **Evidence**: `ordinalForMessageId` 421–422 returns null if not in map; `buildStateSyncPayload` 639–644 builds map from full cache read only; tail cache docs 219–234.
- **Suggested Fix**: Reuse `readRawSessionMessageById` in `ordinalForMessageId` / compartment path; or include compartment endpoints in tail prime.

## Finding 6: `blockedUntilReset` after peer reject can drop in-flight passes without re-queue
- **Severity**: medium
- **Location**: `shadow-sender.ts` `processPass` (~960), `runQueue` catch (~887–904), `enqueue` mismatch path (~1028–1043)
- **Confidence**: medium
- **Issue**: On `shadow_transform` peer reject, `blockedUntilReset = true` (~896). `processPass` returns immediately at 960 if blocked **after** a reset in the same item—OK. But the **current** pass’s transform may already have been sent before reject on a prior item; subsequent queued passes are shifted and may hit blocked at 960 and return without sending transform, while queue drains—coverage loss. `enqueue` mismatch clears queue and pushes reset (~1028–1043)—good. Connection failure sets `requireResetReason` but not always `blockedUntilReset` (~898–902)—passes may retry without full resync.
- **Evidence**: 960 `if (state.blockedUntilReset) return`; 898–902 only sets `initialized = false` and `requireResetReason`.
- **Suggested Fix**: On connection failure, mirror peer-reject gating (`blockedUntilReset` until reset ack); after reset in `processPass`, optionally re-queue skipped pass.

## Finding 7: `SubcShadowTransport` global `pending` chain + 5s timeout + socket close
- **Severity**: medium
- **Location**: `shadow-sender.ts` `SubcShadowTransport` (~1089–1213, ~1392–1404)
- **Confidence**: medium
- **Issue**: All sessions share one serialized `pending` promise chain (~1115–1120)—slow session blocks others. `readTerminalFor` spins discarding non-matching frames until timeout (~1398–1404); on timeout, in-flight request fails, `send_failures` increment, but socket may still be half-open until daemon closes—next call may hit stale reader state. Socket `close` clears routes (~1176–1179) but does not reject in-flight `readTerminalFor` waiters explicitly beyond `closed` flag.
- **Evidence**: Single `this.pending` for all `call`; `REQUEST_TIMEOUT_MS = 5_000` (~47); no session-level isolation.
- **Suggested Fix**: Per-route or per-session transport queues; on timeout destroy socket and force reconnect; correlate in-flight abort.

## Finding 8: `buildMagicContextHookConfig` full spread — low regression risk if hook stays disciplined
- **Severity**: low
- **Location**: `create-session-hooks.ts` (~18–32)
- **Confidence**: high (for “no current bug”)
- **Issue**: Spread passes entire `MagicContextPluginConfig` into hook config. Extra keys are inert only if `createMagicContextHook` never iterates unknown keys. `project-security.ts` strips `shadow_transform` from **project** config (~234–238), so repo cannot arm shadow—good. No evidence spread breaks consumers; tests pin pass-through (~39–54 `create-session-hooks.test.ts`).
- **Evidence**: Comment 19–26; security strip 234–238.
- **Suggested Fix**: Optional: satisfy hook config type via `satisfies` / pick typed fields for documentation; not a soak blocker.

## Finding 9: Rust shadow isolation — binding checks present; live lane not written by shadow handlers
- **Severity**: low (verified controls)
- **Location**: `crates/mc-module/src/lib.rs` `shadow_binding` (~783–813), `handle_transform_value` (~1944–1949), `handle_agent_drops` (~1711–1715), shadow handlers (~2148+, ~2250+)
- **Confidence**: high
- **Issue**: Shadow ops require `shadow:<sid>` binding; plain transform rejected on shadow binding (`shadow_dispatch_enforces_shadow_route_precedence` ~7004–7030). Shadow transform uses `binding.session` (shadow id) and `shadow_project_path` (~2348–2351)—writes scoped to shadow session rows. Divergence recording does not touch live session id.
- **Evidence**: Tests at 7004–7030; `plain_transform_on_shadow_binding` / `shadow_binding_required`.
- **Suggested Fix**: Audit remaining dispatch arms for `is_shadow_session` if new ops added (only `ctx_reduce` found with `non_shadow_op_on_shadow_binding`).

## Finding 10: `apply_shadow_state_sync` seq gate — duplicate sync correctly rejects; not idempotent replay
- **Severity**: low
- **Location**: `crates/mc-store/src/lib.rs` (~2116–2119, ~2122–2124); test ~7105–7116
- **Confidence**: high
- **Issue**: Re-sending same `expected_shadow_seq` after success yields `shadow_seq_mismatch`—by design. Compartment upsert is `ON CONFLICT DO UPDATE` (~4002)—re-applying same seq generation is impossible without seq bump. Acceptable for observe-only soak.
- **Evidence**: `shadow_reset_and_state_sync_gate_generation_and_seq` duplicate sync test.

---

### False-positive filtering performed
- Read `packages/plugin/src/plugin/hooks/create-session-hooks.test.ts`, `shadow-sender.test.ts` (wire flattening, queue, exception on enqueue).
- Read `crates/mc-module/src/lib.rs` shadow tests (~7004–7146); no `PARITY.md` for shadow lane (only `packages/pi-plugin/PARITY.md` and smart-notes PARITY—out of scope).
- Confirmed prior fixes: `toFlatWireBody` (~799–801) matches Rust top-level serde; ordinal by-id fallback (~385–396) addresses input starvation; `tool_provider` route (~1133–1137).

---

## Summary
| Severity | Count |
|----------|-------|
| Critical | 1 |
| High | 3 |
| Medium | 4 |
| Low | 3 |

**Overall risk**: Shadow soak will run without affecting live **correctness** of transform output (enqueue errors caught in `transform.ts` ~2226–2228; shadow is separate route/store). However, **live latency** can regress with shadow enabled (Findings 3–4), and **shadow mirror correctness** is broken for real compartment sync and M0 mutations (Findings 1–2)—so divergence telemetry will be misleading noise until fixed.

**Verdict: HOLD** (do not treat production soak as authoritative parity evidence yet).

**Single most important reason**: **`state_sync` compartment JSON from TS does not deserialize into `ShadowCompartmentWire`**—once sessions sync compartments to Rust, shadow state is wrong or sync fails entirely, which defeats the soak’s purpose (Finding 1).

</COUNCIL_MEMBER_RESPONSE>

