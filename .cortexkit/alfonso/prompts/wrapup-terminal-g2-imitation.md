# Clear the remaining ruled backlog: wrapup terminal classes, G2 eligible-tail, mid-completion imitation strip

Repo: ~/Work/Projects/CortexKit/magic-context, branch subc-migration, crates/mc-module (+ mc-store only if a migration is genuinely needed; expected: none). Gates: `cargo test -p mc-module --lib`, `cargo clippy -p mc-module`. Comments explain WHY; no plan/audit references; no em-dashes.

These four items were ruled with the Claude Code gateway peer during the phase-2 proof drive and are now being cleared immediately (project rule: no deferrals). All rulings are final; implement, do not re-litigate.

ITEM 1 — session.wrapup TERMINAL for NoModels. Today HistorianDriveError::NoModels (empty historian model chain, historian.rs:949 and :1130) falls into run_wrapup_firing's catch-all (lib.rs around 2926-2950) as retryable snapshot_unavailable, so the caller retries forever against a configuration absence that is stable for the module's lifetime. Change: classify NoModels as a TERMINAL wrapup failure: `{ok:false, disposition:"failed", rounds:<n>, reason:"no_models", detail:...}` with the rounds field present, the command ledger row RETAINED (terminal dispositions record; recoverables do not — keep that contract), and no auto-retry signal. Follow the existing terminal_wrapup_response shape; if it cannot carry a failed disposition today, extend it minimally.

ITEM 2 — same question for unknown_module on the producer route open (module_id "broca" hardcoded, historian_producer.rs:31). Ruled nuance: a module that is STILL STARTING looks momentarily identical to one that is absent. Implement a bounded transient window: the first unknown_module in a wrapup request stays retryable; if the SAME wrapup request observes unknown_module again after a re-attempt separated by >= 30s within its budget, classify terminal failed reason:"runner_module_unavailable" with rounds + retained ledger row. Track the observation in the request-scoped state (the wrapup drive loop), not durable state.

ITEM 3 — G2 eligible-tail fix. resolve of the newest-authored-user for temporal gap markers currently requires the user message to be the literal tail element, so a trailing in-array system reminder (real Claude Code traffic) makes G2 skip the gap silently. Ruled rule: walk back from the tail skipping entries with role=system OR meta.synthetic; if the first authored entry found is a user message, it is the eligible tail and gets the marker (persisted-at-first-sight replay contract unchanged); if it is an authored assistant, mid-turn, no marker. Cache-safety (verified at ruling): only previously-SKIPPED turns gain markers — no persisted decision exists for them, so no existing session's replayed bytes change. Find the eligibility check in transform.rs (temporal marks machinery) and apply exactly this rule. Regression tests: user+trailing-system gets a marker; user+trailing-synthetic gets a marker; authored-assistant tail gets none; previously marked turns replay byte-identical.

ITEM 4 — mid-completion tag imitation strip. strip_leading_tag_imitations (transform.rs) normalizes only LEADING §N§ runs in assistant text, so a model that emits an imitation mid-completion (e.g. "preamble... §22§ Delta") gets it AUTHORED into Claude Code's durable history, where it passes verbatim on false passes forever. Ruled scope: extend the strip to well-formed §N§ tokens ANYWHERE in assistant-authored text on ACTIVE passes, with two hard carve-outs: (a) inside code fences/inline code spans (backtick-delimited) stay verbatim; (b) the strip only removes tokens matching the exact well-formed §<digits>§ shape followed by whitespace or punctuation — malformed or quoted-in-prose references like "tag §12§ was dropped" ... careful: that IS well-formed. Ruled disambiguation: strip only tokens at the START of a line or after a sentence boundary followed by non-reference prose; if that heuristic proves unimplementable cleanly, restrict to line-leading runs (start of any line, not just the first) which covers the observed imitation class with zero false-positive risk. Choose the line-leading variant unless you find a strictly better precise rule; document the choice. False passes keep authored bytes verbatim (existing contract). Tests: mid-text line-leading imitation strips on active, survives on false; code-fence content never touched; genuine prose reference mid-sentence never touched (under the line-leading variant this is trivially true).

Every change lands with mutation-sensitive tests. Report per item: what changed, where, test names.

## ITEM 3 CORRECTION (parent investigation, supersedes the item-3 text above)

`eligible_authored_user_tail` (transform.rs:2871) ALREADY walks back past synthetic/system/tool entries, and that code shipped in the drive-era binary — so the naive-tail assumption above is stale and the rig's missing-marker finding has a different mechanism. Do NOT change the walk blindly. Investigate with repro tests FIRST, in this order:

(a) Trailing role=system reminder after an idle-gap authored user: write the test; expectation is it PASSES at HEAD (walk skips system). If it fails, fix the walk and you are done.
(b) The reminder riding as a standalone role=USER message whose text is entirely <system-reminder>-wrapped (real CC traffic shape): is_authored_user_message treats any non-synthetic user text as authored, so the REMINDER becomes the authored tail, the gap computes against the same-request reminder (sub-threshold), and no marker mints. If this repro fails at HEAD, the fix is a narrow authored-discriminator: a user message whose entire text content consists of system-reminder-wrapped blocks is transport, not authored (prefer any codec/meta discriminator over content sniffing if one exists — check what the opencode/CC codecs expose; report your choice).
(c) Frontier pre-advance: can a pass observe the authored user (advancing max_seen_ordinal / the overlay frontier) without minting its temporal mark (e.g. mint-ineligible pass shape), permanently blocking the mint via the is_new check? Write the two-pass repro; if it fails, the fix is frontier-advances-only-past-messages-whose-mark-was-decided (minted or explicitly below-threshold), never past silently-skipped ones.

Deliverable for item 3: the repros for all three mechanisms, the real fix for whichever fails, and a written note naming the actual mechanism. If ALL THREE pass at HEAD, say so explicitly and port the repros as regression tests; the rig finding then needs live re-triage and your report must state that honestly.
