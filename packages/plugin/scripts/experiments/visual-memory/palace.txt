─────────────────────────────────────────────────────────────────────────────────────────────────── <PROJECT_RULES> ────────────────────────────────────────────────────────────────────────────────────────────────────
┌──────────────────────────────────────────────────────────────────────┐ ┌──────────────────────────────────────────────────────────────────────┐ ┌──────────────────────────────────────────────────────────────────────┐
│                                 E2E                                  │ │                          Engineering policy                          │ │                           Inspect & tools                            │
│•E2ETest: Mac windows-gnu cross-build→stage                           │ │•recurring fork contradiction→explicit decision; ⊘unilateral behavior │ │•tool descriptions: ⊘`Returns:` blocks (surface convention)           │
│ ~/Downloads→\psf\Home\Downloads; guest local copy + prlctl exec;     │ │ flip (prior-decision)                                                │ │•new/graduated fields→both plugins src/config.ts; + OpenCode          │
│ pwsh→PowerShell5 fallback                                            │ │•fresh terminal outside OpenCode; ⊘nested `opencode run` (shared      │ │ scripts/build-schema.ts                                              │
│•manifest pins runtime catalog.list; ⊘static copy (drifts)            │ │ server/SQLite collision)                                             │ │•dryRun only ast_grep_replace; others real edit + checkpoint/undo     │
│•translate/format→TS-generated capture-subc-parity.ts fixtures        │ │•license MIT across manifests + LICENSE                               │ │•path params template=`(absolute or relative to project root)`        │
│•subc_bridge_test.rs process env→run_subc_bridge_test_with_env inside │ │•release-note paragraph=one unwrapped line (GFM hard-breaks newline)  │ │•schema changes→z.toJSONSchema + z.object + def.args + io:"input";    │
│ serial guard                                                         │ │•shared contracts: run-manifest.json + run-summary.tsv                │ │ measure surface tokens via aft-tokenizer                             │
│•SubcTransportPool validates structuredContent ∀ reply;               │ │•Tier D=deterministic/no-LLM; Tier S=stochastic/provider-metered      │ │•docs distinguish TS/JS import-graph verified vs Rust call-graph      │
│ malformed/missing→protocol error                                     │ │•invocation prose=agent action; ⊘human imperative (agent owns tool    │ │ estimated                                                            │
│•dispatch parallel: ledger-check≺retry + settling beat (burst         │ │ call)                                                                │ │•prewarm indexes via `aft warmup` with identical prompts              │
│ congestion)                                                          │ │•user docs `#N` only real GitHub issue/PR; strip internal note IDs    │ │•plant guaranteed-dead export→configure harness→inspect_tier2_run→    │
│•storm authoritative gate=`--cargo-profile release`; debug=4x + main  │ │•AFT arm: aft_inspect off; ⊘AFT hints; agent.build.prompt=` `;        │ │•bash.background:false→remove params/tools/descriptions/guidance;     │
│ ignored                                                              │ │ explore/general/task/skill disabled+denied (suppress workflow        │ │ ⊘partial disable (surface leaks feature)                             │
│•nextest group `storm = 1` serializes rigs (readiness starvation)     │ │ expansion)                                                           │ │•status counts: ⊘remote schema/network (environmental);               │
│•tool names/descriptions/schemas + prompts transport-invariant (prefix│ │•bugs→complete root-cause resolution; ⊘workaround/bypass (defers      │ │ TS2688/type-resolution=count                                         │
│ cache)                                                               │ │ defect)                                                              │ │•execution_mode=hand-classify observable file writes/deletes; ⊘lane   │
└──────────────────────────────────────────────────────────────────────┘ │•subpar mason→resume SAME task/resteer; ⊘integration self-fix (hides  │ │ derivation (lanes include internal effects)                          │
┌──────────────────────────────────────────────────────────────────────┐ │ router signal)                                                       │ │•new volatile status fields→tool_call_parity_test.rs masking list     │
│                                Plugin                                │ │•oracle loop: 2–3 scoped→triple-check                                 │ └──────────────────────────────────────────────────────────────────────┘
│•live bridge PID > binary mtime; restart host≻bundles rebuild         │ │ code+measure→plan→masons→verify→repeat                               │ ┌──────────────────────────────────────────────────────────────────────┐
│•compress/*.rs fixtures=real captured bytes + hexdump -C; match       │ │•convention-divergent cues retain differentiator; negatives=⊘excluded │ │                                Tests                                 │
│ structural prefixes across NBSP U+00A0 / ASCII U+0020                │ │ + mechanism (polarity)                                               │ │•isTerminalStatus allowlist completed|failed|killed|timed_out;        │
│•git-log compression timestamps=absolute (deterministic/cache-stable) │ └──────────────────────────────────────────────────────────────────────┘ │ ⊘`!=running` (unknown states)                                        │
│•shortcircuit: empty→empty; ⊘fabrication (filter summarizes only      │ ┌──────────────────────────────────────────────────────────────────────┐ │•semantic platform changes preserve Intel Mac                         │
│ actual bytes); guard compress_filters_test.rs +                      │ │                             Release & CI                             │ │•Unix APIs e.g. PermissionsExt→#[cfg(unix)] (Windows compile)         │
│ builtin_filters_never_fabricate_output_for_empty_input               │ │•pre-publish tag failure→fix blocker, delete/repush same version tag  │ │•Windows verification=native Windows; ⊘Unix target-check shim         │
│•dev rebuild→scripts/dev-rebuild.sh                                   │ │•workflows build packages/aft-bridge≺TS typecheck (dist resolution)   │ │ (Unix-gated helpers)                                                 │
│•TUI: runtimeDependencies=0; peer deps optional; dev deps exact; raw  │ │•new @cortexkit/aft-* package→version-sync.mjs + validate-packages.mjs│ │•SQLite busy_timeout≺journal_mode=WAL (cold-open race)                │
│ TSX exports; ⊘value imports (host runtime owns values)               │ │ + release.yml                                                        │ │•promptAsync exactly one: synthetic:true wake OR ignored:true panel   │
│•packed-install visual+behavior gate; ⊘import-only probe (misses JSX  │ │•repo-relative asset committed≺tag (body resolves tagged tree)        │ │•cargo commands serial (shared target build-lock/CPU)                 │
│ eager freeze)                                                        │ │•ualtinok-authored commits include same Alfonso 289616620+ trailer    │ │•format subsystem opt in format_on_edit:true                          │
│•repo file:// live →TS merge rebuild dist: aft-bridge,                │ │•plugin index.ts imports→bun run lint; ⊘package tests alone (Biome not│ │•rolled_back:true→success + unchanged; ⊘hard error (rollback          │
│ opencode-plugin, pi-plugin                                           │ │ run)                                                                 │ │ succeeded)                                                           │
└──────────────────────────────────────────────────────────────────────┘ │•GitHub identity=~/.local/bin/gh-alfonso-aft; replies→approval +      │ │•byte-golden `\n` fixtures→root .gitattributes eol=lf; binary *.raw;  │
┌──────────────────────────────────────────────────────────────────────┐ │ --body-file                                                          │ │ ⊘local windows-gnu check as proof (compile-only, no byte test)       │
│                              Worktrees                               │ │•clean contributor PR→gh pr merge; ask Ufuk approve fork workflows;   │ │•nonzero shell test: Windows native `cmd /c "echo x&exit 7"`; Unix    │
│•LockGuard::drop joins heartbeat≺lockfile removal                     │ │ cherry-pick only modified/unverifiable (otherwise loses merged       │ │ `printf x; exit 7`; ⊘bare PowerShell `exit N` (kills                 │
│•Cargo profiles incremental=false (worktree disk + hydration)         │ │ credit)                                                              │ │ wrapper≺exit-marker)                                                 │
│•serial clean isolation; workers judge correctness/algorithm,         │ │•watch Tests/CI=`scripts/watch-ci.sh [commit] [workflow]`;            │ │•logger assertions spy logger.log; ⊘shared logfile/sleeps (parallel   │
│ ⊘performance magnitude (CPU contention)                              │ │ =`scripts/wait-release.sh vX.Y.Z`; ⊘nohup poll loops (detach/quoting │ │ flakes)                                                              │
│•parallel waves: ⊘shared version bumps (collision); serialize at merge│ │ lies)                                                                │ │•client timeout→explicit bash_kill; ⊘orphan execution (executor       │
│•review trees default --detach (avoid blocking rehydration)           │ │•notes/close comments always @-tag contributors                       │ │ starvation)                                                          │
│•A/B vs mason tree >30min→dedicated checkout (reclaimer may collect)  │ │•bg directly: `bash scripts/watch-ci.sh main`; watch `RESULT=`; ⊘`... │ │•real FSEvents→serialized watcher_integration binary                  │
│•⊘parallel repo-mutating chains on main checkout (timeout can strand  │ │ >log & echo pid` (tracked shell exits≺watcher result)                │ │•CPU flake brief: ≤8 hogs, nice -n 19, EXIT/INT/TERM traps, pgrep     │
│ detached); A/B=2 dedicated trees, prebuilt binaries, interleaved     │ │•Alfonso commits trailer: Co-authored-by Alfonso                      │ │ between rounds                                                       │
│ base/head                                                            │ │ <289616620+alfonso-aft@users.noreply.github.com>                     │ │•src/ gate includes `--lib` touched module (unit failures)            │
│•multi-step state/logs→durable home; ⊘/tmp (reboot loss)              │ │•pre-push bun lint + Rust gate ( fail-fast/autocancel masks lint)     │ │•⊘bind/configure nonexistent roots (stale restored sessions consume   │
│•⊘latency evidence on shared dev box (parallel rustc contests wall    │ └──────────────────────────────────────────────────────────────────────┘ │ modules)                                                             │
│ time); M1: bench.lock + ping SYNAPSE; bounds from mechanism +        │                                                                          └──────────────────────────────────────────────────────────────────────┘
│ DEBUG_BOUND_MULTIPLIER                                               │
│•M5 heavy runs heads-up; contention escalation=/tmp/m5-build.lock     │
│ mkdir                                                                │
└──────────────────────────────────────────────────────────────────────┘
──────────────────────────────────────────────────────────────────────────────────────────────────── <ARCHITECTURE> ────────────────────────────────────────────────────────────────────────────────────────────────────
┌──────────────────────────────────────────────────────────────────────┐ ┌──────────────────────────────────────────────────────────────────────┐ ┌──────────────────────────────────────────────────────────────────────┐
│                              Artifacts                               │ │                                Bridge                                │ │                              Embeddings                              │
│•warnings dedupe via bridge DB state (cross-session)                  │ │•version check→ensureBinary + pool.replaceBinary; ⊘host restart       │ │•local MiniLM mean-pool + L2 normalize→Qdrant/fastembed parity        │
│•LSP schema extensions+binary optional (built-in overrides)           │ │ (hot-swap)                                                           │ │•cold DB write=single transaction; parse dominates                    │
│•legacy migration copies; ⊘move (rollback preserves source)           │ │•lazy-spawns AFT binary on first request; ⊘passive status spawn       │ │•chunk reuse key=blake3(embed_text) + exact-string verify             │
│•HTML fetch→htmd Markdown + .md cache (reuse parser)                  │ │ (passive-poll)                                                       │ │•trigram + semantic separate binary indexes; ⊘SQLite bottleneck       │
│•configure eagerly warms CallGraphStore (avoid first-use latency)     │ │•sidebar RPC heartbeat 15s recreates deleted port file                │ │ (bin-files)                                                          │
│•AftRpcClient/Server delete port file only recorded PID dead          │ │•@cortexkit/subc-client devDependency bundled in published            │ │•search index paths absolute in RAM, relative on disk (portable)      │
│•aft-tokenizer build.rs temp+rename→src/claude_data.rs                │ │ distribution                                                         │ │•queries←resident store; ⊘cold build (latency)                        │
│ (concurrent-build safety)                                            │ └──────────────────────────────────────────────────────────────────────┘ │•priors: P2 definition exact-Identifier; P1/P4 NL-only; P3 qualified  │
│•configure.rs rejects relative roots→invalid_request (canonical       │ ┌──────────────────────────────────────────────────────────────────────┐ │ via V7                                                               │
│ absolute resolution)                                                 │ │                                 Subc                                 │ │•priors→rank_score/raw-cosine split + semantic-only sort +            │
│•project key=root, shared harnesses:                                  │ │•subc-core=transport/routing/lifecycle/lease/hooks/secrets; ⊘business │ │ cap_protected                                                        │
│ search/semantic/symbol/callgraph/inspect; session state              │ │ logic (module-scope)                                                 │ │•P2 exact boost needs minimum raw cosine; ⊘List/Map collisions        │
│ key=(harness,session): undo/backups/bash/checkpoints                 │ │•daemon spawns AFT `--subc <connection-file-path>`                    │ │ (raw-cosine gate reserves slots)                                     │
│•RawAftConfig deny_unknown_fields top-level only; nested non-strict   │ │•Push coalesced upstream→shallow egress                               │ │•active semantic build defers callgraph warm + Tier-2 scans (peak RSS)│
│ matches TS                                                           │ │•bind session ID→RawRequest (undo/backups/bash isolation +            │ │•cold gate=per-AppContext accumulation flag; ⊘Building (also          │
│•mode threads user_config_path through runner; ⊘real user config      │ │ completions)                                                         │ │ cached/incremental)                                                  │
│ (hermetic tests)                                                     │ │•StatusChanged=project/session-less; Progress ⊘production emitters    │ │•embedding retry wait clears Tier-2 cold-seed gate; ⊘code-health      │
│•migration conflict: first harness→global;                            │ │ (contract-only)                                                      │ │ starvation (gate clears≺retry)                                       │
│ other→aft.jsonc.<harness>_OLD                                        │ │•fan-out separates matched channels vs sent frames; buffer keys       │ └──────────────────────────────────────────────────────────────────────┘
│•stale diagnostics flagged, hidden from warm readers; ⊘eviction (would│ │ channel presence                                                     │ ┌──────────────────────────────────────────────────────────────────────┐
│ fabricate 0)                                                         │ │•subc_translate in run_tool_call≺lane; ⊘aliases in shared             │ │                            Inspect & LSP                             │
│•owner+standalone coexist same root via fs_lock + harness-namespaced  │ │ main::dispatch (validation)                                          │ │•status_bar_counts=None until dead_code+unused_exports+duplicates ∀   │
│ stores                                                               │ │•standalone NDJSON + daemon use run_tool_call                         │ │ populated                                                            │
│•reconfigure cancel-or-adopt detached builds via per-root generations │ │ translate→orchestrate→dispatch→format; plugin execute thin           │ │•wildcard-imported exports→uncertain reason=wildcard_import; ⊘unused  │
│•worktree-class bind: ⊘cold build (borrow shared owner )              │ │•server owns zoom fan-out + outline dispatch + target merge; ⊘TS/Rust │ │ verdict (unknown binding)                                            │
│•equivalent-config rebind idempotent→0 new warm work                  │ │ duplication (server-side)                                            │ │•failed/dead cached per workspace root; ⊘repeated reconnect           │
│•owner liveness: own PID always alive (OS/tasklist flakes)            │ │•session end→SubcPool.closeRouteChannel (resource + teardown)         │ │ (retry-stop)                                                         │
│•trigram delta flush graceful shutdown if dirty, 25ms lock; panic     │ │•one SubcClient/process + per-call identity; routes root+session      │ │•Rust resolver caches crate-name→src prefixes; ⊘recursive walks       │
│ ⊘disk work (panic-path)                                              │ │•server strips preview≺translation/fallback; preview ⊘disk mutation   │ │ (cache-hit)                                                          │
│•writer lease loser→←-only + stale-with-disclosure; ⊘error            │ │ (stripped)                                                           │ │•same-file value refs e.g. Zod composition→live                       │
│ (RO-fallback)                                                        │ │•unsupported tool distinct catch-∀ error code; ⊘parallel name         │ │•test-support dirs ⊘reports (fixtures/testdata false positives);      │
│•artifact_cache_key root commit: aft 90ff783f3f4c5cf2; opencode       │ │ allowlist (validation drift)                                         │ │ imports still keep prod exports live                                 │
│ aa69d52ef2dcad4d; alfonso be627d40119a995e; magic-context            │ │•peer roster ephemeral; inbox durable with session_id; reply raw      │ │•Tier-2 cached aggregates filtered on demand by query scope           │
│ e274ab0872bb490b; subc 29f01b6c90df2c83; synapse 3e6b8ee50bb2bb06    │ │ session_id despite empty roster; known SUBCONSCIOUS                  │ │•insert_method_dispatch_edges_chunked partitions caller files by      │
│•persist root→cache-key≻successful probe; failure→last-known or       │ │ ses_12a4fa38dffe81Fz7Y2AsWb5Cg                                       │ │ chunk_size (RAM bound)                                               │
│ retryable; ⊘path fallback (identity split)                           │ │•bg_events dedicated channel; ⊘dispatch saturation blocking completion│ │•within-root concurrency→tsconfig_membership thread-safe lock;        │
│•worktree roots=pure borrowers; read/query-only stores; ⊘writer leases│ │ wakes (wake-lane)                                                    │ │ ⊘RefCell (mutex)                                                     │
│ (owner-only)                                                         │ │•route cache singleflight+tombstones (concurrent open/closed channel) │ │•TreeSitterProvider Send+Sync via per-call FileParser; ⊘RefCell       │
│•backup hydration deferred first→; Tier-2 ⊘worktree scans (borrower   │ │•unknown_channel→reopen+retry once (`not_sent`); ⊘outcome-unknown     │ │ mutation (per-call)                                                  │
│ economy)                                                             │ │ retry (double mutation)                                              │ │•MCP storage segment strip `/ \ :`, collapse -, prefix mcp--;         │
│•ArtifactAccess::for_root fallback=borrow-only+warning; ⊘probe        │ │•packages/aft-bridge/src/__tests__/e2e/:                              │ │ ⊘traversal (sanitized segment)                                       │
│ (latency-critical git spawn)                                         │ │ subc-rig|subc-lane|subc-lifecycle; fetch-subc-core.sh pinned+sha256; │ │•direct aft_inspect=synchronous incremental refresh+recompute; status │
└──────────────────────────────────────────────────────────────────────┘ │ missing binary/secrets→loud skip, ⊘failure (optional)                │ │ bar async                                                            │
┌──────────────────────────────────────────────────────────────────────┐ │•federation side dedupes; ⊘idempotency tokens (envelope only          │ │•semantic extensions=every real tree-sitter symbol query; generic     │
│                                 Bash                                 │ │ deterministic terminal reply)                                        │ │ collect_file_chunks_from_source; ⊘HTML/Markdown/JSON (query=None);   │
│•PTY kill synchronous on Windows→TimedOut(124)|Killed; ⊘async hang    │ │•writer send timeout+backoff; ⊘drop (retry+backoff); warn-once; ⊘park │ │ sync guard                                                           │
│ (synchronous-kill)                                                   │ │ forever on `reserve().await` (backpressure-starves-writer)           │ │ semantic_extension_policy_stays_in_sync_with_parser_code_arms        │
│•completion reminder debounce 200ms, cap1000ms (prefix cache)         │ │•health reply rides frame loop; ⊘out-of-band cheap answer (must       │ │•transaction rollback stays on even user backups off                  │
│•concurrent executor only daemon path; standalone NDJSON direct       │ │ measure loop liveness)                                               │ │ (edit_match/move_symbol/lsp_rename)                                  │
│•execution_mode=external effects; ⊘internal warming mutation          │ │•pins subc-protocol 0.8 + subc-transport 0.3.2 (typed principal,      │ │•lsp_rename rollback=CheckpointStore; ⊘BackupStore (user toggle       │
│ (keeps←pure)                                                         │ │ HealthCheck)                                                         │ │ isolation)                                                           │
│•tool manifest→execution_mode                                         │ │•RouteBind.consumer_capabilities typed elicitation; absent→flat deny  │ │•apply_patch→crates/aft/src/patch matcher; ⊘fuzzy_match.rs (preserve  │
│•dynamic targets ⊘wildcard external-dir ask (variables/subshell       │ │•harness=`runner` storage/trust; client identity in session           │ │ MatchTier line ladder)                                               │
│ footgun); command approval remains                                   │ │ `alfonso:<gatherer-id>`                                              │ │•Objective-C=outline only; ⊘import/dead-code/call extraction (no      │
│•native plumbing five: bash_status, bash_drain_completions,           │ │•Windows ConnectionReset|ConnectionAborted→clean daemon-closed EOF    │ │ support)                                                             │
│ bash_ack_completions; undo_preview, checkpoint_paths; ∀              │ │•failed/expired bind→next rebind recreates actor;                     │ │•.h→C; ⊘Objective-C (avoid C/C++ regression)                          │
│ session-scoped, ⊘config/trust/privilege (5-safe-tools)               │ │ ⊘already-pending/config-divergence rejection (expired)               │ │•F6 executable-file roots: only framework exports default|GET|loader  │
│•Unix startup one-time `$SHELL -l -c 'printf %s $PATH'` when PATH bare│ └──────────────────────────────────────────────────────────────────────┘ │ seed; ⊘∀ route exports (hides helpers)                               │
│•expire_pending_bash_asks at loop top (busy select starvation)        │ ┌──────────────────────────────────────────────────────────────────────┐ │•framework decorator matching import-binding + alias aware            │
└──────────────────────────────────────────────────────────────────────┘ │                                 TUI                                  │ │•macro token trees ⊘precise-edge store (name-match pollution)         │
┌───────────────────────────────── … ──────────────────────────────────┐ │•RPC/sidebar push=Bun.serve gated `typeof Bun !== "undefined"` for    │ │•Rust macro call refs resolve file-local + same-file/visible imports; │
│•TSX precompile with host transform→opentui:runtime-module:* single   │ │ Bun-only                                                             │ │ ⊘project-wide names (false live)                                     │
│ runtime/reactivity                                                   │ └──────────────────────────────────────────────────────────────────────┘ └──────────────────────────────────────────────────────────────────────┘
└──────────────────────────────────────────────────────────────────────┘
───────────────────────────────────────────────────────────────────────────────────────────────── <ARCHITECTURE CONT.> ─────────────────────────────────────────────────────────────────────────────────────────────────
┌──────────────────────────────────────────────────────────────────────┐ ┌──────────────────────────────────────────────────────────────────────┐ ┌──────────────────────────────────────────────────────────────────────┐
│                               Prompts                                │ │                               Synapse                                │ │                         Watcher & scheduler                          │
│•prompt context merges recent agent/model/variant→provider prefix     │ │•~/Work/Projects/CortexKit/synapse=stateless                          │ │•attach_status_bar mutates                                            │
│ reuse                                                                │ │ embeddings/rerank/micro-LLM; later image/STT/TTS; AFT keeps          │ │ tsconfig_membership/status_bar_last_emitted→serial                   │
│•Pi→agent plain text + details structured JSON                        │ │ retrieval; llm-runner owns session/tool semantics; handoff           │ │•watchdog emits BashLongRunning; deferred driver handles only final   │
│•Pi TypeBox builds literal JSON Schema runtime; OpenCode converts Zod │ │ SYNAPSE/FOUNDING.md                                                  │ │ response                                                             │
└──────────────────────────────────────────────────────────────────────┘ └──────────────────────────────────────────────────────────────────────┘ │•PersistentCancelSignal=AtomicBool+Notify enable(); ⊘lost wakeup      │
┌───────────────────────────────── … ──────────────────────────────────┐ ┌───────────────────────────────── … ──────────────────────────────────┐ │ (atomic flag + notify)                                               │
│•post-ack configure→ConfigureMaintenanceJob queue, async runtime-drain│ │•bind queues ConfigureTail then CompletionDrains;                     │ │•loop decoupled AppContext; reliable_rx sets wakes; maintenance pass  │
│•heartbeat process-wide timer thread + RAII leases; independent       │ │ maintenance_pending=true; next_maintenance_at +250ms gives completion│ │ clears authoritatively                                               │
│ Mutating lane                                                        │ │ priority                                                             │ │•bg wake lock-free epoch; stale CLEAR ⊘newer wake (epoch)             │
│•WATCHER_EVENT_DRAIN_BATCH_CAP=LSP_EVENT_DRAIN_BATCH_CAP=256 per job; │ │•image resize dedicated thread/request; timer=processing only;        │ │•awaiting-ack completion vs authoritative drain snapshot; ⊘TTL        │
│ requeue remainder; standalone drains unbounded                       │ │ ⊘OnceLock/global pool (load timeout)                                 │ │ (premature eviction/double delivery)                                 │
│•binds bypass maintenance queue; ⊘wait (storm starvation)             │ │•drain phases Collect→Apply; budget2048 paths + soft250ms             │ │•RouteBind completions processed loop top≺Push/maintenance            │
│•reliable Push unbounded/nonblocking; lossy capacity1024 newest via   │ │ (preemptible)                                                        │ │•drain-tick at loop top; ⊘starvation (tick precedes select)           │
│ LossyOverflow                                                        │ │•←-first interactive admission; writer promotion age=6s               │ │•health/root list try-lock only→busy/empty; ⊘blocking responder       │
└──────────────────────────────────────────────────────────────────────┘ └──────────────────────────────────────────────────────────────────────┘ │ (try-lock)                                                           │
                                                                                                                                                  └──────────────────────────────────────────────────────────────────────┘
──────────────────────────────────────────────────────────────────────────────────────────────────── <CONSTRAINTS> ─────────────────────────────────────────────────────────────────────────────────────────────────────
┌──────────────────────────────────────────────────────────────────────┐ ┌──────────────────────────────────────────────────────────────────────┐ ┌──────────────────────────────────────────────────────────────────────┐
│                            Host platform                             │ │                             OpenCode SDK                             │ │                               Release                                │
│•raw.githubusercontent text/plain→path-extension-first; ⊘source cached│ │•1.15.x `InstanceRef not provided` upstream; ⊘programmatic AFT fix    │ │•GITHUB_TOKEN publish suppresses `: published`; ⊘downstream reliance  │
│ as Markdown (MIME)                                                   │ │ (Effect loses ref); use unaffected version                           │ │ (GITHUB_TOKEN)                                                       │
│•rustdoc HTML chrome buttons/self-links/permalinks→explicit htmd      │ │•tree-sitter-vue script=raw_text; ⊘TS AST patterns inside .vue        │ │•GitHub asset tags/cache dirs v-prefixed; bare→404/cache miss; body   │
│ cleanup                                                              │ │ (raw_text)                                                           │ │ from tag                                                             │
│•ORT prebuilt: macOS arm64, Linux/Windows x64+arm64; ⊘Intel Mac       │ │•⊘publish file events/internal file-time APIs (not exposed)           │ │•npm Trusted Publishing ⊘first publish (registry→existing package)    │
│ prebuilt (manual install)                                            │ │•header arrays/objects→JSON.stringify; scalar auto-render; ⊘[object   │ │•actions/setup-node@v5 registry-url injects always-auth=false warning;│
│•.git/info/exclude ⊘files inside worktrees (Git limitation)           │ │ Object] (scalar-UI)                                                  │ │ ⊘repo-side fix (actions/setup-node issue 1379)                       │
│•accepted TcpStream inherits nonblocking→set blocking or←WouldBlock   │ │•LSP auto-install ⊘Bun requirement (Node/npm-only users)              │ │•crates.io API→User-Agent                                             │
│•Windows std::fs::rename atomically replaces; ⊘pre-delete (crash      │ │•notification noReply ⊘model/variant (InstanceRef crash);             │ └──────────────────────────────────────────────────────────────────────┘
│ window)                                                              │ │ sendIgnoredMessage omits, wakes include                              │ ┌──────────────────────────────────────────────────────────────────────┐
│•ignore crate←~/.gitconfig + ~/.gitignore_global; tests neutralize    │ │•ast_grep_replace variadics named e.g. $$$BODY (substitution needs    │ │                                Shell                                 │
│ HOME+USERPROFILE                                                     │ │ binding)                                                             │ │•Windows npm.cmd; Unix npm; `spawn`; ⊘.cmd resolution                 │
│•global core.fsmonitor=true→daemon/repo/worktree; ⊘under churn        │ │•api.ui.Dialog owns Enter/Esc keybinds                                │ │ (explicit-executable)                                                │
│ (fseventsd saturation)                                               │ │•Desktop plugins=Node/Electron→RPC portable node:http                 │ │•priority: $→pwsh.exe→powershell.exe→Git bash.exe→cmd.exe floor       │
│•daemon minimal env lacks node→node formatters/LSP break              │ │•Always Allow grants=InstanceState memory; ⊘restart persistence       │ │•child stdin detached; ⊘inherit (←-Host blocks forever vs fail-fast)  │
│•tasklist may→empty under contention→live owner misread dead          │ │ (volatile)                                                           │ │•native bash ignores non-absolute permission tokens + advisory only;  │
│•terminated socket=ConnectionReset|ConnectionAborted, ⊘clean EOF      │ │•Bun bundler dynamic imports need literal specifier; ⊘variable        │ │ ⊘blocking external_directory ask (advisory)                          │
│ (reset)                                                              │ │ (workspace not inlined)                                              │ │•PIPESTATUS + `set -o pipefail` ⊘portable across pwsh/cmd/Unix        │
│•Git for Windows ⊘verbatim `\\?\` clone source (normalize canonicalize│ │•OpenAI-compatible tool params root `{type:'object'}`;                │ │ (shell-specific)                                                     │
│ output)                                                              │ │ ⊘anyOf|oneOf|allOf (DeepSeek400/GLM empty args)                      │ │•fish PATH probe needs fish syntax; ⊘POSIX print (fails/misbehaves)   │
│•macOS26 fresh-binary wedge under compile                             │ │•⊘skip Solid transform in node_modules (eager children/props freeze   │ └──────────────────────────────────────────────────────────────────────┘
│ load=XProtect/AppleSystemPolicy; fix sudo pkill -9 xprotectd + scan  │ │ first paint)                                                         │ ┌──────────────────────────────────────────────────────────────────────┐
│ daemon; probe clang trivial C, ⊘copied /bin/echo (launch-constraint) │ │•OpenTUI 0.4 supports opentui:runtime-module:*; 0.3 fallback compiled │ │                                 Subc                                 │
│•macOS ad-hoc signature ID derives filename→renamed binary different  │ │ TSX→raw                                                              │ │•provider streams expose stable session/thread IDs e.g. UUIDv7;       │
│ SHA-256                                                              │ │•function params standard types; ⊘empty `{}`/Type.Any (strict         │ │ ⊘stateless reconstruction (UUIDv7)                                   │
│•backups namespace=harness only; ⊘project key (sessions share dir)    │ │ validator rejects)                                                   │ │•route.open allocates fresh monotonic channel/call; ⊘identity dedupe  │
└──────────────────────────────────────────────────────────────────────┘ └──────────────────────────────────────────────────────────────────────┘ │ (monotonic)                                                          │
                                                                                                                                                  │•bind timeout load-sensitive; subc-client>=0.3.3 retry budget30s      │
                                                                                                                                                  │•RouteBind reconnect/reopen re-runs configure→possible storms         │
                                                                                                                                                  │•health on shared frame loop may timeout under CPU→false supervisor   │
                                                                                                                                                  │ restart                                                              │
                                                                                                                                                  └──────────────────────────────────────────────────────────────────────┘
─────────────────────────────────────────────────────────────────────────────────────────────────── <CONFIG_VALUES> ────────────────────────────────────────────────────────────────────────────────────────────────────
┌──────────────────────────────────────────────────────────────────────┐ ┌──────────────────────────────────────────────────────────────────────┐ ┌──────────────────────────────────────────────────────────────────────┐
│                                 Bash                                 │ │                              Callgraph                               │ │                                 E2E                                  │
│•bash_status/bash_kill/transport timeout ⊘bridge restart (own timeout │ │•AFT_CALLGRAPH_BUILD_WAIT_MS default0; tests large→sync build         │ │•E2ETest aft-ready snapshot stores shared-folder path; revert wipes   │
│ handling)                                                            │ │•max_callgraph_files deleted (legacy in-memory layer removed)         │ │ external repoint; repo move→switch snapshot→shf-host-del/add→delete  │
│•subagent async commands synchronous unless                           │ │•extensions +java,kt,kts,rb,swift,scala,lua,pl,pm                     │ │ old→new aft-ready                                                    │
│ bash.subagent_background=true                                        │ └──────────────────────────────────────────────────────────────────────┘ │•CI NEXTEST_TEST_THREADS=num-cpus                                     │
│•enablement resolves top-level →experimental.bash.*→old               │ ┌──────────────────────────────────────────────────────────────────────┐ │•SUBC_CORE_BIN→cached binary on Linux/macOS CI                        │
│ flat→tool_surface defaults                                           │ │                                Config                                │ │•Windows host ssh -i ~/.ssh/id_ed25519 ufuka@192.168.1.47; repo       │
│•bash.timeout positive integer; hard kill cap survives promotion      │ │•dataHome: XDG_DATA_HOME Unix; LOCALAPPDATA Windows; then standard    │ │ %USERPROFILE%\aft; rustc1.96.1; git C:\Program Files\Git\cmd\git.exe;│
│•object→unspecified rewrite/compress/background=true                  │ │ home                                                                 │ │ PowerShell5.1                                                        │
│•bash.foreground_window=15s                                           │ │•safe semantic settings deep-merge user+project                       │ │•Windows=ROG Ally bare commands; macOS=M1                             │
└──────────────────────────────────────────────────────────────────────┘ │•.jsonc parse/write via comment-json (preserve comments)              │ │ test@tests-MacBook-Pro.local; M1 /Users/test/bench.lock owner.json;  │
┌──────────────────────────────────────────────────────────────────────┐ │•getCacheDir Windows `%LOCALAPPDATA%\aft\bin`, fallback APPDATA or    │ │ ask SYNAPSE; ⊘/Users/test/bench-* or ~/models (bench-home)           │
│                                Images                                │ │ ~/AppData/Local                                                      │ └──────────────────────────────────────────────────────────────────────┘
│•resize longer-side cap=1024px                                        │ │•lsp.versions canonical pin for npm + GitHub-release installs         │ ┌──────────────────────────────────────────────────────────────────────┐
│•image processing timeout=10s (bounded pool)                          │ │•formatter_timeout_secs/type_checker_timeout_secs user-only; project  │ │                              Embeddings                              │
└──────────────────────────────────────────────────────────────────────┘ │ stripped+warned                                                      │ │•semantic.timeout_ms=25s (≺parent bridge timeout)                     │
┌──────────────────────────────────────────────────────────────────────┐ │•format_on_edit=false • format_on_edit idle debounce=90s              │ │•search cache key=git root commit; non-git=canonical root path        │
│                               Release                                │ │•user ~/.config/cortexkit/aft.jsonc; project                          │ │•prepare ONNX iff semantic_search + fastembed/unset;                  │
│•private secrets CK_CI_APP_ID + CK_CI_APP_PRIVATE_KEY                 │ │ <project>/.cortexkit/aft.jsonc; legacy→.MOVED_READPLEASE             │ │ ⊘openai_compatible/ollama (HTTP)                                     │
│•[profile.stage]: thin LTO, codegen-units=4, strip=false, debug=1;    │ │•aft_zoom container menu threshold=150 lines                          │ │•MAX_EMBED_TEXT_CHARS=1600 (~400 tokens <512 backend)                 │
│ `cargo build --profile stage -p agent-file-tools`; target/stage;     │ │•enabled:boolean default=true, project-safe                           │ │•raw ort weights=Qdrant/all-MiniLM-L6-v2-onnx                         │
│ releases fat-LTO stripped                                            │ │•multi_root.scan_depth=3; max_roots=32                                │ │ tokenizer.json+model.onnx                                            │
└──────────────────────────────────────────────────────────────────────┘ │•disabled_tools string[] e.g. ["edit"] prevents replacing host        │ │•fingerprint=backend+model+normalized base_url+dimension+chunk        │
┌──────────────────────────────────────────────────────────────────────┐ │ built-in (disabled-name-gate)                                        │ │ version; change→full rebuild                                         │
│                              Scheduler                               │ │•harness fingerprint `[0-9a-f]{32,64}`; malformed→fail loud           │ │•local all-MiniLM-L6-v2 backend string=fastembed (fingerprint         │
│•Phase4 live actor soft cap≈8                                         │ │•duplicate thresholds cost≥20 + span≥10 lines                         │ │ compatibility)                                                       │
│•interim process-wide maintenance build cap=2                         │ │•Pi optionalInt schema: Type.Optional + Type.Union +                  │ │•expansion: cosine≥0.60, rank0, non-test, ≤250 lines                  │
│•reliable-push drain=32/turn • maintenance submissions=4/tick         │ │ Type.Integer({minimum,maximum}) + Type.String()                      │ │•MAX_TOP_K overfetch=100                                              │
│•watcher/LSP drain cap=256 • watcher slice budget=2048 paths          │ │•daemon RLIMIT_NOFILE=8192                                            │ │•semantic cache V7; accepts V6 on disk                                │
│•watcher slice soft deadline=250ms • writer promotion age=6s          │ └──────────────────────────────────────────────────────────────────────┘ └──────────────────────────────────────────────────────────────────────┘
└──────────────────────────────────────────────────────────────────────┘
─────────────────────────────────────────────────────────────────────────────────────────────── <NAMING + KNOWN_ISSUES> ────────────────────────────────────────────────────────────────────────────────────────────────
┌──────────────────────────────────────────────────────────────────────┐ ┌──────────────────────────────────────────────────────────────────────┐ ┌──────────────────────────────────────────────────────────────────────┐
│                                 CLI                                  │ │                              Federation                              │ │                               Task IDs                               │
│•crates.io=agent-file-tools; Rust lib+=aft                            │ │•harness class `fed:<fp>`                                             │ │•! older cue `bash-<8hex>` vs Alfonso `bg-`                           │
│•module restart|stop|start|status|list; health; describe              │ │•malformed fingerprint code=bad_harness_fingerprint                   │ │•bg `bash-<16hex>` vs Alfonso delegated `bg-`                         │
└──────────────────────────────────────────────────────────────────────┘ │•storage slug `fed--<prefix16>--<hash8>`                              │ └──────────────────────────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────────────────────────┐ │•gatherer harness `runner`; session `alfonso:<gatherer-id>`           │ ┌──────────────────────────────────────────────────────────────────────┐
│                             Tool schema                              │ │•trusted module-direct principals=alfonso-core, broca                 │ │                               Tool API                               │
│•built-in LSP id=python; ⊘pyright (config key mismatch)               │ └──────────────────────────────────────────────────────────────────────┘ │•aft_edit ⊘literal `\n` match ergonomics (escape becomes newline)     │
│•params camelCase; filePath preferred over file                       │ ┌──────────────────────────────────────────────────────────────────────┐ │•shared sessions ⊘same-path undo isolation (agents overwrite backups) │
│•config keys snake_case; plugin preferred over plugins                │ │                                 Subc                                 │ │•hoisted edit ⊘weak-model safety (replace + advanced modes mixed)     │
│•callgraph flat name_match edge suffix `~`; exact/type_match unmarked │ │•Subconscious daemon→                                                 │ │•aft_navigate ⊘some cross-file refs (LSP resolves more)               │
│•permission IDs aft_search vs grep independently gated                │ │•MCP segment `mcp--<sanitized-client>-<raw-name-hash>`                │ │•TS buildUnifiedDiff ⊘optimal insertion summaries (heuristic          │
│•expected clone marker `aft:expected-duplicate`                       │ │•worktree bridge pidfile `<worktreePath>.aft-bridge.pid`              │ │ suboptimal)                                                          │
└──────────────────────────────────────────────────────────────────────┘ │•subc/MCP routes bare delete|move|import|refactor|safety; agent IDs   │ │•hoisted apply_patch ⊘huge metadata.diagnostics (Biome repos attach   │
                                                                         │ retain aft_                                                          │ │ payload)                                                             │
                                                                         └──────────────────────────────────────────────────────────────────────┘ │•tool.definition hook ⊘model/agent info (cannot tailor definitions)   │
                                                                                                                                                  └──────────────────────────────────────────────────────────────────────┘
