──────────────────────────────────── <PROJECT_RULES> ─────────────────────────────────────  ┌────────────────────────────────────────────────────────────────────────────────────────┐  ┌────────────────────────────────────────────────────────────────────────────────────────┐
┌────────────────────────────────────────────────────────────────────────────────────────┐  │                                  Knowledge & workflow                                  │  │                                    Plugin & tooling                                    │
│                                    Repo & delivery                                     │  │                                                                                        │  │                                                                                        │
│                                                                                        │  ├────────────────────────────────────────────────────────────────────────────────────────┤  ├────────────────────────────────────────────────────────────────────────────────────────┤
├────────────────────────────────────────────────────────────────────────────────────────┤  │•recurring fork contradiction→explicit decision; ⊘unilateral behavior flip              │  │•tool descriptions: ⊘`Returns:` blocks (surface convention)¦new/graduated fields→both   │
│•pre-publish tag failure→fix blocker, delete/repush same version tag¦workflows build    │  │(prior-decision)¦fresh terminal outside OpenCode; ⊘nested `opencode run` (shared        │  │plugins src/config.ts; + OpenCode scripts/build-schema.ts¦dryRun only ast_grep_replace; │
│packages/aft-bridge≺TS typecheck (dist resolution)¦new @cortexkit/aft-*                 │  │server/SQLite collision)¦license MIT across manifests + LICENSE¦release-note            │  │others real edit + checkpoint/undo¦path params template=`(absolute or relative to       │
│package→version-sync.mjs + validate-packages.mjs + release.yml¦repo-relative release    │  │paragraph=one unwrapped line (GFM hard-breaks newline)¦semantic platform changes        │  │project root)`¦live bridge PID > binary mtime; restart host≻bundles rebuild¦schema      │
│asset committed≺tag (body resolves tagged tree)¦LockGuard::drop joins heartbeat≺lockfile│  │preserve Intel Mac¦GitHub identity=~/.local/bin/gh-alfonso-aft; replies→approval +      │  │changes→z.toJSONSchema + z.object + def.args + io:"input"; measure surface tokens via   │
│removal¦ualtinok-authored commits include same Alfonso 289616620+ trailer¦plugin        │  │--body-file¦shared contracts: run-manifest.json + run-summary.tsv¦Tier                  │  │aft-tokenizer¦docs distinguish TS/JS import-graph verified vs Rust call-graph estimated │
│index.ts imports→bun run lint; ⊘package tests alone (Biome not run)¦SQLite              │  │D=deterministic/no-LLM; Tier S=stochastic/provider-metered¦Cargo profiles               │  │•plant guaranteed-dead export→configure harness→inspect_tier2_run→inspect               │
│busy_timeout≺journal_mode=WAL (cold-open race)¦rolled_back:true→success + unchanged;    │  │incremental=false (worktree disk + hydration)¦serial clean isolation; workers judge     │  │•bash.background:false→remove params/tools/descriptions/guidance; ⊘partial disable      │
│⊘hard error (rollback succeeded)¦clean contributor PR→gh pr merge; ask Ufuk approve fork│  │correctness/algorithm, ⊘performance magnitude (CPU contention)¦prewarm indexes via `aft │  │(surface leaks feature)¦compress/*.rs fixtures=real captured bytes + hexdump -C; match  │
│workflows; cherry-pick only modified/unverifiable (otherwise loses merged credit)¦watch │  │warmup` with identical prompts¦invocation prose=agent action; ⊘human imperative (agent  │  │structural prefixes across NBSP U+00A0 / ASCII U+0020¦git-log compression               │
│Tests/CI=`scripts/watch-ci.sh [commit] [workflow]`; Release=`scripts/wait-release.sh    │  │owns tool call)¦user docs `#N` only real GitHub issue/PR; strip internal note IDs¦AFT   │  │timestamps=absolute (deterministic/cache-stable)¦status counts: ⊘remote schema/network  │
│vX.Y.Z`; ⊘nohup poll loops (detach/quoting lies)¦byte-golden `\n` fixtures→root         │  │arm: aft_inspect off; ⊘AFT hints; agent.build.prompt=` `; explore/general/task/skill    │  │(environmental); TS2688/type-resolution=count¦execution_mode=hand-classify observable   │
│.gitattributes eol=lf; binary *.raw; ⊘local windows-gnu check as proof (compile-only, no│  │disabled+denied (suppress expansion)¦release notes/close comments always @-tag          │  │file writes/deletes; ⊘lane derivation (lanes include internal effects)¦shortcircuit:    │
│byte test)¦bg directly: `bash scripts/watch-ci.sh main`; watch `RESULT=`; ⊘`... >log &  │  │contributors¦bugs→complete root-cause resolution; ⊘workaround/bypass (defers defect)    │  │empty→empty; ⊘fabrication (filter summarizes only actual bytes); guard                  │
│echo pid` (tracked shell exits≺watcher result)¦parallel waves: ⊘shared version bumps    │  │•subpar mason→resume SAME task/resteer; ⊘integration self-fix (hides router signal)     │  │compress_filters_test.rs::builtin_filters_never_fabricate_output_for_empty_input¦dev    │
│(collision); serialize at merge¦review trees default --detach (avoid blocking           │  │•⊘latency evidence on shared dev box (parallel rustc contests wall time); M1: bench.lock│  │rebuild→scripts/dev-rebuild.sh¦new volatile status fields→tool_call_parity_test.rs      │
│rehydration)¦A/B vs mason tree >30min→dedicated checkout (reclaimer may collect)¦Alfonso│  │+ ping SYNAPSE; bounds from mechanism + DEBUG_BOUND_MULTIPLIER¦M5 heavy runs heads-up;  │  │masking list¦TUI: runtimeDependencies=0; peer deps optional; dev deps exact; raw TSX    │
│commits trailer: Co-authored-by Alfonso <289616620+alfonso-aft@users.noreply.github.com>│  │contention escalation=/tmp/m5-build.lock mkdir¦oracle loop: 2–3 scoped→triple-check     │  │exports; ⊘value imports (host runtime owns values)¦packed-install visual+behavior gate; │
│•pre-push bun lint + Rust gate (CI fail-fast/autocancel masks lint)¦⊘parallel           │  │code+measure→plan→masons→verify→repeat¦convention-divergent cues retain differentiator; │  │⊘import-only probe (misses JSX eager freeze)¦repo file:// live plugin→TS merge rebuild  │
│repo-mutating chains on main checkout (timeout can strand detached); A/B=2 dedicated    │  │negatives=⊘excluded + mechanism (polarity)                                              │  │dist: aft-bridge, opencode-, pi                                                         │
│trees, prebuilt binaries, interleaved base/head¦multi-step state/logs→durable home;     │  └────────────────────────────────────────────────────────────────────────────────────────┘  └────────────────────────────────────────────────────────────────────────────────────────┘
│⊘/tmp (reboot loss)                                                                     │  ┌────────────────────────────────────────────────────────────────────────────────────────┐  ───────────────────────────────────── <ARCHITECTURE> ─────────────────────────────────────
└────────────────────────────────────────────────────────────────────────────────────────┘  │                                    Runtime & tests                                     │  ┌────────────────────────────────────────────────────────────────────────────────────────┐
┌────────────────────────────────────────────────────────────────────────────────────────┐  │                                                                                        │  │                                  Artifacts & storage                                   │
│                                   Code intelligence                                    │  ├────────────────────────────────────────────────────────────────────────────────────────┤  │                                                                                        │
│                                                                                        │  │•isTerminalStatus allowlist completed|failed|killed|timed_out; ⊘`!=running` (unknown    │  ├────────────────────────────────────────────────────────────────────────────────────────┤
├────────────────────────────────────────────────────────────────────────────────────────┤  │states)¦Unix APIs e.g. PermissionsExt→#[cfg(unix)] (Windows compile)¦Windows            │  │•warnings dedupe via bridge DB state (cross-session)¦LSP schema extensions+binary       │
│•status_bar_counts=None until dead_code+unused_exports+duplicates ∀ populated           │  │verification=native Windows; ⊘Unix target-check shim (Unix-gated helpers)¦promptAsync   │  │optional (built-in overrides)¦legacy migration copies; ⊘move (rollback preserves source)│
│•wildcard-imported exports→uncertain reason=wildcard_import; ⊘unused verdict (unknown   │  │exactly one: synthetic:true wake OR ignored:true panel¦E2ETest: Mac windows-gnu         │  │•HTML fetch→htmd Markdown + .md cache (reuse parser)¦configure eagerly warms            │
│binding)¦failed/dead LSP cached per workspace root; ⊘repeated reconnect (retry-stop)    │  │cross-build→stage ~/Downloads→\psf\Home\Downloads; guest local copy + prlctl exec;      │  │CallGraphStore (avoid first-use latency)¦AftRpcClient/Server delete port file only      │
│•local MiniLM mean-pool + L2 normalize→Qdrant/fastembed parity¦Rust resolver caches     │  │pwsh→PowerShell5 fallback¦cargo commands serial (shared target build-lock/CPU)¦E2E      │  │recorded PID dead¦aft-tokenizer build.rs temp+rename→src/claude_data.rs                 │
│crate-name→src prefixes; ⊘recursive walks (cache-hit)¦cold DB write=single transaction; │  │manifest pins catalog.list; ⊘static copy (drifts)¦translate/format→TS-generated         │  │(concurrent-build safety)¦configure.rs rejects relative roots→invalid_request (canonical│
│parse dominates¦chunk reuse key=blake3(embed_text) + exact-string verify¦trigram +      │  │capture-subc-parity.ts fixtures¦format subsystem opt in format_on_edit:true¦nonzero     │  │absolute resolution)¦project key=root, shared harnesses:                                │
│semantic separate binary indexes; ⊘SQLite bottleneck (bin-files)¦search index paths     │  │shell test: Windows native `cmd /c "echo x&exit 7"`; Unix `printf x; exit 7`; ⊘bare     │  │search/semantic/symbol/callgraph/inspect; session state key=(harness,session):          │
│absolute in RAM, relative on disk (portable)¦same-file value refs e.g. Zod              │  │PowerShell `exit N` (kills wrapper≺exit-marker)¦subc_bridge_test.rs process             │  │undo/backups/bash/checkpoints¦mode threads user_config_path through runner (hermetic    │
│composition→live¦test-support dirs ⊘reports (fixtures/testdata false positives); imports│  │env→run_subc_bridge_test_with_env inside serial guard¦SubcTransportPool validates       │  │tests; ⊘real user config)¦migration conflict: first harness→global;                     │
│still keep prod exports live¦Tier-2 cached aggregates filtered on demand by query scope │  │structuredContent ∀ reply; malformed/missing→protocol error¦logger assertions spy       │  │other→aft.jsonc.<harness>_OLD¦stale diagnostics flagged, hidden from warm readers;      │
│•insert_method_dispatch_edges_chunked partitions caller files by chunk_size (RAM bound) │  │logger.log; ⊘shared logfile/sleeps (parallel flakes)¦dispatch parallel:                 │  │⊘eviction (would fabricate 0)¦owner+standalone coexist same root via fs_lock +          │
│•within-root concurrency→tsconfig_membership thread-safe lock; ⊘RefCell (mutex)         │  │ledger-check≺retry + settling beat (burst congestion)¦client timeout→explicit bash_kill;│  │harness-namespaced stores¦reconfigure cancel-or-adopt detached builds via per-root      │
│•TreeSitterProvider Send+Sync via per-call FileParser; ⊘RefCell mutation (per-call)¦MCP │  │⊘orphan execution (executor starvation)¦storm authoritative gate=`--cargo-profile       │  │generations¦worktree-class bind: ⊘cold build (borrow shared owner )¦equivalent-config   │
│storage segment strip `/ \ :`, collapse -, prefix mcp-- (⊘traversal)¦direct             │  │release`; debug=4x + main ignored¦nextest group `storm = 1` serializes rigs (readiness  │  │rebind idempotent→0 new warm work¦owner liveness: own PID always alive (OS/tasklist     │
│aft_inspect=synchronous incremental refresh+recompute; status bar async¦queries←resident│  │starvation)¦tool names/descriptions/schemas + prompts transport-invariant (prefix cache)│  │flakes)¦trigram delta flush graceful shutdown if dirty, 25ms lock; panic ⊘disk work     │
│store; ⊘cold build (latency)¦semantic extensions=every real tree-sitter symbol query;   │  │•real FSEvents→serialized watcher_integration binary¦CPU flake brief: ≤8 hogs, nice -n  │  │(panic-path)¦writer lease loser→←-only + stale-with-disclosure; ⊘error (RO-fallback)    │
│generic collect_file_chunks_from_source; ⊘HTML/Markdown/JSON (query=None); sync guard   │  │19, EXIT/INT/TERM traps, pgrep between rounds¦src/ gate includes `--lib` touched module │  │•artifact_cache_key root commit: aft 90ff783f3f4c5cf2; opencode aa69d52ef2dcad4d;       │
│semantic_extension_policy_stays_in_sync_with_parser_code_arms¦priors: P2 definition     │  │(unit failures)¦⊘bind/configure nonexistent roots (stale restored sessions consume      │  │alfonso be627d40119a995e; magic-context e274ab0872bb490b; subc 29f01b6c90df2c83; synapse│
│exact-Identifier; P1/P4 NL-only; P3 qualified via V7¦priors→rank_score/raw-cosine split │  │modules)                                                                                │  │3e6b8ee50bb2bb06¦persist root→cache-key≻successful probe; failure→last-known or         │
│+ semantic-only sort + cap_protected¦P2 exact boost needs minimum raw cosine (⊘List/Map │  └────────────────────────────────────────────────────────────────────────────────────────┘  │retryable; ⊘path fallback (identity split)¦worktree roots=pure borrowers;               │
│collisions reserving slots)¦transaction rollback stays on even user backups off         │  ┌────────────────────────────────────────────────────────────────────────────────────────┐  │read/query-only stores; ⊘writer leases (owner-only)¦backup hydration deferred first→;   │
│(edit_match/move_symbol/lsp_rename)¦lsp_rename rollback=CheckpointStore; ⊘BackupStore   │  │                                   Routes & controls                                    │  │Tier-2 ⊘worktree scans (borrower economy)¦ArtifactAccess::for_root                      │
│(user toggle isolation)¦apply_patch→crates/aft/src/patch matcher; ⊘fuzzy_match.rs       │  │                                                                                        │  │fallback=borrow-only+warning; ⊘probe (latency-critical git spawn)                       │
│(preserve MatchTier line ladder)¦Objective-C=outline only; ⊘import/dead-code/call       │  ├────────────────────────────────────────────────────────────────────────────────────────┤  └────────────────────────────────────────────────────────────────────────────────────────┘
│extraction (no support)¦.h→C; ⊘Objective-C (avoid C/C++ regression)¦F6 executable-file  │  │•subc-core=transport/routing/lifecycle/lease/hooks/secrets; ⊘business logic             │  ┌────────────────────────────────────────────────────────────────────────────────────────┐
│roots: only framework exports default|GET|loader seed; ⊘∀ route exports (hides helpers) │  │(module-scope)¦RawAftConfig deny_unknown_fields top-level only; nested non-strict       │  │                                     Runtime & TUI                                      │
│•framework decorator matching import-binding + alias aware¦macro token trees            │  │matches TS¦daemon spawns AFT `--subc <connection-file-path>`¦concurrent executor only   │  │                                                                                        │
│⊘precise-edge store (name-match pollution)¦Rust macro call refs resolve file-local +    │  │daemon path; standalone NDJSON direct¦Push coalesced upstream→shallow egress¦bind       │  ├────────────────────────────────────────────────────────────────────────────────────────┤
│same-file/visible imports; ⊘project-wide names (false live)                             │  │session ID→RawRequest (undo/backups/bash isolation + completions)                       │  │•prompt context merges recent agent/model/variant→provider prefix reuse¦PTY kill        │
└────────────────────────────────────────────────────────────────────────────────────────┘  │•StatusChanged=project/session-less; Progress ⊘production emitters (contract-only)      │  │synchronous on Windows→TimedOut(124)|Killed; ⊘async hang¦version check→ensureBinary +   │
┌────────────────────────────────────────────────────────────────────────────────────────┐  │•fan-out separates matched channels vs sent frames; buffer keys channel presence        │  │pool.replaceBinary; ⊘host restart (hot-swap)¦bridge lazy-spawns AFT binary on first     │
│                                   Scheduler & drains                                   │  │•execution_mode=external effects; ⊘internal warming mutation (keeps←pure)¦tool          │  │request; ⊘passive status spawn (passive-poll)¦completion reminder debounce 200ms,       │
│                                                                                        │  │manifest→execution_mode¦subc_translate in run_tool_call≺lane; ⊘aliases in shared        │  │cap1000ms (prefix cache)¦sidebar RPC heartbeat 15s recreates deleted port file¦Pi→agent │
├────────────────────────────────────────────────────────────────────────────────────────┤  │main::dispatch (validation)¦dynamic bash targets ⊘wildcard external-dir ask             │  │plain text + details structured JSON¦@cortexkit/subc-client devDependency bundled in    │
│•attach_status_bar mutates tsconfig_membership/status_bar_last_emitted→serial¦active    │  │(variables/subshell footgun); command approval remains¦standalone NDJSON + daemon use   │  │published distribution¦RPC/sidebar push=Bun.serve gated `typeof Bun !== "undefined"` for│
│semantic build defers callgraph warm + Tier-2 scans (peak RSS)¦cold gate=per-AppContext │  │run_tool_call translate→orchestrate→dispatch→format; plugin execute thin¦server owns    │  │Bun-only TUI¦Unix startup one-time `$SHELL -l -c 'printf %s $PATH'` when PATH bare      │
│accumulation flag; ⊘Building (also cached/incremental)¦embedding retry wait clears      │  │zoom fan-out + outline dispatch + target merge; ⊘TS/Rust duplication (server-side)      │  │•expire_pending_bash_asks at loop top (busy select starvation)¦TSX precompile with host │
│Tier-2 cold-seed gate (⊘code-health starvation)¦watchdog emits BashLongRunning; deferred│  │•session end→SubcPool.closeRouteChannel (resource + teardown)¦one SubcClient/process +  │  │transform→opentui:runtime-module:* single runtime/reactivity¦Pi TypeBox builds literal  │
│driver handles only final response¦PersistentCancelSignal=AtomicBool+Notify enable()    │  │per-call identity; root+session¦server strips preview≺translation/fallback; preview     │  │JSON Schema; OpenCode converts Zod                                                      │
│(⊘lost wakeup)¦loop decoupled AppContext; reliable_rx sets wakes; maintenance pass      │  │⊘disk mutation (stripped)¦unsupported tool distinct catch-∀ error code; ⊘parallel name  │  └────────────────────────────────────────────────────────────────────────────────────────┘
│clears authoritatively¦bg wake lock-free epoch; stale CLEAR ⊘newer wake (epoch)         │  │allowlist (validation drift)¦peer roster ephemeral; inbox durable with session_id; reply│  ───────────────────────────────────── <CONSTRAINTS> ──────────────────────────────────────
│•awaiting-ack completion vs authoritative drain snapshot; ⊘TTL (premature               │  │raw session_id despite empty roster; known SUBCONSCIOUS ses_12a4fa38dffe81Fz7Y2AsWb5Cg  │  ┌────────────────────────────────────────────────────────────────────────────────────────┐
│eviction/double delivery)¦RouteBind completions processed loop top≺Push/maintenance     │  │•bg_events dedicated channel; ⊘dispatch saturation blocking completion wakes (wake-lane)│  │                                  OpenCode & providers                                  │
│•drain-tick at loop top; ⊘starvation (tick precedes select)¦health/root list try-lock   │  │•route cache singleflight+tombstones (concurrent open/closed channel)                   │  │                                                                                        │
│only→busy/empty; ⊘blocking responder (try-lock)¦post-ack                                │  │•~/Work/Projects/CortexKit/synapse=stateless embeddings/rerank/micro-LLM; later         │  ├────────────────────────────────────────────────────────────────────────────────────────┤
│configure→ConfigureMaintenanceJob queue, async runtime-drain¦heartbeat process-wide     │  │image/STT/TTS; AFT keeps retrieval; llm-runner owns session/tool semantics; handoff     │  │•1.15.x `InstanceRef not provided` upstream; ⊘programmatic AFT fix (Effect loses ref);  │
│timer thread + RAII leases; independent Mutating lane                                   │  │SYNAPSE/FOUNDING.md¦unknown_channel→reopen+retry once (`not_sent`); ⊘outcome-unknown    │  │use unaffected version¦tree-sitter-vue script=raw_text; ⊘TS AST patterns inside .vue    │
│•WATCHER_EVENT_DRAIN_BATCH_CAP=LSP_EVENT_DRAIN_BATCH_CAP=256 per job; requeue remainder;│  │retry (double mutation)¦packages/aft-bridge/src/__tests__/e2e/:                         │  │(raw_text)¦SDK ⊘publish file events/internal file-time APIs (not exposed)¦header        │
│standalone unbounded¦binds bypass maintenance queue; ⊘wait (storm starvation)¦reliable  │  │subc-rig|subc-lane|subc-lifecycle; fetch-subc-core.sh pinned+sha256; missing            │  │arrays/objects→JSON.stringify; scalar auto-render; ⊘[object Object] (scalar-UI)¦LSP     │
│Push unbounded/nonblocking; lossy capacity1024 newest via LossyOverflow¦bind queues     │  │binary/secrets→loud skip, ⊘failure (optional)¦native plumbing five: bash_status,        │  │auto-install ⊘Bun requirement (Node/npm-only users)¦notification noReply ⊘model/variant │
│ConfigureTail then CompletionDrains; maintenance_pending=true; next_maintenance_at      │  │bash_drain_completions, bash_ack_completions; undo_preview, checkpoint_paths; ∀         │  │(InstanceRef crash); sendIgnoredMessage omits, wakes include¦provider streams expose    │
│+250ms gives completion priority¦image resize dedicated thread/request; timer=processing│  │session-scoped, ⊘config/trust/privilege (5-safe-tools)¦federation side dedupes;         │  │stable session/thread IDs e.g. UUIDv7; ⊘stateless reconstruction (UUIDv7)               │
│only; ⊘OnceLock/global pool (load timeout)¦drain phases Collect→Apply; budget2048 paths │  │⊘idempotency tokens (envelope only deterministic terminal reply)¦writer send            │  │•ast_grep_replace variadics named e.g. $$$BODY (substitution needs binding)             │
│+ soft250ms (preemptible)¦←-first interactive admission; writer promotion age=6s        │  │timeout+backoff; ⊘drop (retry+backoff); warn-once; ⊘park forever on `reserve().await`   │  │•api.ui.Dialog owns Enter/Esc keybinds¦native bash ignores non-absolute permission      │
└────────────────────────────────────────────────────────────────────────────────────────┘  │(backpressure-starves-writer)¦health reply rides frame loop; ⊘out-of-band cheap answer  │  │tokens + advisory only; ⊘blocking external_directory ask (advisory)¦Desktop             │
┌────────────────────────────────────────────────────────────────────────────────────────┐  │(must measure loop liveness)¦pins subc-protocol 0.8 + subc-transport 0.3.2 (typed       │  │plugins=Node/Electron→RPC portable node:http¦Always Allow grants=InstanceState memory;  │
│                                   Platform & Windows                                   │  │principal, HealthCheck)¦RouteBind.consumer_capabilities typed elicitation; absent→flat  │  │⊘restart persistence (volatile)¦Bun bundler dynamic imports need literal specifier;     │
│                                                                                        │  │deny¦harness=`runner` storage/trust; client identity in session `alfonso:<gatherer-id>` │  │⊘variable (workspace not inlined)¦OpenAI-compatible tool params root `{type:'object'}`; │
├────────────────────────────────────────────────────────────────────────────────────────┤  │•Windows ConnectionReset|ConnectionAborted→clean daemon-closed EOF¦failed/expired       │  │⊘anyOf|oneOf|allOf (DeepSeek400/GLM empty args)¦RouteBind reconnect/reopen re-runs      │
│•npm.cmd; Unix npm; `spawn`; ⊘.cmd resolution (explicit-executable)¦GITHUB_TOKEN publish│  │bind→next rebind recreates actor; ⊘already-pending/config-divergence rejection (expired)│  │configure→possible storms¦⊘skip Solid transform in node_modules (eager children/props   │
│suppresses `release: published`; ⊘downstream reliance (GITHUB_TOKEN)¦GitHub asset       │  └────────────────────────────────────────────────────────────────────────────────────────┘  │freeze first paint)¦OpenTUI 0.4 supports opentui:runtime-module:*; 0.3 fallback compiled│
│tags/cache dirs v-prefixed; bare→404/cache miss; body from tag¦shell priority:          │  ──────────────────────────────────── <CONFIG_VALUES> ─────────────────────────────────────  │TSX→raw¦function params standard types; ⊘empty `{}`/Type.Any (strict validator rejects) │
│$SHELL→pwsh.exe→powershell.exe→Git bash.exe→cmd.exe floor¦npm Trusted Publishing ⊘first │  ┌────────────────────────────────────────────────────────────────────────────────────────┐  └────────────────────────────────────────────────────────────────────────────────────────┘
│publish (registry→existing package)¦actions/setup-node@v5 registry-url injects          │  │                                Runtime config & limits                                 │  ┌────────────────────────────────────────────────────────────────────────────────────────┐
│always-auth=false warning; ⊘repo-side fix (actions/setup-node issue 1379)¦child stdin   │  │                                                                                        │  │                                    Semantic & cache                                    │
│detached; ⊘inherit (←-Host blocks forever vs fail-fast)¦raw.githubusercontent           │  ├────────────────────────────────────────────────────────────────────────────────────────┤  │                                                                                        │
│text/plain→path-extension-first; ⊘source cached as Markdown (MIME)¦crates.io            │  │•dataHome: XDG_DATA_HOME Unix; LOCALAPPDATA Windows; then standard home                 │  ├────────────────────────────────────────────────────────────────────────────────────────┤
│API→User-Agent¦rustdoc HTML chrome buttons/self-links/permalinks→explicit htmd cleanup  │  │•bash_status/bash_kill/transport timeout ⊘bridge restart (own timeout handling)¦safe    │  │•semantic.timeout_ms=25s (≺parent bridge timeout)¦search key=git root commit;           │
│•ORT prebuilt: macOS arm64, Linux/Windows x64+arm64; ⊘Intel Mac prebuilt (manual        │  │semantic settings deep-merge user+project¦subagent async commands synchronous unless    │  │non-git=canonical root path¦getCacheDir Windows `%LOCALAPPDATA%\aft\bin`, fallback      │
│install)¦PIPESTATUS + `set -o pipefail` ⊘portable across pwsh/cmd/Unix (shell-specific) │  │bash.subagent_background=true¦.jsonc parse/write via comment-json (preserve comments)   │  │APPDATA or ~/AppData/Local¦prepare ONNX iff semantic_search + fastembed/unset;          │
│•.git/info/exclude ⊘files inside worktrees (Git limitation)¦accepted TcpStream inherits │  │•lsp.versions canonical pin for npm + GitHub-release installs¦enablement resolves       │  │⊘openai_compatible/ollama (HTTP)¦MAX_EMBED_TEXT_CHARS=1600 (~400 tokens <512 backend)   │
│nonblocking→set blocking or←WouldBlock¦std::fs::rename atomically replaces; ⊘pre-delete │  │top-level bash→experimental.bash.*→old flat→tool_surface defaults¦bash.timeout positive │  │•raw ort weights=Qdrant/all-MiniLM-L6-v2-onnx tokenizer.json+model.onnx                 │
│(crash window)¦route.open allocates fresh monotonic channel/call; ⊘identity dedupe      │  │integer; hard kill cap survives promotion                                               │  │•AFT_CALLGRAPH_BUILD_WAIT_MS default0; tests large→sync build                           │
│(monotonic)¦ignore crate←~/.gitconfig + ~/.gitignore_global; tests neutralize           │  │•formatter_timeout_secs/type_checker_timeout_secs user-only; project stripped+warned    │  │•fingerprint=backend+model+normalized base_url+dimension+chunk version; change→full     │
│HOME+USERPROFILE¦global core.fsmonitor=true→daemon/repo/worktree; ⊘under churn          │  │•bash object→unspecified rewrite/compress/background=true¦Phase4 live actor soft cap≈8  │  │rebuild¦local all-MiniLM-L6-v2 backend string=fastembed (fingerprint compatibility)     │
│(fseventsd saturation)¦daemon minimal env lacks node→node formatters/LSP break¦bind     │  │•format_on_edit=false¦bash.foreground_window=15s¦format_on_edit idle debounce=90s¦user  │  │•expansion: cosine≥0.60, rank0, non-test, ≤250 lines¦max_callgraph_files deleted (legacy│
│timeout load-sensitive; subc-client>=0.3.3 retry budget30s¦tasklist may→empty under     │  │~/.config/cortexkit/aft.jsonc; project <project>/.cortexkit/aft.jsonc;                  │  │in-memory layer removed)¦extensions +java,kt,kts,rb,swift,scala,lua,pl,pm¦MAX_TOP_K     │
│contention→live owner misread dead¦terminated socket=ConnectionReset|ConnectionAborted, │  │legacy→.MOVED_READPLEASE¦resize longer-side cap=1024px¦image processing timeout=10s     │  │overfetch=100¦V7; accepts V6 on disk                                                    │
│⊘clean EOF (reset)¦Git for ⊘verbatim `\\?\` clone source (normalize canonicalize output)│  │(bounded pool)¦aft_zoom container menu threshold=150 lines¦enabled:boolean default=true,│  └────────────────────────────────────────────────────────────────────────────────────────┘
│•health on shared frame loop may timeout under CPU→false supervisor restart¦fish PATH   │  │project-safe¦E2ETest aft-ready snapshot stores shared-folder path; revert wipes external│  ──────────────────────────────────────── <NAMING> ────────────────────────────────────────
│probe needs fish syntax; ⊘POSIX print (fails/misbehaves)¦macOS26 fresh-binary wedge     │  │repoint; repo move→switch snapshot→shf-host-del/add→delete old→new aft-ready¦CI         │  ┌────────────────────────────────────────────────────────────────────────────────────────┐
│under compile load=XProtect/AppleSystemPolicy; fix sudo pkill -9 xprotectd + scan       │  │NEXTEST_TEST_THREADS=num-cpus¦multi_root.scan_depth=3; max_roots=32¦disabled_tools      │  │                                      Names & IDs                                       │
│daemon; probe clang trivial C, ⊘copied /bin/echo (launch-constraint)¦macOS ad-hoc       │  │string[] e.g. ["edit"] prevents replacing host built-in (disabled-name-gate)            │  │                                                                                        │
│signature ID derives filename→renamed binary different SHA-256¦backups namespace=harness│  │•SUBC_CORE_BIN→cached binary on Linux/macOS CI¦private release secrets CK_CI_APP_ID +   │  ├────────────────────────────────────────────────────────────────────────────────────────┤
│only; ⊘project key (sessions share dir)                                                 │  │CK_CI_APP_PRIVATE_KEY¦interim process-wide maintenance build cap=2¦harness fingerprint  │  │•! older cue `bash-<8hex>` vs Alfonso `bg-`¦crates.io=agent-file-tools; Rust lib+CLI=aft│
└────────────────────────────────────────────────────────────────────────────────────────┘  │`[0-9a-f]{32,64}`; malformed→fail loud¦reliable-push drain=32/turn¦maintenance          │  │•built-in LSP id=python; ⊘pyright (config key mismatch)¦tool params camelCase; filePath │
───────────────────────────────────── <KNOWN_ISSUES> ─────────────────────────────────────  │submissions=4/tick¦duplicate thresholds cost≥20 + span≥10 lines¦watcher/LSP drain       │  │preferred over file¦config keys snake_case; plugin preferred over plugins¦Subconscious  │
┌────────────────────────────────────────────────────────────────────────────────────────┐  │cap=256¦Windows host ssh -i ~/.ssh/id_ed25519 ufuka@192.168.1.47; repo                  │  │daemon→subc¦callgraph flat name_match edge suffix `~`; exact/type_match unmarked¦MCP    │
│                                      Known limits                                      │  │%USERPROFILE%\aft; rustc1.96.1; git C:\Program Files\Git\cmd\git.exe; PowerShell5.1¦Pi  │  │segment `mcp--<sanitized-client>-<raw-name-hash>`¦worktree bridge pidfile               │
│                                                                                        │  │optionalInt=Type.Optional(Type.Union([Type.Integer({minimum,maximum}),Type.String()]))  │  │`<worktreePath>.aft-bridge.pid`¦subc/MCP routes bare delete|move|import|refactor|safety;│
├────────────────────────────────────────────────────────────────────────────────────────┤  │•Windows=ROG Ally bare commands; macOS=M1 test@tests-MacBook-Pro.local; M1              │  │agent IDs retain aft_¦bg task `bash-<16hex>` vs Alfonso delegated `bg-`¦permission IDs  │
│•aft_edit ⊘literal `\n` match ergonomics (escape becomes newline)¦shared sessions       │  │/Users/test/bench.lock owner.json; ask SYNAPSE; ⊘/Users/test/bench-* or ~/models        │  │aft_search vs grep independently gated¦expected clone marker `aft:expected-duplicate`   │
│⊘same-path undo isolation (agents overwrite backups)¦hoisted edit ⊘weak-model safety    │  │(bench-home)¦daemon RLIMIT_NOFILE=8192¦watcher slice budget=2048 paths¦watcher slice    │  │•harness class `fed:<fp>`¦malformed fingerprint code=bad_harness_fingerprint¦storage    │
│(replace + advanced modes mixed)¦aft_navigate ⊘some cross-file refs (LSP resolves more) │  │soft deadline=250ms¦writer promotion age=6s¦[profile.stage]: thin LTO, codegen-units=4, │  │slug `fed--<prefix16>--<hash8>`¦gatherer harness `runner`; session                      │
│•TS buildUnifiedDiff ⊘optimal insertion summaries (heuristic suboptimal)¦hoisted        │  │strip=false, debug=1; `cargo build --profile stage -p agent-file-tools`; target/stage;  │  │`alfonso:<gatherer-id>`¦CLI: module restart|stop|start|status|list; health; describe    │
│apply_patch ⊘huge metadata.diagnostics (Biome repos attach payload)¦tool.definition hook│  │releases fat-LTO stripped                                                               │  │•trusted module-direct principals=alfonso-core, broca                                   │
│⊘model/agent info (cannot tailor definitions)                                           │  └────────────────────────────────────────────────────────────────────────────────────────┘  └────────────────────────────────────────────────────────────────────────────────────────┘
└────────────────────────────────────────────────────────────────────────────────────────┘
