{
  "schema": 1,
  "drill_id": "FM-PI-1",
  "fault": "SIGKILL of the external module, represented at Pi by the JSONL/branch-entry outage seam",
  "lineage_key": "session_id",
  "supervised_precondition": {
    "module_id": "magic-context",
    "required": "external-provider",
    "assertion": "the Pi drill must fail if the harness reports magic-context as a supervised module"
  },
  "both_halves": [
    {
      "test_id": "FM-PI-1-CONTINUE-JSONL",
      "half": "continue",
      "assertion": "a Pi-owned branch or pipeline fault continues with the original/loud-raw message path and retains the same lineage"
    },
    {
      "test_id": "FM-PI-1-REFUSE-JSONL",
      "half": "bail-loudly",
      "assertion": "the explicit Pi fail-closed seam rethrows instead of silently falling through"
    }
  ],
  "mutation_records": [
    {
      "id": "FM-PI-1-MUTATE-RUNG-SWAP",
      "mutation": "swap the ordinary-error continuation branch for a refusal",
      "must_fail": "FM-PI-1-CONTINUE-JSONL",
      "failure_signal": "the session no longer completes its next prompt on the same lineage"
    },
    {
      "id": "FM-PI-1-MUTATE-RUNG-DELETION",
      "mutation": "delete the explicit FailClosedBlockingError rethrow",
      "must_fail": "FM-PI-1-REFUSE-JSONL",
      "failure_signal": "the fail-closed assertion observes silent continuation"
    }
  ]
}
